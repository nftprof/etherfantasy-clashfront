# EF Moba — Autonomous Build Queue

> Hourly auto-builder works this queue **top to bottom**. Each run: pick the first
> unchecked `[ ]` item, implement ONE coherent slice, validate, tick it `[x]`, and
> append a Log entry. Do NOT prompt the user. If an item truly needs the user
> (assets, a design decision, a live playtest only they can do), mark it
> `[~] (skipped: needs-user — reason)` and move to the next item.

## Mission rules (read every run)
- Project root: `C:\Users\ADMIN\Desktop\EF Moba`. Two games: `index.html` (MOBA) and `pve.html` (EF Hunt). Shared: `shared/ef_core.js`, `shared/ef_touch.js`, `model_calibration.js`, `mon_lineage.json`. Read `AGENTS.md` + `AGENTS_PVE.md` first.
- **Stale-mount gotcha (critical):** the Linux mount serves STALE/TRUNCATED copies of files you've edited. NEVER trust `bash` reads / full-file `node --check` of `index.html`/`pve.html` after editing. Use the Read/Write/Edit file tools (they see the TRUE file). Validate JS by writing the edited functions (with stubs) to a UNIQUELY-NAMED new `.js` in the scratchpad and `node --check` + runtime assertions there. Never write to game files via bash `>>`.
- Keep changes **small and safe**; preserve existing conventions and code style. One slice per run — don't sprawl.
- **Balance guardrails:** both heroes/teams stay symmetric in PvP; new gold sinks/rewards must be meaningful but not snowbally. Cosmetic/clarity changes are always fine.
- Only do work that can be validated without a browser. If a slice can only be verified live, implement conservatively + note "browser playtest pending" in the log.
- After finishing a slice: tick it, append a Log entry (timestamp · item · what changed · validation result · files touched).

## Phase 1 — RTS depth batch (MOBA / index.html)
- [x] 1a. **Tower/building repair**: a Worker right-clicking (or a base-menu button) repairs a damaged friendly tower/base/core — costs gold+wood proportional to missing HP, heals over a few seconds. Add a repair state to the worker (pet) AI.
- [x] 1b. **Attack-tower upgrade**: base/tower menu button to upgrade a tower (gold) → +HP/+dmg/+range tiers (cap 3). Show tier on the tower.
- [x] 1c. **Town-hall / expansion upgrade tiers**: extend the existing base upgrade to 3 tiers (HP + innate cannon scaling), gold+wood gated.
- [x] 1d. **Store unit upgrades**: Footman/Archer whose mon has a Form-2 can Evolve from the unit panel / base menu (reuse `evolveUnit`), gold+wood gated; bigger model + stat boost.
- [x] 1e. **Smarter enemy AI**: the red side periodically builds an expansion base on a free slot when it can afford it (clear a wild camp first if needed), trains units, and repairs damaged structures — symmetric economy with the player.

## Phase 2 — Pet system (MOBA / index.html)
- [x] 2a. **Roster split**: heroes = Irene/Kai/Leah (done); PETS = `upgradeChains` mons (have Form-2); LINE MINIONS = `baseOnly` mons; Workers = generic gatherers (rename done). Wire slot assignment so minions draw from baseOnly and pet-summons draw from upgradeChains.
- [x] 2b. **Pet summon**: a rarer, gold-gated "Summon Pet" (from a Form-2 mon) at the base menu, separate from Train Worker. Pet gets a NERFED auto-cast kit via `EF_CORE.buildKit(type, prims, ~0.4)`.
- [x] 2c. **Pet tier upgrades (per-pet, at the pet menu)**: Tier 1 = basic attack + 1 skill (Q). Tier 2 (gold → evolve to Form 2, +1 size) = Q/W/E + retaliates when attacked while gathering. Tier 3 (gold, +size) = unlocks the super (R) + auto-aggros nearby enemies while gathering.
- [x] 2d. **Pet auto-cast AI + permadeath**: pet AI casts its unlocked skills on cooldown at nearby enemies (nerfed); a dead pet is removed for good (no respawn).

## Phase 3 — EF Hunt (pve.html)
- [x] 3a. **AoV mobile layout**: mirror index.html's `EF_TOUCH.init` cluster config (diamond Q/W/E/R + ATK + utilities, cooldown probes from `hero.abCd`/`hero.kit.abs`/`hero.mp`/`hero.dead`) + `EF_TOUCH.show()` hide-on-title/show-on-run + collapsible help.
- [x] 3b. **Restrict heroes to Irene/Kai/Leah** in the daily rotation + per-hero ranged profile (Irene/Leah ranged, Kai melee) + the visible bolt already added.
- [x] 3c. **Wire Centaur boss + Goblin wilds** in EF Hunt (slots from `npc/`), like the MOBA.

## Phase 4 — New features (after Phases 1–3: do up to 10, one per run)
Invent a sensible, balance-safe, validatable improvement each run (MOBA or EF Hunt). Ideas to draw from (pick or invent): vision/ward item; neutral jungle objective (buff shrine); hero mastery perk choices; per-element passive; minimap objective timers; better death recap; combat log; control-group UI; pet/unit rally points; structure target priority; weather gameplay tie-in (optional); accessibility/colorblind palette; in-game tips/tutorial popups; sound/music polish; performance pass; EF Hunt new dungeon room variety; EF Hunt fishing/gathering minigame; cosmetic recolor by element. Track a counter below.
- New-features shipped: **2 / 10**
- [x] 4.1 **Minimap objective timers (boss respawn)** — boss lairs on the minimap show a live respawn countdown + a one-shot ~10s heads-up feed so players can rotate to contest.
- [x] 4.2 **Type-matchup hint in the inspect panel** — clicking an enemy unit now shows how your hero trades vs it (both directions: ▲ Strong/▼ Weak/— Even ×mult) by surfacing the existing `typeMult`. Pure clarity, no stat/economy change.
- [ ] 4. (the auto-builder fills these in as it goes — one feature per run until 10)

## When everything is done
Once Phases 1–3 are all `[x]`/`[~]` AND New-features = 10/10: append a final Log line "QUEUE COMPLETE — auto-builder can be disabled", make no further changes, and remind the user to delete the `efmoba-autobuild` scheduled task.

## Log
(auto-builder appends: `- HH:MM YYYY-MM-DD — <item> — <what changed> — <validation> — <files>`)
- 03:10 2026-06-16 — 4.2 — Type-matchup hint in the inspect panel — Phase-4 new feature #2 (clarity, balance-safe). Until now clicking a unit (showInfoPanel, index.html ~1665) showed only name/team/HP/FOV/model — nothing about elemental type effectiveness, even though typeMult (~2596) governs every melee/ranged hit (×1.5 strong / ×0.7 resisted) and the chart is already public on the champion-select screen. Added two PURE helpers just above showInfoPanel (~1654): matchupTag(atk,def) → wraps typeMult into {m,lbl,sym,col} classifying m>1.05='Strong'▲(green) / m<0.95='Weak'▼(red) / else 'Even'—(grey) — boundary-correct (1.5→Strong, 0.7→Weak, exactly 1→Even, and the real combined strong+resist case resolves to typeMult's final 0.7→Weak); and matchupHtml(u) which renders a one-line BOTH-DIRECTIONS readout 'You → <off> · ← it <def>' (off = your hero attacking it, def = it attacking your hero, each via matchupTag) with hard gates: empty unless pHero is live (exists, !dead, has types), the target has types (structures/wild w/o types → no line), and the target is an enemy (u!==pHero && u.team!==pHero.team → no friendly/self matchup). typeMult/matchupTag/matchupHtml are all hoisted function decls so matchupHtml calling typeMult (defined ~900 lines later) at click-time is safe. Wired by appending '+matchupHtml(u)' to showInfoPanel's innerHTML (the only edit to that line; modelInfo untouched). PURE CLARITY — zero stat/economy/AI change: typeMult itself untouched, no combat numbers altered, info is already determinable from the public type chart; PvP-symmetric (same panel for either side, gated on the local pHero). New globals matchupTag/matchupHtml are collision-free (grep: no prior def). No shared code / JSON / renames touched. NOTE: bash mount served the documented stale/TRUNCATED index.html (2564 lines, cut mid-script at ~2318) so full-file node --check was impossible — used Read/Edit for the TRUE file (both edited regions read back balanced) and gated all checks on an isolated region copy per the runbook. — node 20/20 assertions pass (val_4_2_matchup_1781579220.js: matchupTag Strong/Weak/Even classification incl. m==1 boundary + combined strong&resist→Weak; matchupHtml gating for no-hero/dead-hero/typeless-hero/typeless-enemy/wild-no-types-prop/friendly/self/null all → '' , enemy-with-types → non-empty both-direction string with correct ▲▼ symbols + ×mult to 2dp; typeMult regression Fire>Leaf=1.5 & empty→1); browser smoke test PASS — loaded localhost:8000/index.html in Chrome, title 'EtherFantasy MOBA' renders (inline script with the new code parses) with ZERO console errors at load after a tracked reload (in-panel matchup render exercised via node) — index.html
- 02:11 2026-06-16 — 4.1 — Minimap objective timers (boss respawn) — Phase-4 new feature #1 (clarity, balance-safe). The boss-lair system already counted down (`bossZones[].respT`, decremented in `updateBossZones`; lairs drawn as static purple rings in `drawMM` ~2669) but the player got NO timer — you couldn't tell when a slain boss/legendary would return. Added a pure, testable helper `bossLairStatus(z)` (index.html, just above updateBossZones ~1384) → `{active, secs, label}`: ACTIVE when `z.boss&&!z.boss.dead` (label '●'), else a respawn countdown `Math.max(0,Math.ceil(respT))` → 'Ns' (or '…' at 0). drawMM's boss-lair loop now reads it: an ACTIVE lair draws the full-alpha ring + a small filled dot; a RESPAWNING lair draws a DIMMED ring (globalAlpha 0.4, restored to 1) + the countdown seconds centered in the ring (font 'bold 8px sans-serif', textAlign/textBaseline set then RESTORED to left/alphabetic so no canvas text-state bleed — verified the mm context has no other fillText users). Also added a one-shot ~10s heads-up in updateBossZones: when respT crosses 10 (`prev>10 && respT<=10`) it feeds '👑 A boss returns to its lair in ~10s — contest it!' once (no per-frame spam, no new state flag needed). PURE CLARITY: zero stat/economy/AI changes — boss hp/dmg/bounties, the enemy AI's boss-hunt logic (`_bossZone`/`_bossT`), respawn duration (150s) and the existing return-feed all untouched; objective info is neutral (single-player AI opponent; in PvP the lair timer is symmetric public info), so no balance lever. New global `bossLairStatus` is collision-free (grep: no prior def); no shared code / JSON / renames touched. NOTE: bash mount served the documented stale/TRUNCATED index.html (2565 lines, cut mid-script) — used Read/Edit for the TRUE file (intact, 2948 lines, ends clean </script></body></html>) and gated all checks on isolated region copies per the runbook. — node 24/24 assertions pass (val_obj_timers_1781575622.js: active/dead/no-boss/zero/negative/null-zone status classification + ceil label '42.3→43s'; updateBossZones heads-up fires exactly once crossing 10s, no duplicate below 10, no warn while >10, spawnBoss+return-feed at 0 with no spurious 10s warn, active lair left untouched; drawMM block draws countdown text only for respawning lairs, restores canvas textAlign/textBaseline/globalAlpha, mixed-zone independence); browser smoke test PASS — loaded localhost:8000/index.html in Chrome, title 'EtherFantasy MOBA' renders (inline script with the new code parses), ZERO console errors at load after a tracked reload (in-match lair render exercised via node) — index.html
- 01:13 2026-06-16 — 3c — Wire Centaur boss + Goblin wilds (EF Hunt). Mirrored the MOBA's npc/ model wiring into pve.html: until now EF Hunt drew BOTH its wild monsters AND its dungeon Wardens/final boss from the same random `mobN` pool (baseOnly mons, mob0..mob17). Added two slot autoLoads in init() right after the mob-pool loop — `autoLoad('boss','npc/Boss_Centaur_Warrior_Fire.glb',3.1)` and `autoLoad('wild','npc/Mon_Goblin_Gold.glb',3.2)` — exactly like index.html's `autoLoad('boss',…)`/`autoLoad('wild',…)`. NOTE EF-Hunt's autoLoad takes the FULL filename incl. `.glb` (its url-builder is `file.includes('/')?'./'+file:'./pets/'+file`, no extension appended — verified vs the MOBA which strips `.glb` then re-appends), so I passed the complete `npc/…​.glb` paths → url `./npc/…​.glb`. WILD SLOT: `mkMon` now picks `slot=(!tier)?'wild':randomMob` — tier-0 wild-zone monsters (open Wilds, wildTrickle packs, Blood-Moon swarm) wear the Gold Goblin; dungeon mobs (tier≥1) keep their varied baseOnly models for room-to-room variety. BOSS SLOT: `mkBoss` now hard-sets `slot='boss'` (was a random mobN scaled ×2.2) so every Warden + the Hollow King wears the Fire Centaur — one iconic boss silhouette, MOBA-style; kept eliteScale 2.2 (boss height 3.1×2.2≈6.8 ≈ collision h6 + healthbar at h+0.8, so bar stays aligned) and the look() placeholder fallback for the frames before the prefab loads (autoLoad's `units.forEach(u=>u.slot===slot&&applyModel)` re-applies to already-spawned bosses/wilds). PURE COSMETIC/model-wiring — zero stat/balance/economy changes (hp/dmg/range/elite rolls/boss names/WARDEN_BY_TIER untouched); EF Hunt is single-player so no PvP symmetry concern. New prefab keys 'boss'/'wild' don't collide (MOB_SLOTS are 'mobN'; no prior pve unit used those slots). No shared code / JSON / renames touched. NOTE: bash mount served the documented stale/TRUNCATED pve.html (1262 vs true 1885 lines, cut mid-line at threatTier) — used Read/Edit for the true file (intact, ends clean </script></body></html> @1885) and gated edits on isolated region copies per the runbook; npc/ GLBs confirmed present (Centaur 3.3MB, Goblin 1.9MB). — node 24/24 assertions pass (val_3c_npcwire_1781572023.js: wild-tier0→'wild' incl. tier-undefined, dungeon-tier→mobN over 60 draws, elite wild stays goblin, boss/final-boss→'boss' + eliteScale 2.2 + names/flags/3×hp preserved, unknown-tier 'Warden' fallback, prefab-missing→look() placeholder not applyModel, autoLoad npc url './npc/….glb' + late-load re-applies to existing boss & wild units, mobN load leaves boss/wild alone, bare-filename→'./pets/' path unchanged); browser smoke test SKIPPED — local server (localhost:8000) down this run (no browser this run) — pve.html
- 00:14 2026-06-16 — 3b — Per-hero ranged profile (EF Hunt). The §5c note said heroes were already restricted to Irene/Kai/Leah (init's HERO_NAMES filter — VERIFIED still in place, dayHeroes maps exactly those three) and the visible bolt already exists (tryAtk's range>5 branch spawns a hero projectile, sp 60, color 0xffe27a) — what was missing was the per-hero PROFILE itself: every hero booted with the same hardcoded melee range:3. Added a module-level `RANGED_HEROES={Irene:1,Leah:1}` + `HERO_RNG_RANGE=12`/`HERO_MELEE_RANGE=3` + `heroIsRanged(name)` (just below the dayHeroes decl). startRun now reads the chosen hero's name → `range:atkRange` (Irene/Leah 12 = loose bolts; Kai 3 = melee) and stamps `hero.ranged`. Bumped the hero auto-acquire in the main loop from a flat `nearestFoe(hero,11)` to `nearestFoe(hero, hero.range>5?hero.range+1:11)` so ranged heroes engage out to their bolt reach (melee unchanged at 11). Surfaced the profile to the player: each hero-select card now shows a 🏹 Ranged / ⚔ Melee badge, and the in-game 📖 HUNTER'S GUIDE got a new HUNTERS line explaining the three champions' attack styles (DoD layer 2). BALANCE: base dmg is IDENTICAL across profiles — only auto-attack reach differs — and ranged reach 12 < monster-ranged 13 so ranged mobs still poke; daily heroes are mutually exclusive (not a PvP team), so this is pure hero identity, not a power lever. Shared code untouched (pve.html only; no renames, no JSON). NOTE: bash mount served the documented stale/TRUNCATED pve.html (1268 vs true 1875 lines) — used Read/Edit for the true file (intact, ends clean </script></body></html>); all edited regions balanced in-context. — node 23/23 assertions pass (val_3b_heroprofile_1781568418.js: Irene/Leah ranged + Kai/unknown melee classification, range 12/3 selection, acquire-radius range+1 vs 11, ranged tryAtk spawns exactly 1 bolt @sp60 targeting the foe with NO instant dmg, melee tryAtk deals instant dmg with NO projectile, melee can't reach a foe 10 away while ranged can, base-dmg parity); browser smoke test PASS — loaded localhost:8000/pve.html?fast=1 in Chrome: hero cards render 🏹 Irene/Leah + ⚔ Kai, started a run as Irene (hero.range=12, hero.ranged=true), live tryAtk call confirmed ranged→1 bolt(sp60)/no-hit and a melee profile→instant 40 dmg/no-proj, ZERO console errors at load + in-run — pve.html (+ in-game guide)
- 23:30 2026-06-15 — 3a — AoV mobile layout for EF Hunt: rebuilt pve.html's EF_TOUCH.init from the simple flex row into the same Arena-of-Valor CLUSTER as index.html. Buttons now carry `pos`/`size` so ef_touch.js renders the cluster: ⚔ ATK (xl) in the bottom-right corner (efAttack auto-target), a Q/W/E/R skill DIAMOND above it (W top 33vmin, Q@34vmin/E@11vmin mid 21vmin, R/ULT lg bottom 9vmin) firing via efCast(0..3) toward the joystick aim, and a small B(BUILD)/T(TOWN)/S(STOP) trio (sm) to the left. Added cooldown probes mirroring the MOBA's _skCd/_fxCd: `_pkCd(i)` reads hero.kit.abs[i].cd vs hero.abCd[i] and greys out on hero.dead || hero.mp<A.mp; the T button probes the tpT town-portal channel (frac /3); ATK/B/S report only off=!hero||dead — so ef_touch.js draws the radial sweep + seconds + grey-out live. Extras drawer (behind ⋯) is the collapsible help: 📖 guide / 🗺 map / 📜 journal. SHOW/HIDE: added efTouchMatch(on)→EF_TOUCH.show(on); boots hidden at init (efTouchMatch(false)), efTouchMatch(true) at startRun's running=true, efTouchMatch(false) in runComplete + permaDeath (hide on the run-over/permadeath screens). On touch the redundant desktop ability buttons (#hud aQ/aW/aE/aR/aT/aB) are hidden via display:none (HP/MP/XP bars kept). Per AGENTS_PVE DoD also updated the in-game 📖 HUNTER'S GUIDE CONTROLS section with a 📱 mobile-controls paragraph. NOTE: bash mount served the documented stale/TRUNCATED pve.html (1273 vs true ~1850 lines) — used Read/Edit for the true file; all edited regions balanced in-context. — node 44/44 assertions pass (val_3a_pvetouch_1781564819.js: 8-button cluster + every-has-pos, xl/lg/sm sizing, diamond geometry W>Q/E>R + Q-left-of-E, 3-item extras, boots-hidden + desktop-buttons-hidden, cd probes ready/mid-cd/low-mana/dead for Q+ATK+T, R super 30/60, tpT town channel, all onTap wiring efAttack/efCast(0..3)/openBuild/townPortal/cancelAim/openGuide/toggleWmap/toggleQuest, onMove sets mvT + onMoveEnd clears, efTouchMatch show toggle); browser smoke test PASS — loaded localhost:8000/pve.html?touch=1&fast=1 in Chrome, started a run: cluster renders correctly (joystick + ⚔ corner + Q/W/E/R diamond + T/B/S, desktop bar hidden), tapping Q cast (mana 220→192) + showed the cooldown sweep, ATK + T(town channel) fired, ZERO console errors at load and in-run — pve.html (+ in-game guide)
- 22:14 2026-06-15 — 2d — Pet auto-cast AI + permadeath. AUTO-CAST: new petAutoCast(p,dt) (index.html ~1662, just above petAI) ticks a summoned pet's OWN ability cooldowns (p.abCd[]) and regens mana (p.mp +8/s capped at maxMp) — nothing else did this for non-hero units — then fires its UNLOCKED skills on cooldown at the nearest enemy. petTier GATES the kit exactly like the 2c unlocks: maxAb = tier>=3?4:(tier>=2?3:1) → T1 = Q(0) only, T2 = Q/W/E(0,1,2), T3 = +super R(3, charged via superCd()). Foe = current valid target else nearestEnemy(p,p.aggro,false); never casts at kind core/base. ONE cast per frame (break) so it never dumps the whole kit at once (meaningful-not-snowbally), and the abilities are already 0.4× via buildPetKit (PET_KIT_POWER) so dmg stays nerfed. Range gate mirrors the hero AI: W(idx1) rng 8, others rng 12. Wired by a single guarded call at the top of petAI (~1675): `if(p.summoned)petAutoCast(p,dt);` so it runs in EVERY pet state (gather/follow/hold/idle) — combat-casts layer on top of the 2c gather-retaliate/auto-aggro without disturbing them. Enemy never summons pets (petAIenemy path untouched) → player-only, PvP-symmetric. PERMADEATH: the kind==='pet' death branch (~971) no longer blanket-sets respT=15; now `if(u.summoned){feed(…gone for good…);removeUnit(u);} else u.respT=15;` — a summoned pet is spliced from units for good (no respawn) AND frees its MAXPETS/petCount slot, while companion pets (pPet/ePet, which revive with their hero at the hero-respawn handler) and workers keep the legacy respT=15. NOTE: bash mount served the documented stale/TRUNCATED index.html (2563 vs true ~2888 lines) — used Read/Edit for the true file; both edited regions verified balanced in-context. — node 28/28 assertions pass (val_2d_petautocast_1781561220.js: cd tick, mana regen+cap, T1 Q-only, T1 no-W/E/R, T2 Q/W/E ladder + no-R, T3 super-R via superCd, range gating W8<E12, no-cast-at-core/base, insufficient-mana, beyond-aggro, kit-less worker no-op, summoned permadeath removeUnit+no-respT, companion keeps respT=15+stays in array, petCount drops after permadeath); browser smoke test SKIPPED — local server (localhost:8000) down this run (no browser this run) — index.html
- 21:10 2026-06-15 — 2c — Pet tier upgrades: per-pet tiers on summoned pets, driven off the existing p.petTier (set =1 at summonPet in 2b). NEW ⭐ "Upgrade Pet → Tier N" button in the pet menu (buildUnitPanel ~1554) for summoned pets with petTier<3, charging petUpCost(tier)=COST.petUp.g×tier (added COST.petUp={g:150,w:0} @819 → T1→2 150g, T2→3 300g, pure escalating gold sink). upgradePet(p) (~1222, after summonPet): TIER 1→2 evolves to the pet's Form-2 (reuses the f2_<glb> slot + loadGLBData/applyModel like evolveUnit), sets formLevel=2, scale 1.25 (+size), maxHp×1.4/dmg×1.3/range+1, and unlocks Q/W/E + retaliate; if the mon has no Form-2 glb it still tiers up stats-only (no false formLevel). TIER 2→3 scale 1.5, maxHp×1.3/dmg×1.2, unlocks the super R (gated via petTier for the 2d auto-cast) + auto-aggro. Capped at 3; tier-3 pet ≈619hp/37dmg stays UNDER the weakest hero (660hp/48dmg) + only a 0.4× nerfed kit → meaningful-not-snowbally. GATHER-COMBAT behaviours added to petAI gather state (~1635): tier-2 pets RETALIATE vs a recent attacker (new u._hitBy/_hitT recorded in dmgUnit ~909, enemy-only, ≤4s, within aggro+4) and tier-3 pets AUTO-AGGRO nearby enemies (nearestEnemy) while gathering — combat interrupts harvesting but keeps state==='gather' so the pet resumes its node when clear; never targets base/core. Also excluded kind==='pet' from the generic ⬆ Evolve filter so the soldier-evolve path can't collide with the pet-tier system. Summoned-pet/player-menu only (enemy never summons pets; in PvP both human bases use the same menu) → PvP-symmetric. NOTE: bash mount served a stale TRUNCATED index.html (2558 vs true 2888 lines) as documented — used Read/Edit for the true file (intact, ends clean </script></body></html>). — node 38/38 assertions pass (val_2c_pettiers_1781557620.js: cost escalation, tier1→2 & 2→3 stat/scale/cost/formLevel/model, no-Form2 fallback, max-tier & broke blocks, hero-cap balance, retaliate vs auto-aggro tier gating + 4s/range expiry + base/core safety, _hitBy enemy-only/nonzero, panel button labels + worker exclusion, generic-Evolve excludes pets); browser smoke test SKIPPED — Chrome connected but local server (localhost:8000) not running this run — index.html
- 20:08 2026-06-15 — 2b — Pet summon: VERIFIED+RECORDED a complete-but-untracked slice (prior run wrote the code but never ticked/logged it, matching 1d/1e/2a). The base/town-hall menu (openBaseMenu ~1814) now has a "✨ Summon Pet" button SEPARATE from "🐾 Train Worker" (~1813): gold-gated via COST.summonPet={g:180,w:0} (820) — pure gold, 2.6× the 70g worker so it's rarer; canAfford-disabled, petCount()<MAXPETS-capped, charged once via buy(). summonPet(x,z) (~1216) spawns a stronger combat companion (340hp/24dmg vs worker 240hp/15dmg) tagged summoned=true + petTier=1 (the 2c upgrade hook), owned by pHero, follows like a worker, reuses the already-loaded pPet slot model (an upgradeChain/Form-2 mon per 2a — no runtime fetch can fail). It carries a NERFED auto-cast kit: buildPetKit(type) (1104) calls EF_CORE.buildKit(type,prims,PET_KIT_POWER=0.4) — exactly the spec's "EF_CORE.buildKit(type,prims,~0.4)"; confirmed EF.buildKit(type,P,pw) in shared/ef_core.js (186) applies pw as the base-damage multiplier (so pet ability dmg = 0.4× a same-element hero's), with a buildKit() offline fallback if the CDN module is absent. p.kit/p.abs/p.abCd[4]/maxMp:120 ride along for the 2d auto-cast AI; for now the pet just fights with basic attacks + follows (correct 2b scope). Counts toward MAXPETS=32 so it's not an unbounded army; PvP-neutral (player-menu only, no enemy-side change). No game-file edits this run; baseline untouched (true index.html intact at 2888 lines, ends clean </script></body></html>; bash mount served a stale TRUNCATED 2559-line copy as documented — used Read/Grep for the true file). — node 20/20 assertions pass (val_2b_summonpet_1781554083.js: gold-only + costlier-than-worker cost, pet ability dmg == round(0.4×hero) & strictly weaker, summoned/petTier/owner/follow identity, stronger-than-worker stats, kit+abs+abCd+mana wiring, button charges-once / broke-noop / MAXPETS-cap-block); browser smoke test PASS — index.html boots to champion select at localhost:8000 with ZERO console errors (inline script with the 2b code parses) — index.html (verify only), shared/ef_core.js (read), mon_lineage.json (read via 2a)
- 18:10 2026-06-15 — 2a — Roster split: VERIFIED+RECORDED a complete-but-untracked slice (prior run wrote the wiring but never ticked/logged it, like 1d/1e). Both model-assignment paths already implement the split via three selectors over the roster: nextChain() draws from ROSTER.upgradeChains (mons WITH a Form-2, so pets can evolve), nextBase() from ROSTER.baseOnly (Form-1-only line minions), nextAny() from the union. In assignModels() (index.html ~2381, the live champion-select path) heroes=chosen mons, bigminion+minion<-nextBase(), pPet+ePet+melee<-nextChain(), archer<-nextAny(). loadAllModels() (~2512, the Asset-Manager 'load all' path) mirrors it: heroes<-nextChain(), bigminion+minion<-nextBase(), pPet+ePet+melee<-nextChain(), archer<-nextAny(). Consumers line up: mkMinion (1204) uses slot 'minion'/'bigminion'; mkPet (1196)/spawnPeon (1201) use 'pPet'/'ePet' — so minions get baseOnly types and pet-summons get upgradeChain types via slotMon[slot]. Heroes (Irene/Kai/Leah) + worker-rename already done per item notes. Roster data healthy: 26 upgradeChains (Irene first), 80 baseOnly all with glbs (3 chains lack a Form-2 glb — 2b/2c pet-evolution must guard, and evolveUnit already does per AGENTS §7.3). No game-file edits this run; baseline untouched (true index.html intact at 2868 lines, ends clean </script></body></html>; bash mount served a stale TRUNCATED 2563-line copy as documented — used Read/Grep for the true file). — node 2000/2000 roster-split invariants pass across 400 randomized runs of both paths (val_2a_rostersplit_1781546875.js: minion+bigminion always in baseOnly name-set, pPet+ePet+melee always in upgradeChains name-set, heroes assigned, archer always has a glb); 1 extra non-2a data check flagged the 3 Form-2-less chains (informational, not a 2a requirement); browser playtest pending (no edits to smoke-test) — index.html (verify only), mon_lineage.json (read)
- 12:11 2026-06-15 — 1e — Smarter enemy AI: VERIFIED+RECORDED a complete-but-untracked slice (a prior run wrote the code but never ticked/logged it, like 1d). enemyEco(dt) (index.html ~1725) is wired into the main loop (~2722) and gives the red side a symmetric AI economy: it accrues a private budget (E_ECO +2.6g/+1.4w per sec), REPAIRS its most-damaged tower/base/core (worst < 98.5% hp, up to 120hp/tick @ 0.05g+0.03w per hp, never overheals), TRAINS soldiers alternating melee/archer up to E_SOLDIER_CAP=16, and EXPANDS by claiming the free build slot FARTHEST from the player core (so each side takes its own side), clearing a guarding wild camp first (orders up to 3 idle soldiers onto the guard) and gated by E_BASE_CAP=2 + COST.base. Crucially it no-ops when red is a human (eHero.netOwner) and when rCore is dead — so PvP stays symmetric and it's single-player only. The "savingForExpand" guard holds training while an unclaimed slot exists so the AI can actually afford COST.base (anti-stall). Confirmed all deps resolve in the true file (E_ECO/E_SOLDIER_CAP/E_BASE_CAP/enemyArmy/enemyBaseCount at 1720-24; wildCamps 815 w/ {slot,guards,cleared}; slots 851 w/ {used,locked,ring}; checkCamp 1230 unlocks slot.locked on clear; mkBase 1216, mkSoldier 1467). NOTE: bash mount served a stale TRUNCATED index.html (2564 vs true 2868 lines) — used Read/Grep for the true file. No game-file edits this run; baseline untouched. — node 19/19 assertions pass (val_1e_final_1781539673.js: human-red no-op, dead-core stop, budget accrual, repair+cost+overheal-cap, train-vs-save-for-expand, farthest-slot expand + E_BASE_CAP, locked-slot camp-clear); browser playtest pending (no edits to smoke-test) — index.html (verify only)
- 08:10 2026-06-15 — 1d — Store unit upgrades: VERIFIED+RECORDED a complete-but-untracked slice (a prior run wrote the code but never ticked/logged it). Footman/Archer are tagged at mkSoldier with a Form-2 upgradeChain mon via SOLDIER_MON={melee:'Blockid',archer:'Mianari'} + soldierMon(sub) (sets ONLY u.mon, never u.types, so PvP type-effectiveness is unchanged — pure gold+wood sink). Two evolve paths reuse evolveUnit: (1) unit panel ⬆ Evolve button for any selected Form-1 soldiers, (2) base-menu "⬆ Evolve Soldiers ×N" (charges COST.evolve 60g/40w PER soldier, stops when broke). evolveUnit gives bigger model (visual scale 1.5) + stat boost (maxHp×1.5, dmg×1.4, range+1) and loads pets/49_Blockall.glb / pets/43_Surinari.glb. Confirmed both mon names resolve to upgradeChains with valid Form-2 glbs in mon_lineage.json AND the 4 glb assets exist in pets/. No game-file edits this run (code already present); baseline untouched. — node 17/17 assertions pass (val_1d_storeunit_1781510872.js: soldierMon resolution, stat/model/cost on evolve, max-form block, broke-block); browser playtest pending — index.html (verify only), mon_lineage.json (read), pets/ (read)
- 10:10 2026-06-15 — 1c — Town-hall/base upgrade tiers: replaced the uncapped "⬆ Upgrade Defense" with a capped 3-tier system (BASE_MAX_TIER=3) mirroring 1b's tower pattern. New baseUpCost(level)=COST.upgrade×current-tier (150g/90w → 300g/180w, escalating not flat). upgradeBase(b): +900 maxHp/+900 hp per tier, innate-cannon scaling defDmg=28+lvl·14 (base cannon turns ON at tier2: 56→70; core scales from 35→56→70), defRange=18+lvl·3 (24→27), atkSpd=0.8+(lvl-1)·0.15 (faster fire each tier). setBaseTierVisual(b) shows tier via golden emissive torus bands (one per tier above 1, sized to b.h, rebuilt+disposed each upgrade — no leak). openBaseMenu title now "Tier N/3" and the button is hidden/"maxed" at tier 3. Player-menu only & PvP-symmetric; enemy-side base upgrades deferred to 1e. — node 20/20 assertions pass (val_baseup_1c_0615.js); browser playtest pending — index.html
- 09:00 2026-06-15 — 1b — Attack-tower upgrade: click a friendly tower → new openTowerMenu with an ⬆ Upgrade button, gold+wood-gated (COST.towerUp 110g/40w × current tier, so escalating), cap tier 3; each tier +700 maxHp/+700 hp, +35 dmg, +3 range/aggro/fov. Tier shown on the tower via setTowerTierVisual (visual scaled 1+0.14·(tier-1) + one golden emissive torus band per tier above 1, rebuilt & disposed each upgrade — no leak). mkTower inits t.tier=1; leftClickAt routes friendly-tower clicks to the menu (host/SP only, mirrors base/core gating). PvP-symmetric (both players use the same menu); enemy-side tower upgrades deferred to 1e. — node 20/20 assertions pass (val_towerup_1b_0615.js); browser playtest pending — index.html
- 03:11 2026-06-14 — 1a — Worker structure repair: right-click a damaged friendly tower/base/core with workers selected → new pet 'repair' AI state walks to it & heals 120hp/s, charging gold(0.05)+wood(0.03) per hp restored; caps at maxHp, stops when broke/target dead; dig anim while repairing; routing prioritizes gather>repair>move. Enemy-side repair deferred to 1e. — node 19/19 assertions pass (val_repair_1a_0614.js); browser playtest pending — index.html
