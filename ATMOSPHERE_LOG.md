# EF Moba — Atmosphere & Immersion Log

One small, validated, lightweight improvement per run. Newest at bottom.

- 2026-06-30T21:08Z — **Night STARFIELD (day/night × clear-sky).** Added a high, cool additive Points field (`_stars`, STAR_N = 120 desktop / 60 mobile) that emerges ONLY on deep, CLEAR (sunny) nights — a celestial sky layer deliberately distinct from the low warm drifting **fireflies** (which also glow when cloudy, at y 1.2–5.2, drifting/bobbing) and from the twilight ground **mist**. Built ONCE next to the firefly/mist setup: a tiny round cool sprite baked into a 16px radial-gradient canvas (white→`200,220,255`→transparent), scattered high above the arena (`y 58–98`, spread ±170 in xz), `MeshBasicMaterial`-free `PointsMaterial` (color `0xeaf2ff`, size 1.5, AdditiveBlending, `depthWrite=false`), `frustumCulled=false`, opacity 0. In `weatherTick` (reusing the existing `dayN`) a tiny block eases `_starMat.opacity` toward `starF×clear×0.9` where `clear=1` ONLY for sunny (cloudy/rain/storm hide the stars) and `starF=clamp((0.35−dayN)/0.35)` — a DARKER night band than the fireflies' `(0.5−dayN)/0.5`, so stars need a deeper, clearer night than the bugs: at dayN=0.45 (flies already glowing) stars are still fully off, and they reach full brightness only at deep night (dayN→0). While visible the field is anchored to the view (`position.set(camFocus.x,0,camFocus.z)`) and drifts slowly via `rotation.y+=dt*0.006` for a subtle celestial wheel. So a long clear match that reaches deep night quietly fills the upper sky with cool stars, which vanish the moment clouds/rain roll in or dawn returns. No file copied (EF_Audio mount unavailable this run; pure geometry/canvas, no audio). **Perf:** zero new per-frame heap alloc — one BufferGeometry (120 pts) + one 16px canvas sprite + one PointsMaterial built ONCE; per-frame cost is a single opacity ease + (only while visible) one group rotation/position set — NO per-vertex work; fully OFF at the low-FPS tier (`fxLevel>=2` forces target 0 and fades the field out) and `visible=false` whenever opacity≤0.02 so any non-clear / daytime match never touches it. Composes with day/night (shares `dayN`), fireflies (stars high + cool / flies low + warm coexist on a deep clear night), the dawn mist, weather (gated to sunny only), biome fog and storm flash — all untouched, as are rain/puddle/wind-bed/owl/death-fx/footstep/voice/VFX/net. **Validation:** extracted the exact weatherTick star block into `starfield_check.js` with plain stubs, `node --check` + 15 asserts (starts invisible; deep clear night converges ~0.9 & visible; noon clear off; cloudy/rain/storm nights all off; fxLevel≥2 fades out & hides; dayN=0.45 fully off — darker band than flies; dayN=0.175→target 0.45 & converges; no overshoot past 0.9 over 2000 ticks & stays ≤0.9; starF clamped [0,1] across a −0.2→1.2 dayN sweep; visible→anchored to camFocus xz with y=0 + celestial drift applied) all pass.
- 2026-06-30T20:47Z — **Lone night OWL hoot (ambient life × day/night).** Added `sfxOwl()` — a soft, LOW two-to-three note "whoo-hooo" (sine voices ~280–360Hz, each with a gentle downward slide −40/−55/−50, vol .015–.020), deliberately distinct from the existing high cricket trill (4.2kHz) so deep night gains a second, lower ambient layer underneath the crickets. Wired a new `(weather==='sunny'||weather==='cloudy') && step%8===5` branch into the EXISTING `startMusic` interval (a previously-unused step phase: %8===5 is %4===1, so NO collision with the %4===0 birds/crickets/gust ambience or the %4===2 fountain branch, and distinct from %8===4 storm-rumble & %8===6 cloudy distant-thunder). Recomputes `dayN` locally from `gameT`/`DAYLEN` (same sine formula as weatherTick/crickets, no new global) and fires ONLY on deep-fair nights (`dayN<0.3`) at 10% chance — rarer & lower than the crickets (which run from `dayN≤0.45`), so an owl calls out occasionally in the dark on clear/cloudy nights and is silent by day, in rain and in storm. No file copied (EF_Audio mount unavailable this run; pure WebAudio synthesis, reuses tone()). **Perf:** zero new objects/geometry/textures, no per-frame heap alloc or per-frame cost — event-driven `tone()` fired at most once per 8 music steps (~4s) at 10% on deep-fair nights only, 2–3 short sine voices ≤ .020 vol so combat SFX always sit on top; fully gated by `S_MUTE`/`S_VOL`/`AC` via tone() and the `if(!AC||S_MUTE)return` guard. No flash/no per-frame change → composes cleanly with crickets, birds, gusts, fountain, distant-thunder, the storm-bolt system and day/night (shares dayN). Untouched weather-state/biome/mist/firefly/death-fx/footstep/voice/VFX/net. **Validation:** extracted `sfxOwl`+the branch gate into `owl_check.js` with tone/setTimeout stubs + synchronous flush, `node --check` + 19 asserts (muted→silent; no-AC→silent; 2–3 sine voices all low 200–360Hz, all vol ≤.024, all sliding downward; high-rnd→exactly 2 voices; fires only sunny/cloudy + phase 5 + deep-night dayN<0.3 + rnd<.10; storm & rain never fire; wrong phase 4/6 no fire; high rnd no fire; noon dayN=1.0 suppresses; phase 5 is %4===1 with no ambience/fountain collision & distinct from storm/cloudy phases) all pass.
- 2026-06-30T20:28Z — **Dawn/dusk ground mist (day/night × weather ASMR).** Added a single large flat translucent mist sheet (`_mist`, a 170×170 `PlaneGeometry` laid flat at y=1.1) that hugs the ground and fades IN through twilight when the air is calm — distinct from the far-field `scene.fog`/biome haze (that's a scene fog colour; this is a near-ground drifting veil you see patches of). Built ONCE next to the rain/firefly setup: a 64px canvas of 7 soft white radial blobs at randomized alpha, `RepeatWrapping` + `repeat(2,2)` so the texture tiles and scrolls seamlessly, baked into a `MeshBasicMaterial` (color `0xcdd8e6`, transparent, `depthWrite=false`, opacity 0, `renderOrder=3`, `frustumCulled=false`). In `weatherTick` (reusing the existing `dayN`) a tiny block eases `_mistMat.opacity` toward `tw×wMul×0.34` where `tw=max(0,1−|dayN−0.28|/0.28)` is a triangular twilight band peaking at dayN≈0.28 (zero at deep night dayN=0 and by mid-morning dayN=0.56) and `wMul`=1 fair / 0.4 rain / 0 storm. So the arena wears a soft morning ground-mist that builds as dawn climbs (match opens thin at dayN≈0.018), peaks at twilight, burns off by midday, returns at dusk, thins in deep night, washes out in rain and is gone in storm. While visible the sheet is anchored to the view like rain (`position.set(camFocus.x,1.1,camFocus.z)`) and its texture offset scrolls slowly (`offset.x+=dt*0.008`, `offset.y+=dt*0.004`) so the patches drift. Composes with day/night (shares `dayN`), fireflies (night/fair — mist + motes coexist beautifully at twilight), weather, biome fog and storm flash — all untouched. No file copied (EF_Audio mount unavailable this run; pure geometry/canvas, no audio). **Perf:** zero new per-frame heap alloc — one PlaneGeometry + one canvas texture + one MeshBasicMaterial built ONCE; per-frame cost is a single opacity ease + (only while visible) two texture-offset adds + one position.set — NO per-vertex work; fully OFF at the low-FPS tier (`fxLevel>=2` forces target 0) and `visible=false` whenever opacity≤0.02 so a midday/storm match never touches it. **Validation:** extracted the build block + the exact weatherTick mist block into `dawnmist_check.js` with THREE/canvas stubs, `node --check` + 10 asserts (starts invisible/not-visible; frustumCulled off; renderOrder 3; repeat-wrapping & 2×2 tiling; y=1.1; twilight peak target=0.34; midday burns off; deep-night band=0 & no mist; match-open thin ~0.06; storm fully off; rain halves to ~0.136 & converges; fxLevel≥2 disables; converges to 0.34 with no overshoot & stays ≤0.34; texture scrolls x>y & anchored to camFocus xz) all pass.
- 2026-06-30T20:08Z — **Night fireflies (ambient life × day/night).** Added a small additive Points swarm of warm yellow-green motes (`_flies`, FLY_N = 18 desktop / 10 mobile) that only glow as a long match drifts into night AND skies are fair — bugs hide in rain/storm. Built ONCE next to the rain/bolt setup: a soft round glow baked into a 32px radial-gradient canvas sprite (`_flyTex`), scattered over a 120×120 field at y 1.2–5.2, AdditiveBlending, `frustumCulled=false`, opacity 0. In `weatherTick` (reusing the existing `dayN`) a tiny block eases `_flyMat.opacity` toward `nightF×fair×0.8` where `nightF=clamp((0.5−dayN)/0.5)` (motes fade in as light dies, 0 once dayN>0.5) and `fair=1` only for sunny/cloudy; the whole swarm is anchored to the view like rain (`position.set(camFocus.x, sin(perf*0.6)*0.4, camFocus.z)`) and slowly drifts via `rotation.y+=dt*0.05`. So a long game that reaches dusk on a clear night quietly fills with wandering glow-motes around the player, and they vanish the moment it rains or day returns. No file copied (EF_Audio mount unavailable this run; pure geometry, no audio). **Perf:** zero new per-frame heap alloc — one BufferGeometry + one canvas sprite + one PointsMaterial built ONCE; per-frame cost is a single opacity ease + (only while visible) a group rotation/position set — NO per-vertex work; fully OFF at the low-FPS tier (`fxLevel>=2` forces target 0) and `visible=false` when opacity≤0.02 so a daytime/foul match never touches it. Composes with day/night (shares `dayN`), weather (gated to sunny/cloudy), biome fog, storm flash — all untouched, as are rain/puddle/wind-bed/death-fx/footstep/voice/VFX/net. **Validation:** extracted the build stubs + the exact weatherTick fly block into `fireflies_check.js` with THREE/canvas-free stubs, `node --check` + 9 asserts (noon-fair stays invisible; deep-night-fair converges ~0.8 & visible; rain & storm hide; cloudy-night glows; fxLevel≥2 disables; no overshoot past 0.8 over 1000 ticks & in [0,1]; dusk dayN=0.3→~0.32; follows camFocus xz + drifts + y-bob bounded 0.4; dayN>0.5→nightF clamps 0) all pass.
- 2026-06-30T19:47Z — **Biome-tinted fog/haze (landscape variety per match).** Extended the existing per-match biome system (previously ground-only) so the **far-field fog now carries the biome too**: added a 3rd value (fog tint) to each `_BIOMES` row — meadow `0x14202e` (neutral, common case), jungle `0x0f1e16` (green-dark), desert `0x2a2417` (warm dusty), wetland `0x10221f` (murky teal) — plus a `_biomeFog` Color + `_hasBiomeFog` flag + `_BIOMEFOGMIX=0.3`, all built ONCE. `setBiome` now also sets `_biomeFog` and **re-biases the CURRENT weather's `fogTgt`** (`fogTgt.setHex(WMOODS[weather].fog); fogTgt.lerp(_biomeFog,0.3)`) so the arena opens already wearing the biome haze even though weather is picked just before setBiome at match start; `setWeather` applies the same 0.3 bias after its `setHex` so every mid-match weather change keeps the biome tint. Fog fills the whole far screen, so this makes the biome read far more strongly than the ground retint alone — a desert match feels warm/dusty to the horizon, jungle green-dark, wetland murky — while still composing with weather (fog stays a 0.3 blend between the weather fog and the biome tint). No file copied (pure colour math; EF_Audio mount unavailable this run). **Perf:** zero new objects/geometry/textures and **zero per-frame cost** — the existing `fogCol.lerp(fogTgt)` ease (line ~648) and the storm sky-flash lerp are untouched; the only added work is a single `lerp` at match start (setBiome) and one `lerp` per weather change (every 50–120s). Day/night doesn't touch fog colour, so they compose; storm bolt flash still lerps fog/sky toward `_skyFlash` on top. Untouched weather-state/rain/puddle/ground-retint/death-fx/footstep/cricket/bird/fountain/wind-bed/voice/VFX/net. **Validation:** extracted `_BIOMES`+`_biomeFog`+`setBiome`+`setWeather` into `biomefog_check2.js` with a THREE.Color stub, `node --check` + 10 asserts (pre-biome→pure weather fog; meadow→stays neutral; jungle→less blue & green/blue ratio rises; desert→warmer r; biome bias survives a weather change to storm; mix stays strictly between weather & biome colour; unknown biome→meadow neutral fallback; jungle+cloudy→exact 0.3 lerp on g) all pass.
- 2026-06-30T19:30Z — **Continuous low WIND BED (ambient life / weather ASMR).** Added `ensureWindAmb()` — ONE looping low-passed (480Hz) BROWN-noise buffer source (built once on first audio gesture, mirrors the existing `ensureRainAmb` rain-hiss bed) that sits UNDER everything as a soft, deep wind "whoosh". Deliberately distinct from the rain hiss bed (higher 1100Hz lowpass) and from the event-driven `sfxGust` swells: this is a *continuous* atmospheric floor so the arena always feels outdoors/alive even in fair, still weather. Wired its gain ease into `weatherTick` right after the rain-bed line — target `= (storm .05 | rain .03 | cloudy .024 | sunny .008) × (1 + 0.35×(1−dayN))`, eased at `dt*1.5` — so wind swells in foul weather and **rises at night** (reusing the day/night `dayN` already computed two lines above; no new global). Max possible gain .0675 (storm at midnight), very quiet so combat SFX always sit on top. No file copied (EF_Audio mount unavailable this run; pure WebAudio synthesis). **Perf:** zero new geometry/textures, no per-frame heap alloc — a single BufferSource+lowpass+gain built ONCE; per-frame cost is one `if(_windGain)` gain ease (a few math ops) sharing the existing `AC&&!S_MUTE` weatherTick block; routes through `sfxG` so the 🔊 mute/volume slider owns it (mute zeroes the master → bed silent), and the ease is itself gated by `!S_MUTE`. Composes cleanly with the rain bed, the gust/bird/cricket event ambience, the storm bolt system, and day/night (shares dayN). Untouched weather-state/rain-bed/biome/death-fx/footstep/voice/VFX/net. **Validation:** extracted `ensureWindAmb`+the weatherTick ease into `windbed_check.js` with WebAudio node stubs, `node --check` + asserts (no-AC→not built; muted→not built/updated; built exactly once over 50 ticks; gain eases monotonically toward target & never overshoots over 400 ticks, converges to storm target; weather ordering storm>rain>cloudy>sunny at fixed dayN; night windier than day; cloudy noon==.024 & midnight==.024×1.35; all targets ≤.07 across weathers & all dayN, max .0675; source loops; gain starts at 0) all pass.
- 2026-06-30T19:08Z — **Weather-CHANGE audio cue (weather ASMR).** Added `sfxWeatherShift(w)` — when the sky actually shifts MID-MATCH you now HEAR it, not just read the feed line. The cue's character matches the NEW weather: **storm** = a strong rising gust (`noiseHit(1.1,.06)` + a trailing `noiseHit(.7,.045)` at 380ms) then a far herald rumble (`sfxDistantThunder` at 720ms — the storm is coming); **rain** = `sfxGust()` wind swell + 4 sparse high "first-patter" sine ticks (2200–2900Hz, vol .012, 70ms apart starting at 220ms); **cloudy** = a low darkening wind (two soft noise swells, .04/.028); **sunny** = a gentle wind-down (`noiseHit(.55,.03)`) + a `sfxBird` lift at 240ms (skies clearing). Wired into the EXISTING `weatherTick` transition branch (line ~632): captured the picked weather into `nw`, then `setWeather(nw);sfxWeatherShift(nw)`. Fires ONLY on a real in-match weather change (the `weatherT<=0` branch, every 50–120s at most) — never at match start (audio isn't unlocked yet, and the branch doesn't run on frame 0). Previously a weather change was silent except for the blue feed text; now the transition has a signature you can hear coming. No file copied (EF_Audio mount unavailable this run; pure WebAudio synthesis, reuses sfxGust/sfxBird/sfxDistantThunder). **Perf:** zero new objects/geometry/textures, no per-frame heap alloc or per-frame cost — event-driven `tone()`/`noiseHit()` fired at most once per 50–120s from the already-running weather transition; all tone voices ≤ .024 vol so combat SFX always sit on top; fully gated by `S_MUTE`/`S_VOL`/`AC` via tone()/noiseHit() and the `if(!AC||S_MUTE)return` guard. Composes cleanly with the storm-bolt flash+crack (storm still owns those) and the cloudy distant-thunder loop. Untouched weather-state/day-night/biome/death-fx/footstep/cricket/bird/fountain/voice/VFX/net. **Validation:** extracted `sfxWeatherShift`+the transition wiring into `wshift_check.js` with tone/noiseHit/sfxGust/sfxBird/sfxDistantThunder stubs and a synchronous setTimeout flush, `node --check` + 11 asserts (muted→silent; no-AC→silent; storm→herald 46Hz thunder + ≥3 noise swells; rain→exactly 4 high ticks + gust; cloudy→noise-only, exactly 2 swells; sunny→bird lift + wind-down; every tone volume ≤ .12 across all four weathers) all pass.
- 2026-06-30T18:44Z — **Distant pre-storm thunder for CLOUDY weather (weather ASMR).** Added `sfxDistantThunder()` — a soft, ALWAYS-FAR rumble with NO flash and NO sharp crack (deliberately distinct from the storm-bolt `sfxThunder`): a deep 46Hz sub swell (vol .075) + a long soft noise roll (1.0s, .05) + a trailing roll (260ms later) + a fading 60Hz after-rumble (520ms later) — every voice quieter than a storm bolt (.13–.2) so combat SFX always sit on top. Wired a new `weather==='cloudy' && step%8===6 && rnd<.12` branch into the EXISTING `startMusic` interval (a previously-unused step phase, so no collision with pads/plucks/storm-rumble/ambience/fountain branches). Cloudy weather previously shared birds/crickets with sunny and had no signature of its own; now gathering clouds audibly threaten an approaching storm before the rain/lightning arrives. No file copied (EF_Audio mount unavailable this run; pure WebAudio synthesis). **Perf:** zero new objects/geometry/textures, no per-frame heap alloc or per-frame cost — event-driven `tone()`/`noiseHit()` fired only ~once per 8 steps at 12% chance while cloudy, from the already-running music interval; fully gated by `S_MUTE`/`S_VOL` via tone()/noiseHit(). No flash/no per-frame weatherTick change → composes cleanly with the storm bolt system (storm still owns the flash+near crack). Untouched weather-state/day-night/biome/death-fx/footstep/cricket/bird/gust/fountain/voice/VFX/net. **Validation:** extracted `sfxDistantThunder`+branch gate into `distant_thunder_check.js`, `node --check` + asserts (muted→silent; audible→1 immediate tone+1 noise then 2+2 after delays; all volumes ≤ storm bolt; all tones low sine ≤70Hz with no 150Hz square crack; branch fires only cloudy+step%8===6+rnd<.12, suppressed for storm/sunny/wrong-step/high-rnd; step 14 also fires; .12 threshold exact; phase 6 collides with no existing music branch) all pass.
- 2026-06-30T18:40Z — **Fountain water trickle (ambient life × proximity).** Added `sfxFountain()` — a soft burbling spring: a faint water-hiss bed (`noiseHit(.18,.012)`) + 3–5 randomized droplet "plip" sines (620–1140Hz, ±detune, vol .012), all very quiet so combat SFX sit on top — and wired a new `step%4===2` branch into the EXISTING `startMusic` ambience interval that fires it (60% chance) ONLY when the view (`camFocus`) sits within r≈38 (`_fd<1444`, squared dist) of either spawn `FOUNTAINS`. So your home fountain / spawn-heal zone audibly trickles when you're camped there or hovering the camera over it, and goes quiet midfield. No file copied (EF_Audio mount unavailable this run; pure WebAudio synthesis). **Perf:** zero new objects/geometry/textures, no per-frame heap alloc or per-frame cost — the proximity check is a 2-fountain squared-distance loop run from the existing music interval (~once per 4 steps), event-driven `tone()`/`noiseHit()` only; `typeof FOUNTAINS` guard for load order; fully gated by `S_MUTE`/`S_VOL` via tone()/noiseHit(). Untouched weather/day-night/biome/death-fx/footstep/cricket/bird/gust/voice/VFX/net. **Validation:** extracted `sfxFountain`+proximity gate into `fountain_check.js`, `node --check` + asserts (muted→silent, one hiss bed + 3–5 droplets each→one tone, near at fountain centre & 30u, NOT near at 50u & midfield, step%4===2 fires 100/400) all pass.
- 2026-06-30T18:28Z — **Biome variety per match (ground retint).** Added a `_BIOMES` table (meadow / jungle / desert / wetland, each a dry+wet ground colour pair) and `setBiome(b)`, called once at match start in `startGame` after the random-weather pick. Instead of touching the ground material directly, `setBiome` **mutates the shared `_gDry`/`_gWet` Color objects in place**, so weatherTick's existing per-frame `ground.material.color.lerp(_gScratch,…)` eases the whole field toward the new biome over ~1s for free and still composes correctly with the wet-weather darkening (rain blends dry→wet of the *current* biome). Match opens in a random biome (meadow weighted 2× so the AoV lawn stays the common case; jungle=darker green, desert=warm tan, wetland=murky blue-green). No file copied (pure colour retint; EF_Audio mount unavailable this run). Scope kept to ground only (scatter/fog retint skipped this run — scatter colours are baked into instanced attributes at load and re-tinting them is not "a handful of objects"; ground fills the screen so it carries the biome read). **Perf:** zero new objects/geometry/textures, no per-frame heap alloc — the table + function are built ONCE, `setBiome` runs only at match start (a couple of `setHex` calls), and the per-frame cost is the *already-existing* ground lerp (unchanged). fxLevel-agnostic (no FX added). Untouched weather/day-night/rain/puddle/death-fx/footstep/voice/VFX/net. **Validation:** extracted `_BIOMES`+`setBiome`+the weatherTick blend line into `biome_check.js` with a THREE.Color stub, `node --check` + 9 asserts (unknown→meadow fallback, desert warm r>g>b, jungle wet darker & green-dominant, all four dry colours distinct, rain=0→dry base / rain full→wet base of the active biome, in-place mutation keeps stable refs) all pass.
- 2026-06-30T18:08Z — **Wet footstep splash (weather ASMR × movement).** Added `sfxSplash()` (a soft wet slap `noiseHit(.07,.05)` + a tiny water "plip" sine + a 38ms-delayed trailing droplet, all very quiet so combat SFX sit on top) and a new `tickWetSteps(dt)` wired after `tickDeathFx` in the main loop. While the PLAYER hero is actually `_moving` in `rain`/`storm` weather, it fires a splash at a footfall cadence paced by its own countdown timer (`_stepT`: ~0.34s in rain, ~0.30s in storm, +0–0.06s jitter) — so the wet ground audibly squelches under you only when you walk in the wet. No file copied (pure WebAudio synthesis; EF_Audio mount reuse unnecessary). **Perf:** zero new objects/geometry/textures, no per-frame heap alloc; idle fast-path returns immediately on the low-FPS tier (`fxLevel>=2`), in dry weather, or when the hero is still/dead (`_stepT` reset to 0). Only fires a handful of short pooled `tone`/`noiseHit` voices per second while moving in rain. Fully gated by `S_MUTE`/`S_VOL` via tone()/noiseHit(). Guests never reach this branch (netGuestFrame returns earlier) → harmless cosmetic divergence, same as the existing hurt-thud. Untouched weather/day-night/death-fx/voice/VFX. **Validation:** extracted tickWetSteps+sfxSplash gating to `wetsteps_check.js`, `node --check` + 8 asserts (dry=silent, rain~10s≈27 steps, still-hero silent+timer-reset, dead-hero silent, low-FPS-tier silent, storm cadence ≥ rain, muted silent, null-hero guard) all pass.
- 2026-06-30T17:48Z — **Dramatic hero death (sink + shrink + topple collapse).** Replaced the instant `grp.visible=false` on hero death (line ~2149) with `heroSink(u)`: the fallen hero now SINKS into the ground (~2.3u over 0.7s) while shrinking (1→0.3×) and toppling sideways (rotation.z +0.7) before hiding — a readable AoV-style collapse beat layered on the existing `fxDeathBurst`. Unit is set `.dead` the same frame (all game/AI/net logic still ignores it instantly), so this is purely a lingering cosmetic. Driven by a new `tickDeathFx(dt)` wired after `weatherTick` in the main loop. No file copied (pure transform animation). **Perf:** zero new objects/geometry/textures; `_deathFx`/`_SINKDUR` built ONCE; per-frame cost is a single `if(!_deathFx.length)return` guard (zero) unless a hero is mid-collapse (a handful per game, ≤~0.7s each); fxLevel>=2 (low-FPS tier) falls back to the plain instant hide. Self-restores grp y/scale/rotation (incl. non-zero bases) so respawn (respT≥6s ≫ 0.7s) is always clean; early-revive path also restores + dequeues. Untouched weather/audio/voice/VFX/respawn/net. **Validation:** extracted heroSink+tickDeathFx to `herosink_check.js`, `node --check` + 7 asserts (visible-then-hidden-then-restored, sinking/shrinking/toppling mid-collapse, early-revive restore+dequeue, low-FPS plain-hide+not-queued, no double-queue, empty fast-path, non-zero base scale/rot preserved) all pass.
- 2026-06-30T17:30Z — **Night-time cricket chorus (ambient life × day/night).** Added `sfxCricket()` (a soft 3–4 pip high-sine trill, ~4.2–5.1kHz, vol .013) and wired the existing `startMusic` ambience branch to the day/night cycle: in fair weather it now fires daytime **birds** when `dayN>0.45` and night **crickets** when below — `dayN` recomputed locally in the music loop from `gameT`/`DAYLEN` (same formula as `weatherTick`, no new global). Foul weather still gets wind gusts. No file copied (EF_Audio mount unavailable this run; pure WebAudio synthesis). **Perf:** zero new objects, no per-frame cost — one extra `Math.sin` every 4 music steps (~2s) and an event-driven `tone()` train; fully gated by `S_MUTE`. Untouched weather/bird/gust/voice/VFX. **Validation:** extracted the ambience branch + cricket pip logic to `cricket_check.js`, `node --check` + asserts (match opens at night→crickets eligible; noon `dayN≈1`→birds; deep night `dayN≈0`→crickets; `dayN∈[0,1]` across 0–3000s; storm fires only gust; above-threshold rnd suppresses cricket) all pass.
- 2026-06-30T21:30Z — **Rare SHOOTING STAR / meteor streak (night sky × clear weather).** Added a single occasional meteor that streaks across the sky on DEEP CLEAR nights, layered on the existing `_stars` starfield. Built ONCE next to the starfield: `_shoot`, a 2-vertex `THREE.Line` (head + trailing tail) with an additive `LineBasicMaterial` (color `0xcfe2ff`, opacity 0, `depthWrite=false`, `frustumCulled=false`, hidden), plus reused scratch `_shootHead`/`_shootVel` `Vector3`s and `_shootT` (active timer) / `_shootCD` (cooldown) scalars. A new block in `weatherTick`, gated entirely behind `if(_stars.visible)` (so it ONLY runs on the same deep-clear-night condition that shows the stars, and is fully OFF in daytime/cloud/rain/storm AND at the low-FPS tier where `_stars.visible` is already false), runs a cooldown (`10–26s`) then spawns a meteor: random high start (`y 80–95`, ±200 in xz around `camFocus`), random horizontal heading at `120–180 u/s` with a downward `15–40 u/s` drift, life `0.7–1.0s`. Each active frame the head advances (`addScaledVector`), the tail is written `vel×0.18` behind it (the streak), opacity = `min(1,_shootT/0.2)×0.9×_starMat.opacity` so it fades out in the final 0.2s and is NEVER brighter than the ambient starfield. When the starfield hides mid-flight (dawn breaks / clouds roll in) the meteor is force-cleared so no orphan streak survives into daytime. No file copied (EF_Audio mount unavailable this run; pure geometry, no audio). **Perf:** zero new per-frame heap alloc — one 2-vertex BufferGeometry + one LineBasicMaterial + two Vector3 scratch built ONCE; idle cost while stars are up is a single `_shootCD-=dt` + compare; while active (rare, <1s, a few times per long clear night) it writes 6 floats + one opacity calc — NO per-vertex loop, NO new objects; the whole block is skipped entirely whenever `_stars.visible` is false (daytime, foul weather, or `fxLevel>=2`). Composes with the starfield (shares its night/clear gate and `_starMat.opacity`), fireflies, mist, day/night, weather, biome fog, storm flash — all untouched, as are rain/puddle/wind-bed/owl/death-fx/footstep/voice/VFX/net. **Validation:** extracted the build + the exact weatherTick shooting-star block into `shootstar_check.js` with Vector3/geo/material stubs, `node --check` + 15 asserts (idle keeps line hidden & decrements cooldown; meteor spawns after cooldown with 0.7–1.0s life, high in sky, travelling downward; visible & moving across sky during flight; peak opacity ≤ 0.9×star ambience; ends → line hidden & cooldown re-armed 10–26s; tail trails head by vel×0.18; brightness scales with `_starMat.opacity`; fades out in final 0.2s; meteor force-cleared the frame the starfield hides; no spawn while starfield hidden) all pass.
- 2026-06-30T21:48Z — **Gusty-shower rain envelope (weather ASMR — rain comes & goes).** Weather is a discrete state (sunny/cloudy/rain/storm), so within a rain or storm SPELL the downpour was a flat, unchanging curtain. Per the task's "rain SHOULD come and go over time" hint, added `rainEnv` in `weatherTick` — a slow intensity envelope that makes the rain **swell and ebb within a spell** (gusty showers passing through) instead of a constant sheet: `rainEnv=(M.rain>0)?clamp(0.5+0.32·sin(gameT·0.11)+0.18·sin(gameT·0.047+1.7), 0.3, 1):1`. Two out-of-phase sines (periods ≈57s & ≈134s) break the obvious periodicity so the lulls/peaks don't feel mechanical; clamped to [0.3,1] so rain NEVER fully vanishes while the sky still reads "rain" (avoids looking broken). Drives only the **visible rain density** (`rainMat.opacity` target → `M.rain*rainEnv`) and the **rain-hiss bed** (`_rainGain` target → `M.rain*rainEnv*0.14`), so you both see and hear the shower build and pass. Ground wetness, puddles and the wind bed deliberately stay on raw `M.rain` so the ground stays wet through a lull (realistic — the field doesn't dry out between gusts). Dry weather (`M.rain===0`) sets `rainEnv=1`, leaving the sunny/cloudy targets bit-for-bit unchanged. No file copied (EF_Audio mount unavailable this run; pure math, no audio/objects). **Perf:** zero new objects/geometry/textures, zero per-frame heap alloc — adds just **2 `Math.sin` + a clamp per frame** inside the already-running weatherTick (and only matters while raining; in dry weather it's a constant 1 that changes nothing); the rain particle pool, puddle InstancedMesh, hiss bed and storm-bolt system are all untouched, as is `fxLevel` auto-degrade (rain still sheds first under low FPS). Composes with day/night, biome fog, mist, fireflies, starfield, shooting-star, wind bed, weather-shift cue, distant-thunder and the storm flash+crack — all untouched, as are death-fx/footstep/owl/voice/VFX/net. **Validation:** extracted `rainEnv` into `rainenv_check.js`, `node --check` + 21339 asserts (dry weather→env exactly 1; during rain env always within [0.3,1] across a 5000s sweep; env genuinely dips toward the 0.30 floor AND reaches ~0.999 full downpour; swells/ebbs with spread 0.70 within a 200s window so rain visibly comes & goes; effective visual target `M.rain·env` never exceeds the weather cap; hiss target `M.rain·env·0.14` bounded ≥0; envelope smooth frame-to-frame, max per-50ms step <0.01 so no visible jumps) all pass.
- 2026-06-30T22:30Z — **Per-biome ambient AUDIO signature (landscape variety × ambient life).** The per-match biome system was visual-only (ground retint + fog tint); now each biome has its OWN VOICE on top of the shared birds/crickets/gusts. Exposed the chosen biome as a new `_curBiome` global (set inside `setBiome`, defaulting `'meadow'`, unknown→`'meadow'`) and added `sfxBiomeAmb(b)` — **wetland** = a low buzzy frog croak (sawtooth double-pulse ~140–195Hz + a 40% third croak, vol ≤.022), **jungle** = a single sustained daytime cicada buzz (one sawtooth ~2500–2800Hz, dur .45–.65, vol .009 — deliberately BELOW and distinct from the high NIGHT cricket pip train at 4.2–5.1kHz), **desert** = a lone distant hawk screech (1–2 descending sawtooth voices ~1380–1600Hz sliding −480/−520, vol ≤.015); **meadow stays silent here** (it already owns the birds). Wired a new `step%8===3` branch (a previously-unused phase — no collision with the %4===0 birds/crickets/gust, %4===2 pluck/fountain, %8===4 storm-rumble, %8===5 owl, %8===6 cloudy-thunder, %16 pads/%2 plucks) into the EXISTING `startMusic` interval, gated `_curBiome!=='meadow' && weather!=='storm'` (storm keeps its own bolt/rumble voice). Day/night-aware via the same local `dayN` formula used by the crickets/owl: frogs croak day AND night near water (22% chance), cicadas only in daylight (`dayN>0.4`, 20%), the hawk only in bright daylight (`dayN>0.45`, 10% — rarer). So a jungle match audibly buzzes with cicadas by day, a wetland match croaks with frogs around the clock, a desert match gets the occasional far hawk cry, and a meadow match is unchanged. No file copied (EF_Audio mount unavailable this run; pure WebAudio synthesis, reuses tone()). **Perf:** zero new objects/geometry/textures, no per-frame heap alloc or per-frame cost — event-driven `tone()` fired at most once per 8 music steps (~4s) at 10–22% from the already-running music interval; every voice ≤.022 vol so combat SFX always sit on top; fully gated by `S_MUTE`/`S_VOL`/`AC` via tone() and the `if(!AC||S_MUTE)return` guard; meadow & storm short-circuit before any work. No flash/no per-frame change → composes cleanly with the biome ground/fog tint, birds/crickets/owl/gust/fountain/distant-thunder/wind-bed ambience, day/night (shares dayN), the storm bolt system and all the visual fields (fireflies/mist/stars/shooting-star/dust). Untouched weather-state/death-fx/footstep/voice/VFX/net. **Validation:** extracted `sfxBiomeAmb`+the branch gate into `biomeamb_check.js` with tone/setTimeout stubs + synchronous flush, `node --check` + 1221 asserts (muted→silent; no-AC→silent; frog 2–3 sawtooth voices all <210Hz & vol ≤.024; cicada exactly 1 sustained sawtooth voice 2500–2900Hz dur ≥.45 vol ≤.012 & below the 4kHz cricket band; hawk 1–2 descending sawtooth voices vol ≤.016; meadow→silent over a full 64-step sweep; storm→silent even in a real biome; fires ONLY on step phase 3; jungle silent at deep night dayN<0.3 but fires at noon; wetland fires day AND night; desert provably rarer than jungle over 20k trials) all pass.
- 2026-06-30T22:10Z — **Daytime sun-lit DUST / POLLEN motes (day/night × clear-sky).** Filled the previously bare BRIGHT-DAYLIGHT sky: every prior drifting Points field (fireflies, starfield, mist, shooting-star) is night/twilight-gated, so a long match at high noon had no airborne life. Added `_dust`, a low warm-gold additive Points field (`DUST_N`=22 desktop / 12 mobile) of sun-lit motes that emerge ONLY in bright daylight under CLEAR (sunny) skies — the explicit daytime counterpart to the night fireflies. Built ONCE next to the starfield/shooting-star setup: a soft warm sprite baked into a 16px radial-gradient canvas (white→`255,231,180`→transparent), scattered low over the arena (`y 0.8–6.2`, ±55 in xz), `PointsMaterial` (color `0xffe9c2`, size 0.85, AdditiveBlending, `depthWrite=false`), `frustumCulled=false`, opacity 0. In `weatherTick` (reusing the existing `dayN`) a tiny block eases `_dustMat.opacity` toward `dayF×sunny×0.5` where `sunny=1` ONLY for sunny weather (cloudy/rain/storm hide the dust) and `dayF=clamp((dayN−0.55)/0.45)` — a band that needs a HIGHER sun than the fireflies' `(0.5−dayN)/0.5` night band, so the two fields are **provably mutually exclusive** (fireflies need dayN<0.5, dust needs dayN>0.55 — they NEVER co-render) and dust is off through dawn/twilight, reaching full only toward midday. While visible the field is anchored to the view (`position.set(camFocus.x, sin(perf*0.4)*0.3, camFocus.z)`) with a gentle y-bob and drifts via `rotation.y+=dt*0.02`. So a long clear daytime match quietly fills the air with warm drifting motes that fade out as evening falls (handing off to fireflies, then stars) and vanish the moment clouds/rain roll in. No file copied (EF_Audio mount unavailable this run; pure geometry/canvas, no audio). **Perf:** zero new per-frame heap alloc — one BufferGeometry (22 pts) + one 16px canvas sprite + one PointsMaterial built ONCE; per-frame cost is a single opacity ease + (only while visible) one group rotation/position set — NO per-vertex work; fully OFF at the low-FPS tier (`fxLevel>=2` forces target 0 & fades out) and `visible=false` whenever opacity≤0.02 so any night / non-sunny / low-FPS match never touches it. Composes with day/night (shares `dayN`), fireflies (mutually exclusive day/night bands), starfield/shooting-star (clear-night only), dawn mist, weather (gated to sunny), biome fog and storm flash — all untouched, as are rain/puddle/wind-bed/owl/death-fx/footstep/voice/VFX/net. **Validation:** extracted the weatherTick dust block into `dustmotes_check.js` with plain stubs, `node --check` + 16 asserts (starts invisible; sunny midday converges ~0.5 & visible; sunny night off; dayN=0.55 boundary dayF=0 & off; cloudy/rain/storm all off; fxLevel≥2 fades out & hides; no overshoot past 0.5 over 3000 ticks & stays [0,0.5]; dayF clamped [0,1] across −0.2→1.2 sweep; PROVEN no fireflies/dust overlap band across full dayN; dayN=0.775→dayF 0.5→target 0.25; visible→anchored to camFocus xz with drift) all pass.
- 2026-06-30T22:47Z — **Night MOON disc (day/night × clear-sky).** Gave the night sky an anchored focal body to complement the scattered `_stars` Points field: added `_moon`, a single soft billboarded `THREE.Sprite` that rises on FAIR nights. Built ONCE next to the starfield/shooting-star/dust setup: a 64px canvas moon baked into a radial gradient (white core → pale `244,246,236` → cool rim `200,214,245` → transparent) with three faint `source-atop` craters, into a `SpriteMaterial` (`transparent`, `depthWrite:false`, `fog:false` so the dark night fog never tints it, opacity 0), `scale(16,16,1)`, `renderOrder=2`, hidden. In `weatherTick` (reusing the existing `dayN`) a tiny block eases `_moonMat.opacity` toward `moonF×fairM×0.85` where `fairM`=1 sunny / 0.45 cloudy / 0 rain & storm (the moon shows through thin cloud, dimmed, but is gone in rain/storm) and `moonF=clamp((0.45−dayN)/0.45)` — a SHALLOWER night band than the deep starfield's 0.35, so the moon **rises a touch earlier than the stars** (at dayN=0.4 the moon already glows while `_stars` are still fully off) and reaches full only deep in the night. While visible it hangs high in the sky anchored to the view (`position.set(camFocus.x+70, 74, camFocus.z−50)`) so it sits in the upper sky and tracks the player like an infinitely-distant body. So a long fair match that drifts into night gains a single bright moon over the arena (full & crisp on a clear night, softer through clouds) that fades out at dawn and vanishes the moment rain/storm rolls in. No file copied (EF_Audio mount unavailable this run; pure geometry/canvas, no audio). **Perf:** zero new per-frame heap alloc — one Sprite + one 64px canvas texture + one SpriteMaterial built ONCE; per-frame cost is a single opacity ease + (only while visible) one position.set — NO per-vertex work, NO geometry; fully OFF at the low-FPS tier (`fxLevel>=2` forces target 0 & fades it out) and `visible=false` whenever opacity≤0.02 so any daytime / rain / storm / low-FPS match never touches it. Composes with the starfield (moon = anchored disc, stars = drifting field; both share the clear-night gate, moon a touch earlier), shooting-star, fireflies (low warm motes vs high cool moon), mist, dust, day/night (shares `dayN`), weather (gated to sunny/cloudy), biome fog and storm flash — all untouched, as are rain/puddle/wind-bed/owl/death-fx/footstep/voice/VFX/net. **Validation:** extracted the exact weatherTick moon block into `moon_check.js` with plain stubs, `node --check` + 12+ asserts (starts invisible; deep sunny night converges ~0.85×band & visible; sunny noon off; dayN=0.45 boundary off; cloudy night dimmed to exactly 0.45× sunny & visible; rain & storm nights off; fxLevel≥2 fades out & hides; no overshoot past 0.85 over 5000 ticks & non-negative; moonF clamped [0,1] across −0.2→1.2 sweep; PROVEN moon rises before stars — up at dayN=0.4 while starF=0; visible→anchored to camFocus+sky offset y=74; dayN=0.225→target 0.425) all pass.
- 2026-06-30T23:30Z — **Drifting daytime CLOUDS (day/night × fair-sky).** Filled the bare daytime sky (which only had the sun disc + dust motes) with a small pool of soft billboarded cloud puffs that drift across the upper sky by day — the daytime counterpart to the night moon/stars. Built ONCE next to the sun/moon/dust setup: `CLOUD_N`=5 desktop / 3 mobile `THREE.Sprite`s sharing ONE 128px canvas (9 soft white radial blobs baked into a puffy mass) + ONE `SpriteMaterial` (`transparent`, `depthWrite:false`, `fog:false` so day fog never tints them, opacity 0), each sprite scaled wide & flat (`sc×0.55` height, sc 34–64), `renderOrder=3` (so wisps pass in FRONT of the renderOrder-2 sun), hidden; each puff carries a per-cloud offset `{x,y:52–74,z:−30..−150,v:3–7}` in `_cloudOff`. In `weatherTick` (reusing the existing `dayN`) a tiny block eases the shared `_cloudMat.opacity` toward `cloudW×cloudD×0.6` where `cloudW`=1 cloudy / 0.32 sunny / 0 rain & storm (fullest under cloud, a few wisps in full sun, gone in foul weather) and `cloudD=clamp((dayN−0.32)/0.4)` — a DAYTIME band (off below dayN 0.32) so clouds are a daytime feature and the night sky stays clean for the moon/stars (clouds & stars never co-render: stars need dayN<0.35 deep night, clouds need dayN>0.32 climbing day, and clouds are gated to sunny/cloudy where the deep starfield is sunny-only). While visible each puff drifts on the wind (`o.x+=o.v×dt`, wrapping `o.x−=280` past +140) and is anchored to the view (`position.set(camFocus.x+o.x,o.y,camFocus.z+o.z)`) so the clouds track the player like distant sky. So a long fair daytime match gains a few slow clouds sliding across the sky — a full deck under overcast (cloudy), a couple of wisps under clear sun — that fade out at dusk (handing the sky to the moon, then the stars) and vanish the moment rain/storm rolls in. No file copied (EF_Audio mount unavailable this run; pure geometry/canvas, no audio). **Perf:** zero new per-frame heap alloc — ≤5 Sprites + one 128px canvas + one shared SpriteMaterial built ONCE; per-frame cost is a single shared opacity ease + a short ≤5-iteration loop (one `+=` & one `position.set` per visible puff, just `visible=false` when hidden) — NO per-vertex work, NO geometry; fully OFF at the low-FPS tier (`fxLevel>=2` forces target 0 & fades them out) so any night / rain / storm / low-FPS match never positions them. Composes with the sun disc (clouds renderOrder 3 in front of the sun's 2), moon/stars/shooting-star (clear-night only, provably non-overlapping day band), fireflies, dust, mist, day/night (shares `dayN`), weather (gated sunny/cloudy), biome fog and storm flash — all untouched, as are rain/puddle/wind-bed/owl/footstep/biome-ambient/death-fx/voice/VFX/net. **Validation:** extracted the build counts + the exact weatherTick cloud block into `clouds_check.js` with plain stubs, `node --check` + asserts (desktop 5 / mobile 3, built hidden renderOrder 3; starts at 0; cloudy midday converges ~0.6 & visible; sunny midday faint ~0.192 & visible; night off & hidden both weathers; dayN=0.32 boundary off; rain & storm off & hidden; fxLevel≥2 fades out & hides; no overshoot past 0.6 over 6000 ticks & non-negative; cloudD clamped [0,1] across −0.2→1.2 sweep; dayN=0.52 cloudy→0.3; clouds drift on x, wrap, & stay anchored to camFocus; x bounded over a 20k-tick run) all pass.
- 2026-06-30T23:10Z — **Daytime SUN disc (day/night × clear-sky).** Gave the bright daytime sky an anchored focal body to complement the night `_moon` (a disc) and `_stars` (a field): added `_sunSp`, a single soft billboarded warm `THREE.Sprite` that hangs high in the sky during the day on clear/light-cloud skies. Built ONCE next to the moon/dust setup: a 64px canvas sun baked into a radial gradient (white core `255,255,245` → warm `255,246,206` → amber `255,212,132` → transparent `255,180,90`), into a `SpriteMaterial` (`transparent`, `depthWrite:false`, `fog:false` so the day fog never tints it, `AdditiveBlending`, opacity 0), `scale(22,22,1)`, `renderOrder=2`, hidden. In `weatherTick` (reusing the existing `dayN`) a tiny block eases `_sunMat.opacity` toward `sunF×fairS×0.7` where `fairS`=1 sunny / 0.3 cloudy (a hazy sun through thin cloud) / 0 rain & storm, and `sunF=clamp((dayN−0.5)/0.3)` — a band that needs daytime PAST the moon's 0.45 night band, so the sun and moon are **provably mutually exclusive** (moon needs dayN<0.45, sun needs dayN>0.5 — a 0.05 gap where neither shows; they NEVER co-render) and the sun is off through dawn/dusk/night, reaching full only toward midday. While visible it hangs high anchored to the view (`position.set(camFocus.x−66,80,camFocus.z+54)`) on the opposite side from the moon for variety, tracking the player like an infinitely-distant body. So a long fair daytime match gains a single bright sun over the arena (full & crisp under clear skies, softer/hazy through cloud) that fades out at dusk — handing off to the moon, then the stars — and vanishes the moment rain/storm rolls in. No file copied (EF_Audio mount unavailable this run; pure geometry/canvas, no audio). **Perf:** zero new per-frame heap alloc — one Sprite + one 64px canvas texture + one SpriteMaterial built ONCE; per-frame cost is a single opacity ease + (only while visible) one position.set — NO per-vertex work, NO geometry; fully OFF at the low-FPS tier (`fxLevel>=2` forces target 0 & fades it out) and `visible=false` whenever opacity≤0.02 so any night / rain / storm / low-FPS match never touches it. Composes with the moon (sun = day disc, moon = night disc; provably non-overlapping bands), starfield/shooting-star (clear-night only), fireflies (low warm night motes), dust (daytime motes), mist, day/night (shares `dayN`), weather (gated sunny/cloudy), biome fog and storm flash — all untouched, as are rain/puddle/wind-bed/owl/death-fx/footstep/biome-ambient/voice/VFX/net. **Validation:** extracted the exact weatherTick sun block into `sundisc_check2.js` with plain stubs, `node --check` + 5042 asserts (starts at 0; sunny midday converges ~0.7 & visible; sunny deep night off; dayN=0.5 boundary sunF=0 & off; cloudy day dimmed to exactly 0.3× & visible; rain & storm off; fxLevel≥2 fades out & hides; no overshoot past 0.7 over 5000 ticks & non-negative; sunF clamped [0,1] across −0.2→1.2 sweep; dayN=0.65→sunF 0.5→target 0.35; PROVEN sun & moon never co-render across the full dayN range) all pass.
- 2026-06-30T17:13Z — **Dynamic day/night cycle.** Added a long, subtle time-of-day sweep in `weatherTick()`: `dayN∈[0,1]` from a sine of `gameT` (DAYLEN=1100s) modulates sun + hemi intensity *targets* (multiplied onto the weather moods so weather+time compose) and warms `sun.color` from white→dusk-orange as light fades. Match opens near dawn (dayN≈0.02), peaks ~503s in, drifts to dusk/night over a long game; soft night floor (sun ×0.5) keeps play readable. No file copied. **Perf:** zero new objects, no per-frame heap alloc (scratch colours `_sunDay/_sunDusk/_sunScratch` + `DAYLEN` built once); idle cost = a few math ops + 2 in-place colour ops/frame. Untouched flash/weather/fog logic. **Validation:** extracted math to `daynight_check.js`, `node --check` + asserts (dayN stays in [0,1], opens dim & rising, noon lands mid-game ~503s, sun-mul floor ≥0.5) all pass.
- 2026-06-30T23:50Z — **RAINBOW after the rain (weather ASMR — rain comes & goes).** Gave a passing shower a visual payoff: a single faint billboarded arc that rises in the sky ONLY when the weather actually CLEARS from rain/storm to fair (sunny/cloudy) DURING DAYLIGHT. Built ONCE next to the sun/moon/cloud sprites: `_rainbow`, a `THREE.Sprite` over a 128px canvas of 7 soft ROYGBIV stroke-arcs (`ctx.arc(64,120,58−i·4.3,π,2π)`, lineWidth 4, low alpha 0.42 each), into a `SpriteMaterial` (`transparent`, `depthWrite:false`, `fog:false` so day fog never tints it, opacity 0), `scale(120,120,1)`, `renderOrder=2`, hidden; plus a single `_rbLife` scalar (seconds the current arc has left). The EXISTING weatherTick transition branch now captures the previous weather (`_pw=weather` before `setWeather`) and, when `(_pw==='rain'||'storm') && (nw==='sunny'||'cloudy')`, recomputes `dayN` locally and sets `_rbLife=18` ONLY if it's daylight (`dayN>0.45`) and not the low-FPS tier — so the arc appears exactly once per real clearing transition, never at night, never when heading INTO rain, never on a rain→storm (still-foul) shift, never on a no-prior-rain cloudy→sunny. A tiny per-frame block (placed after the clouds block) decrements `_rbLife`, eases `_rbMat.opacity` toward `fade×rbDay×0.5` where `fade=min((18−_rbLife)/1.5, _rbLife/3, 1)` (fade IN over the first 1.5s, HOLD, fade OUT over the last 3s of its 18s life) and `rbDay=clamp((dayN−0.4)/0.2)` (the arc also dims toward dusk and is 0 at deep night, so if the sun sets while it's up it gracefully fades), forced to 0 at `fxLevel>=2`; while visible it hangs in the sky anchored to the view (`position.set(camFocus.x+34,30,camFocus.z−100)`, opposite the sun's `−66,+54`). So a daytime match that has rain pass and skies clear gets a soft rainbow rising over the arena for ~18s, then quietly fading — and night/foul/low-FPS matches never see it. No file copied (EF_Audio mount unavailable this run; pure geometry/canvas, no audio). **Perf:** zero new per-frame heap alloc — one Sprite + one 128px canvas + one SpriteMaterial built ONCE; idle cost is a single `if(_rbLife>0)` decrement + one opacity ease toward 0 (arc absent the vast majority of the time); while visible (rare, ≤18s after a clearing) one extra `position.set` — NO per-vertex work, NO geometry; fully OFF at the low-FPS tier and `visible=false` whenever opacity≤0.02 so non-rainbow matches never touch it. Composes with the sun disc (rainbow renderOrder 2, opposite side), moon/stars/shooting-star (night-gated, rainbow is daytime), clouds/dust, mist, fireflies, day/night (shares `dayN`), weather-shift cue + distant-thunder + rain envelope (rainbow keys off the SAME real transition), biome fog and storm flash — all untouched, as are rain/puddle/wind-bed/owl/footstep/biome-ambient/death-fx/voice/VFX/net. **Validation:** extracted the trigger + per-frame ease into `rb2.js` with plain stubs, `node --check` + 19 asserts (rain→sunny & storm→cloudy in daylight trigger 18s; rain→sunny at NIGHT no arc; into-rain / cloudy→sunny-no-prior-rain / rain→storm all no arc; fxLevel≥2 no arc; fades in over first second & visible; mid-life target == peak 0.5 & opacity near peak; life expires after 18s → target 0 → faded out & hidden a few s later; never exceeds peak 0.5 over full life; dusk dims the arc & deep night forces target 0; fxLevel rising mid-life forces target 0; fade clamped [0,1] across full life sweep) all pass.
- 2026-07-01T00:10Z — **Distant daytime BIRD FLOCK flyover (ambient life × day/night).** The daytime sky had static bodies (sun/moon/stars/clouds/dust) but no daytime *event* — the night sky's occasional `_shoot` meteor had no daylight counterpart. Added `_birdSp`, a single dark billboarded `THREE.Sprite` of a small V-formation of gull silhouettes that occasionally drifts across the fair daylight sky. Built ONCE next to the sun/cloud sprites: a 64px canvas of five soft "M"-shaped wing strokes (`quadraticCurveTo`, `rgba(38,42,52,0.82)`, lineWidth 2) arranged in a V, into a **normal-blended** `SpriteMaterial` (`transparent`, `depthWrite:false`, `fog:false` so day fog never tints the birds, opacity 0 — NOT additive, so the silhouettes read DARK against the bright sky, unlike the additive sun/stars/dust), `scale(18,18,1)`, `renderOrder=3`, hidden; plus `_birdT` (active flight timer), `_birdCD` (cooldown 20–52s), `_birdDur` (this flight's length) scalars and reused scratch `_birdPos`/`_birdVel` `Vector3`s. A new block in `weatherTick`, gated `dayLit = fxLevel<2 && dayN>0.52 && (sunny||cloudy)` (so it ONLY runs in bright daylight under fair/cloudy skies — provably OFF at night, in rain, in storm, and at the low-FPS tier), runs a cooldown then spawns a flyover: random L→R or R→L direction, starts offscreen (`x=∓80`, high `y 58–76`, `z −50..−120`), crosses at `18–24 u/s` with a gentle downward/lateral drift, life `6–9s`. Each active frame the flock advances (`addScaledVector`) and opacity = `min(1, el/1.5, _birdT/1.5)×0.6×(dayLit?1:0)` so it fades IN over the first 1.5s, holds, fades OUT over the last 1.5s, and is force-killed to 0 the instant weather turns foul or night falls mid-flight (the `dayLit` factor) — no orphan flock survives into rain/night. So a long fair daytime match gets an occasional distant flock crossing high overhead — the daylight echo of the night's shooting star — silent by design (the audio birds already own the chirps) and gone in foul weather / at night. No file copied (EF_Audio mount unavailable this run; pure geometry/canvas, no audio). **Perf:** zero new per-frame heap alloc — one Sprite + one 64px canvas + one SpriteMaterial + two Vector3 scratch built ONCE; idle cost while daylit is a single `_birdCD-=dt` + compare (flock absent the vast majority of the time), while active (rare, ~7s, a few times per long fair day) one `addScaledVector` + one opacity calc + one `position.set` — NO per-vertex loop, NO geometry, NO new objects; spawning is skipped whenever `dayLit` is false (night, rain, storm, or `fxLevel>=2`). Composes with the shooting-star (bird = day event, meteor = night event; provably disjoint gates), sun/moon/stars/clouds/dust, fireflies/mist/rainbow, day/night (shares `dayN`), weather, biome fog and storm flash — all untouched, as are rain/puddle/wind-bed/owl/footstep/biome-ambient/death-fx/voice/VFX/net. **Validation:** extracted the exact spawn+per-frame logic into `birdflock_check.js` with plain Vector3/sprite stubs, `node --check` + 20+ asserts across 12 cases (night→never spawns; rain & storm daytime→never; fxLevel≥2→never; dayN=0.52 boundary→never; sunny midday→appears & peak opacity in (0.4,0.6]; cloudy midday→appears; flight duration [6,9]s & |velX|∈[18,24] & spawn y 58–76; after flight→hidden, opacity 0, cooldown re-armed [22,52]; fade-in ramps below peak in first 0.1s; weather→rain mid-flight forces opacity 0 & hides; both L→R and R→L directions occur; opacity always within [0,0.6] across a 30k-tick mixed-weather/day/fxLevel run) all pass.
- 2026-07-01T00:29Z — **Night edge-VIGNETTE (day/night × weather ASMR).** The scene already darkens the LIGHTING at night (via `lightMul`) and fills the sky with moon/stars, but the SCREEN itself never framed the darkness — the corners stayed as bright as noon. Added `#nightvig`, a fixed full-screen DOM overlay (a cool dark-blue radial gradient, `transparent 40%` → `rgba(8,12,30,.74)` at the edges, `z-index:8` so it sits UNDER the red low-HP danger vignette at z9 and never masks it, `pointer-events:none`, opacity 0) that eases its corners darker as the light dies — a cozy dusk frame that leaves the readable centre (inner 40%) completely clear so play is never obscured. Modelled on the existing `#lowhp` overlay pattern. Driven in `weatherTick` (reusing the existing `dayN`) by a tiny block: `nvF=clamp((0.5−dayN)/0.5)` (0 by day → 1 at deep night) × a small weather boost `nvW` (storm 1 / rain .85 / cloudy .72 / sunny .6 — foul nights feel heavier) × 0.85, eased into a cached `_nvO` at `dt*0.6` and written to the cached element's `style.opacity` ONLY when it moves >0.004 (so no per-frame DOM churn, and it settles to a static value once the light stops changing). So a long match that drifts into night gradually gains darkened screen corners (deepest on a stormy night, lighter on a clear one), which lift again at dawn — composing with the lighting dim, moon/stars/fireflies and the low-HP red vignette (independent z-layers). Element looked up ONCE (cached, or `false` if absent) so no repeated `getElementById`. No file copied (EF_Audio mount unavailable this run; pure DOM/CSS, no audio, no Three objects). **Perf:** ZERO Three.js cost — no new geometry/textures/materials/objects and no per-frame heap alloc; per-frame work is a couple of math ops + one eased scalar + (only when it actually moves) a single compositor-only `style.opacity` write (throttled to Δ>0.004, so most frames write nothing); opacity changes don't trigger layout/reflow. fxLevel-agnostic by design (a DOM overlay costs the same on low-end, and actually helps the night read on weak GPUs), so it's kept on at all tiers. Untouched weather-state/rain/puddle/biome/mist/firefly/star/moon/sun/cloud/dust/rainbow/bird-flock/death-fx/footstep/owl/biome-ambient/voice/VFX/net and the low-HP vignette. **Validation:** extracted the exact weatherTick block into `nightvig_check.js` with a DOM-element stub, `node --check` + 9 assert groups (noon→fades to ~0 & opacity 0; deep sunny night→target exactly 0.51 & converges; storm night→~0.85 & darker than sunny; weather ordering storm>rain>cloudy>sunny at fixed night; dayN=0.5 & past-noon boundary→nvF 0/off; nvF clamped [0,1] across a −0.2→1.2 sweep; no overshoot past 0.85 over 3000 storm-night ticks; DOM-write throttle skips sub-0.004 deltas; element cached/created once) all pass.
- 2026-07-01T00:48Z — **Wind-chime shimmer (ambient life × day/night × fair weather).** Added `sfxChime()` — 2–4 soft high bell notes drawn from a fixed C6 pentatonic set (`_CHIME`=[1046.5,1174.7,1396.9,1568,1864.7,2093]Hz), each a pure `sine` (dur .9–1.4s, vol .010–.014, gentle downward `-f*.12` slide) staggered 110–200ms apart so it rings out like a breeze passing through a chime — a delicate high daytime layer distinct from the low owl (280–360Hz night), the high crickets (4.2–5.1kHz night trill), the birds and the biome cicada/frog/hawk voices. Wired into the previously-UNUSED `step%8===7` music phase (no collision with %16 pads / %2 & %4===2 plucks / %4===0 birds-crickets-gust / %4===2 fountain / %8===3 biome / %8===4 storm-rumble / %8===5 owl / %8===6 cloudy-thunder), gated `(weather==='sunny'||'cloudy')` + daytime (`dayN>0.45`, same local sine formula as the crickets/owl/biome branches, no new global) at 8% chance — so a fair daytime match occasionally gets a soft chime shimmer, and it's silent at night, in rain and in storm. No file copied (EF_Audio mount unavailable this run; pure WebAudio synthesis, reuses tone()). **Perf:** zero new objects/geometry/textures, no per-frame heap alloc or per-frame cost — the `_CHIME` array is a module-level constant built ONCE; event-driven `tone()` fired at most once per 8 music steps (~4s) at 8% on fair days only, 2–4 short sine voices ≤.014 vol so combat SFX always sit on top; fully gated by `S_MUTE`/`S_VOL`/`AC` via tone() and the `if(!AC||S_MUTE)return` guard; a staggered `setTimeout` train, no timer of its own. Composes cleanly with birds/crickets/owl/gust/fountain/distant-thunder/biome-ambient/wind-bed and day/night (shares dayN). Untouched weather-state/rain/puddle/biome/mist/firefly/star/moon/sun/cloud/dust/rainbow/bird-flock/night-vignette/death-fx/footstep/voice/VFX/net. **Validation:** extracted `sfxChime`+the branch gate into `chime_check.js` with tone/setTimeout stubs + synchronous flush, `node --check` + 10 assert groups over 3000 randomized runs (muted→silent; no-AC→silent; every run 2–4 notes, all `sine`, all freqs from `_CHIME`, all vol ≤.015, all downward slide, dur .9–1.4; gate fires sunny & cloudy daytime; high-rnd suppresses; night dayN=0.3 no fire; rain & storm no fire; dayN=0.45 boundary no fire) all pass.
