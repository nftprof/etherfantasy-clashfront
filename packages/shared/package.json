{
  "name": "@clashfront/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Canonical Clash Front types, CONSTANTS, enums, id helpers, seeded RNG, and balance.json loader (docs/08-data-models.md as code).",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"dist/test/**/*.test.js\"",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
