# BUILD LOG — twilight-veil-cron

Append one line per autonomous build run: `YYYY-MM-DD HH:MM — <backlog id> <feature> — files — validation`.

- 2026-06-14 (manual seed) — N11/N12/N13 ending ladder + OVA viewer + Holy Grail epilogue — pve.html (ENDINGS/OVA/playEnding/playOVA, runComplete hook, title Memories link, save fields) — node head-check clean.
- 2026-06-16 04:15 UTC — N1 episode framework — pve.html (SAVE.episode + wipeSave preserve, EPISODES[] config, epUnlocked/episodeOf/chapBtnLabel, #chapsel chapter-select overlay + #chapBtn title button, renderChapters/openChapters, runComplete chapter advance, guide CHAPTERS line) + AGENTS_PVE.md — node logic test pass + head-check clean (only frozen-tail EOF).
- 2026-06-16 05:14 UTC — N4 Episode 3 "Rite of Blades" — pve.html (EP_STORY[3] intro/outro blood-pact cutscenes, riteBrand() sigil-brand awakening the first shadow skill via the N3 corruption gauge + persistent residue-8 run-start floor, maybeEpisodeScene rite hook, guide CHAPTERS line) + wiki.html + AGENTS_PVE.md + MASTERPLAN_24H.md (N4 ticked) — node logic test pass (18 asserts, n4_rite_0616.js) + head-region node-check clean (frozen-tail EOF only).
- 2026-06-16 07:18 UTC — N10 The Betrayal (Ayume true final boss) — pve.html (kill-intercept: felling Kage in Ep7 → beginBetrayal not runComplete; EP_STORY[7].betrayal 15-line throne cutscene [Ayume=Leah the architect, Yui absorbed]; beginBetrayal/ascendAyumeBoss promote the Ayume companion into the true final boss [kind=mon, boss/finalBoss/ayumeBoss, 900·diffMult(7)·3 hp]; new if(b.ayumeBoss) 3-phase moveset in bossSpecial; her death → runComplete → playEnding ladder; betrayalStarted reset in startRun; guide CHAPTERS line) + wiki.html + AGENTS_PVE.md + MASTERPLAN_24H.md (N10 ticked) — node logic test pass (20 asserts, n10_betrayal_0616.js) + betrayal-literal parse/eval (15 lines) + head-region node-check clean (frozen-tail EOF only; tail edits validated verbatim in the logic test). Campaign now playable end-to-end (N1–N13 done).
- 2026-06-16 07:55 UTC — N15 Sin-Realm re-skin + Realm-lord renames — pve.html (DUNGEONS[2..5] names → Valley of Midas/Feastfall/Isle of Dominus/Enamora; REGIONS+WARDEN_BY_TIER[3..6]+FACTIONS wardens → Aurelian the Gilded King [Greed]/Throngullet the Endless Maw [Gluttony]/Vael the Crowned Tyrant [Power]/Seraphel the Beguiling Rose [Lust]; Twilight-Veil sin lore on each REGIONS.d; 4 bossSpecial feed strings; warden↔region↔faction-lead invariant preserved, legacy QUEST wardens untouched) + in-game 📖 guide (GOAL realm list + WARDENS line) + wiki.html (new Sin-Realms bullet) + AGENTS_PVE.md + MASTERPLAN_24H.md (N15 ticked) — node logic test pass (30 asserts, n15_test.js: real tables eval-extracted, invariant holds tiers 3–6, no stale names in QUESTS) + head-region node-check clean (frozen-tail EOF only) + full-file Grep confirms zero old realm/warden names remain.
- 2026-06-16 08:45 UTC — N17 Memory-shard collectibles → lore codex + OVA fragments — pve.html (MEM_SHARDS[] 9 story memories [7 chapter + 2 frag:true OVA fragments 📖 Bookshop/✒ First Page] + MEM_BY_EP{1..7} + memCount/grantMemory[idempotent, "record complete" feed at 9/9]/grantEpMemory, placed after shardBonus in head; grants wired — grantEpMemory at top of maybeEpisodeScene [chapter played→its memory], grantMemory('bookshop') in beginBetrayal [throne opens], grantMemory('firstpage') in playEnding tier>=6 branch [braces added round the brace-less if]; SAVE.mem PERMANENT — DEF_SAVE + loadSave default + wipeSave preserve; questUI "📜 MEMORIES OF THE VEIL — n/9" codex section after WARDENS; pure lore, no combat effect) + in-game 📖 guide (J-journal line + MEMORIES paragraph) + wiki.html (codex bullet) + AGENTS_PVE.md + MASTERPLAN_24H.md (N17 ticked) — node lo
- 2026-06-16 08:58 UTC — N18 Dialogue voice & scene-turn stings — pve.html (DLG_VOICE per-character voice profiles + dlgVoice(who) [tonal accent + subtle EF.VOICE name-bark, 70ms-gated, hero=heroName, barkindle woof, kage low/yume high] + DLG_STING{open,close,dark,light}+dlgSting after sfxPortal; wired 4 head call sites — dlgNext per-line dlgVoice + close sting, playDialogue open sting, _dlgChoose dark/light sting on the N16 pick; all try/catch + AC/S_MUTE-guarded so 🔊 mutes all) + in-game 📖 guide (CHAPTERS voice/stings sentence) + wiki.html (cutscenes bullet) + AGENTS_PVE.md + MASTERPLAN_24H.md (N18 ticked) — node logic test pass (12 asserts, n18_voice_0616.js: narrator accent-only/no-bark, hero barks heroName, vol .5, barkindle woof, 70ms gate, fallback, open=2 tones, dark=tone+noise, bogus=no-op) + node --check clean + head-region node-check clean (frozen-tail EOF only; defs in frozen-tail audio region applied via exact-match Edit, confirmed by Read-tool Grep). ALL N-items N1–N18 done — campaign playable end-to-end AND voiced.
- 2026-06-16 09:18 UTC — W1 fog-of-war minimap + world map — pve.html (run-scoped fog grid: FOG_CELL/fogSeen/fogResetW/fogReveal/fogSeenAt/fogPaint after wmapXY; fogReveal per-frame in updHUD, fogResetW in startRun; drawMM paints fog after terrain + gates nodes/drops/eggs/non-hero units by fogSeenAt [hero+portals always shown]; drawWmap paints fog under labels/waypoints/hero; guide EXPLORING paragraph) + wiki.html (Fog of war bullet) + AGENTS_PVE.md + MASTERPLAN_24H.md (W1 ticked) — node logic test pass (16 asserts, w1_fog_0616.js) + verbatim reconstruction node-check of fogPaint/drawWmap/drawMM (w1_recon_0616.js, parses+runs). Bash mount served a stale pre-edit snapshot (0 grep hits) so no in-file head node-check; validated via Read-tool reconstruction. FIRST W-item — all N1-N18 done, campaign playable end-to-end. Browser F12 the gate.
- 2026-06-16 11:34 UTC — W7 hidden vault behind a pressure-plate puzzle — pve.html (SEPULCHER + mkPlate/sepReset/spawnHiddenVault/updateSepulcher after the W5 block; spawnHiddenVault() in startRun after spawnRealmPuzzle; updateSepulcher(dt) in the hero loop after updatePuzzle; far NE corner 100,104 — a slab buried flush with the ground ringed by 4 rune-plates, Fisher–Yates step order each run, STAND-on-plate-in-order [d<2.2], wrong plate → sepReset, solve raises the slab + mkCache guaranteed Rare+ cache beside it; distinct from W5 [step ON vs walk NEAR; vault HIDDEN until solved]; isCursed-gated; reuses mkCache/genCacheItem/feed/fxRing/PZ_COLS) + in-game 📖 guide (🔒 THE HIDDEN VAULT line) + wiki.html (Hidden Vault bullet) + AGENTS_PVE.md + MASTERPLAN_24H.md (W7 ticked) — node logic test pass (23 asserts, w7_vault_0616.js) + real-block reconstruction node-check+run (w7_real_0616.js, P.g.position path). Bash mount served a stale pre-edit snapshot (0 grep hits, frozen-tail EOF) so no in-file head node-check — validated via Read-tool edits + isolated/reconstruction node checks. Browser F12 the gate.
- 2026-06-16 12:55 UTC — N19 The Veil's Tally (per-run dark/light alignment) — NEW on-theme item: all N1–N18 & W1–W10 were already ticked, so per the §7 close-out clause the cron adds one small validated on-theme item (the explicit N16 polish-open payoff). pve.html (head block after maybeChoiceMoment: let darkPicks/lightPicks + tallyReset() + recordChoice(corr) [corr>0→dark++, <0→light++, 0 ignored] + ALIGN_TIERS[5, keyed by net=dark−light: ≥3 Veil-Bound / ≥1 Shadow-Touched / ≥0 Knife's Edge / ≥-2 Tempted-but-Holding / else Pure of Heart] + alignment(); wired at 3 existing sites — recordChoice(ch.corr) in _dlgChoose after addCorruption, tallyReset() in startRun beside fogResetW/cacheStats, and a "🌗 YOUR PATH THIS DESCENT" section in questUI before FACTIONS [🌑 n shadowed · 🕯 m held → label]; run-scoped, NO new save fields, NO new systems — rides N16 CHOICE_MOMENTS + addCorruption) + in-game 📖 guide (CHOICE-MOMENTS paragraph 🌗 YOUR PATH sentence) + wiki.html (choice bullet alignment note) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N19 added & ticked) — node logic test pass (15 asserts via /tmp heredoc, dodging the bash-mount truncation: reset, …) — validation OK.
- 2026-06-16 13:55 UTC — N22 Scent-mask (human food pauses the Underworld hunt) — NEW on-theme close-out item: all N1–N21 & W1–W10 already ticked, so per §7's close-out clause the cron adds one small validated on-theme item (pays off N21's explicit polish-open "the scent-mask actually pausing the §11.3 hunt-AI" — eating food already *said* "the dark loses your scent" but did nothing). pve.html (run-scoped `let scentMaskedT=0;` above corruptionTick [NO new save fields, NO new systems]; the `d.type==='food'` pickup sets `scentMaskedT=Math.max(.,9)` [~9s, never shortens a longer mask]; `corruptionTick(dt)` [per-frame] decrements it and fires one "👁 The dark finds your scent again." feed at expiry; the monster-AI acquire `else{` branch gains `const masked=(scentMaskedT>0 && !u.boss && !u.provoked)` forcing acquire `range` to 0 while masked → non-boss, un-struck hunters lose you & break off/return home, but BOSSES ignore the mask and any `provoked` mob you've struck keeps coming; `scentMaskedT=0` reset in startRun beside corrReset) + in-game 📖 guide (🍞 ANCHORS sentence rewritten to the real mechanic) + wiki.html (Anchors bullet rewritten from the old "blood in the water" flavour) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N22 added & ticked) — node logic test PASS (17 asserts, n22_scentmask.js: fresh run unmasked + aggroNow mob hunts 999, food→9, masked non-boss aggroNow/normal mobs →range 0, boss & provoked still 999, re-eat never reduces a longer mask, tick decrements by dt + one re-arm feed at expiry, no further decrement/feed once expired, aft- 2026-06-16 14:58 UTC — N25 Endings Witnessed gallery (📜 Journal) — NEW on-theme close-out item: all N1–N24 & W1–W10 already ticked, so per §7's close-out clause the cron adds one small validated on-theme item (the explicit §2 ladder/replay payoff — endings were tracked in SAVE but never shown to the player). pve.html (questUI: a read-only "🎭 ENDINGS WITNESSED — n/9" section inserted between the N17 MEMORIES codex and the N19 YOUR PATH section; reads permanent SAVE.endingsSeen{}/bestEnding/ovaUnlocked [NO new save fields, NO new systems], rows 1–9 show ENDING_TITLE[t] once that tier is seen [else 🔒 ???], bestEnding tagged ◆ furthest, footnote flags diff-6 OVA + diff-9 Holy Grail gates + ▶ Memories unlock; mirrors N17 codex/W10 atlas) + in-game 📖 guide (YOUR ENDING sentence) + wiki.html (ending bullet) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N25 added & ticked) — node logic test PASS (18 asserts, n25_endings.js: fresh 0/9 + 9 locked + titles hidden, one-seen 1/9 + exactly one ◆ furthest, diff-6 OVA note + non-best unmarked, all-nine 9/9 + 0 locked) + head-region node-check parsed clean through to the frozen-tail truncation (edit @ line ~408; bash mount CURRENT this run, grep found the edit).
- 2026-06-16 15:15 UTC — N26 Reclaim stings (distinct audio for rare finds) — NEW on-theme close-out item: all N1–N25 & W1–W10 already ticked, so per §7's close-out clause the cron adds one small validated on-theme item (the "reclaim SFX" polish-open repeated across N17/W2/W3/W8/N21). pve.html (audio region after dlgSting, ~1142: RECLAIM_STING{cache:rising 523/784/1047 triangle arpeggio + noise tail, anchor:warm 392→523 sines, memory:detuned 660/663 + 990 shimmer} + reclaimSting(kind) [AC/S_MUTE-guarded, _gate('rcl_'+kind,120) per-kind throttle, try/catch]; wired at 3 existing pickup sites — d.type==='cache' branch reclaimSting('cache') [fallback sfxEquip], d.type==='food' branch reclaimSting('anchor') [fallback sfxCoin], grantMemory reclaimSting('memory') [was silent]; rides the existing tone()/noiseHit() engine, no new systems/save fields, 🔊-muted with the rest) + in-game 📖 guide (FINDS ON THE ROAD line) + wiki.html (caches bullet) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N26 added & ticked) — node logic test PASS (7 asserts, n26_reclaim.js: anchor 1 immediate tone@392, memory 2 detuned sines, cache all setTimeout-scheduled, _gate blocks rapid same-kind, distinct kinds both fire, S_MUTE suppresses, bogus no-op) + node --check clean; defs confirmed in true file via Read/Grep at ~1142–1146 + 3 call sites (378/1837/1841); bash mount served the usual stale frozen-tail (864 lines) so head node-check saw only the expected final-line EOF. Browser F12 the gate.
- 2026-06-16 15:58 UTC — N28 Reveal flash (visual half of the reveal beat) — NEW on-theme close-out item: all N1–N27 & W1–W10 already ticked, so per §7's close-out clause the cron adds one small validated on-theme item (the explicit N27 polish-open "a brief screen-flash/shake paired with the stab"). pve.html (audio region after dlgSting ~1153: `revealFlash()` + a `_revealFxKf` once-flag — lazily builds a pointer-through `#revealFx` full-screen overlay [z-index 80, BELOW the #dlg box at 88 so cutscene text stays crisp] that pulses a blood-dread radial vignette to opacity 1 then fades [.07s in / .55s out], and shakes #dlgBox via a `.revShaking` class driving a once-injected `@keyframes revShake`; PURELY cosmetic and NOT audio-gated — runs even when 🔊 muted, unlike dlgSting which early-returns on !AC||S_MUTE — so the moment lands with sound off; wired at the SAME two reveal sites as N27, covering all three beats — dlgNext per-line hook fires it when `ln.sting==='reveal'` [betrayal line + alignment-ending narrator line], maybeWhisper fires it beside dlgSting('reveal') on a Corruption band-cross; reuses the dialogue DOM, no new save fields/systems) + in-game 📖 guide (CHAPTERS voice/stings sentence) + wiki.html (cutscenes bullet) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N28 added & ticked) — node logic test PASS (16 asserts, n28_revealflash.js, mocked DOM: keyframe injected once / not re-injected, overlay created pointer-events:none + z-index 80, flashes to opacity 1 then scheduled fade→0, dlgBox gains then loses the shake class, overlay reused not duplicated, no throw + still flashes when #dlgBox absent) + node --check clean; defs confirmed in true file at 1153–1167 + hooks at 488/1007 via Read/Edit/Grep; bash mount served the usual stale frozen-tail (859 l- 2026-06-16 16:35 UTC — N30 Cutscene keyboard controls — NEW on-theme close-out item: all N1–N29 & W1–W10 already ticked, so per §7's close-out clause the cron adds one small validated on-theme item (pays off N29's explicit "a keyboard shortcut" polish-open; serves the campaign-replay goal — the whole story is now playable from the keyboard). pve.html, two surgical edits: (1) a NEW keydown listener after _dlgSkipEl.onclick (~560, head) that acts only when dlgActive() — Space/Enter→dlgNext (advance, mirrors the backdrop click), Esc→dlgSkip (the N29 skip path), and while a moral choice is up (_dlgChoosing) 1/2 click the dark/light choice buttons (#dlgChoices button[0|1]); preventDefault on Space (no page scroll); NEVER advances/skips while _dlgChoosing so an N16/N19 choice is never bypassed; (2) a one-line guard at the top of the in-run hotkey handler (~2717): if(dlgActive())return; so game hotkeys (b/t/m/j/q…) no longer leak into a cutscene (running=true during episode scenes). Reuses dlgNext/dlgSkip/the choice buttons — no new systems, no new save fields. Guide CHAPTERS block + wiki.html cutscenes bullet updated. Validated: node logic test PASS (12 asserts, n30_cutscene_keys.js, mocked DOM: Space/Enter/Spacebar→next, Esc→skip, Space preventDefault, unrelated key ignored, inactive→no-op, choosing→Esc/Space inert, 1→first/2→second choice, 3→no-op) + node --check clean + head-region node-check clean (598 visible script lines parse; only the expected frozen-tail EOF at the truncation; the listener block is in the head — the hotkey-guard edit is in the frozen tail, applied verbatim via exact-match Edit). Browser F12 the gate.
- 2026-06-16 16:55 UTC — N31 Controls hint on the dialogue box (discoverability) — NEW on-theme close-out item (all N1–N30 & W1–W10 ticked; pays off N30's explicit polish-open "a HUD hint ('Space ▸ / Esc ⏭') on the box"). The N29 skip tab + N30 keyboard controls were invisible unless you read the guide; this surfaces them on the box. pve.html, two surgical purely-cosmetic head edits: (1) the persistent #dlgHint footer (~205, display-toggled only) now reads "▶ click / Space  ·  Esc ⏭"; (2) _showChoices (~456) builds buttons via forEach((ch,ci)=>…) and prefixes each label (ci+1)+'.  '+ch.label so dark/light options render "1.  …"/"2.  …", matching the N30 1/2 choice hotkeys. NO new elements/save fields/systems — reuses the dialogue DOM. 📖 guide CHAPTERS block + wiki.html cutscenes bullet updated. Validated: node logic test PASS (9 asserts, n31_hint.js: labels numbered 1./2., prefix index == N30 key→button map, lone choice still "1.", hint string has click/Space/Esc/⏭) + node --check clean + head-region node-check clean (596 visible script lines parse, frozen-tail EOF only; both edits in the head, exact-match Edit on the true file). Browser F12 the gate.
- 2026-06-16 17:20 UTC — N32 Alignment HUD path glyph — NEW on-theme close-out item (all N1–N31 & W1–W10 ticked; the explicit N19 polish-open "a tiny HUD path glyph"). pve.html (one line appended in updHUD's #statusInd block after the N23 scent-mask append, ~2827: if((darkPicks+lightPicks)>0){…} → live 🌑 shadow / 🕯 light / ☯ balanced + alignment().label, coloured by alignment().col, only after a choice is made, appends after injured/cursed/masked with two-space sep and never overwrites their colour; rides N19 tallyReset/alignment/darkPicks/lightPicks — NO new save fields/systems/elements, try/caught) + in-game 📖 guide (CHOICE MOMENTS top-bar glyph note) + wiki.html (choice bullet) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N32 added & ticked) — node logic test PASS (13 asserts, n32_alignglyph.js: no-choice→empty/untouched, dark→🌑 Veil-Bound + tier colour, light→🕯 Tempted-but-Holding, equal→☯ Knife's Edge, append keeps prior text first + prior colour) + node --check clean + head-region node-check clean (595-line head parses, frozen-tail EOF at 596 only). Edit confirmed at line 2827 via Read-tool Grep (bash mount stale tail, 856 l). Browser F12 the gate.
- 2026-06-16 17:42 UTC — N33 The Whisper's record in the 📜 Journal — NEW on-theme close-out item (all N1–N32 & W1–W10 ticked; the explicit N20/N32 polish-open "surface the Whisper's taunts" — they were a transient feed line only). pve.html (3 surgical run-scoped edits, NO new save fields/systems, rides the N20 whisperSeen/WHISPER_BANDS Whisper: (1) new `let whisperLast='';` beside whisperSeen ~1023 + whisperReset() clears it; (2) maybeWhisper stores `whisperLast=ln;` after picking the taunt ~1035; (3) new "🕷 THE WHISPER — n/4 heard" questUI section ~419 between 🌗 YOUR PATH and ⚔ FACTIONS, shown only when whisperSeen>0, reads whisperSeen/WHISPER_BANDS.length + italicises whisperLast, singular/plural handled) + in-game 📖 guide (HUMANITY ⇄ CORRUPTION Whisper sentence) + wiki.html (corruption bullet) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N33 added & ticked) — node logic test PASS (14 asserts, /tmp/n33_whisper.js: fresh empty + record hidden, band1→1/4 + singular time + last words, big jump catches up all 4 + plural times + highest line, one feed/band no spam, no re-fire same band, reset clears) + node --check clean + head-region node-check clean (589-line head parses, frozen-tail EOF only). questUI edit in the head; whisperLast/maybeWhisper edits in the frozen tail, applied via exact-match Edit, confirmed via Read-tool Grep. Browser F12 the gate.
- 2026-06-16 17:55 UTC — N34 Per-ending first-person teaser (Endings Witnessed gallery) — NEW on-theme close-out item (all N1–N33 & W1–W10 ticked; the explicit N25 polish-open). Also reconciled N33 — it shipped earlier in pve.html/wiki/AGENTS/§7 but had no BUILD_LOG line; this entry records it. pve.html (ENDING_TEASER{1..9} after ENDING_TITLE ~583 — 9 first-person echoes per §2 ladder; questUI ENDINGS-WITNESSED row builder ~412 appends an italic teaser sub-line ONLY for witnessed endings, locked rows stay spoiler-free; reads the permanent SAVE.endingsSeen the N25 roll uses, NO new save fields/systems) + in-game 📖 guide (YOUR ENDING) + wiki.html (ending bullet) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N33 confirmed + N34 added & ticked) — node logic test PASS (21 asserts, n34_teaser.js: 9 first-person teasers, Holy-Grail redemption, Two-Saved Critical Limit, fresh=no leak+9 locked, one-seen=teaser shown+others hidden+one ◆ furthest, all-seen=9 teasers+0 locked) + head-region node-check clean (591 head lines parse, frozen-tail EOF at 592 only; both edits in the head). Browser F12 the gate.
- 2026-06-16 19:55 UTC — P4 Ayume companion wears Leah's GLB (Yui wears Irene's) — TOPMOST UNCHECKED P0 PLAYTEST FIX (P1–P3 ticked). The N5 companions rendered as tinted placeholder humanoids (look(u,'hero',col)) with the GLB swap deferred, and the live concern was the Ayume slot must use Leah's model, never Irene's. pve.html (new `const COMP_GLB={ayume:'hero/Leah_Set_Default.glb',yui:'hero/Irene_Token_000001.glb'}` after COMP_PARTY ~968; spawnCompanion tail ~981 sets `u.slot='comp_'+who` and for a mapped companion applyModel if prefabs[slot] is loaded else autoLoad(slot,glb,5) with the tinted humanoid as a one-frame fallback while the GLB streams in; Ayume→Leah NEVER Irene, Yui→Irene's Token_000001 GLB; the slot carries through beginBetrayal/ascendAyumeBoss so the architect's reveal is literally Leah's face; reuses autoLoad/applyModel/prefabs, no new systems/save fields, comp_ayume/comp_yui slots don't collide with barkindle/mobN/boss) + in-game 📖 guide (CHAPTERS companions clause — "each now standing beside you in their own true form: Ayume wears Leah's face; Yui, Irene's") + wiki.html (Companions bullet true-form note) + AGENTS_PVE.md (5c P4 bullet) + MASTERPLAN_24H.md (P4 ticked) — node logic test PASS (12 asserts, /tmp/p4_comp.js: Ayume loads Leah GLB & NEVER Irene + placeholder-while-loading, Yui loads Irene GLB & NEVER Leah, loaded-prefab reuse applies without reload/placeholder, unknown companion→placeholder only) + verbatim spawnCompanion node --check (p4_verbatim.js PARSE OK). Bash mount served the usual stale tail (0 grep hits for COMP_GLB, frozen-tail EOF) so head node-check couldn't see the edits — confirmed via Read-tool on the true file (lines 965–985, head region). Browser F12 the gate.
ale snapshot so no in-file head node-check. Browser F12 the gate.
- 2026-06-16 10:40 UTC — W4 Flash/Charge traversal gate — The Sundered Ledge — pve.html (WIRED the orphaned W4 scaffold: `spawnVeilIsles()` added in startRun after spawnCaches [~2306]; `blockGaps(hero)` added in the hero update block after avoidObstacles(hero) [~2497]. The pre-existing `mkVeilIsle`/`spawnVeilIsles`/`blockGaps`/`VEIL_ISLES` [~1724-1746] place a flat ledge disc at far SE (115,-115; islandR 13) ringed by a black **void moat** annulus 13→20 + violet rim, with its own `mkCache` sealed on it; `blockGaps` snaps any WALKING hero in the annulus back to the nearer rim so you can never walk across — only a **dash** [Kai's Charge E = pDash range 15, or a Mystic/Lightning Blink] lands at d<islandR, which blockGaps leaves untouched; dash from the ledge rim to leave) + in-game 📖 guide (🌑 THE SUNDERED LEDGE line in EXPLORING) + wiki.html (Sundered Ledge bullet) + AGENTS_PVE.md + MASTERPLAN_24H.md (W4 ticked) — node logic test pass (9 asserts, w4_gap.js: incremental 0.3 & 0.5/frame walkers never reach inside islandR; Charge from d20/d27 lands on the ledge; 5-range hop falls short/gated; egress from the ledge rim clears the moat) + verbatim stubbed-THREE reconstruction node --check + run (w4_recon.js: isle spawns, cache placed @115,-115, d=16 walker snapped to 13, null-safe). Bash mount served a stale snapshot so no in-file head node-check; validated via Read-tool reconstruction. Browser F12 the gate.
- 2026-06-16 10:55 UTC — W5 realm traversal puzzle — The Lantern Altar (colour-lock vault) — pve.html (new tail block ~1748 after blockGaps: `VEIL_PUZZLE` state + `PZ_COLS` + `mkLantern(x,z,col)` [post + dark-emissive orb], `pzReset()`, `spawnRealmPuzzle()`, `updatePuzzle(dt)`; `spawnRealmPuzzle()` call in startRun after spawnVeilIsles [~2344], `updatePuzzle(dt)` call in the hero loop after blockGaps [~2536]. spawnRealmPuzzle plants an altar at far NW (-122,70; R≈140<150, on-foot, NOT gap-gated) ringed by 3 lanterns ~120° apart [only one ever within the 3.6 light-range] and Fisher–Yates-shuffles a 3-colour solve order each run; updatePuzzle reveals the rune sequence ONCE within 22u [clued], lights the next-in-order lantern when the hero walks within 3.6 [emissive+sfx+fxRing+progress++], a WRONG lantern feeds "gutter out" + pzReset [all unlit, progress 0], and the final correct light sets solved → fxRing + mkCache at the altar [guaranteed Rare+ W3 vault hoard, NOT vacuumed — walk to it]; isCursed-gated like all loot; reuses mkCache/genCacheItem/feed/fxRing/sfxEquip — no new systems) + in-game 📖 guide (🏮 THE LANTERN ALTAR line in EXPLORING) + wiki.html (Lantern Altar bullet) + AGENTS_PVE.md + MASTERPLAN_24H.md (W5 ticked) — node logic test pass (13 asserts, w5_puzzle_0616.js run as w5_clean.js after `tr -d '\000'` stripped mount-injected NUL bytes: 3-lantern spawn + order permutation, clue-once, full-sequence solve→vault cache, solved-inert, first-light-advances, wrong-order full reset, no-vault-on-fail, Blight gate). Bash served a stale pre-edit pve.html/AGENTS snapshot so no in-file head node-check; the 3 edit sites confirmed at 1753/1768/2344/2536 via Read-tool Grep; head-region node-check clean (frozen-tail EOF only). SECOND W-item after the Sundered Ledge. Browser F12 the gate.
- 2026-06-16 11:15 UTC — W6 waypoint/vista unlock (reveal nearby map + finder's reward) — pve.html (new `discoverWaypoint(idx,d)` helper after the world-map click handler [~1391]: idempotent on `wpFound[idx]`, sets found, `fogReveal`s the waypoint corner [r6] AND the whole dungeon room [`Math.ceil(d.r/FOG_CELL)+2`], sfxEquip + discovery feed, then an isCursed-gated finder's reward — `bank.gold += 40+tier*20` + `skXP('slay',10+tier*6)` + a "✦ Vista bonus" feed; the in-loop discovery hook at ~2525 now calls `discoverWaypoint(z.i+1,z.d)` instead of the bare wpFound-set [the startRun spawn-zone set at ~2349 stays a plain assignment so starting in a zone is NOT a free reward]) + in-game 📖 guide (🗺 WAYPOINTS & VISTAS line in EXPLORING) + wiki.html (Waypoints & vistas bullet) + AGENTS_PVE.md + MASTERPLAN_24H.md (W6 ticked) — node logic test pass (19 asserts, w6_waypoint_0616.js: first-discover reward/sfx/feed, fog revealed at corner+centre, idempotent no-double, tier-scaled reward, cursed=reveal-but-no-reward, null-safe) + verbatim reconstruction node --check + run (w6_recon_0616.js). Bash mount served a stale pre-edit snapshot (934 lines, 0 grep hits for discoverWaypoint) so no in-file head node-check; both edit sites confirmed at 1391/2525 via Read-tool Grep. THIRD W-item. Browser F12 the gate.
- 2026-06-16 16:25 UTC — N29 Skip-scene affordance for dialogue — NEW on-theme close-out item: all N1–N28 & W1–W10 already ticked, so per §7's close-out clause the cron adds one small validated on-theme item (a replay-QoL win for the now-complete 7-episode campaign — long cutscenes like the 15-line betrayal are tedious to re-watch). pve.html (head dialogue region: a `⏭ skip` tab `#dlgSkip` span added inside `#dlgBox` [now position:relative] top-right; new `dlgSkip()` right after `dlgActive()` empties `_dlgQ` + calls `dlgNext()` so the queue-empty branch fires the N18 close sting + the scene's onDone exactly as a natural finish [episode/beat state preserved] — but it NEVER skips past an N16/N19 moral choice: if a `choices:` line is still queued it slices `_dlgQ` to START at that choice and presents it, and it's inert while choosing / presents a pending-but-unshown choice; the `_dlgSkipEl.onclick` handler [wired beside `_dlgEl.onclick=dlgNext` ~559] `ev.stopPropagation()`s so the tap doesn't also bubble to the `#dlg` backdrop advance; reuses the dialogue DOM + dlgNext/_showChoices — no new systems, no new save fields) + in-game 📖 guide (CHAPTERS block ⏭ skip sentence) + wiki.html (cutscenes bullet) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N29 added & ticked) — node logic test PASS (15 asserts, n29_skip.js via /tmp heredoc to dodge the scratch-mount freeze: plain-scene skip closes + onDone-once + close-path, inert while choosing, STOPS at a queued choice + presents the right one + ends after it resolves, pending-choice presented not skipped, no-op when closed) + node --check clean + head-region node-check clean (593 visible script lines parse, only the expected frozen-tail EOF; span@201, dlgSkip@499, wiring@559 all in the head). Browser F12 the gate.
- 2026-06-16 18:16 UTC — N35 "The Story So Far" in-run chapter recap (📜 Journal) — NEW on-theme close-out item: all N1–N34 & W1–W10 already ticked, so per §7's close-out clause the cron adds one small validated on-theme item (the chapter objective lived ONLY on the title's chapter-select via renderChapters' e.obj; once in a hunt nothing reminded you which episode you were in or its goal). pve.html (one surgical head edit in questUI ~397, between the ACT box and the 🔆 ELEMENTAL SHARDS header: `if(typeof EPISODES!=='undefined'){…}` reads selEp→episodeOf(cur) + epUnlocked() and renders "📖 THE STORY SO FAR — Episode N: Name" + setting/stance + a 🎯 objective line + a 7-row ladder marking cleared ✔ [e.id<unl], here ▸ [e.id===cur, gold + "← you are here"], frontier ◆, locked 🔒 ["???" name hidden + dimmed, e.id>unl]; pure read-only display over existing EPISODES data, NO new save fields/systems, reuses episodeOf/epUnlocked/selEp) + in-game 📖 guide (📖 THE STORY SO FAR paragraph before the MEMORIES block) + wiki.html (in-run chapter recap bullet) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (N35 added & ticked) — node logic test PASS (20 asserts, n35_storysofar.js: fresh frontier=1/Ep1 → header+objective+setting+▸here+Ep2 locked/??? + exactly one you-are-here + 6 locked; mid frontier=5 replaying Ep3 → header=replayed + Ep1/2/4 ✔ + Ep3 ▸ + Ep5 ◆ + Ep6/7 🔒; endgame frontier=7 → finale header + 0 locked + Ep6 ✔) + node --check clean + the complete edited questUI function extracted from the true file & node-checked in isolation (parses clean; edit in the bash-visible head this run — grep confirmed the new string present). Browser F12 the gate.
- 2026-06-16 18:38 UTC — N36 In-run objective banner — NEW on-theme close-out item (all N1–N35 & W1–W10 ticked; pays off the N35/N8 "surface the objective in-run" polish-open — N35 put the goal in the 📜 Journal but it only shows if the player opens the Journal). pve.html, two surgical edits: (1) a head helper `showObjective()` after chapBtnLabel (~653, bash-visible) — feeds "🎯 Episode N — Name: obj" in warm gold 0xffd9a0 for the current chapter [selEp→episodeOf, falls back to epUnlocked()], all try/caught; (2) the startRun cutscene tail (~2718) now passes an onDone into maybeChoiceMoment so the banner fires AFTER the intro/outro cutscene AND the N16 dark/light choice resolve [maybeChoiceMoment(function(){showObjective();})], with an `else showObjective()` when the choice system is absent so the banner always lands. Reuses the feed() toast + EPISODES data — NO new save fields/systems/DOM. + in-game 📖 guide (📖 THE STORY SO FAR paragraph — on-screen banner note) + wiki.html (in-run chapter recap bullet) + AGENTS_PVE.md (5x bullet) + MASTERPLAN_24H.md (N36 added & ticked) — node logic test PASS (9 asserts, n36_objective.js: selEp→that chapter name+obj+warm colour, selEp-undefined→epUnlocked fallback, Ep7 finale, bogus selEp→Ep1 fallback no-throw) + node --check clean + head-region node-check clean (577-line head extract parses to the frozen-tail EOF only; helper edit at line 653 in the head, startRun-tail wiring confirmed via Read-tool Grep at ~2718). Browser F12 the gate.
- 2026-06-16 19:12 UTC — P2 Barkindle revive at the Kennel (town, for gold) — FIRST P0 PLAYTEST FIX (the new task file ranks P0 above N/W; P1 was fixed manually, P2 is the topmost unchecked). pve.html: (1) new `KENNEL_POS={x:-7,z:5}` town prop (doghouse box + cone roof + arched mouth + glowing food bowl, ~1358) + a clickable infoSpot "🐾 The Kennel"; (2) run-scoped `let barkindleDown=false;` + `reviveCost()=50+diff*15` + `reviveBarkindle()` (~2566, after updateEggs) — mirrors the egg-pet mid-run spawn formula (170+diff·20+12·(lvl-1))·vetMult HP / (12+diff·2.5)·… dmg, applies petTier/PET_TIERS scale, re-applies the barkindle GLB via applyModel/autoLoad+look, fxRing + feed; (3) pet-death branch in hit() (~2324: the `else removeUnit(u)` now sets `barkindleDown=true` + a "fallen — revive at the Kennel" feed when the dying pet is `kind:'pet'&&slot:'barkindle'&&petName:'Barkindle'`); (4) openBuild() gains a **town Kennel branch** (~2612, between the forge and wild branches) — when in town within 10u of KENNEL_POS it shows a forge-style "🐾 Revive Barkindle" button (`ok=barkindleDown&&gold>=cost`, dis otherwise, "alive & well" when not down) → reviveBarkindle(); town fallback feed mentions the Kennel; (5) `barkindleDown=false` reset in startRun beside the Barkindle spawn (~2734). Reuses mkUnit/applyModel/petTier/PET_TIERS/openBuild — NO new systems, NO new save fields (run-scoped, matching the fresh per-run Barkindle spawn). Pet death is no longer permanent for the signature companion. + in-game 📖 guide (PETS block — 🐾 Barkindle revive sentence) + wiki.html (new "Barkindle is never lost for good" bullet) + AGENTS_PVE.md (5c bullet) + MASTERPLAN_24H.md (P2 ticked). Validated: node logic test PASS (19 asserts, /tmp/p2_revive.js via heredoc to dodge the scratch-mount freeze: no-revive-when-not-down, Barkindle death flags / generic pet death doesn't, revive blocked by gold [no state change], cost scales diff1=65/diff9=185, success spawns Barkindle full-HP friendly pet + deducts gold + clears flag, Primal tier+eliteScale applied, openBuild gating enabled only when down & affordable) + head-region node-check clean (570 visible script lines parse, frozen-tail EOF only; all P2 edits are in the frozen tail — validated via the verbatim reconstruction logic test per the documented dev gotcha) + Read-tool Grep confirms all 5 edit sites present. Browser F12 the gate.
- 2026-06-16 18:58 UTC — N37 Objective compass needle on the minimap — NEW on-theme close-out item (all N1–N36 & W1–W10 ticked; pays off the EXPLICIT N8/N35/N36 polish-open "a minimap objective marker / compass needle toward the chapter goal" — the compass was diegetic flavour [Yume's Ep6 gift] with no actual navigation aid). pve.html, two surgical edits: (1) head helper `objectiveDest()` after showObjective (~654, bash-visible) — reads selEp→episodeOf [falls back to epUnlocked()] and returns the current chapter's destination zone centre {x,z,r,name}: Ep1/Carnival [zone<0]→the town hub {TOWN.x,TOWN.z,TOWN.r}, else DUNGEONS[e.zone] centre+radius; try/caught, read-only over EPISODES/DUNGEONS/TOWN; (2) at the END of drawMM [frozen-tail ~2839, after the units loop] a try/caught block draws a gold compass needle [#ffd96a line + arrowhead] from the hero's minimap point P(hx,hz) toward objectiveDest() [ang=atan2(od.z-hz,od.x-hx), len 12px] and RESTS [draws nothing] once hypot(dest-hero)<=od.r [inside the destination zone]; works in both wild [cx=0] and dungeon [cx=zone centre] projections since it anchors on the hero's own P() point; NO new save fields/systems, reuses objectiveDest/P/mmc/EPISODES/DUNGEONS. + in-game 📖 guide (🧭 THE OBJECTIVE COMPASS line in EXPLORING) + wiki.html (Objective compass bullet) + AGENTS_PVE.md (5x bullet) + MASTERPLAN_24H.md (N37 added & ticked) — node logic test PASS (13 asserts, /tmp/n37.js: Ep1→town hub, Ep2→DUNGEONS[0], Ep7→Abyssal Vault [Castle/Yomi-no-Tō spire], arrived gating 50<60 rests/70>60 shows, angles π/4 & π/2 & 0, selEp=0→epUnlocked fallback, Ep1 rests in town/points home in the wilds) + verbatim stubbed-mmc/THREE reconstruction of the drawMM tail (/tmp/n37recon.js parses+runs) + head-region node-check clean (571-line head extract parses; only the expected frozen-tail EOF at 572; objectiveDest in head parsed, needle in frozen tail applied via exact-match Edit, confirmed at 2839–2847 via Read-tool Grep). Outputs-dir scratch mount froze the edited test's tail so the test re-ran via /tmp heredoc per the documented dodge. Browser F12 the gate.
- 2026-06-16 19:30 UTC — P3 MOBA-style pet command for Barkindle — SECOND P0 PLAYTEST FIX (P1 manual, P2 last run; P3 is now the topmost unchecked P0, ranked above all N/W). Ported from index.html's pet select+command. pve.html, 6 surgical edits: (1) helper block before pointerdown (~2768) — run-scoped `let petSel,petSelRing` + `makePetRing()` [blue RingGeometry added to scene], `selectPet(u)` [ring on, sfx/fxRing/feed], `clearPetSel()`, `commandPet(pt,foe)` [foe→cmdTarget+target, else→cmdMove; then clearPetSel so control returns to the hero]; (2) left-click handler — pick a team-0 pet within 2.6u → selectPet → return (before the infoSpot/node/portal inspection); (3) right-click handler — `if(petSel&&!petSel.dead){commandPet(pt,foe);return;}` inserted right after `const foe=` and BEFORE the hero move/attack logic so the hero never also moves; (4) keydown — while a pet is selected, S = stop (clear all orders + deselect), H = hold (cmdHold + deselect), each `return`s before the hero S/Esc/QWER hotkeys; (5) pet AI block (~2990) — the existing autonomous flee/assist/30u-follow/node-gather AI WRAPPED in an `else{u.cmdTarget=null; …}` preceded by the command chain [ring-follow update; `cmdHold`→defend a foe within range+0.6, no chase; `cmdTarget`&&!dead→chase+tryAtk until it dies; `cmdMove`→moveTo then clear]; deliberately NOT a `continue` so the shared updateAnim/threat code at the loop tail (3030-3033) still animates the commanded pet; brace count: added one `}` at the node-gather close (`}}`→`}}}`); (6) startRun Barkindle spawn + the hit() pet-death branch both `clearPetSel()` to drop a stale selection/ring. No new systems, NO new save fields (run-scoped, matching the per-run Barkindle spawn). Files: pve.html + in-game 📖 guide (PETS block "COMMANDING A PET" line) + wiki.html (Commanding a pet bullet) + AGENTS_PVE.md (5c P3 bullet) + MASTERPLAN_24H.md (P3 ticked). Validated: node logic test PASS (21 asserts, /tmp/p3_pet.js: select sets ring on pet, one command [move|attack] deselects + clears the other order, cmdMove persists while travelling then clears on arrival→auto resumes, cmdTarget chases+kills then auto-clears→auto resumes, hold defends a foe in range but never chases a distant one, stop fully clears→autonomous, commandPet on a dead pet just clears selection) + verbatim source parse check (/tmp/p3_verbatim.js node --check OK on the actual inserted text) + Read-tool brace verification of the wrapped pet-AI block (2990-3012, balanced `}}}`). The bash mount serves the frozen 833-line stale tail (head node-check shows only the expected mid-script EOF), so tail edits validated via the §5-gotcha method (Read-tool brace checks + isolated node --check), Grep confirms all edit sites present (23 hits). Browser F12 the gate.
