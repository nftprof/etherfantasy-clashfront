# EF MOBA — Map / Environment Polish Log

Rotation checklist (one coherent slice per run):

- [x] 1. Grass texture overhaul
- [x] 2. Organic lanes (spline ribbons, worn dirt)
- [x] 3. Ground colour variation (biome splotches)
- [x] 4. Scatter decoration (InstancedMesh tufts/rocks/flowers/bushes)
- [x] 5. Map border / tree-line vignette
- [x] 6. High-ground read (tint/texture HILLS)
- [x] 7. Structure ground decals
- [x] 8. Lighting/fog mood
- [x] 9. Polish refinements (refine weakest) — run 1: grass anti-tiling; run 2: grass macro-patches + de-squared/rotated tiling; run 3: lane centreline meander; run 4: in-field trees → layered pines; run 5: scatter flowers → stem+blossom two-tone; run 6: perimeter tree-line → two-tier pines; run 7: scatter bushes → multi-lobe leafy form; run 8: scatter rocks → clustered baked-shaded boulders

> Note: `/play` must be rebuilt/redeployed (+ obfuscation build re-run) for players to see these. Locally a hard-refresh shows them.

## Entries

- 2026-06-27T23:07Z — item #1 / Grass texture overhaul — Replaced the 128px `makeGrassTexture` (random 1–2px specks → pixel mush) with a 512px seamless layered meadow: base green + 54 soft radial meadow splotches (4 tones, lighter/darker patches) + 900 smooth quadratic blade strokes (4 greens) + fine ±value noise; every splotch/blade is stamped at 9 wrapped offsets (toroidal) so the tile repeats with no visible seam, and `grassTex.repeat` lowered 34→18 to reduce obvious tiling. anisotropy=4 for cleaner grazing-angle sampling. — Validated: scratch `grass_scratch_v1.js` node --check + run OK, gen ~4ms, correct stamp counts (486 patch arcs, 8100 blade strokes). — Files: index.html (makeGrassTexture, grassTex.repeat).
- 2026-06-27T23:25Z — item #2 / Organic lanes — Replaced straight rectangular `laneStrip` PlaneGeometry strips with `laneRibbon`: a centripetal `CatmullRomCurve3` through each lane's existing waypoints (LANES data untouched), sampled at len/3 segments, built into a triangle-strip BufferGeometry with per-vertex width wobble (4.4±~25%, dual-sine seeded per lane) for a trodden look, draped on terrain via heightAt+0.06. New `makeLaneTexture` (128px): layered dirt specks (3 tones) + faint longitudinal drag streaks, with alpha faded to 0 toward both long edges so the path blends into grass (no hard rectangle border). Material now transparent+depthWrite:false+renderOrder:1 over the opaque ground. Lanes now curve and look worn, not ruler-straight. — Validated: scratch `lane_ribbon_scratch_v1.js` node --check + run OK; 3 lanes = 688 verts / 682 tris total, no NaN, strip-aligned indices, Y draped correctly. — Files: index.html (makeLaneTexture, laneMat, laneRibbon, lane loop). Note: `makeDirtTexture` now unused (left in place, revers- 2026-06-28T00:12Z — item #5 / Map border / tree-line — Added a bounded-arena read: (1) a square vignette frame — ONE 256px canvas plane over the map, chebyshev-distance smoothstep alpha so only the outer ~18% band darkens (start ≈137.8w, ground edge 168w), transparent+depthWrite:false so it never z-fights the ground/lanes and leaves bases(115w)/cores(132w) untouched; (2) a perimeter tree-line — 200 seeded pines as TWO InstancedMeshes (foliage cone + trunk cylinder) placed in the outer band (99–118*MAPK = 138.6–165.2w, clear of lanes/bases/cores); (3) 70 instanced border boulders, same band. All castShadow=false, seeded RNG (identical every load), placed once, zero per-frame work. — Validated: scratch border_treeline_scratch_v1.js node --check + run = ASSERT PASS (200 trees + 70 rocks placed, 0 out-of-band, 0 NaN, core/base < vignette-start & < tree-band, no overhang past ground edge). — Files: index.html (new border/tree-line IIFE after the scatter IIFE).
- 2026-06-28T00:40Z — item #7 / Structure ground decals — Added worn-dirt circles/soft shadows under every fixed structure so they sit in trodden earth instead of floating on clean grass: ONE 256px procedural decal canvas (radial earthy gradient → fully transparent rim, +3 worn rings, +160 source-atop scuff specks, deterministic seed) on ONE InstancedMesh of 21 flat CircleGeometry(1,28).rotateX(-PI/2) discs — 2 cores (r=10.5·MAPK), 2 fountains (8), 12 towers (4.6), 5 build pads (3.6) — at the exact initRTS/startGame coords (data untouched), draped via heightAt+0.05, MeshBasicMaterial transparent depthWrite:false opacity .6 renderOrder 1, per-instance Y-spin + slight shade variance via instanceColor. castShadow=false, built once at load, zero per-frame work. — Validated: scratch decals_scratch_v1.js node --check + run = ASSERT PASS (21 decals, all flat scale y=1, y>0, shades in [.78,1], 1 InstancedMesh added). — Files: index.html (new STRUCTURE GROUND DECALS IIFE after the border/tree-line IIFE).
- 2026-06-28T01:08Z — item #9 / Polish refinement (weakest = grass tile-grid) — Target the project's named #1 complaint ("flat repeating grass"): the texture was seamless within a tile but still repeated on a visible AXIS-ALIGNED SQUARE grid (`repeat.set(18,18)`). Two zero-cost changes to existing objects (no new draws/geometry/per-frame work): (1) enriched `makeGrassTexture` with 6 very-large soft macro patches (r 160–310px, 3 tones, alpha .22, toroidally stamped) FIRST in the paint order so even one tile reads as a varied hand-painted meadow with big tonal sweeps; (2) broke the grid by de-squaring + rotating the tile sampling — `repeat.set(15,17)` (different H/V period kills the square lattice) + `center(0.5,0.5)` + `rotation=0.16` (tile grid no longer aligns to world/camera axes). Seamless wrapping preserved (rotation samples the still-toroidal field via RepeatWrapping); anisotropy=4 kept. — Validated: scratch `grass_scratch_v2.js` node --check + run = ASSERT PASS (540 patch arcs = 6·9 macro + 54·9 splotch, each with a gradient; repeat 15×17; center .5,.5; rotation .16; anisotropy 4; gen 3ms). — Files: index.html (makeGrassTexture, grassTex repeat/center/rotation).
- 2026-06-28T01:25Z — item #9 / Polish refinement (weakest = lanes still read straight) — Directly attacks the named #1 complaint's "ruler-straight lanes": the mid lane's 3 waypoints are collinear, so its ribbon drew as a dead-straight diagonal. Added a gentle organic meander to the VISUAL ribbon centreline in `laneRibbon` only — a low-freq lateral offset `taper·(2.0·sin(tt·2π+seed)+1.0·sin(tt·14.77+seed·2.3))` along the path normal, where `taper=sin(π·tt)` fades it to exactly 0 at both ends so ribbons still meet bases/structures cleanly. LANES waypoint COORDINATES untouched (units still path the true line); amplitude (max ~2.3u) kept below the 4.4u half-width so a unit on the true centreline always stays ≥1.27u inside the painted path. Now even the mid lane snakes like a trodden trail. Built ONCE at load, zero new per-frame work. — Validated: scratch `lane_meander_scratch_v1.js` node --check + run = ASSERT PASS (3 lanes; meanders true, un- 2026-06-28T01:39Z — item #9 / Polish refinement (weakest = crude in-field "trees") — The 32 scattered in-field decorative trees were single flat dark cones (0x2a4030, ConeGeometry, 6-sided) pinned at a CONSTANT y=2.5 (ignoring terrain) — cardboard cutouts that looked far cruder than the seeded instanced perimeter tree-line and undercut the "hand-crafted" read. Replaced each with a layered low-poly pine: a tapered trunk (CylinderGeometry 0.4→0.6, brown 0x4a3522, shared mat) + two stacked foliage cones (7-sided, sizes baseR & baseR*0.66) with per-tree green variation via setHSL(0.30±, 0.42–0.58 sat, 0.20–0.28 light) + flatShading, each tree terrain-DRAPED at heightAt(x,z) and given a random Y spin. Crucially the placement do-while and `obstacles.push({x,z,r:2})` are byte-for-byte unchanged, so pathing/collision/minimap are untouched — only the rendered mesh changed. — Validated: scratch deco_trees_scratch_v1.js node --check + run = ASSERT PASS (32 groups, obstacles +32 all r:2, no NaN, draped Y==heightAt, all keep-out invariants |x-z|≥18·MAPK & lane clearances intact, each tree = trunk+2 cones). — Files: index.html (in-field tree loop, ~l.607). FPS-safe: 32 static groups (96 trivial meshes, was 32), built ONCE at load, castShadow off, no textu- 2026-06-28T02:10Z — item #9 / Polish refinement (weakest = perimeter tree-line single cones) — The 200 perimeter pines (the map's largest tree mass, forming the bounded-arena backdrop) were single flat `ConeGeometry(2.6,8,7)` lollipops — cruder than the layered in-field pines upgraded last run, an inconsistency that undercut the hand-crafted read. Gave each pine a TWO-TIER silhouette: a wide lower cone `ConeGeometry(3.0,6.5,7)` (centre gy+5.2·s) + a narrower upper cone `ConeGeometry(2.0,5.5,7)` (centre gy+8.6·s), trunk unchanged (gy+1.5·s). Per-tree green via setHSL(0.30±,0.34–0.50 sat,0.16–0.24 light); upper tier +0.05 lightness (clamped ≤0.45) for a sun-lit top. Same seeded RNG/band/placement — layout byte-identical, only the rendered foliage changed. — Validated: scratch `treeline_two_tier_scratch_v1.js` node --check + run = ASSERT PASS (200 pines; fol/fol2/trk each 200 matrices; both foliage tiers 200 colours; all in [IN,OUT] band; trunk<lower<upper Y order; upper tier lighter & clamped; no NaN; 3 meshes added). — Files: index.html (perimeter tree-line IIFE ~l.730). FPS-safe: one extra capped InstancedMesh (200 
- 2026-06-28T02:51Z — window closed — safe to delete the efmoba-map-polish task
- 2026-06-28T04:06Z — window closed (confirmed, no changes made) — safe to delete the efmoba-map-polish task
- 2026-06-28T04:21Z — window closed (confirmed, no changes made) — safe to delete the efmoba-map-polish task

- 2026-06-28T04:36Z — window closed (confirmed, no changes made) — safe to delete the efmoba-map-polish task
- 2026-06-28T04:53Z — item #9 / Polish refinement (weakest = flat fountain pads) — The two home-fountain pads were plain flat dark discs (`MeshLambertMaterial color 0x18243a` + faint emissive), the least-finished structure read on the map. Added `makeFountainTex(hex)`: a baked 256px procedural "magical spawn-circle" disc — `#18243a` base, a team-tinted central radial glow (3 stops), 6 concentric ripple rings (fading alpha), 24 evenly-spaced rune ticks around an outer ring (r=104), and a bright centre dot — mapped onto the EXISTING CircleGeometry(8,28) pad (default radial UVs centre the design). Cached per team colour (exactly 2 textures), anisotropy=4, emissive team tint kept. NO new geometry, NO collision/obstacle change (heroes still spawn/heal on the same pad), zero per-frame cost (built once in mkFountainPads at load). The blue/red ring + pulse around the rim are untouched. — Validated: scratch `fountain_tex_scratch_v1.js` node --check + run = ASSERT PASS (texture returned w/ anisotropy 4; 1 gradient/3 stops; arc=8 glow+6 rings+dot; stroke=30 = 6 rings+24 ticks; 24 moveTo/lineTo ticks; fill=2; rgba channel extraction 0xff5d5d→255,93,93; cache reuses same ref w/ no extra draws; distinct colour→distinct texture). — Files: index.html (new makeFountainTex + mkFountainPads pad material). FPS-safe: 2 static cached textures, no per-frame work, reversible (revert pad material to color:0x18243a).
- 2026-06-28T07:24Z — item #9 / Game feel (new: floating HEAL numbers) — popDamage existed but heals were silent. Added `popHeal(u,amt)` — a green "+N" mirror of popDamage that reuses the EXISTING `.dmg` float keyframe + `#dmgLayer` (new CSS `.dmg.heal{color:#5dff9b}`), early-outs on amt<=0 (already-full HP) and off-screen (v.z>1). Wired into the three DISCRETE, cooldown-gated heal events only (never per-frame regen): pet support heal F (owner +N and each nearby healed ally), the 🧪 potion (+25%), and the Avatar-of-War super (+30%). Each call site captures `_hp0` before the `Math.min(maxHp,...)` clamp and passes the TRUE delta, so a heal at full HP shows nothing. AoV-style hype now reads on both sides of combat. — Validated: scratch `heal_pop_scratch_v1.js` node --check + run = ALL ASSERTS PASS (one green "+24" node for 23.6; no node for amt<=0; no node off-screen; delta clamps 35/10/0; potion delta 25). — Files: index.html (.dmg.heal CSS, new popHeal fn, petHeal owner+ally, Avatar-of-War f, heroPotion). FPS-safe: pure DOM/CSS, event-driven only, zero per-frame cost, reversible (remove popHeal + 5 call edits).
- 2026-06-28T07:39Z — item #9 / Polish refinement (weakest = static lawn ignores weather) — The rain/storm system already darkens fog, sun, hemi light and rain density, but the GROUND stayed the same bright dry lawn (0xbfd6a2) in a downpour — a flat read that broke the weather mood. Added a weather-reactive WET-GRASS tint: two `THREE.Color` constants `_gDry=0xbfd6a2` / `_gWet=0x86a06b` + one reusable `_gScratch`, all built ONCE next to the ground mesh. In `weatherTick` (which already eases sun/hemi/fog each frame) the lawn colour eases toward dry→wet by how heavy the rain is (`M.rain/0.75`: storm=full wet, rain≈60%, sunny=dry) via two IN-PLACE Color ops (`_gScratch.copy(_gDry).lerp(_gWet,..)` then `ground.material.color.lerp(_gScratch,dt*1.2)`) — zero heap allocation, no new draws/geometry, gated by rain amount. Grass darkens & deepens as a storm rolls in and dries back to the bright AoV lawn when skies clear. — Validated: scratch `wetgrass_scratch_v1.js` node --check + run = ALL ASSERTS PASS (sunny stays exactly dry over 300f; storm converges to 0x86a06b; rain settles at 60% wet & strictly darker than dry on all channels; dries back to dry when clear; per-frame ease factor 0.02 in (0,1) so it never overshoots). — Files: index.html (_gDry/_gWet/_gScratch after ground mesh; one lerp in weatherTick). FPS-safe: 2 Color ops/frame in an already-per-frame fn, no alloc, reversible (remove the 2 added lines).

- 2026-06-28T07:53Z — game feel / MINIMAP territory tint (readability/orientation) — The minimap began each frame with a flat `#0c1420` fillRect, giving no read of WHICH half of the map is yours (an AoV-style orientation cue that was missing). Baked ONE 150x150 offscreen `mmBase` canvas at load: the dark base field + two faint radial team-territory glows — blue (64,124,214 @ .18) centred on the team-B home corner (bottom-left, mm 4.5,145.5) and red (214,72,72 @ .18) on the team-R corner (top-right, mm 145.5,4.5), each a 3-stop radial (r=128) fading to fully transparent so the centre stays neutral and structures/blips read clearly. drawMM now blits `mmBase` via ONE drawImage in place of the old fillRect — per-frame minimap cost UNCHANGED (one fill -> one drawImage), no new geometry/textures in the GL scene, built once. — Validated: scratch `mm_tint_scratch_v1.js` node --check + run = ALL ASSERTS PASS (canvas 150x150; exactly 3 fillRect = 1 base + 2 glows; 2 radial gradients, each 3 stops with outer fully transparent & alpha decreasing inward; blue glow in bottom-left quadrant, red in top-right, each centred on its fountain corner; correct blue/red colours). — Files: index.html (new mmBase IIFE after mmY ~l.3504; drawMM first line). FPS-safe & reversible (delete mmBase + revert drawMM first op to `mmc.fillStyle='#0c1420';mmc.fillRect(0,0,150,150);`).
- 2026-06-28T08:07Z — weather juice / STORM SKY-FLASH (lightning drama) — Bolts already flashed the hemi LIGHT (lit the ground) but the sky/background stayed dark during a strike — the lightning never lit the sky itself, a flat read vs a real/AoV storm. Added one baked Color `_skyFlash=0x4a5d7d` (pale storm blue-grey, built once next to fogCol). In weatherTick, right after the per-frame `scene.background.copy(fogCol)`/`fog.color.copy(fogCol)`, gated `if(flashT>0)`: in-place lerp background toward _skyFlash by `fk=min(.7,flashT*4)` and fog.color by `fk*.6` (fog brightens less, keeps depth). fk is capped <.7 so it never blows out, and decays with the existing flashT (~0.16s, only on storm bolts ~every 2.5–8s). Zero heap alloc (one shared Color, in-place lerps), no new geometry/draws, gated behind the rare flash window — fully performance-neutral. Now the whole sky pops with each bolt then fades back to dark storm fog. — Validated: scratch `skyflash_scratch_v1.js` node --check + run = ALL ASSERTS PASS (no-flash sky==fog & dark; flash frame sky 0.797 vs 0.177 baseline, fog brightens less; capped below full _skyFlash; monotonic decay; returns to dark fog after). — Files: index.html (_skyFlash const ~l.323; flash lerp in weatherTick ~l.336). Reversible (delete _skyFlash + the one gated if-line).
- 2026-06-28T09:08Z — item #9 / Game feel (new: SCREEN-SHAKE on hype banners) — The biggest AoV-style moments — First Blood, Double→Penta multi-kills, Killing Spree/Rampage/Unstoppable — already fired a `bigBanner()` + a generated stinger (sfxFirstBlood/sfxMulti/sfxSpree) but gave NO tactile punch; the existing `addShake()` (one decaying scalar, already fxLevel>=2-gated, event-driven, used by the super & structure-collapse) was the obvious missing pairing. Added one `addShake()` call to each of the three mutually-exclusive banner branches: First Blood `addShake(0.7)`, multi-kill `addShake(min(1.4,0.45+killer.multi*0.2))` (Double .85 → Penta clamps 1.4, scales with the count), spree `addShake(0.55)`. Only one branch fires per takedown, so at most one shake per kill. No new geometry/textures/DOM, no per-frame cost (addShake just raises the already-decaying shakeMag), auto-suppressed at the low-FPS tier via the existing fxLevel guard inside addShake, and never exceeds the built-in 2.2 cap. — Validated: scratch `bannershake_scratch_v1.js` node --check + run = ALL ASSERTS PASS (FB=0.7; mk 2/3/4/5/9 = .85/1.05/1.25/1.4/1.4 clamped & monotonic; spree=0.55; addShake hard-caps 2.2; fxLevel>=2 → 0; keeps max, never lowers). — Files: index.html (3 addShake calls in the kill-banner chain ~l.1502-1505). FPS-safe & reversible (delete the 3 addShake(...) calls).
- 2026-06-28T10:08Z — generated audio / weather-reactive NATURE AMBIENCE bed — The music intensity already followed the weather but there was NO ambient nature layer: fair skies and downpours sounded equally "empty" between music notes. Added two soft, event-driven sfx — `sfxBird()` (a 2–3 note descending sine chirp, ~1500–2400Hz, vol .016–.024) and `sfxGust()` (two trailing low-pass noise swells, vol .03–.045) — and fired them rarely & probabilistically from the EXISTING `startMusic` interval (every 4th step: birds at 13% in sunny/cloudy, wind/rain swells at 16% rain / 22% storm). No new timer, no per-frame cost, reuses tone()/noiseHit()/sfxG graph, respects AC/S_MUTE, kept very quiet so combat SFX always sit on top. Ambience now tracks weather: birdsong on the bright AoV lawn, rising wind/rain hiss as a storm rolls in. — Validated: scratch `ambience_scratch_v1.js` node --check + run = ALL ASSERTS PASS (sunny low-rng = 3 soft sine chirps, up-then-down slide; sunny high-rng & off-beat steps silent; rain = 2 soft noise swells; rain/storm probability thresholds .16/.22 gate correctly; muted & no-AC = silent). — Files: index.html (new sfxBird/sfxGust after sfxClash ~l.448; one ambience line in startMusic interval ~l.563). FPS-safe & reversible (delete the 2 fns + the ambience if-block).
- 2026-06-28T11:24Z — game feel / LOW-HP DANGER frame on HP bars (readability/target-priority) — The HP bar already had a ghost-drain chunk, glossy sheen, tick marks and slow/haste pips, but no "this unit is nearly dead" cue — so a 5%-HP runner read the same as a 95%-HP one at a glance (the core AoV "finish it" target-priority signal was missing, and for enemies the fill is already red so length alone is a weak read). Added a static bright-red outline drawn inside `drawBar` only when a LIVING unit is at <=28% HP: `if(pc>0&&pc<=0.28){c.strokeStyle='rgba(255,46,46,0.95)';c.lineWidth=1.6;c.strokeRect(1.2,1.2,61.6,7.6);}`. The `pc>0` guard means dead/0-HP units never frame; static (no pulse) so it adds NOTHING per frame beyond the existing draw — drawBar is only called on hp-change/regen, and this is a literal-string strokeStyle + one strokeRect (no gradient, no object alloc), safe in the regen hot loops. Pops on both green (ally) and red (enemy) bars. — Validated: scratch `lowhp_bar_scratch_v1.js` node --check + run = ASSERT PASS (8/8: full-HP no frame; 28% inclusive triggers; 29% none; low-HP enemy frames w/ correct red colour; 0-HP dead none; exactly one strokeRect, no loop; geometry inside 64x10 canvas). — Files: index.html (drawBar ~l.1021). FPS-safe & reversible (delete the one `if(pc>0&&pc<=0.28){...}` line).
- 2026-06-28T11:38Z — item #9 / Structure polish (weakest = flat-coloured tower shafts) — Towers were the last major structure on flat single-colour Lambert (shaft 0x55657f) while bases/cores/fountains got pedestals/shards/baked discs — a cardboard read up close. Added `makeStoneTex()`: ONE 128px baked stone-block canvas (dark mortar base 0x3d4757 + 30 offset brick courses [5 blocks × 6 rows, alternating ±16px joint offset] each with per-block ±tone variation 0.84–1.12× of 0x5a6880 + 260 fine ±speckle), RepeatWrapping repeat(2,3) anisotropy 4, cached in `_towerStoneTex` (built ONCE on the first tower, shared by all 12). Tower shaft material → color 0xffffff + map=_towerStoneTex so it now reads as a fortified stone turret; band/cap accent colours + emissive team orb untouched. setTowerTierVisual (scale + gold rings) and obstacle/collision data unchanged. — Validated: scratch `stone_tex_scratch_v1.js` node --check + run = ALL ASSERTS PASS (1 base fill 128², 30 brick fills, 260 speckles, repeat 2×3, anisotropy 4, all 30 block rgb channels 0–255, all 260 speckle alphas in [0,0.1]). — Files: index.html (new makeStoneTex + `_towerStoneTex` before placeholder ~l.1040; tower shaft material in placeholder tower branch). FPS-safe: 1 shared cached 128px texture, built once, no new geometry/draws/per-frame work, reversible (revert shaft material to color:0x55657f, delete makeStoneTex + _towerStoneTex).
- 2026-06-28T12:30Z — game feel / SHUTDOWN hype (banner+sting+shake; was feed-only) — Ending an enemy's ≥3-kill streak (`shut>0`) only printed a `feed()` line while First Blood / multi-kills / spree milestones all got the full bigBanner + stinger + screen-shake treatment — a real AoV hype gap (the celebrated "Shutdown!" moment was silent & bannerless). Added `sfxShutdown()` (a bright "cash-in the bounty" ka-ching: 1568Hz square ching + impact noise, then a 1046→1318Hz triangle resolve + 1976Hz coin shimmer — distinct from sfxKill 523/784 & sfxCoin 1230/1840) and slotted SHUTDOWN into the EXISTING kill banner-priority chain as a new tier just below spree: changed the final `else sfxKill();` to `else if(shut){bigBanner('💰 SHUTDOWN',…,0xffd34d,46);sfxShutdown();addShake(0.6);} else sfxKill();`. New priority: First Blood > multi-kill > spree > SHUTDOWN > plain kill — so exactly ONE banner/sound/shake per kill (no stacking, and plain sfxKill is suppressed when shutdown headlines). shake 0.6 sits between spree (0.55) & first-blood (0.7). Reuses tone()/noiseHit()/bigBanner()/addShake() (addShake already fxLevel>=2-gated); `mine` framing handles you-vs-enemy; `u.streak` read before its 1609 reset. NO geometry/textures/DOM, event-driven (once per qualifying takedown), zero per-frame cost. — Validated: scratch `shutdown_scratch_v1.js` node --check + run = ALL ASSERTS PASS (sfxShutdown leads 1568Hz+impact, freq distinct from kill/coin, muted & no-AC silent; chain: FB/mk/spr each still win over shutdown; shutdown headlines→banner+sfxShutdown+shake 0.6 & NOT sfxKill; no-shutdown→sfxKill & no shutdown banner; enemy-side banners too; shake ordering 0.55<0.6<0.7). — Files: index.html (sfxShutdown ~l.447; kill banner chain ~l.1612). FPS-safe & reversible (delete sfxShutdown + revert the `else if(shut){…}` tier back to `else sfxKill();`).
are (boss respT=150s), respects AC/S_MUTE, NO geometry, NO per-frame work, fully reversible. — Validated: scratch `boss_fanfare_scratch_v1.js` node --check + run = ASSERT PASS (mine: gong 98 sine + 5 sawtooth ascending [262,330,392,523,659] + bell 1046 + sine tail 523, no 110 toll; enemy: 4 square descending [330,294,247,196] + 110 sine toll, no bell; banner/ping colour+size+crit framing; null-killer→enemy framing + 'item' fallback, no throw; muted & AC-off both audio-silent but visual banner/ping still fire; timer offsets 120+i·95 exact). — Files: index.html (sfxBoss ~l.442; boss branch ~l.1366). FPS-safe: zero per-frame cost, reversible (delete sfxBoss + the 3 wiring lines in the boss branch).

- 2026-06-28T07:07Z — weather juice / RAIN STREAK quality (drops were round dots) — The rain cloud (ONE THREE.Points, 1 draw call) rendered every drop as a plain round `PointsMaterial` dot (size .5, no map) — reads as floating specks, not falling rain. Added `rainStreakTex`: a baked-once 64px canvas sprite — a vertical bright core column (3-stop linear gradient, transparent at both ends) drawn as 7 stacked 1px columns (x −3..+3) with a symmetric horizontal alpha falloff (centre full, edges faint) → a soft vertical RAIN STREAK with transparent sides. Applied as `rainMat.map` (tinted by the existing 0x9ab8d8 blue) and bumped `size` .5→1.4 so the streak length reads; transparent/depthWrite:false unchanged. Now drops look like real rain streaks. NO new geometry/draw calls (same single Points cloud), NO per-frame work (texture built ONCE at load), the FPS auto-degrade (drawRange halving/off) and per-frame fall animation are byte-unchanged. — Validated: scratch `rain_streak_scratch_v1.js` (canvas-mock) node --check + run = ASSERT PASS (vertical gradient 3 stops, middle brightest, ends transparent; 7 columns 1px×58 spanning 29..35 centred on 32; centre alpha 1, edges symmetric & dimmer but >0; CanvasTexture w/ LinearFilter; rainMat map+size 1.4+transparent+depthWrite off). — Files: index.html (rainStreakTex + rainMat ~l.287). FPS-safe & reversible (delete rainStreakTex, revert rainMat to `{color:0x9ab8d8,size:.5,transparent:true,opacity:0,depthWrite:false}`).

- 2026-06-28T06:55Z — game feel / HERO-RESPAWN materialization burst + player chime (NEW visual+audio; respawns were SILENT/blank) — A hero RETURNING to the fight was the last big combat beat with no juice: in the respawn branch the hero just flicked `grp.visible=true` on the fountain pad with a `drawBar` — no effect, no sound — even though DEATH now bursts (fxDeathBurst). Added the mirror, `fxRespawn(p,col)` (after fxLevelUp): a team-tinted landing ring (`ring_shock`, draped heightAt+0.16, grow 1.6) + a TALL bright spawn-beam column (`glow_soft` at y=4.2, size 5.0) + a white core flash (`impact_star`) — team-tinted (B→0x88bbff / R→0xff7766) so you read whose hero is back. Plus `sfxRespawn()` (after sfxBoss), a player-ONLY warm "back in the fight" rising chime — 392/587/784 `sine` triad + a bright 1175Hz `triangle` topper. Wired ONE line into the respawn branch right after `drawBar(h)`: `fxRespawn(h.grp.position, h.team===T.B?…); if(h===pHero)sfxRespawn();`. Reuses the EXISTING fx texture atlas (no new PNGs/geometry), the `fxs` 160 cap + auto fade/cleanup, GATED behind `fxLevel>=2` (skipped at the low-FPS tier); sfx respects AC/S_MUTE. Event-driven & rare (hero respawns only), zero per-frame cost. — Validated: scratch `respawn_scratch_v1.js` node --check + run = ASSERT PASS (fxLevel<2 → exactly 3 fx in order ring_shock-ground/glow_soft-bill/impact_star-bill, team tint on ring+column & white core, tall column y=4.2 size 5.0, opacities in (0,1], no NaN; red-team & default-white paths; fxLevel>=2 → 0 fx; sfxRespawn = 4-note strictly-ascending 392/587/784 sine + 1175 triangle; muted & AC-off both silent). — Files: index.html (fxRespawn ~l.1258; sfxRespawn ~l.455; respawn branch ~l.3531). FPS-safe & reversible (delete fxRespawn + sfxRespawn + the one wiring line, leaving drawBar intact).

- 2026-06-28T08:24Z — game feel / STRUCTURE-COLLAPSE burst (NEW visual; tower/base/core demolitions were visually SILENT) — Hero death/respawn/level-up all got layered fx bursts, but a tower/base/core being destroyed — the biggest map-state beats in a MOBA — only fired sfxStructure + a feed line + a minimap ping; the structure mesh just blinked away via removeUnit with ZERO visual at the rubble (an AoV juice gap — in AoV a tower explodes into dust/debris). Added `fxStructureFall(p,h,col)` after fxRespawn: a wide dust shock ring on the ground (`ring_shock`, draped heightAt+0.14) + two billowing rubble-dust puffs (`glow_soft`, dusty 0xb8a98c/0xa89c86, low + rising smoke plume) + a debris-scatter flash (`impact_lines`, 0xffe6b0) — every size/height scaled by `sc=max(1,h/10)` so a tall core bursts bigger than a tower; the shock ring is team-tinted (red 0xff5555 = YOUR building fell / green 0x66ff88 = enemy's) to match the existing mmPing colour cue, dust kept neutral. Wired ONE call into each of the tower (l.1505), base (l.1509) and core (l.1513) destroy branches, right beside the existing sfx/ping. Reuses the EXISTING fx texture atlas (ring_shock/glow_soft/impact_lines — no new PNGs/geometry), the `fxs` 160 cap + auto fade/cleanup, and is GATED behind `fxLevel>=2` (skipped at the low-FPS tier). Event-driven & rare (a handful of structures per game), zero per-frame cost. — Validated: scratch `structfall_scratch_v1.js` node --check + run = ALL ASSERTS PASS (fxLevel<2 → exactly 4 fx in order ring_shock-ground/glow_soft/glow_soft/impact_lines, all centred on the structure, ring team-tinted & dust neutral, opacities in (0,1], smoke plume above low dust; size/height scale with h, sc clamps ≥1 for undefined h, undefined col→white; fxLevel>=2 and >=3 → 0 fx). — Files: index.html (fxStructureFall ~l.1294; tower/base/core branches ~l.1505/1509/1513). FPS-safe & reversible (delete fxStructureFall + the 3 wiring lines, leaving sfxStructure/mmPing intact).

- 2026-06-28T08:55Z — map visuals / TOWER SILHOUETTE (weakest structure read: the 12 lane towers used the `placeholder('tower')` fallback = a plain tapered cylinder + a glow orb floating at the tip, the crudest fixed structure on the map vs the now-detailed fountains/decals/cores). Gave each tower a defensive-turret read: kept the tapered stone shaft `CylinderGeometry(2,3,9,8)` (y4.5), added a mid string-course band `CylinderGeometry(2.25,2.25,0.8,8)` (y6.7, lighter 0x6a7894) + a WIDER battlement cap `CylinderGeometry(2.8,2.4,1.7,8)` (y9.4, darker 0x47566d) that overhangs the shaft top (cap top r2.8 > shaft top r2) so it reads as a parapet, and reseated the team crystal `SphereGeometry(1.2,10,8)` (was 1.3) into the cap socket at y11.2 (emissive team colour unchanged). +2 small low-poly 8-seg meshes per tower (24 extra static meshes total across 12 towers), built ONCE with the unit in placeholder() — zero per-frame cost, no textures/network, no collision/data change (mkTower hp/range/h untouched; bar still at h*0.55+2.5). — Validated: scratch `tower_turret_scratch_v2.js` node --check + run = ALL ASSERTS PASS (4 meshes added in shaft→band→cap→crystal order; Y strictly increasing 4.5<6.7<9.4<11.2; cap top r2.8 wider than shaft top 2 & wider than band; crystal keeps team colour+emissive .5; all cylinders 8-seg + crystal sphere 10×8 = low poly; crystal top <13u). — Files: index.html (placeholder kind==='tower' branch ~l.992). FPS-safe & reversible (revert the tower branch to `b`+`g` only).
- 2026-06-28T08:42Z — game feel / SCREEN SHAKE (NEW; the map had ZERO camera shake — a core AoV/MOBA "feel" element that was entirely absent, `grep shake` = no matches). Big on-screen impacts (your ultimate, a structure crashing down) read as flat with no kinetic punch. Added a minimal one-scalar shake system: `let shakeMag=0` + `addShake(m)` (event-driven, raises shakeMag toward a 2.2 cap, GATED `if(fxLevel>=2)return` so the low-FPS tier gets no motion) + `applyShake(dt)` (jitters cam.position x/z by ±0.5·mag & y by ±0.225·mag, then exponentially decays `shakeMag*=pow(0.0009,min(dt,.05)/0.40)`; early-returns to exactly 0 when ≤0.02). `applyShake(dt)` is called ONCE right after each `cam.lookAt(...)` (both camera-update sites, via replace_all) so it offsets AFTER the per-frame `cam.position.lerp` — the lerp recomputes the unshaken target every frame, so shake NEVER accumulates/drifts. Wired TWO triggers: (1) player ULTIMATE cast in `castSkill` (`ai===3`, after sfxSuper) → `addShake(0.85)` (player-gated & always on-screen); (2) `fxStructureFall` end → proximity-gated `addShake(min(1.4,0.55+sc·0.35))` only when the rubble is within 60u of camFocus (`_sdx²+_sdz²<3600`), magnitude scaling with structure height via the existing `sc`. NO new geometry/textures/DOM, NO heap allocation (one scalar, in-place cam.position math), idle per-frame cost = a single `shakeMag<=0.02` compare. — Validated: scratch `shake_scratch_v1.js` node --check + run = ALL ASSERTS PASS (addShake max/clamp-2.2/fxLevel≥2-gated; idle leaves cam untouched & snaps to 0; full decay in 0.217s/13 frames, never negative, per-axis offset bounded by ±0.5·mag (x/z) & ±0.225·mag (y), max single-frame offset 0.354u; huge-dt stall still decays ≥0; proximity gate: tower sc=1→0.9, core sc=1.6→1.11, very-tall clamps to 1.4, off-screen >60u→0, 59u→shakes). — Files: index.html (shake system ~l.265; applyShake at both cam.lookAt sites; castSkill ai===3 ~l.2727; fxStructureFall end ~l.1304). FPS-safe & reversible (delete the shake block, the two `applyShake(dt);` appends, the `addShake(0.85)` in castSkill, and the structure-collapse shake line).
- 2026-06-28T09:38Z — map visuals / CORE (NEXUS) silhouette (weakest important-structure read: after towers got a turret cap, bases a building+crystal, and fountains baked textures+rings, the two team CORES — the win-condition nexus — were still just a lone spinning `IcosahedronGeometry(4.5)` crystal on ONE flat `CylinderGeometry(6,7,2,8)` disc, the crudest fixed structure left). Rebuilt the `placeholder` core branch into a proper nexus: a TWO-STEP stone pedestal (lower wide `CylinderGeometry(7.5,8.5,1.4,8)` @y0.7 in 0x33405c + upper `CylinderGeometry(5.6,6.8,1.6,8)` @y2.2 in 0x3a4660) replacing the single disc, the SAME team crystal unchanged (`IcosahedronGeometry(4.5,0)`, emissive team col .35, y5, still `u.spin` so the spin loop is untouched), and a ring of 6 emissive guardian shards (`ConeGeometry(0.55,2.2,5)`, shared geo+mat, team col emissive .4) evenly spaced at radius 5.4 / y3.6 around the crystal. Crystal Y, u.spin identity, hp/range/data all byte-unchanged (only the rendered mesh changed). — Validated: scratch `core_nexus_scratch_v1.js` node --check + run = ALL ASSERTS PASS (9 meshes = 2 steps+crystal+6 shards; u.spin===crystal; crystal still ico(4.5)@y5; step1 wider & Y order step1<step2<crystal; 6 shards share ONE geo+ONE mat, ring r5.4 @y3.6, evenly 60° apart; crystal+shards emissive = team colour). — Files: index.html (placeholder kind==='core' branch ~l.1009). FPS-safe: +8 static low-poly meshes per core × ONLY 2 cores (=16 total, shared shard geo/mat), built ONCE with the unit, castShadow default, NO textures/network, NO per-frame cost, fully reversible (revert the branch to the lone crystal + single CylinderGeometry(6,7,2,8) base).
- 2026-06-28T09:52Z — weather juice / RAIN PUDDLES + specular hints (menu item explicitly listed, not yet done) — The weather system already darkened fog/sun/grass (wet-grass tint) and quality-streaked the rain, but the GROUND showed no standing water — no puddles, no reflective sheen, the one obvious "it just rained" cue still missing. Added baked wet-pool decals: ONE 128px procedural puddle texture (cool bluish radial pool fading to a transparent rim + a faint brighter meniscus rim ring + a soft off-centre specular sky-glint and a smaller secondary glint = the reflective highlights that sell "wet") on ONE InstancedMesh of 30 flat `CircleGeometry(1,24).rotateX(-PI/2)` discs scattered across the open field via a seeded mulberry32 RNG (identical layout every load), each slightly squashed (z=.78–1.0·x) & spun for an organic non-circular read, draped via heightAt+0.07, kept ≥16·MAPK clear of both nexus cores. Material starts at opacity 0 / visible=false; in weatherTick one allocation-free ease nudges opacity toward `rain*0.6` (storm⇒~.45, rain⇒~.27, sunny⇒0) and toggles visibility >0.02 — so pools fade IN as a storm builds and dry out when skies clear. GATED: at the low-FPS tier (fxLevel>=2) the target is forced to 0 so puddles vanish with the rain. castShadow=false, renderOrder=1 (under the HP bars @20), built ONCE at load, zero per-frame allocation (one opacity lerp in an already-per-frame fn). — Validated: scratch `puddle_scratch_v1.js` node --check + run = ALL 31 ASSERTS PASS (texture: 3 radial gradients = pool+2 glints w/ transparent rims, 3 fills + 1 stroke rim, composite source-atop→source-over restored, anisotropy 2; layout: 30 placed, flat scale.y==1, shades [.85,1], z squashed [.78,1]·x, no NaN, all inside ±96·MAPK & ≥16·MAPK off both cores, deterministic re-seed; easing: storm→.45 no overshoot/visible, rain→.27, clear→<.02 & hidden, fxLevel>=2→off, per-frame factor .02∈(0,1)). — Files: index.html (`_puddleInst` decl ~l.333; puddle ease in weatherTick ~l.352; new PUDDLES IIFE after STRUCTURE GROUND DECALS ~l.927). FPS-safe & reversible (delete the IIFE, the `_puddleInst` decl, and the one ease block in weatherTick).
- 2026-06-28T10:23Z — game feel / RANGED-IMPACT spark on heroes (hit-spark symmetry gap) — Hit sparks were one-sided: a hero's OWN attacks already burst on impact (melee `fxHit` at the swing ~l.1665, ranged via dmgUnit's `src.kind==='hero'&&src.range>4` path ~l.1487), but a hero TAKING fire from a tower or a ranged minion/soldier projectile showed nothing — the projectile just blinked out, no "I'm getting shot" read. Added ONE guarded call at the projectile-impact branch (~l.3870, right after the `dmgUnit(pr.tgt,…)` that resolves a landed projectile): `if(pr.tgt.kind==='hero'&&!(pr.src&&pr.src.kind==='hero'))fxHit(pr.tgt,mm>1)` — sparks a hero target hit by a NON-hero projectile (towers, ranged minions/soldiers), excluding hero sources so it never double-sparks with the existing 1487 path; `mm>1` shows the big `impact_star` on a type-advantage hit (else `impact_lines`). Reuses the EXISTING `fxHit`/fx atlas (no new PNGs/geometry), bounded to ≤hero count (towers/minions only ever focus a handful of heroes), event-driven (per landed projectile, not per frame), and `fxLevel>=2`-gated inside `fxBill` so the low-FPS tier shows nothing. — Validated: scratch `hitspark_proj_scratch_v1.js` node --check + run = ALL ASSERTS PASS (tower→hero = 1 spark small; tower→hero type-adv = big star; hero source excluded → 0 spark; non-hero target → 0; ranged-minion→hero = 1 spark; targetless skillshot guarded by `if(pr.tgt)` → no throw, 0 spark). — Files: index.html (projectile-impact branch ~l.3870). FPS-safe & reversible (delete the one `if(pr.tgt.kind==='hero'…)fxHit(...)` clause).
- 2026-06-28T09:23Z — game feel / HP-BAR glossy sheen (readability) — The unit health bars (`drawBar`) already had a ghost-drain chunk, absolute-HP tick marks and slow/haste status pips, but the colored health fill itself was dead-flat — a plain coloured strip vs AoV's glossy "pill" bars. CRITICAL: `drawBar` runs inside the per-frame HP-regen loops (boss/base/hero/altar, `*dt`), a hot path — so the addition had to be allocation-free. Added a baked sheen via TWO pure `fillRect`s on the health portion only (`w=62*pc`): a bright top edge `rgba(255,255,255,0.22)` over (1,1,w,2) + a dark bottom edge `rgba(0,0,0,0.18)` over (1,7,w,2), leaving the mid-band full team colour → a rounded-pill 3D read with NO createLinearGradient/createRadialGradient (zero heap alloc per call). Gated `if(pc>0)` so dead/0-HP units draw no zero-width artifact; inserted AFTER the fill and BEFORE the tick/pip passes so ticks & status pips still sit on top. Team colour, ghost chunk, ticks and pips all byte-unchanged. — Validated: scratch `hpbar_sheen_scratch_v1.js` (mock 2D ctx logging every op + counting gradient allocs) node --check + run = ALL ASSERTS PASS (half-HP: sheen (1,1,31,2) + shade (1,7,31,2), both within y1–9 fill band, width ≤ health-fill width, ZERO gradient allocs; 0-HP: no sheen/shade; full structure: sheen full width 62, slow+haste pips & ticks still drawn, zero allocs; draw order fill < sheen < pips). — Files: index.html (`drawBar` ~l.941). FPS-safe (2 extra fillRects on a 64×10 canvas, no allocation) & reversible (delete the one `if(pc>0){…}` sheen line).
- 2026-06-28T10:55Z — game feel / FLOATING GOLD numbers on the player's last-hits (AoV-style last-hit cue) — Gold from `bounty()` was completely invisible: a clean last-hit on a minion/soldier/jungle-camp or a hero takedown silently incremented `bank.gold`, with no on-target feedback — the satisfying "+gold" pop that makes last-hitting feel good in AoV/LoL was missing (heals already had `popHeal`, damage `popDamage`, but gold nothing). Added `popGold(u,amt)` — a golden "+N◆" mirror of `popHeal` that reuses the EXISTING `.dmg` float keyframe + `#dmgLayer` (new CSS `.dmg.gold{color:#ffcf3a;font-size:13px;text-shadow:…#b97a00}`), early-outs on amt<=0 / missing grp / off-screen (v.z>1). Wired into the FOUR discrete kill-bounty branches, each GATED `if(killer===pHero)` so only the human player's own last-hits/takedowns pop (no ally/AI/online-peer spam): minion (+22), soldier (+30), jungle/boss wild (+26 / boss +220), and enemy hero takedown (+120). The popGold call sits right beside the existing `bounty(killer,N)` and BEFORE `removeUnit(u)`, so it reads `u.grp.position` while the corpse still exists. `bounty()` gold math, XP, CS counters and every other branch are byte-unchanged. — Validated: scratch `popgold_scratch_v1.js` node --check + run = ALL POPGOLD ASSERTS PASS (one "+22◆" node on-screen w/ class `dmg gold`, px left/top, dx within ±22px arc; boss "+220◆"; amt<=0 / off-screen(z>1) / null-or-grp-less → nothing; gating: ally last-hit→0 pops, pHero last-hit→1 pop, null killer→no throw). — Files: index.html (`.dmg.gold` CSS ~l.32; new `popGold` after `popHeal` ~l.3560; 4 gated calls in the minion/soldier/wild/hero kill branches ~l.1530/1534/1538/1608). FPS-safe & reversible: pure DOM/CSS, event-driven only (≤1 pop per player last-hit), zero per-frame cost — remove `popGold` + the `.dmg.gold` rule + the 4 `if(killer===pHero)popGold(...)` clauses.
- 2026-06-28T11:53Z — game feel / MINIMAP player FACING arrow (orientation readability) — The player blip on the minimap had only a static white ring (you-are-here) but no facing cue — a standard AoV/LoL "where am I looking" read was missing, so you couldn't tell your hero's heading at a glance from the map. Added a small white arrowhead on the pHero blip only, pointing in the hero's world heading. The hero heading comes from the EXISTING `u.visual.rotation.y - u.faceOffset` (same formula the skill-aim path uses ~l.1883); world dir (sin a, cos a) maps onto the minimap screen axes as (cos a, -sin a) given mmX∝+z / mmY∝-x, so the arrow always points the way the hero is moving on the map. Drawn as a 3-point triangle: tip 10px out, base 4.5px out, base half-width 3.2px — built with literal moveTo/lineTo/fill, no gradient, no object alloc. Guarded `if(u.visual)`; only ever runs for the ONE pHero blip (the loop already skips dead units). — Validated: scratch `mm_facing_scratch_v1.js` node --check + run = ASSERT PASS (16/16: arrow aligns with actual mm movement dir across 7 headings incl. cardinals; facing vec unit length; tip 10px / base mid 4.5px / tip ahead of base; base width 6.4px; base ⟂ facing axis; a=0 +z→points right, a=π/2 +x→points up; no NaN). — Files: index.html (drawMM pHero blip branch ~l.3792). FPS-safe: 3 trig + a 3-segment fill for a single blip per frame, zero heap alloc; reversible (delete the `if(u.visual){…}` arrow block, leaving the white ring stroke intact).
- 2026-06-28T12:08Z — generated audio / RANGED super-effective CLASH stinger (melee/ranged audio symmetry gap) — `sfxClash()` (the metallic type-advantage clash) already fired for MELEE player hits (l.1695, where melee damage resolves inline) but RANGED player hits resolve damage on PROJECTILE IMPACT (the projs loop ~l.3921, `dmgUnit(pr.tgt,pr.dmg*mm,pr.src,mm)`), which had NO clash — so a ranged carry landing a type-advantage shot got the visual crit star/fxHit big-star (mm>1) but was SILENT, an inconsistency vs every melee hero. Added the symmetric trigger right after the impact `dmgUnit`: `if(mm>1&&pr.src===pHero&&pr.tgt.team!==pr.src.team)sfxClash();` — player-only (pr.src===pHero), enemy-target guarded, self-throttled by the EXISTING `_gate('clash',110)` inside sfxClash (simultaneous landings merge into one ching), event-driven (per landed projectile, not per frame), reuses the existing tone()/noiseHit() sfxClash graph (no new sfx fn/graph), respects AC/S_MUTE via tone(). NO geometry/textures/DOM, zero per-frame cost. — Validated: scratch `ranged_clash_scratch_v1.js` node --check + run = ALL RANGED-CLASH ASSERTS PASS (super-effective player ranged hit→1 clash; mm==1 & mm<1→0; ally/AI source→0; same-team target→0; 2 hits within 110ms→1 clash (gate); 2 hits >110ms apart→2; null pHero / enemy projectile→0, no throw). — Files: index.html (projectile-impact branch ~l.3921). FPS-safe & reversible (delete the one `if(mm>1&&pr.src===pHero&&pr.tgt.team!==pr.src.team)sfxClash();` clause; melee l.1695 clash stays intact).
- 2026-06-28T12:38Z — game feel / PLAYER-HURT screen-shake (tactile damage feedback gap) — addShake already fired on YOUR ultimate (0.85) and nearby structure-collapses, but taking a heavy hit yourself gave zero kinetic feedback — getting bursted read as flat. Added ONE guarded line in dmgUnit right after `u.hp-=amt;drawBar(u);`: `if(u===pHero&&amt>0&&src&&src.team!==u.team){const _hf=amt/(u.maxHp||1);if(_hf>=0.08&&_gate('hurt',140))addShake(Math.min(0.5,0.18+_hf*1.1));}` — shakes only when the PLAYER hero takes a meaningful enemy hit (≥8% maxHp), magnitude scaling with fraction of max HP lost (capped 0.5, below the ultimate's 0.85). Throttled via the EXISTING `_gate('hurt',140)` so a burst combo lands one punchy shake not a buzz; addShake is already fxLevel>=2-gated (no motion at the low-FPS tier) & 2.2-capped. Event-driven (per damage event, not per frame), reuses addShake/_gate — no new state/geometry/DOM, zero per-frame cost. — Validated: scratch `playerhurt_shake_scratch_v1.js` node --check + run = ALL 11 ASSERTS PASS (<8% no shake; 8% fires & ≤0.5; 100% caps 0.5; magnitude scales; two hits <140ms → one shake; >140ms re-fires; ally/same-team src → none; non-pHero target → none; fxLevel>=2 suppressed; amt<=0 none; null src safe). — Files: index.html (dmgUnit ~l.1547). FPS-safe & reversible (delete the one `if(u===pHero...)addShake(...)` line).
- 2026-06-28T11:08Z — generated audio / LAST-HIT COIN "ching" (audio counterpart to last run's popGold) — The player's farm last-hits popped a golden "+N◆" number (popGold, added 10:55) but were SILENT — the satisfying coin *ching* that makes last-hitting feel good in AoV/LoL had no sound. A purpose-built `sfxCoin()` already existed (two ascending square tones 1230→1840Hz) but was wired ONLY to dropped-gold pickup (updateDrops ~l.2034), never to direct last-hits. Wired `sfxCoin()` into the three FARM last-hit branches beside the existing popGold, each already gated `if(killer===pHero)` (player only — no ally/AI/peer spam): minion (+22), soldier (+30), and NON-boss jungle camp (+26). DELIBERATELY skipped the boss (+220, already has the grand `sfxBoss` fanfare) and the enemy-hero takedown (+120, already has First Blood/multi-kill/spree stingers) so the coin never clashes with a bigger cue. Each call is throttled by the EXISTING `_gate('lhit',90)` so simultaneous same-frame deaths merge into one ching (no audio stacking) while distinct last-hits >90ms apart each ring. Reuses sfxCoin/_gate/tone — no new sfx fn, no new graph; `tone()` already early-returns on `!AC||S_MUTE`. — Validated: scratch `lasthit_coin_scratch_v1.js` node --check + run = ALL ASSERTS PASS (minion +22 + 1 coin; ally/tower killer → silent + no pop; two kills within 90ms → 2 pops but 1 coin; kills >90ms apart → 2 coins; soldier +30 + coin; jungle +26 + coin; BOSS +220 pop but NO coin; muted → pop shows, coin silent). — Files: index.html (minion/soldier/wild last-hit branches ~l.1531/1535/1539). FPS-safe: event-driven only (≤1 gated ching per player last-hit), zero per-frame cost & reversible (revert the 3 branches to `if(killer===pHero)popGold(...);` — drop the `{...if(_gate('lhit',90))sfxCoin();}`).
