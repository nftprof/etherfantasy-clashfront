# WORLD_LOG — overworld / world-map build log (job: tv-worldmap-build)

One line per run: the world feature added + how it was validated.

- 2026-06-27 — **NO-OP (build window closed)**: at run start `date -u` = 2026-06-27T22:38:19Z, which is after the task's STOP cutoff of 2026-06-25T01:10:00Z. Per the task's STOP directive, made NO edits to pve.html / WORLD_MAP.md / wiki.html, set scheduled task `tv-worldmap-build` enabled=false, and recorded the closed window here. Ending.

- 2026-06-24 — **W53 The Feastgate** (the FOURTH rung of WORLD_DESIGN §3's overworld realm-gateway ladder, continuing W50 the Mossgate = EAST/tier-1, W51 the Howling Gate = NORTH/tier-2, W52 the Gilded Gate = WEST/tier-3: each realm gets a diegetic gateway in its own COMPASS SECTOR, the realm's cx/cz sign IS its direction. Feastfall is cz−520 → SOUTH, the next sector after EAST/NORTH/WEST): added `function mkFeastgate()` + its call in pve.html after `mkGildedGate();` (def & call, immediately above the W19 block). A frost-rimed grey-blue stone gateway in the SOUTH-central Wilds at `(-15,-58)` (open moonlit plains, flat LOW heightAt≈0 — every mesa distance exceeds its radius — the southward approach toward Feastfall) — a low ice-glow footing + a great gateway ARCH whose opening faces **SOUTH (±z)** (toward Feastfall, like the Howling Gate's NORTH opening: pillars offset along x, veil plane faces ±z by default) holding a DORMANT ice-veil (faint pale sheet, not yet open), a **four-notch tier-IV waystone** (the fourth rung, vs the Mossgate's one / Howling Gate's two / Gilded Gate's three) on the NORTH/town approach, and — previewing the W22 Frozen Feast interior beyond — a frostbitten banquet-table w/ frozen platters + a guest frozen mid-bite into an ice statue + a colonnade of icicles + drifting snow motes at its foot. Palette = the realm ice-blue `DUNGEONS[3].col 0x3a8ac2` + frost-white accents. A "see the way in, can't open it yet" hook in the W29 Riftgate / W45 Sundergate / W48 Midward Gate / W50 Mossgate / W51 Howling Gate / W52 Gilded Gate lore-gateway language (❄ lore infoSpot at `(-15,-50)`, **no portal/fast-travel wired** — the functional plaza portal is UNTOUCHED, so travel is unchanged and nothing can break). **PURELY VISUAL — 0 obstacles added**; anchored on `heightAt` at flat LOW plains, nothing pushed to `obstacles[]`, so no clamp/traversal/secret/cache coords shift. In-bounds (R≈60 <150 & inside ±165) & min **≈26u** clear of the nearest feature (the Fenwater Beck footbridge `(-16,-84)`) & **≈60u** from town. Validated `node --check` (SYNTAX OK on the verbatim `mkFeastgate` fragment) + a **756-assert** geometry test on a sandbox-native harness with the verbatim fragment + the real `MESAS`/`heightAt`/`TOWN` inlined (33 meshes; **0 obstacles**; exactly 1 infoSpot named "Feastgate"; centre + footprint (r8 ring) flat LOW heightAt≈0; every mesh finite; R<150 (maxR 65.3) & inside ±165; every mesh >15u from 19 south features (min 26.0u, Fenwater footbridge); >30u from town; arch silhouette maxY 10.6 — ALL 756 PASS). Re-Read the exact anchor before editing; atomic reversible insert. (bash mount serves a stale tail per discipline → the node checks ran on a sandbox-native harness, never the mount.) Updated WORLD_MAP.md (§2 SOUTH-plains Feastgate landmark + §5 W-table +W53 + §6 backlog: S ticked, 3 sector gateways remain) and wiki.html with the player-facing entry.
- 2026-06-24 — **W52 The Gilded Gate** (the THIRD rung of WORLD_DESIGN §3's overworld realm-gateway ladder, continuing W50 the Mossgate = EAST/tier-1 and W51 the Howling Gate = NORTH/tier-2: each realm gets a diegetic gateway in its own COMPASS SECTOR, the realm's cx/cz sign IS its direction. Valley of Midas is cx−520 → WEST, the next sector after the Mossgate's EAST and the Howling Gate's NORTH): added `function mkGildedGate()` + its call in pve.html after `mkHowlingGate();` (def & call, immediately above the W19 block). A gold-veined stone gateway in the WEST-central Wilds at `(-100,30)` (open moonlit plains, flat LOW heightAt≈0, the westward approach toward the valley) — a low gold-glow footing + a great gateway ARCH whose opening faces **WEST (±x)** (toward Midas, like the Mossgate's EAST opening: pillars offset along z, veil plane rotated to face ±x) holding a DORMANT molten-gold veil (faint gilt sheet, not yet open), a **three-notch tier-III waystone** (the third rung, vs the Mossgate's one / Howling Gate's two) on the EAST/town approach, and — previewing the W21 Gilded Reliquary interior beyond — a half-gilded statue (the covetous "gild themselves solid into statues of their own want") + a heap of cooling gold ingots at its foot. Palette = the realm fire-orange `DUNGEONS[2].col 0xc24a22` + gilt-gold accents. A "see the way in, can't open it yet" hook in the W29 Riftgate / W45 Sundergate / W48 Midward Gate / W50 Mossgate / W51 Howling Gate lore-gateway language (👑 lore infoSpot at `(-92,30)`, **no portal/fast-travel wired** — the functional plaza portal is UNTOUCHED, so travel is unchanged and nothing can break). **PURELY VISUAL — 0 obstacles added**; anchored on `heightAt` at flat LOW plains, nothing pushed to `obstacles[]`, so no clamp/traversal/secret/cache coords shift. In-bounds (R≈104 <150 & inside ±165) & min **≈25u** clear of the nearest feature (the Weeping Hollow falls `(-128,12)`) & >30u from town. Validated `node --check` (SYNTAX OK on the verbatim `mkGildedGate` fragment) + a **53-assert** geometry test on a sandbox-native harness with the verbatim fragment + the real `MESAS`/`heightAt`/`TOWN` inlined (29 meshes; **0 obstacles**; exactly 1 infoSpot named "Gilded Gate"; centre + footprint flat LOW heightAt≈0; every mesh finite; local footprint radius ≤ ~8.5 → R<150 & inside ±165; every centre >15u from 12 west features (min 25.1u, Weeping Hollow); >30u from town; arch silhouette maxY 10.6 — ALL 53 PASS). Re-Read the exact anchor before editing; Grep-confirmed `mkGildedGate` defined + called exactly once. (bash mount serves a stale tail per discipline → the node checks ran on a sandbox-native harness, never the mount.) Updated WORLD_MAP.md (§2 WEST-plains Gilded Gate landmark + §5 W-table +W52 + §6 backlog: W ticked, 4 sector gateways remain) and wiki.html with the player-facing entry.
- 2026-06-24 — **W51 The Howling Gate** (the SECOND rung of WORLD_DESIGN §3's overworld realm-gateway ladder, continuing W50 the Mossgate's de-clustering: realm INTERIORS got distinct silhouettes W19–W25 but their overworld ENTRANCES were all bunched in the town plaza; each realm now gets a diegetic gateway in its own COMPASS SECTOR. Howling Crypt is cz+520 → NORTH, the next sector after the Mossgate's EAST): added `function mkHowlingGate()` + its call in pve.html after `mkMossgate();` (def & call, immediately above the W19 block). A barrow-gate of cold grey-violet crypt stone in the NORTH-central Wilds at `(-45,68)` (open moonlit plains, flat LOW heightAt≈0, the northward approach toward the crypt) — a low glow footing + a great gateway ARCH whose opening faces **NORTH (±z)** (toward the crypt, vs the Mossgate's EAST opening; pillars offset along x, veil plane faces ±z by default) holding a DORMANT phantom-veil (faint violet sheet, not yet open), a **two-notch tier-II waystone** (the second rung, vs the Mossgate's single notch) on the south/town approach, and — previewing the W20 Hollow Barrow interior beyond — leaning gravestone slabs + a bone-pale rib arc + drifting phantom wisps. Palette = the realm phantom-violet `DUNGEONS[1].col 0x6a4a8a` + bone-pale accents. A "see the way in, can't open it yet" hook in the W29 Riftgate / W45 Sundergate / W48 Midward Gate / W50 Mossgate lore-gateway language (⚰️ lore infoSpot at `(-45,60)`, **no portal/fast-travel wired** — the functional plaza portal is UNTOUCHED, so travel is unchanged and nothing can break). **PURELY VISUAL — 0 obstacles added**; anchored on `heightAt` at flat LOW plains, nothing pushed to `obstacles[]`, so no clamp/traversal/secret/cache coords shift. In-bounds (R≈81.5 <150 & inside ±165) & min **≈18.6u** clear of the nearest feature (the Whispering Thicket brush edge `(-80,92)` outerR 18) & **81.5u** from town. Validated `node --check` (SYNTAX OK on the extracted `mkHowlingGate` fragment) + an **11-assert** logic test on a sandbox-native harness with the verbatim fragment + the real `MESAS`/`heightAt`/`TOWN` inlined (25 meshes; **0 obstacles**; exactly 1 infoSpot named "Howling Gate" on the south approach `(-45,60)`; centre flat LOW heightAt≈0; every mesh finite; footprint local radius ≤5.83 toward features → R<150 & inside ±165; thicket brush clearance 18.6u >15; >30u from town; arch silhouette maxY 10.6 — ALL 11 PASS). Re-Read the exact anchor before editing; atomic reversible insert; site clearances pre-checked against a curated north/central feature list (min 24.4u centre-to-brush). (bash mount serves a stale tail per discipline → the node checks ran on a sandbox-native harness, never the mount.) Updated WORLD_MAP.md (§2 NORTH-plains Howling Gate landmark + §5 W-table +W51 + §6 backlog: N ticked, 5 sector gateways remain).
- 2026-06-24 — **W50 The Mossgate** (the FIRST attack on WORLD_DESIGN §3's headline "each [zone] reached from a DIFFERENT part of the main map, forming an ASCENDING challenge ladder" — a genuinely FRESH mandate: the realm INTERIORS got distinct silhouettes (W19–W25) but their overworld ENTRANCES were ALL clustered in the town plaza (`mkPortal` arc at R≈23 near origin), the exact opposite of §3; rotates away from the back-to-back W48/W49 Middle-Realm runs): added `function mkMossgate()` + its call in pve.html after `mkWayreachBeacon();` (L2547 def / L2584 call). It seeds the de-clustering: each realm gets a diegetic overworld GATEWAY in its own COMPASS SECTOR (the realm's cx/cz sign IS its direction — Mossfang cx+520→EAST, Howling cz+520→N, Midas cx−520→W, Feastfall cz−520→S, Isle(−,+)→NW, Enamora(−,−)→SW, Abyssal(+,+)→NE), forming the ascending ladder (tier-1 nearest → tier-7 remotest). The Mossgate is the LOWEST RUNG: a leaf-twined stone gateway in the EAST Wilds at `(110,-45)` (open moonlit plains, flat LOW heightAt≈0, the eastward approach toward Mossfang Hollow) — a moss-glow footing + a great gateway ARCH (two lichened pillars + a living-branch lintel, dominant silhouette y≈10.6) holding a DORMANT green realm-veil (faint sheet, not yet open), a single-notch tier-I waystone at the approach, a cluster of fungal toadstools at the foot (previewing the W19 Mossfang "fungal cathedral" interior beyond), and drifting spores. Palette = the realm green `DUNGEONS[0].col 0x3a6b3a`. A "see the way in, can't open it yet" hook in the established lore-gateway language of the W29 Riftgate / W45 Sundergate / W48 Midward Gate (🍃 lore infoSpot at `(102,-45)`, **no portal/fast-travel wired** — the functional plaza portal is UNTOUCHED, so travel is unchanged and nothing can break). Also corrected the stale "Portal Plaza" infoSpot text (it claimed "Three portals... tier 1/2/3" but there are 7 realms) → now names the seven-realm tower of challenge and points to the new scattered gateways. **PURELY VISUAL — 0 obstacles added**; anchored on `heightAt` at flat LOW plains, nothing pushed to `obstacles[]`, so no clamp/traversal/secret/cache coords shift. In-bounds (R≈119 <150 & inside ±165) & min **34.0u** clear of every feature (the Wayfarer's Wharf binding) & **111.9u** from town. Validated `node --check` (SYNTAX OK on the verbatim fragment) + a **726-assert** geometry test on the real `MESAS`/`heightAt`/`TOWN` (28 meshes; **0 obstacles**; exactly 1 in-bounds infoSpot named "Mossgate"; centre flat LOW plains heightAt≈0 + footprint flat; every mesh finite & R<150 & ±165 & >15u from 21 features & >30u from town; arch silhouette maxY 10.6 — ALL 726 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkMossgate` is defined + called exactly once and the function body pushes nothing to `obstacles[]`. (bash mount serves a stale tail per discipline → the node checks ran on a sandbox-native harness with the verbatim fragment + real heightfield inlined.) Updated WORLD_MAP.md (§2 east-Wilds Mossgate landmark + §5 W-table +W50 + §6 backlog: the 6 remaining sector gateways) and wiki.html with the player-facing entry.
- 2026-06-24 — **W49 The Wayreach Beacon** (§2 layer-3 MIDDLE REALM — deepens the most under-built pillar: layer-3 had only ONE overworld presence, the W48 Midward Gate, vs many features for the human Wilds / underworld realms / staged-unlock gates; rotates fresh after W47's §3b watershed): added `function mkWayreachBeacon()` + its call in pve.html after `mkMidwardGate();` (L2491 def / L2523 call). Where the Midward Gate is the calm THRESHOLD to the Middle Realm (a henge + dormant veil, W-central), this is a ruined MIDDLE-REALM WATCHTOWER that has half "phased" through into the human world's open SE-central plains FAR from the gate `(35,-29)`: a tall pale lantern-spire (dominant silhouette, crown y≈20) with SOLID lower courses + TRANSLUCENT teal upper courses (half here, half not) + broken crenellation, a DORMANT teal beacon at the crown, a glow-ring footing, fallen pale blocks at its foot (it arrived half-ruined), and drifting cool motes — showing the connective middle layer pressing CLOSER to the human world than the old maps admit ("not one door but a seam"). A "see it, can't reach yet" hook tied to §4's one-way→two-way travel arc: the beacon kindles only once the player has gone down one-way, been hunted, and won the road back (carried by the 🗼 lore infoSpot at `(35,-21)`, no portal/fast-travel wired). Palette matches the Midward Gate (pale stone 0x8a8496/0x6a6478, teal phase 0x6ad0c8, glow 0x4fb8b0/0x166060) so the two read as one layer. **PURELY VISUAL — 0 obstacles added**; anchored on `heightAt` at flat LOW plains, nothing pushed to `obstacles[]`, so no clamp/traversal/secret/cache coords shift. Validated `node --check` (SYNTAX OK on the verbatim fragment) + a **59-assert** geometry test on the real `MESAS`/`heightAt`/`TOWN` (25 meshes; **0 obstacles**; exactly 1 in-bounds infoSpot named "Wayreach Beacon"; centre flat LOW plains heightAt≈0 + footprint flat; every mesh finite & R<150 & ±165 & >15u from 60 features/road-samples (min 46.5u, the Briar Maze) & >30u from town (40.0u); tower silhouette maxY 20 — ALL 59 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkWayreachBeacon` is defined + called exactly once. (bash mount serves a stale tail per discipline → the node checks ran on a sandbox-native harness with the verbatim fragment inlined.) Updated WORLD_MAP.md (§2 Middle-Realm narrative +W49 + §5 W-table +W49) and wiki.html with the player-facing entry.
- 2026-06-24 — **W47 The Fenwater Beck** (§3b watershed logic — "rivers flow DOWNHILL from high to low and end in a lake"; resumes the feature rotation now that the §3b ascent/cliff backbone is solid, by RE-CONNECTING two already-re-grounded features into one believable drainage): added `function mkFenwaterBeck()` + its call in pve.html after `mkGloomfenDescent();` (~L2084). A stream DRAINS the W38 Highcrag Mistfall plunge pool `(-44,-76)` (LOW, heightAt≈0.31) SE across the moonlit plains and DOWN the Gloomfen basin's NW wall into the sunken fen pool `(22,-98)` (sub-LOW, heightAt≈−3.0) — PATH `[[-44,-76],[-26,-82],[-6,-86],[10,-91],[22,-98]]`, with stream tiles ANCHORED on `heightAt` (same proven anchoring as the W39 Descent treads, so the water hugs the slope and visibly cascades down the basin wall), a gentle meander, reed/rock banks, a foam spring-head at the source, a little timber footbridge where it crosses the plain `(-16,-84)`, and a 💧 The Fenwater Beck lore infoSpot at the midpoint `(-6,-86)`. Routed on the basin's NW so the whole beck stays >15u clear of the GROTTO KEY relic `(40,-100)` and north of the Gloomwater river `z=-129`. **PURELY VISUAL — 0 obstacles added**; the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W39 Descent). Validated `node --check` (SYNTAX OK on the verbatim fragment) + a **248-assert** geometry test on the real `MESAS`/`heightAt`/`TOWN`/`WILD_R` (77 meshes; path heightAt monotonic NON-increasing 0.31→0.00→0.00→−2.55→−3.00, source∈[0,1] & terminus≤−2.5 & net descent 3.31≥2.5; every mesh finite & R<150 & within ±165; **0 obstacles**; exactly 1 in-bounds infoSpot named "Fenwater Beck"; min Grotto-Key clearance 18.34u >15, min Briar 49.3u, min town 83.3u >30, minZ −98.48 > −123.5 = north of the Gloomwater channel — ALL 248 PASS). Re-Read the exact anchor before editing; atomic reversible insert. (bash mount serves a stale tail per discipline → the node checks ran on a sandbox-native harness with the verbatim fragment inlined.) Updated WORLD_MAP.md (§2 Highcrag/Gloomfen narrative + §5 W-table +W47) and wiki.html with the player-facing entry.
- 2026-06-22 — **W11 The Veil's Edge** (diegetic map-edge treatment): replaced the abrupt invisible-wall border with a believable edge. Added `mkVeilEdge()` in pve.html (after the scenic-scatter IIFE, ~L1721): a starlit abyss plane, a 4-wall cliff skirt dropping the land into the Veil at ±170, a glowing violet seam frame at the ±165 clamp line, 60 rising mist cones, 12 distant unreachable Veil-shards, and a walk-up `🌌 The Veil's Edge` infoSpot carrying the human-world/Underworld canon. Purely visual — the existing `clampZone` ±165 wall is unchanged, so no traversal/secret coords shift (verified all caches ≤R138, W4 ledge rim ≤128, W5 altar R141, W7 vault R144 stay inside the square clamp). Validated: extracted the function + ran `node --check` (SYNTAX OK) and a logic test (seam outside wilds, cliff outside seam, landmark walk-reachable, no secret stranded — ALL PASS). Re-Read confirmed the insertion is intact and the WILD LANDMARKS block below is undisturbed. (Note: bash mount serves a stale/truncated copy of pve.html, so whole-file checks were done on the extracted fragment per task discipline.) Updated wiki.html with the player-facing entry; created WORLD_MAP.md design doc.
- 2026-06-22 — **W12 The Gloomwater** (river + timber bridge + hidden ford): added a flowing river spanning the deep south of the Wilds (`const GLOOMWATER` IIFE in pve.html after the WILD LANDMARKS calls, ~L1815) — centreline A(-160,-129)→B(160,-129), half-width 5.5: a scrolling water plane (reuses `fallTex`/`WATERFALLS`), muddy banks, a pale south-shore strip, a **timber bridge** (deck+rails at x≈0) and a **hidden ford** of 6 stepping-stones at x≈90, plus reeds + a lore stone. New `riverBlock(hero)` (wired after `blockGaps` in the hero loop) snaps a walker back to its current bank when inside the channel except within the bridge/ford param-windows; banks are open past the river ends. `spawnGloomwaterCache()` (wired beside `spawnVeilIsles` at init, isCursed-gated) drops a cache on the secret shore at the ford's south exit (90,-142.5); a `🌊 The Gloomwater Shore` infoSpot carries the lore and a one-shot "you ford the Gloomwater" feed fires on first crossing. The river only walls the empty `z<-129` shore — all pre-existing landmarks lie north (min z=-115), so nothing is stranded. Validated: extracted the 3 fragments + `node --check` (SYNTAX OK) and a 14-assert logic test on stubbed THREE/globals (in-bounds coords; bridge & ford crossable; off-crossing snaps to the correct bank; open banks beyond ends; no pre-existing content stranded; valid windows; one-shot discovery feed; cache spawns on the south shore — ALL PASS). Re-Read each region before editing; atomic reversible inserts. (bash mount stale per discipline → checks on extracted fragments.) Updated WORLD_MAP.md (gating graph, W-table, backlog tick) and wiki.html with the player-facing entry.
- 2026-06-23 — **W13 The Mooncradle Mere** (swim-gated lagoon island — a NEW traversal ability + the secret it unlocks): added a `const MERE` IIFE in pve.html after `riverBlock` (~L1873): a still **lagoon** at centre `(122,6)` (lagoonR 24, islandR 6) with a sandy **coast ring** + foam line (the east Wilds now read as a shoreline biome), a lone **island** (sand disc + moonpalm) holding a sealed cache, shoreline rocks, and a **🐚 Tide-Charm** relic (torus-knot + teal beam) on the near west shore. New `mereBlock(hero)` (wired after `riverBlock` in the hero loop) does two things: (1) when the hero comes within 3.2u of the relic at `(92,6)` it claims the charm (hides it, fxRing+sfxEquip+feed) and sets `MERE.swim=true`; (2) until swim is unlocked it ring-snaps a walker to the shoreline exactly like `blockGaps` (a walker only ever meets the OUTER half first → the island is unreachable on foot); once swimming, the water is free and a one-shot "you slip into the Mere" feed fires on first crossing. `spawnMereCache()` (wired beside `spawnGloomwaterCache` at init, isCursed-gated) drops the cache on the island centre. A `🐚 The Mooncradle Mere` infoSpot carries the lore. Runtime-only flags (reset each load, charm respawns) — no SAVE touched; scoped to the Mere so it doesn't couple to W12/W4. In-bounds: full beach footprint R≈150 < the ±165 clamp; the relic is on dry land (R 30 > lagoonR 24); no pre-existing landmark falls within the lagoon footprint. Validated: extracted the 3 fragments + `node --check` (SYNTAX OK) and a **16-assert** logic test on stubbed THREE/globals (footprint inside clamp; relic dry/in-bounds/at (92,6); pre-swim walker snapped to rim & never inside islandR; charm grants swim & hides; post-swim no snap in water + wet-flag fires + stays on island; cache on island centre; no landmark stranded; infoSpot registered — ALL PASS). Re-Read each region before editing; atomic reversible inserts. (bash mount serves a truncated tail per discipline → all checks on extracted fragments via the Read tool.) Updated WORLD_MAP.md (§2 biome, §3 gating graph + solvability note, W-table, backlog tick) and wiki.html with the player-facing entry.
- 2026-06-23 — **W15 The Sunder Chasm** (charged-jump canyon vista — a NEW traversal ability + the gap it crosses, clearing the last "Charged Jump" backlog gate): added a `const CHASM` IIFE in pve.html after `grottoBlock` (~L1977): a wind-carved **canyon** in the far north (gap z122→134, 12u wide, span x −30→26) — a dark recessed floor + inner shadow, a rocky south rim, cliff **end-walls** framing it, 8 rising translucent **updraft** cones, and beyond it a raised grassy **vista** shelf `(-2,143)` with a cairn + a violet banner holding a sealed cache. A `🪶 Updraft Feather` relic (quill+vane+beam) sits on the near south brink `(-2,116)`. New `chasmBlock(hero)` (wired after `grottoBlock` in the hero loop): (1) within 3.2u of the feather it claims it (hides it, fxRing+sfxEquip+feed) and sets `CHASM.jump=true`; (2) until the jump is earned it snaps any walker that enters the span north of the rim back to z122 (incl. a flank approach entering the span already at high z → the 12u gap is uncrossable on foot and the vista cache is unreachable); once jumping, the gap is free and a one-shot "you ride the updraft" feed fires on first crossing. `spawnChasmCache()` (wired beside `spawnGrottoCache` at init, isCursed-gated) drops the cache on the vista centre. Two infoSpots (`🪶 The Sunder Chasm`, `⛰ The Far Vista`) carry the lore. Runtime-only flags (reset each load; the feather respawns); scoped to CHASM so no coupling to W4/W13/W14. In-bounds & non-colliding: vista R≈143 & feather R≈116, both <150 sculpted Wilds and inside the ±165 clamp; verified >15u clear of all 6 prior secrets + town/Gloomwater. Validated: extracted the 3 fragments + `node --check` (SYNTAX OK) and a **47-assert** logic test on stubbed THREE/globals (vista & feather in-bounds; feather south of the rim = walkable; clear of every existing feature; pre-jump snap to rim incl. flank entry; open field outside span; claim grants jump & hides feather + feed; post-jump free passage + one-shot leap feed (no re-fire) + stands on vista; cache spawns on vista; gap 12u/span 56u — ALL 47 PASS). Re-Read each region before editing; atomic reversible inserts; Grep-confirmed 12 occurrences & block intact. (bash mount serves a truncated tail per discipline → all checks on extracted fragments via the Read tool.) Updated WORLD_MAP.md (§2 north canyon biome, §3 gating graph + CHARGED JUMP row + W15 solvability note, W-table +W14/W15, backlog tick) and wiki.html with the player-facing entry.
- 2026-06-23 — **W14 The Wardstone Grotto** (relic-key cliff cave — the Zelda "find-key-here / door-opens-there" gate, last backlog item after swim): added a `const GROTTO` IIFE in pve.html after `mereBlock` (~L1929): a cave bored into a dark **cliff massif** (4 granite slabs) at NW `(-128,-48)`, R≈137 — a recessed torus **arch** + black interior mouth, barred by a glowing violet **Wardstone** hemisphere seal (+ ward-rune torus + marker beam). A separate IIFE drops the golden **🗝 Grotto Key** relic (shaft+bow+tooth+beam) far off in the southern Gloomfen at `(40,-100)`. New `grottoBlock(hero)` (wired after `mereBlock` in the hero loop) does two things: (1) within 3.2u of the key it claims it (hides key, ward-rune & seal; fxRing+sfxEquip+feed) and sets `GROTTO.open=true`; (2) until open it snaps a walker OUT to the ward-ring (`d<sealR 4.6` → push to sealR), so the mouth and its cache are unreachable; once open, free passage. `spawnGrottoCache()` (wired beside `spawnMereCache` at init, isCursed-gated) drops the sealed cache at the grotto centre. Two infoSpots (`⛰ The Wardstone Grotto`, `🗝 The Grotto Key`) carry the lore. Runtime-only flags (reset each load, key respawns); scoped to GROTTO so no coupling to W4/W12/W13. In-bounds & non-colliding: grotto R≈137 & key R≈108, both <150 sculpted Wilds and inside the ±165 clamp; verified >15u clear of W4(115,-115)/W5(-122,70)/W7(100,104)/W13(122,6)/Mere-relic(92,6)/Gloomwater. Validated: extracted the 3 fragments + `node --check` (SYNTAX OK) and a **16-assert** logic test on stubbed THREE/globals (grotto & key in-bounds/in-Wilds; clear of all 6 existing secrets; key outside the seal = reachable; sealed walker pushed out to ward-ring; standing on the key opens the cave; post-open walker reaches the cache; cache spawns at centre — ALL PASS). Re-Read each region before editing; atomic reversible inserts; Grep-confirmed 16 occurrences & the inserted block intact. (bash mount serves a truncated tail per discipline → all checks on extracted fragments via the Read tool.) Updated WORLD_MAP.md (§2 Highcrag biome, §3 gating graph + GROTTO row + solvability note + backlog tick) and wiki.html with the player-facing entry.
- 2026-06-23 — **W16 The Whispering Thicket** (forest biome + winding path to a hidden glade/shrine — a classic-RPG forest secret, clearing the §6 forest backlog item; rotated AWAY from the four prior relic/ability gates W12–W15): added a `const THICKET` IIFE in pve.html after `chasmBlock` (~L2045): a dense **forest** stand of 25 close-grown trees (shared trunk/leaf mats, performance-friendly) ringing a **hidden glade** at west `(-80,92)` (innerR 9, outerR 18, centre R≈122) — a mossy glade floor, a **moss shrine** (stone dais + glowing idol + halo) at the heart, and a line of pale **stepping-stones** marking the one **winding path** corridor through the brush. New `thicketBlock(hero)` (wired after `chasmBlock` in the hero loop): the brush band (innerR<d<outerR) snaps a walker OUT to the treeline (outerR) EXCEPT within the angular **path corridor** (`±gapHalf 0.34` rad at `gapAng`, the mouth facing town) — so the glade (d<innerR) is reachable ONLY by finding & following the path; a one-shot "you part the thicket" feed fires on first glade entry. NO new ability/relic — a discoverable PLACE, distinct from W13 swim / W14 key / W15 jump. `spawnThicketCache()` (wired beside `spawnChasmCache` at init, isCursed-gated) drops the cache at the glade centre; a `🌲 The Whispering Thicket` infoSpot sits at the path mouth (treeline) as the find-the-way hint. Runtime-only flags (reset each load); scoped to THICKET so no coupling to W4/W12–W15. In-bounds & non-colliding: centre R≈122 & outer treeline R≈140, both <150 sculpted Wilds and inside the ±165 clamp; min clearance 29.4u (nearest W5) >15u from all 15 existing secrets/landmarks + the Sunder Chasm gap. Validated: reconstructed the exact fragment + `node --check` (SYNTAX OK) and an **18-assert** logic test on stubbed THREE/globals (centre & treeline in-bounds; inner<outer; >15u clear of every feature + chasm gap; on-path walker passes; off-path walker snapped to treeline; open field free; **solvability** — a corridor march reaches the glade; one-shot feed fires once & doesn't re-fire; **off-path straight march is BLOCKED** so the glade stays hidden; cache at glade centre; infoSpot at the path mouth; dense tree count + gap kept clear — ALL 18 PASS). Re-Read each region before editing; atomic reversible inserts; Grep-confirmed 10 occurrences & the block intact. (bash mount serves a truncated tail per discipline → all checks on the reconstructed fragment via the Read/Write tools.) Updated WORLD_MAP.md (§2 west forest biome, §3 gating graph + FIND THE PATH row + W16 solvability note, W-table +W16, §6 forest backlog tick) and wiki.html with the player-facing entry.
- 2026-06-23 — **W18 The Sundering Sea** (a true OCEAN map-edge on the EAST — the §4/§6 diegetic-border TODO "extend the coast to the east edge as a true ocean", rotated AWAY from the interior secret/traversal gates W12–W16 and the W17 road): added `function mkSunderingSea()` + its call in pve.html after the `waterfall(104,70)` calls (~L1880, where `fallTex`/`WATERFALLS` exist). East of the brink (`B=WILD_R+15=165`) it builds: (1) a big **animated ocean** plane (`seaW 420`, centre `x=375`, N–S span `2B+120`) reusing `fallTex()` and **pushed into `WATERFALLS`** so it scrolls via the existing `updateWaterfalls(dt)` loop (no new render hook); (2) a sandy **beach strip** (`x≈156`, full `2B` span) tying into the Mooncradle Mere shoreline; (3) a bright **surf/foam line** at `x≈165.5`; (4) **7 distant isles** (sand dome + dark tree cones) and **9 sea-stacks**, all spawned at `x>B` (unreachable past the surf, depth only); (5) a clifftop **🌊 Lighthouse** (tower+band+emissive lamp) on the NE headland `(152,120)` + a `🌊 The Sundering Sea` infoSpot `(152,96)` carrying the drowned-marches canon. **PURELY VISUAL** — the `clampZone` ±165 square already stops the hero, so **NO obstacle/collision is added** and no traversal/secret/cache coords shift; the east edge now reads as sea instead of the Veil-cliff (the Veil treatment stays on the deep/Underworld-facing side). In-bounds: beach + lighthouse stay inside the ±165 clamp (lighthouse on the beach band, `|x|=152,|z|=120<165`), the lighthouse sits >15u clear of every east-side feature (waterfall, W7, Mere-isle, W4), and all isles/stacks are east of the clamp. Validated: extracted the verbatim function onto stubbed THREE/globals + `node --check` (SYNTAX OK) and a **16-assert** logic test (sea near-edge ≥ brink; beach inner edge inside clamp & outer ≤ ground edge; lighthouse inside clamp + on the beach band + >15u clear of 4 east features; all 7 isles & 9 stacks east of the clamp = unreachable; the sea registered in `WATERFALLS` so it scrolls; **0 obstacles added** so traversal/secrets untouched; exactly one infoSpot, inside the clamp — ALL 16 PASS). Re-Read the region before editing; atomic reversible insert; Grep-confirmed the function is defined + called exactly once and the block is intact. (bash mount serves a truncated tail per discipline → all checks on the extracted fragment via the Read/Write tools.) Updated WORLD_MAP.md (§4 +W18 east-ocean edge & remaining-edges TODO, §5 W-table +W18, §6 backlog ticks: coast→ocean ✅, edges ocean ✅) and wiki.html with the player-facing entry.
- 2026-06-23 — **W17 The Wayfarer's Road** (a guided "route out of town" — the classic-RPG LINEAR OPENING; rotated AWAY from the five prior secret/traversal gates W12–W16 to attack WORLD_DESIGN §1's headline problem: the start spilling onto an open plain): added `mkWayfarersRoad()` + its call in pve.html after `mkVeilEdge();` (~L1782). A worn **flagstone road** runs N from the town gate, centreline `x=8·sin(t·1.3π), z=31→98`: a timber **trailhead arch** at the gate `(0,31)`, 27 flagstones, **carnival lampposts** every 3rd stone (±3.4 off, emissive lanterns), a **tree corridor that thickens** toward the frontier (`dens=1+round(t·2)`, reuses `deco`/`mkTree`, ~100 trees), a 🪧 **Wayfarer's Milestone** infoSpot at the midpoint, and a glowing 🗺 **Frontier Waystone** infoSpot where the road ends `(~ -6,98)` (lore: the world unlocks a stage at a time). **Purely visual — adds NO obstacles/collision**, so the clamp, all traversal gates and every secret/cache are untouched; the road is a guide, not a wall. In-bounds & non-colliding: road centreline + all ~100 trees stay R<150 and outside the town disc (TOWN.r+4 guard) and the R<WILD_R-6 guard; verified clear of the Mere/Chasm-relic/Thicket/Grotto/Emberwood-crown/Gloomwater feature discs. Validated: built the exact function on stubbed THREE/globals + `node --check` (SYNTAX OK) and a logic test (road centreline 41 samples all R<150 & R>town & clear of 6 feature discs; 100 trees all in-bounds; **0 obstacles added**; 2 infoSpots registered — ALL PASS). Re-Read the region before editing (file had been touched by another job → re-Read + retried, atomic reversible insert); Grep + Read confirmed the insert intact and the WILD LANDMARKS/WATERFALLS block below undisturbed. (bash mount serves a truncated tail per discipline → checks on the standalone fragment.) Updated WORLD_MAP.md (§2 Wayfarer's Road landmark, W-table +W17) and wiki.html with the player-facing entry.
- 2026-06-23 — **W19 Mossfang Grove** (DISTINCT ZONE GEOGRAPHY for the tier-1 realm — the FIRST attack on WORLD_DESIGN §3's headline "7 identical recolored arenas" problem; no prior run touched realm interiors, all W12–W18 were Wilds secrets/edges/road, so this rotates into a fresh mandate): added `function mkMossfangGrove()` + its call in pve.html after `mkSunderingSea();` (~L1931, where scene/THREE/DUNGEONS/infoSpots all exist). Mossfang Hollow (DUNGEONS[0], Leaf, T1, cx520/cz0/r60) now reads as a **fungal cathedral** instead of a recolored box: a **giant cathedral toadstool** (h26, capR16 — the dominant silhouette), a **ring of 8 medium toadstools**, **3 vine/root half-torus arches** (buried-cathedral canopy), and a raised **mossy hummock** (the south clearing floor), plus a `🍄 The Fungal Cathedral` lore infoSpot — matching the OVA crossing line "Toadstools rise like cathedrals" and framing T1 as the lowest rung where the reversed Hunt first picks up the player's scent. **PURELY VISUAL** — every mesh is scene-added but NEVER pushed to `obstacles[]`, so no collision, no coord shift, no spawn/gameplay change; gated to no-op unless `DUNGEONS[0].name==='Mossfang Hollow'`. In-bounds & non-colliding: all 9 toadstools + mound + 3 arches sit inside the cx520/cz0/r60 wall ring (x460..580, z-60..60), entirely SOUTH of both room-divider walls (z=-14 & z=20) and >16u clear of the safe entry pad at (470,-50). Reuses shared geos/mats (1 stem geo, 1 dome-cap geo, 5 mats) for perf. Validated: extracted the verbatim function from the live file onto stubbed THREE/globals + `node --check` (SYNTAX OK, fragment runs → 1 infoSpot) and a **42-assert** logic test (every prop/mound/arch inside the wall ring; every prop >16u clear of the safe pad; every prop/mound/arch clear of BOTH divider-wall segments; exactly 9 toadstools + 3 arches; **0 obstacles added** so traversal/secrets/spawns untouched; exactly 1 lore infoSpot; the giant toadstool is the dominant cap; the function no-ops on a non-Mossfang DUNGEONS[0] — ALL 42 PASS). Re-Read the region before editing; atomic reversible insert; one coordinate nudge after the first logic run flagged 4 southern props poking past the -60 wall, re-validated clean. (bash mount serves a truncated/null-terminated copy per discipline → the logic test ran on a sandbox-native copy; the exact verbatim fragment was node-checked via a uniquely-named temp file.) Updated WORLD_MAP.md (new §3c zone-geography tracker table + §5 W-table +W19) and wiki.html with the player-facing entry.
- 2026-06-23 — **W20 The Hollow Barrow** (DISTINCT ZONE GEOGRAPHY for the tier-2 realm — the SECOND §3 tower-of-challenge zone given its own silhouette, following the doc's explicit "Next: rotate to the tier-2 Howling Crypt"; mirrors W19's visual-only pattern on a fresh realm): added `function mkHollowBarrow()` + its call in pve.html after `mkMossfangGrove();` (~L1970). Howling Crypt (DUNGEONS[1], Phantom, T2, cx0/cz520/r60) now reads as a **haunted barrow** instead of a recolored box: a **sunken cracked-stone floor disc** (15→13 taper) at (0,560); a lone **mausoleum** (base+capstone+pyramidal roof+dark doorway) at z≈562 as the **dominant silhouette**; a **ring of 8 leaning gravestone slabs**; **3 bone-pale half-torus ribs** arching overhead; **4 floating phantom wisps** (emissive motes); and a `⚰️ The Hollow Barrow` lore infoSpot — framing T2 as the rung where the Hollow Court's whispers seep up and the reversed Hunt "grows teeth." **PURELY VISUAL** — every mesh is scene/group-added, NEVER pushed to `obstacles[]`, so no collision/coord/spawn change; gated to no-op unless `DUNGEONS[1].name==='Howling Crypt'`. In-bounds & non-colliding: all props sit in the **NORTH band** (z≥544..≤576) of the −60..60 / 460..580 footprint, ≥7u north of the top divider wall (z=540, x −30..46), clear of the bottom divider (z=506), and >16u from the safe entry pad at (−50,470) — trivially, since every prop is z≥544 vs the pad's z=470. Reuses shared geos/mats (one slab geo, one wisp geo, six mats) for perf. Validated: built the verbatim function on stubbed THREE/globals → `node --check` (SYNTAX OK) and an **86-assert** logic test (every prop inside the footprint w/ margin; every prop >16u from the safe pad; every prop clear of BOTH divider segments by >wall-r; every prop z≥544; **0 obstacles added** so traversal/secrets/spawns untouched; exactly 1 lore infoSpot named "Hollow Barrow"; the function no-ops on a non-Howling-Crypt DUNGEONS[1] — ALL 86 PASS). Re-Read the exact anchor before editing (another job may be mid-edit); atomic reversible insert; Grep-confirmed the function is defined + called exactly once and the block is intact. (bash mount serves a truncated tail per discipline → all checks on the verbatim fragment via a sandbox-native harness.) Updated WORLD_MAP.md (§3c tracker row Howling Crypt ✅ + "Next" pointer → tier-3 Valley of Midas + §5 W-table +W20) and wiki.html with the player-facing entry.
- 2026-06-23 — **W22 The Frozen Feast** (DISTINCT ZONE GEOGRAPHY for the tier-4 realm — the FOURTH §3 tower-of-challenge zone given its own silhouette, following the doc's explicit "Next: rotate to the tier-4 Feastfall (Ice)"; mirrors the W19/W20/W21 visual-only pattern on a fresh realm): added `function mkFeastfall()` + its call in pve.html after `mkValleyOfMidas();` (~L2058). Feastfall (DUNGEONS[3], Ice/Gluttony, cx0/cz-520/r60) now reads as a **glacial banquet hall** instead of a recolored box: a **sunken black-ice banquet floor** (slab disc + ice sheet at (0,-478)); **THE ENDLESS MAW** — Throngullet (a tall dark throat cylinder + a pale ice rim torus + a ring of 10 frost fangs, peaking at y≈21.6) as the **dominant silhouette**; a long **frostbitten feast table** (box len 20) down the centre with 4 frozen **platters**; a **ring of 8 gorged guests frozen into ice statues** (round-bellied figures); an **icicle colonnade** of 6 tapered ice pillars; **6 radiating frost-cracks**; and **4 drifting snow motes** — plus a `❄️ The Frozen Feast` lore infoSpot framing T4 as the rung where the gorged devour and are devoured and the reversed Hunt "keeps an empty seat." **PURELY VISUAL** — every mesh is scene/group-added, NEVER pushed to `obstacles[]`, so no collision/coord/spawn change; gated to no-op unless `DUNGEONS[3].name==='Feastfall'`. In-bounds & non-colliding: all props sit in the **NORTH band** (z∈[−487,−468]) of the −60..60 / −580..−460 footprint, ≥13u north of the inner divider wall (z=−500, x −30..46), clear of the outer divider (z=−534) and the north wall ring (z=−460) by ≥8u, and >16u from the safe entry pad at (−50,−570) — trivially, since every prop is z≥−487 vs the pad's z=−570. x range [−17,17] well inside ±60. Reuses shared geos/mats (one base/body/head geo + 5 mats) for perf. Validated: built the verbatim function on stubbed THREE/globals (parent-chain world-position accumulation so guest sub-parts resolve to their group's x,z) → `node --check` (SYNTAX OK) and a **420-assert** logic test (every prop inside the footprint w/ margin; every prop north of the inner divider by ≥7u & clear of the north wall by ≥6u; every prop point→segment-clear of BOTH divider walls by >WALL_R; every prop >SAFE_R+3 from the safe pad; the maw silhouette reaches y≥20; **0 obstacles added** so traversal/secrets/spawns untouched; exactly 1 lore infoSpot named "Frozen Feast" inside the footprint; the function no-ops on a non-Feastfall DUNGEONS[3] — ALL 420 PASS). Re-Read the exact anchor before editing (another job may be mid-edit); atomic reversible insert; Grep-confirmed the function is defined + called exactly once and the W12 block below is intact. (bash mount serves a truncated tail per discipline — the file-tool Edit applied to the real file, while all node checks ran on a sandbox-native /tmp harness since the mount truncated even the new test file's tail.) Updated WORLD_MAP.md (§3c tracker row Feastfall ✅ + "Next" pointer → tier-5 Isle of Dominus + §5 W-table +W22) and wiki.html with the player-facing entry.
- 2026-06-23 — **W23 The Storm Throne** (DISTINCT ZONE GEOGRAPHY for the tier-5 realm — the FIFTH §3 tower-of-challenge zone given its own silhouette, following the §3c "Next: rotate to the tier-5 Isle of Dominus (Lightning)" pointer; mirrors the W19/W20/W21/W22 visual-only pattern on a fresh realm): added `function mkIsleOfDominus()` + its call in pve.html after `mkFeastfall();` (~L2113). Isle of Dominus (DUNGEONS[4], Lightning/Pride, cx-520/cz520/r60) now reads as a **storm-wracked throne-court** instead of a recolored box: a **storm-scarred floor** (slab disc + crag sheet at (-520,562)); **THE STORM THRONE** — Vael, the Crowned Tyrant (two tiered crag plinths + a stone seat + a tall 14u throne back topped with a 5-cone gold crown + a half-torus lightning arc, peaking at y≈25) as the **dominant silhouette**; a **ring of 6 tesla spires** (tapered pylons with emissive arc-tips); **6 shattered/leaning colossus statues** of dethroned pretenders (rotation.z lean); **6 radiating lightning-scars** across the floor; and **4 drifting charged motes** — plus a `⚡ The Storm Throne` lore infoSpot framing T5 as the rung where the proud climb over one another for a throne that brands all who sit it and the reversed Hunt "wears a crown of storms." **PURELY VISUAL** — every mesh is scene/group-added, NEVER pushed to `obstacles[]`, so no collision/coord/spawn change; gated to no-op unless `DUNGEONS[4].name==='Isle of Dominus'`. In-bounds & non-colliding: all props sit in the **NORTH band** (z≥552) of the −580..−460 / 460..580 footprint, ≥12u north of the inner divider wall (z=540, x[−550..−474]), clear of the outer divider (z=506) and the wall rings by ≥6u, and >16u from the safe entry pad at (−570,470) — trivially, since every prop is z≥553 vs the pad's z=470. x range ≈[−535,−505] well inside ±60 of cx. Reuses shared geos/mats (one base/body/head geo + 6 mats) for perf. Validated: built the verbatim function on stubbed THREE/globals with parent-chain world-position accumulation (so shard sub-parts resolve to their group's x,z) → `node --check` (SYNTAX OK) and a **319-assert** logic test (every prop inside the footprint w/ ≥6u wall margin; every prop z≥547 north band; every prop point→segment-clear of BOTH divider walls by >WALL_R; every prop >SAFE_R from the safe pad; the throne silhouette reaches y≥23; **0 obstacles added** so traversal/secrets/spawns untouched; exactly 1 lore infoSpot named "Storm Throne" at the dais centre; the function no-ops on a non-Isle DUNGEONS[4] — ALL 319 PASS). Re-Read the exact anchor before editing (another job may be mid-edit); atomic reversible insert; Grep-confirmed the function is defined + called exactly once and the W12 block below is intact. (bash mount serves a truncated tail per discipline → all checks ran on a sandbox-native /tmp harness with the verbatim fragment.) Updated WORLD_MAP.md (§3c tracker row Isle of Dominus ✅ + "Next" pointer → tier-6 Enamora + §5 W-table +W23) and wiki.html with the player-facing entry.
- 2026-06-23 — **W24 The Beguiling Rose** (DISTINCT ZONE GEOGRAPHY for the tier-6 realm — the SIXTH §3 tower-of-challenge zone given its own silhouette, following the §3c "Next: rotate to the tier-6 Enamora (Earth)" pointer; mirrors the W19–W23 visual-only pattern on a fresh realm, leaving only the tier-7 Abyssal Vault undone): added `function mkEnamora()` + its call in pve.html after `mkIsleOfDominus();` (~L2172). Enamora (DUNGEONS[5], Earth/Lust, cx-520/cz-520/r60) now reads as an **overgrown rose-garden bower** instead of a recolored box: a **verdant terraced floor** (slab disc + vine sheet at (-520,-478)); **THE BEGUILING ROSE** — Seraphel, the Beguiling Rose (a vine-wound column + calyx + a 6-petal ring + a crowning bloom, peaking at y≈22) as the **dominant silhouette**; a **ring of 6 thorn-bramble spires** (tapered, vine-dark, blossom-tipped); a **ring of 6 petrified lovers** frozen mid-embrace (round/leaning figures, `rotation.z` lean toward the bloom); **6 radiating root/vine cracks**; and **4 drifting petal motes** — plus a `🌹 The Beguiling Rose` lore infoSpot framing T6 as the rung where a single impossible bloom lures wanderers off the path and the reversed Hunt "comes wearing the face you most long for." **PURELY VISUAL** — every mesh is scene/group-added, NEVER pushed to `obstacles[]`, so no collision/coord/spawn change; gated to no-op unless `DUNGEONS[5].name==='Enamora'`. In-bounds & non-colliding: all props sit in the **NORTH band** (BZ=cz+42=-478, z∈[−487,−469]) of the −580..−460 / −580..−460 footprint, ≥13u north of the inner divider wall (z=−500), clear of the outer divider (z=−534) and the wall rings by ≥6u, and >16u from the safe entry pad at (−570,−570) — trivially, since every prop is z≥−487 vs the pad's z=−570. x range ≈[−535,−505] well inside ±60 of cx. Coordinate skeleton mirrors the validated W23 (Isle of Dominus) layout; reuses shared geos/mats for perf. Validated: built the verbatim function on stubbed THREE/globals with parent-chain world-position accumulation → `node --check` (SYNTAX OK) and a **313-assert** logic test (51 leaf meshes; every prop inside the footprint w/ ≥6u wall margin; every prop north of the inner divider; every prop point→segment-clear of BOTH divider walls by >WALL_R; every prop >SAFE_R from the safe pad; the bloom silhouette reaches y≥22; **0 obstacles added** so traversal/secrets/spawns untouched; exactly 1 lore infoSpot named "Beguiling Rose" at the bower centre; the function no-ops on a non-Enamora DUNGEONS[5] — ALL 313 PASS). Re-Read the exact anchor before editing (another job was mid-edit → first Edit bounced on a stale-state error, re-Read confirmed the anchor intact + retried, atomic reversible insert); Grep-confirmed the prior W23 block above is undisturbed. (bash mount serves a truncated/wrong tail per discipline — the mount's L2172 region returned Gloomwater code, so all node checks ran on a sandbox-native /tmp harness with the verbatim fragment inlined, not extracted from the mount.) Updated WORLD_MAP.md (§3c tracker row Enamora ✅ + "Next" pointer → tier-7 Abyssal Vault + §5 W-table +W24) and wiki.html with the player-facing entry.
- 2026-06-23 — **W25 The Abyssal Vault** (DISTINCT ZONE GEOGRAPHY for the tier-7 realm — the SEVENTH & FINAL §3 tower-of-challenge zone given its own silhouette, following the §3c "Next: rotate to the tier-7 The Abyssal Vault" pointer; **COMPLETES** WORLD_DESIGN §3's "7 identical recolored arenas" fix — all of W19–W25 now distinct): added `function mkAbyssalVault()` + its call in pve.html after `mkEnamora();` (~L2231). The Abyssal Vault (DUNGEONS[6], Mystic, T7, cx520/cz520/r60) — the seat of the Hollow King, the campaign finale (FINAL_ZONE) — now reads as a **sealed arcane sanctum** instead of a recolored box: an **arcane vault floor** (slab disc + stone sheet at (520,562)); **THE VAULT WARDEN** — a colossal eldritch sentinel (void-stone column + the **Etherheart** void-core orb pulsing in its chest + a void cowl/skull + a crown of 5 arcane sigil-horns, peaking at y≈25) as the **dominant silhouette**; **3 floating sigil-rings** (torus rings hovering at y13/16/19, the warped non-euclidean motif); a **void-rift altar** (dark altar block + a glowing Etherheart rift slab); a **ring of 6 arcane sealing obelisks** (tapered, octahedron sigil-crystal tipped); a **ring of 6 kneeling void-thralls** (souls bound to the throne, `rotation.z` lean); **6 radiating sigil-cracks**; and **4 drifting Etherheart motes** — plus a `🔮 The Abyssal Vault` lore infoSpot framing T7 as the deepest rung where the reversed Hunt closes its jaws and there is nowhere deeper to flee. **PURELY VISUAL** — every mesh is scene/group-added, NEVER pushed to `obstacles[]`, so no collision/coord/spawn change; gated to no-op unless `DUNGEONS[6].name==='The Abyssal Vault'`. In-bounds & non-colliding: all props sit in the **NORTH band** (BZ=cz+42=562, z∈[553,571]) of the 460..580 / 460..580 footprint, ≥13u north of the inner divider wall (z=540), clear of the outer divider (z=506) and the wall rings by ≥6u, and >16u from the safe entry pad at (470,470) — trivially, since every prop is z≥553 vs the pad's z=470. x range ≈[505,535] well inside ±60 of cx. Coordinate skeleton mirrors the validated W24 (Enamora) layout; reuses shared geos/mats for perf. Validated: built the verbatim function on stubbed THREE/globals with parent-chain world-position accumulation → `node --check` (SYNTAX OK) and a **344-assert** logic test (56 leaf meshes; every prop inside the wall ring w/ ≥6u margin; every prop north of the inner divider (z≥547); every prop point→segment-clear of BOTH divider walls by >WALL_R; every prop >SAFE_R+3 from the safe pad; the Warden silhouette reaches y≥22; **0 obstacles added** so traversal/secrets/spawns untouched; exactly 1 lore infoSpot named "Abyssal Vault" inside the footprint; the function no-ops on a non-Abyssal DUNGEONS[6] — ALL 344 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed the function is defined + called exactly once and the W12 GLOOMWATER block directly below is intact. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment inlined.) Updated WORLD_MAP.md (§3c tracker row Abyssal Vault ✅ + "7 recolored arenas fix COMPLETE" note + §5 W-table +W25) and wiki.html with the player-facing entry.
- 2026-06-23 — **W26 The Cragspire** (GRAPPLE-gated rock bluff — the FOURTH & LAST §3 traversal-ability gate, completing the swim/key/charged-jump/grapple set; rotated AWAY from the W19–W25 realm-interior runs back to a Wilds traversal gate, the only unbuilt item in the §3/§6 ability-gate backlog): added a `const BLUFF` IIFE + `spawnBluffCache()` + `bluffBlock()` in pve.html after `thicketBlock` (~L2573), mirroring the validated W14 (Wardstone Grotto) snap-disc pattern. A sheer tiered **rock bluff** in the SW crags at `(-110,-80)` (baseR 6.5) walls off a flat **crown vista** (a cairn + banner) holding a sealed cache you can SEE from below but not walk up to — the rock turns a walker back at its foot. A **🪝 Grappling Claw** relic sits at the base `(-110,-89)` (d=9, just OUTSIDE the snap disc → reachable on foot). `bluffBlock(hero)` (wired after `thicketBlock` in the hero loop): (1) within 3.2u of the claw it claims it (hides claw, fxRing+sfxEquip+feed) and sets `BLUFF.up=true`; (2) until the grapple is earned it snaps any walker that enters the base disc (`d<baseR`) OUT to the ring (the sheer face — only the grapple scales it), so the crown cache is unreachable from any direction; once earned, the foot is free and a one-shot "you hook the anchor and haul yourself up" feed fires on first ascent. `spawnBluffCache()` (wired beside `spawnThicketCache` at init, isCursed-gated) drops the cache at the crown centre. A `🪝 The Cragspire` infoSpot at the foot carries the lore. Runtime-only flags (reset each load; the claw respawns); scoped to BLUFF so no coupling to W4/W13/W14/W15/W16. In-bounds & non-colliding: crown R≈136 & claw R≈141, both <150 sculpted Wilds and inside the ±165 clamp; verified >15u clear of all 16 prior secrets/landmarks/perches + town + the Gloomwater river. **PURELY a snap-disc gate — nothing pushed to `obstacles[]`**, so no collision/coord/spawn change elsewhere. Validated: extracted the verbatim fragment onto stubbed THREE/globals + `node --check` (SYNTAX OK) and a **62-assert** logic test (crown & claw in-bounds/in-Wilds; claw OUTSIDE the snap disc = reachable/solvable; >15u clear of every feature incl. the Gloomwater; pre-grapple continuous approach from 8 compass headings held at d≈baseR — never penetrates the disc; centred walker pushed to the ring; claim grants grapple + hides claw + exactly one fxRing/sfxEquip/feed; post-grapple free passage to crown centre + one-shot ascent feed that does NOT re-fire; cache at the crown centre & gated; isCursed → no spawn; **0 obstacles added**; exactly 1 Cragspire infoSpot at the claw foot; the bluff silhouette reaches crown height; all leaf meshes within 12u of the bluff/claw — ALL 62 PASS). Re-Read the exact anchor before each Edit; atomic reversible inserts; Grep-confirmed BLUFF/bluffBlock/spawnBluffCache defined + both calls wired exactly once (5 occurrences) and the HUNTER'S OVERLOOKS block below undisturbed. (bash mount serves a truncated tail per discipline — the Edits applied to the real file via the Read/Edit tools; all node checks ran on a sandbox-native /tmp harness with the verbatim fragment, since the mount truncated even the test file at L151.) Updated WORLD_MAP.md (§2 SW-crags Cragspire landmark, §3 gating graph + GRAPPLE row + W26 solvability note, §3 backlog "four ability-gates COMPLETE" tick, §5 W-table +W26, §6 backlog tick) and wiki.html with the player-facing entry.
- 2026-06-23 — **W27 The Riven Spires** (an impassable MOUNTAIN RANGE on the NORTH map-edge — the §4/§6 diegetic-border TODO "impassable mountains", rotated AWAY from the W19–W25 realm interiors and the W26 traversal gate back to a map-edge treatment like W11/W18; east is already the W18 Sundering Sea, so this attacks the next remaining flat edge): added `function mkRivenSpires()` + its call in pve.html after `mkSunderingSea();` (~L1942). Beyond the north brink (`z>B=165`) the land buckles into snow-capped granite peaks in **three depth bands** — 22 bare **foothills** hugging the brink (`z≈B+8`), 20 dominant **mid-wall** peaks (`z≈B+34`), 16 tall distant **summits** (`z≈B+95`) — running the width of the world (`x −255→~150`), the range the **Sunder Chasm** canyon cuts through; snow caps sit on the back/mid bands; a `⛰ The Riven Spires` lore infoSpot at the NW brink `(-110,156)` carries the cold-lands-beyond canon. **PURELY VISUAL** — every mesh is scene-added, NEVER pushed to `obstacles[]`, so the `clampZone` ±165 square (already stopping the hero) is unchanged and no traversal/secret/cache coords shift; all peaks sit **north of the brink** (`z>B`, min z≈173 > the Sunder Chasm vista z=143) so nothing interior is stranded, and **west of the Sundering Sea's near edge** (`x<B`) so they never poke through the W18 sea. Reuses one shared cone geo (scaled per peak) + 3 mats for perf. Validated: built the verbatim function on stubbed THREE/globals + `node --check` (SYNTAX OK) and a **17-assert** logic test (58 base peaks (16+20+22) + snow caps; **0 obstacles added**; every peak/cap north of the brink z>B; every peak/cap west of the sea edge x<B; all within the ±380 abyss backdrop; min peak z≥B+8 & >143 (clears the Sunder Chasm vista); exactly 1 infoSpot named "Riven Spires", inside the ±165 square clamp & south of the brink (walk-reachable), >15u clear of the Veil's Edge/Sunder Chasm/Far Vista/Sundering Sea infoSpots; snow caps all north of the brink; mesh count = bases+caps — ALL 17 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkRivenSpires` defined + called exactly once and the W19 block below undisturbed. (bash mount serves a truncated tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment inlined.) Updated WORLD_MAP.md (§4 +W27 north-mountain edge & remaining-edges TODO → only thickening-forest left, §5 W-table +W27, §6 backlog tick: edges mountains ✅) and wiki.html with the player-facing entry.
- 2026-06-23 — **W21 The Gilded Reliquary** (DISTINCT ZONE GEOGRAPHY for the tier-3 realm — the THIRD §3 tower-of-challenge zone given its own silhouette, following the doc's explicit "Next: rotate to the tier-3 Valley of Midas (Fire)"; mirrors the W19/W20 visual-only pattern on a fresh realm): added `function mkValleyOfMidas()` + its call in pve.html after `mkHollowBarrow();` (~L2008). Valley of Midas (DUNGEONS[2], Fire/Greed, T3, cx-520/cz0/r60) now reads as a **molten gold-hoard** instead of a recolored box: a **sunken molten basin** (15→13 taper) with a **lava lake** floor disc at (-520,42); a towering **gilded colossus** — Aurelian, the Gilded King (h24, base+body+head+crown) — as the **dominant silhouette**; a **ring of 8 covetous-soul gilded statues**; **6 radiating lava cracks** across the floor; **3 gold-hoard piles**; and **4 rising ember motes** — plus a `👑 The Gilded Reliquary` lore infoSpot framing T3 as the rung where the covetous gild themselves solid and the reversed Hunt "wears a crown." **PURELY VISUAL** — every mesh is scene/group-added, NEVER pushed to `obstacles[]`, so no collision/coord/spawn change; gated to no-op unless `DUNGEONS[2].name==='Valley of Midas'`. In-bounds & non-colliding: all props sit in the **NORTH band** (z≥27..≤56) of the -580..-460 / -60..60 footprint, north of BOTH room-divider walls (z=-14 x[-566..-490]; z=20 x[-550..-474]) by ≥7u, and >16u from the safe entry pad at (-570,-50) — trivially, since every prop is z≥27 vs the pad's z=-50. Reuses shared geos/mats (one base/body/head geo + 5 mats) for perf. Validated: built the verbatim function on stubbed THREE/globals → `node --check` (SYNTAX OK) and a **92-assert** logic test (every prop inside the footprint w/ its radius margin; every prop >SAFE_R+rad from the safe pad; every prop's south extent ≥4u north of any overlapping divider wall; all props north band z≥~27; **0 obstacles added** so traversal/secrets/spawns untouched; exactly 1 lore infoSpot named "Gilded Reliquary" inside the footprint; the function no-ops on a non-Valley DUNGEONS[2] — ALL 92 PASS). Re-Read the exact anchor before editing (another job may be mid-edit); atomic reversible insert; Grep-confirmed the function is defined + called exactly once and the W12 block below is intact. (bash mount serves a truncated tail per discipline → all checks on the verbatim fragment via a sandbox-native harness.) Updated WORLD_MAP.md (§3c tracker row Valley of Midas ✅ + "Next" pointer → tier-4 Feastfall + §5 W-table +W21) and wiki.html with the player-facing entry.
- 2026-06-23 — **W28 The Smotherwood** (a thickening FOREST WALL on the SOUTH & WEST map-edges — the LAST diegetic-border TODO per WORLD_DESIGN §4/§6 "a forest that thickens to a wall (the SOUTH/WEST flat stretches)"; rotated AWAY from the W19–W25 realm interiors and the W26 traversal gate back to a map-edge treatment like W11/W18/W27, finishing the edge set since east=sea W18 and north=mountains W27): added `function mkBorderWood()` + its call in pve.html after `mkRivenSpires();` (~L2007). Beyond the south brink (`z<-B=-165`) and the west brink (`x<-B`) the Wilds choke into a deepening conifer forest built in **three bands** that THICKEN outward — a sparse short inner fringe (`off≈+6`, h6–11, mid-green, 1 cone), a taller denser mid stand (`off≈+26`, h12–20, dark-green, 2 cones), and a tall black **back-wall** (`off≈+58`, h20–34, dark-green, 2 cones) — so the wood visibly thickens to a wall as it runs out past sight, hemming the eye exactly like a real forest edge. South band x∈[-160,155] (kept `<B` so it never reaches the W18 Sundering Sea); west band z∈[-160,150] (kept `<B` so it never reaches the W27 Riven Spires). A clifftop **🌲 The Smotherwood** infoSpot at the west brink `(-156,10)` carries the canon (the forest wall that hems the human world the way the sea hems the east and the spires the north). **PURELY VISUAL** — every mesh is scene-added, NEVER pushed to `obstacles[]`, so the `clampZone` ±165 square (already stopping the hero) is unchanged and no traversal/secret/cache coords shift; ALL trees sit OUTSIDE the playable square (`z<-B` or `x<-B`, further out than every interior feature incl. the Gloomwater shore z=-129) so nothing interior is stranded, and kept x<B / z<B so they never poke through the sea or the peaks. Reuses one shared cone geo + one trunk geo (scaled per tree) + 3 mats for perf (~108 trees, 300 meshes). **COMPLETES §4's diegetic-edges mandate** (E=sea ✅, N=mountains ✅, S+W=forest ✅, Veil-cliff on the deep/Underworld-facing seam). Validated: built the verbatim function on stubbed THREE/globals + `node --check` (SYNTAX OK) and a **20-assert** logic test (0 obstacles added; exactly 1 infoSpot named "Smotherwood" inside the ±165 clamp & >15u clear of all 8 nearby edge/landmark infoSpots; mesh count = 300; every mesh OUTSIDE the playable square — south z<-B or west x<-B, 0 stray; both bands populated; 0 south trees poke into the sea (x<B); 0 west trees poke into the spires (z<B); south interior edge stays beyond the brink so nothing stranded; back-wall silhouette tall — ALL 20 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkBorderWood` defined + called exactly once and the W19 Mossfang block below undisturbed. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment inlined.) Updated WORLD_MAP.md (§4 +W28 south/west forest edge & "all edges diegetic — §4 complete", §5 W-table +W28, §6 backlog tick: edges forest ✅ → §4 complete) and wiki.html with the player-facing entry.
- 2026-06-23 — **W30 The Weeping Hollow** (a waterfall you can walk BEHIND into a hidden cavern — the LAST unbuilt §6 interior-secret backlog item, "a waterfall you can walk behind into a cavern (Zelda secret)"; rotated AWAY from the recent map-edge (W27/W28), realm-interior (W19–W25) and Riftgate (W29) runs back to a Wilds discovery secret in the vein of W16 the Whispering Thicket): added a `const HOLLOW` IIFE + `spawnHollowCache()` + `cavernBlock()` in pve.html after `waterfall(104,70);` (~L1894). A waterfall on the **western crags** at `(-128,12)` — two flanking cliff blocks + a lintel leave a dark cavern **MOUTH**, a still pool + foam in front, and behind the **water curtain** (reuses `fallTex`, pushed to `WATERFALLS` so it scrolls via the existing `updateWaterfalls`) a recessed dark **chamber** (back wall + dark floor disc + 4 glowing crystals) hiding a **sealed cache** at `(-128,5)`. NO ability/relic — a **discoverable PLACE** (like W16), distinct from the swim/key/jump/grapple ability gates: `cavernBlock(hero)` (wired after `bluffBlock` in the hero loop) fires a one-shot "you slip behind the falls" feed + fxRing when the hero steps within 4u of the cavern interior — **NO snapping, NO obstacle**. `spawnHollowCache()` (wired beside `spawnBluffCache` at init, isCursed-gated) drops the cache behind the falls. A `💧 The Weeping Hollow` infoSpot at the pool `(-128,19)` carries the lore. Runtime-only flag (resets each load). **PURELY a scene + a trigger — NOTHING pushed to `obstacles[]`**, so no collision/coord/spawn shift. In-bounds & non-colliding: footprint R≤135 < the 150 sculpted Wilds and inside the ±165 clamp; verified >15u clear of all 20 features (town, hamlet, briar maze, both Mistfalls, W4/W5/W7, Mere + relic, Grotto + key, Chasm feather + vista, Thicket, Cragspire + claw, Riftgate, both perches). Validated: built the verbatim fragment on stubbed THREE/globals + `node --check` (SYNTAX OK) and a **45-assert** logic test (cache at (-128,5) behind the falls; every footprint extreme R<150 & inside ±165; >15u clear of every one of the 20 features; **0 obstacles added**; the curtain registered in `WATERFALLS` so it scrolls; exactly 1 infoSpot named "Weeping Hollow" inside the clamp; cache spawns behind the falls & isCursed → no spawn; far-away hero → no discovery & no move; stepping into the cavern fires the beat exactly once & never re-fires & never moves the hero (no snapping); a non-hero unit never triggers it — ALL 45 PASS). Re-Read the exact anchor before editing; atomic reversible inserts; Grep-confirmed `HOLLOW`/`spawnHollowCache`/`cavernBlock` defined + both calls wired exactly once and the pre-existing HOLLOW-BARROW/HOLLOW-KING/HOLLOW-BLIGHT text undisturbed. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment inlined.) Updated WORLD_MAP.md (§2 west-crags Weeping Hollow biome, §3 gating graph + WALK BEHIND row, §5 W-table +W30, §6 backlog tick: waterfall-cavern ✅) and wiki.html with the player-facing entry.
- 2026-06-23 — **W29 The Riftgate** (the §2 layer-2 THRESHOLD between worlds made PHYSICAL + the §4 one-way descent expressed diegetically — a fresh mandate: all prior runs built Wilds secrets/edges/road (W11–W18,W26–W28) or realm interiors (W19–W25), but the **Rift itself had no place in the overworld** despite every realm's infoSpot naming "the rung beneath the Rift" and the Ep3 Rite of Blades crossing it): added `function mkRiftgate()` + its call in pve.html after `mkBorderWood();` (~L2019, mirroring the validated visual-landmark pattern of W18/W27/W28). In a clear **NE-plains clearing at (58,46)** (R≈74) the human world tears open: a torn **violet rift seam** (PlaneGeometry 8×17, the dominant silhouette reaching y≈17.5) framed by two **leaning monoliths** (the Guarded Portal), over a dark ritual **dais** (CylinderGeometry + glow rim) with a **black descent throat** (the one-way drop) and **6 radiating sigil-cracks**; a **blood-altar** at the foot with the **Blood Scimitar** planted in it (blade + bleeding hilt + blood-drop, the Ep3 Rite of Blades); and **6 rising motes**. A `🩸 The Riftgate` lore infoSpot at the approach (58,54) carries the §2 layer + §4 one-way-descent canon, matching the script's "the Rift seals without a seam, no door, no way back." **PURELY VISUAL** — every mesh is scene/group-added, NEVER pushed to `obstacles[]`, so no collision/coord/spawn/gameplay change; no portal teleport wired (the §4 directional rule is conveyed by lore, keeping the change atomic & reversible and avoiding coupling to the episode/portal system). In-bounds & non-colliding: centre R≈74 < the 150 sculpted Wilds and inside the ±165 clamp; all props ≤10u of centre (max reach verified 10u); verified >15u clear of all 26 features (town, stash/bounty, hamlet, briar maze, both Mistfalls, the four edges, Gloomwater, Mere + relic, Grotto + key, Chasm relic + vista, Thicket, Cragspire + claw, both Hunter's Overlooks). Validated: built the verbatim function on stubbed THREE/globals (group parent-chain world-position accumulation) + `node --check` (SYNTAX OK) and a **42-assert** logic test (centre R<150 & inside ±165; >15u clear of every one of the 26 features; **0 obstacles added**; all meshes ≤10u of centre = nothing strays; exactly 1 infoSpot named "Riftgate" inside the clamp & R<150 walk-reachable; the seam plane top reaches y≥16 = dominant silhouette; Blood Scimitar blade + blood-altar + 2 framing monoliths + black descent throat + 6 sigil-cracks all present; mesh count ≥23 — ALL 42 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkRiftgate` defined + called exactly once and the W19 Mossfang block below undisturbed. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment inlined.) Updated WORLD_MAP.md (§2 Riftgate landmark + §5 W-table +W29) and wiki.html with the player-facing entry.
- 2026-06-23 — **W31 The Wayfarer's Wharf** (the §1 GOLDEN-RULE staged world-map UNLOCK made PHYSICAL — the FF-II *"a bridge, a pass, a **vessel**"* beat that the W17 Frontier Waystone's own lore already promises but which had no place in the overworld; rotated AWAY from the realm interiors (W19–W25), the map edges (W11/W18/W27/W28), the traversal gates (W13–W15/W26) and the discoverable-place secrets (W16/W30) — the whole §6 backlog is ticked, so this attacks §1's headline mandate, the single biggest unbuilt piece): added `function mkWayfarersWharf()` + its call in pve.html after `mkSunderingSea();` (~L1984). On the **east shore** where the land meets the **Sundering Sea** (W18), a **stone quay** (a 16-long deck on 6 stone pilings) juts off the beach at `(152,-40)` toward the surf, and a **sky-ferry** rides at anchor alongside its seaward side (a `THREE.Group` at (162,−47): hull + top rail + a 4-sided prow cone + a cabin + an 11u **mast** with a furled **sail** plane + a lit **lantern**), with a **gangplank** bridging pier to deck. The "**vessel**" that will one day bear the player across the Sundering Sea to the **wider world** — a classic-RPG *"see the vessel, can't sail yet"* hook (§1 incremental unlock + §4 layered travel). A `🚢 The Wayfarer's Wharf` lore infoSpot at the landward quay-head `(148,-40)` carries the staged-unlock canon ("the wider world opens a stage at a time... this vessel sails only once your story has earned the open sea"); **no fast-travel is wired** (the staged rule is carried by lore, keeping the change atomic, reversible and decoupled from the waypoint/episode systems). **PURELY VISUAL** — every mesh is scene/group-added, NEVER pushed to `obstacles[]`, so nothing snaps or collides and no clamp/traversal/secret/cache coords shift. In-bounds & non-colliding: the landward marker `(148,-40)` is on the W18 beach inside the ±165 **square** clamp & walk-reachable; the seaward-most mesh reaches x≈164.4 — **just inside the surf line** (x≈165.5) so nothing pokes past the water's edge; every mesh `|z|<165`; verified **>15u clear** of all 8 nearby east-side features (the W18 lighthouse/Sea-info (152,96), the Mooncradle Mere (122,6) + Tide-Charm relic (92,6), the W4 Sundered Ledge (115,−115), the W7 sepulcher (100,104), the Gloomwater shore, the Riftgate (58,46) and town). Validated: built the verbatim function on stubbed THREE/globals with parent-chain world-position accumulation (so the boat-group children resolve to world x,z) + `node --check` (SYNTAX OK) and a **33-assert** logic test (**0 obstacles added** so traversal/secrets/spawns untouched; **exactly 1** infoSpot named "Wayfarer's Wharf" at (148,−40), inside the ±165 clamp & ~on the walkable shore; **no mesh crosses the surf line** maxX≤165.5; every mesh `|z|<165`; the vessel silhouette (mast/sail) reaches y≥6; >15u clear of every one of the 8 features; ≥14 meshes built — ALL 33 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkWayfarersWharf` defined + called exactly once (L1984/L2010) between `mkSunderingSea();` (L1976) and `mkRivenSpires();` (L2042), neighbours undisturbed. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment inlined.) Updated WORLD_MAP.md (§2 Wayfarer's Wharf landmark + §5 W-table +W31) and wiki.html with the player-facing entry.
- 2026-06-23 — **W32 The Highcrag Stair** (the FIRST build toward WORLD_DESIGN §3b's 3-tier elevation — "the map has 3 distinct height levels; TRAVEL UP/DOWN is real, you ascend between levels via ramps/switchback paths/stairs" — a genuinely FRESH mandate: every prior run (W11–W31) was a flat-plane secret, map-edge, road, vessel, Riftgate, or realm interior; NONE made the vertical climb between the heightfield's LOW/MID/HIGH shelves read deliberately): added `function mkHighcragStair()` + its call in pve.html after the PERCHES (Hunter's Overlook) infoSpots (~L2837). A worn **switchback stone stairway** climbs the **Highcrag mesa's east face** along a 4-point polyline `(-44,-50)→(-66,-46)→(-70,-64)→(-83,-60)` — from the LOW moonlit plains (foot, heightAt≈0.78) up the MID shelf to the HIGH crag **crown** (heightAt≈7.0) where the **Hunter's Overlook (PERCH[0], -85,-60)** sits, so the ascent to the high tier READS as a real climb instead of a smooth nameless slope. ~22 **step slabs** (shared `BoxGeometry`, two stone mats) sit ON `heightAt()` so they hug the terrain; **rail posts** on the outer/downhill edge every 2nd step; **landing pads** (cylinders) at the foot, the two switchback turns, and the crown approach; a **waymark cairn** at the foot; a **banner** at the top landing (≈2u from the Overlook, delivering you there); a `🪜 The Highcrag Stair` lore infoSpot at the foot naming the LOW plains → MID shelf → HIGH crown tiers. **PURELY VISUAL** — every mesh is scene-added, NEVER pushed to `obstacles[]`; the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W17 Wayfarer's Road). In-bounds & non-colliding: foot R≈67, crown R≈103, both <150 sculpted Wilds and inside the ±165 clamp; **min clearance 33.6u** (nearest = the W26 Cragspire), also >15u clear of the Wardstone Grotto/Grappling Claw/Mistfall/Weeping Hollow/Hamlet/Whispering Thicket and outside the town disc; the top landing intentionally sits ~2u from the Overlook (it leads up to it, not a conflict). Validated: built the verbatim function on stubbed THREE/globals with the REAL `MESAS`/`heightAt` + `node --check` (SYNTAX OK) and a **408-assert** logic test (≥14 steps; every scene node in the ±165 clamp, in the R<150 Wilds, outside town+6; every node >15u from all 7 NW features; **0 obstacles added**; exactly 1 infoSpot named "Highcrag Stair" in-bounds; the top landing <8u from the Overlook; the stair climbs heightAt 0.78→7.0 — ALL 408 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkHighcragStair` defined (L2837) + called exactly once (L2866) with the `let aimMode=...` line (L2867) directly below undisturbed. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined.) Updated WORLD_MAP.md (§2 Highcrag biome + "§3b now IN PROGRESS / next steps" note, §5 W-table +W32) and wiki.html with the player-facing entry.
- 2026-06-23 — **W33 The Cragfall** (a cliff-edge waterfall pouring HIGH→LOW — the SECOND build toward WORLD_DESIGN §3b's 3-tier elevation, the explicit "next §3b step" the W32 run logged ("a cliff-edge waterfall pouring HIGH→LOW per the natural-placement logic"); rotated to §3b's natural-placement-logic mandate, which the two existing free-standing Mistfalls (`waterfall(-46,-96)`, `waterfall(104,70)`) violate ("NEVER a free-standing waterfall on flat ground")): added `function mkCragfall()` + its call in pve.html after `waterfall(104,70);` (~L1904). On the **Highcrag mesa's NORTH flank** the high ground sheds meltwater: the spill **LIP** sits on the HIGH shelf at `(-85,-33)` (real `heightAt`≈7, and *inside* the mesa radius — a genuine plateau edge, not flat ground), a **wet-rock scarp + headwall + two shoulder rocks** frame the drop, a **tilted water curtain** (PlaneGeometry reusing `fallTex`, pushed to `WATERFALLS` so it scrolls via the existing `updateWaterfalls`) sheets down the flank, and a **plunge POOL** with foam + 5 rising spray cones + 4 plunge rocks pools on the LOW plains at `(-85,-10)` (`heightAt`≈0) — so the falls reads as a real HIGH→LOW tier transition ("a waterfall implies a cliff above it and a pool below it; water always seeks the low ground"). A `💧 The Cragfall` lore infoSpot sits at the pool `(-85,-4)`. **PURELY VISUAL** — every mesh is scene/group-added, **NOTHING pushed to `obstacles[]`**, so no collision/clamp/traversal/secret/cache/spawn coords shift (the hero already walks the slope via `heightAt`; this is a landmark, not a barrier — like the W32 stair). In-bounds & non-colliding: pool R≈86 & lip R≈91, both <150 sculpted Wilds and inside the ±165 clamp; the whole footprint is **>15u clear of all 15 nearby NW/town features** (min ≈23u = the Highcrag Stair midpoint; also clear of town, Hamlet, both Mistfalls, Wardstone Grotto + key, Cragspire + claw, Weeping Hollow, Thicket, the Stair foot/bends, both Perches). Validated: built the verbatim function on stubbed THREE/globals with the **real `MESAS`/`heightAt`** + `node --check` (SYNTAX OK) and a **37-assert** logic test (lip `heightAt`≥6 = HIGH & pool `heightAt`≤1 = LOW & drop≥5; lip inside / pool outside the mesa radius = a real edge; every key point in R<150 & ±165; >15u clear of all 15 features (town uses its +30 radius); **0 obstacles added**; exactly 1 curtain in `WATERFALLS`; exactly 1 in-bounds infoSpot named "The Cragfall"; one group of 17 meshes anchored at the LOW pool base with group.y == the low `heightAt` — ALL 37 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkCragfall` defined + called exactly once (L1904/L1937) with the W30 Weeping Hollow block directly below undisturbed. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined.) Updated WORLD_MAP.md (§2 Highcrag biome +Cragfall & §3b progress note, §5 W-table +W33) and wiki.html with the player-facing entry.
- 2026-06-23 — **W34 The Emberwood Ascent** (a graded switchback FOREST TRAIL up the Emberwood crown's SW face to the SECOND Hunter's Overlook — the SECOND deliberate ascent toward WORLD_DESIGN §3b "the map has 3 distinct height levels; travel up/down is real — ascend via ramps/switchback paths/stairs"; the W32 Highcrag Stair (NW) was the FIRST, so this rotates the §3b ascent mandate to the OPPOSITE (SE) corner and a DIFFERENT feature kind — an earth-and-timber trail, not a stone stair): added `function mkEmberwoodAscent()` + its call in pve.html after `mkHighcragStair();` (~L2920, before `let aimMode=…`). The Emberwood crown `(60,105)` h7 is the second HIGH-tier perch (PERCH[1], Hunter's Overlook) but had NO climbing route; now a switchback **trail** zig-zags up its SW face along the polyline `[[43.8,83.4],[54,82],[48,90],[58,104]]` — from the LOW moonlit plains (foot, heightAt≈0) through the MID rim to the HIGH crown (top, heightAt≈7, 2.2u from PERCH[1]) — so the ascent to the second overlook READS as a deliberate climb, not a smooth nameless slope. ~22 packed-earth **tread slabs** (shared `BoxGeometry`, two earth mats) sit ON `heightAt()` so they hug the real terrain; **timber rail posts** on the outer/downhill edge every 2nd tread; **landing pads** (cylinders) at the foot/two switchback turns/crown approach; a **trail signpost** (pole + board) at the foot; an **autumn-rust banner** at the top; a `🥾 The Emberwood Ascent` lore infoSpot at the foot naming the LOW plains → MID rim → HIGH crown tiers. **PURELY VISUAL** — every mesh is scene-added, NEVER pushed to `obstacles[]`; the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W32 Stair / W17 Road). In-bounds & non-colliding: foot R≈94, crown R≈119, both <150 sculpted Wilds and inside the ±165 clamp; every node outside the town disc; >15u clear of every OTHER feature (W7 sepulcher 100,104; Mistfall 104,70; Mooncradle Mere 122,6 + Tide-Charm relic 92,6; Wayfarer's Wharf 152,-40; Riftgate 58,46; Whispering Thicket -80,92; Sunder Chasm vista -2,143; Abandoned Hamlet -80,28; Briar Maze 66,-74; W4 Sundered Ledge 115,-115) — the top landing intentionally sits 2.2u from PERCH[1] (it leads up to it, not a conflict). Mirrors the validated W32 coordinate/mesh skeleton; SW approach chosen so the whole trail stays clear of the east-side features. Validated: built the verbatim function on stubbed THREE/globals with the REAL `MESAS`/`heightAt`/`TOWN` + `node --check` (SYNTAX OK) and a **137-assert** logic test (≥14 treads (got 22); every scene node (39) in the ±165 clamp, in the R<150 Wilds, outside town; every node >15u from all 11 other features; **0 obstacles added**; exactly 1 in-bounds infoSpot named "Emberwood Ascent"; the top landing <8u (2.24u) from PERCH[1]; the trail climbs heightAt 0.00→1.58→6.05→7.00, non-decreasing, ascent≥5 — ALL 137 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkEmberwoodAscent` defined (L2920) + called exactly once (L2950) with `mkHighcragStair();` (L2911) above and `let aimMode=false…` (L2951) directly below undisturbed. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined.) Updated WORLD_MAP.md (§2 Emberwood biome +Ascent & §3b progress note "both crowns now climbed", §5 W-table +W34) and wiki.html with the player-facing entry.
- 2026-06-23 — **W35 The Highcrag Scarp** (an escarpment cliff band marking a tier BOUNDARY as a sheer SHELF EDGE — the FOURTH build toward WORLD_DESIGN §3b's 3-tier elevation and the FIRST tier-boundary cliff (W32 stair + W34 trail are ascents, W33 is a water-drop); the W34 run logged "next §3b steps: more tier-boundary cliffs/scarps so the LOW↔MID↔HIGH transitions read as shelves — an escarpment band at a MID→LOW boundary", so this rotates the §3b mandate to its remaining unbuilt kind: a cliff face, not a path or a falls): added `function mkHighcragScarp()` + its call in pve.html after `mkEmberwoodAscent();` (L2951 def / L2983 call, before the `let aimMode` line). Along the **Highcrag upper mesa's SOUTH rim** (7 stations on the arc θ −100°→−66° at faceR 40 from the mesa centre `(-85,-60)`) the HIGH crag (the h7 mesa top) now visibly **shears off and steps a full tier down onto the MID lower shelf** (MESAS[1] `(-60,-100,r30,h3.5)`) — the §3b "stepped two-height areas / Diablo-III" reference made physical, instead of a smooth nameless slope. Each station builds a tall rock **FACE slab** + an offset jagged **crag block** (the cliff face), a HIGH-side **lip cap** at lipR 33 (the shelf edge you stand on), and two LOW/MID-side **talus boulders** at talR 47 (fallen rock at the cliff foot); every mesh sits on `heightAt()` so it hugs the descending slope. A `🪨 The Highcrag Scarp` lore infoSpot at the foot `(-76,-106)` carries the canon. **PURELY VISUAL** — every mesh is scene-added, NEVER pushed to `obstacles[]`; the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W32 Stair / W33 Cragfall / W34 Ascent). In-bounds & non-colliding: every placed point R<150 sculpted Wilds & inside the ±165 clamp; the arc's east end was tightened from −55° to −66° after the first test flagged two SE talus boulders 13–14u from the free-standing Mistfall `(-46,-96)` → re-validated all >15u clear. Verified >15u clear of 11 nearby features (W26 Cragspire + Grappling Claw, W14 Wardstone Grotto, W30 Weeping Hollow, both W33 Cragfall points, the SW Mistfall, the W32 Highcrag Stair foot, Overlook[0], the Abandoned Hamlet, the Whispering Thicket). Validated: built the verbatim function on stubbed THREE/globals with the **real `MESAS`/`heightAt`/`TOWN`** + `node --check` (SYNTAX OK on the fragment extracted from the live file) and a **492-assert** geometry test (every face/crag/lip/talus/infoSpot point in R<150 & ±165; every point >15u from all 11 features; each lip `heightAt`≥5 = HIGH tier top; each lip `heightAt` > its base `heightAt` = the face descends outward = anchored on a REAL slope, not free-standing on flat ground; each base `d`∈[28.5,46] from the mesa centre = inside the mesa slope band) **+ a runtime test on the verbatim fragment (0 obstacles added; exactly 1 infoSpot named "Highcrag Scarp"; 35 meshes built — ALL PASS)**. Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkHighcragScarp` defined (L2951) + called exactly once (L2983) with the `let aimMode` line directly below undisturbed. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined.) Updated WORLD_MAP.md (§2 Highcrag §3b progress note "two ascents + one water-drop + one tier-boundary cliff", §5 W-table +W35) and wiki.html with the player-facing entry.
- 2026-06-23 — **W36 The Emberwood Scarp** (a MID→LOW tier-boundary escarpment — the SECOND tier-boundary cliff toward WORLD_DESIGN §3b's 3-tier elevation, after the W35 Highcrag Scarp's HIGH→MID rim, and the FIRST MID→LOW one; the W35 run logged "next §3b steps: a second scarp on a MID→LOW boundary (e.g. the Emberwood rise's south face)", so this rotates the §3b mandate to exactly that): added `function mkEmberwoodScarp()` + its call in pve.html after `mkHighcragScarp();` (L2991 def / L3018 call, before the `let aimMode` line). Along the **Emberwood rise's SW face** (7 stations on the arc θ −140°→−113° at faceR 34 from the rise centre `(95,55)`, MESAS[2] `(95,55,r42,h3.5)`) the MID rust-wooded flat now visibly **shears off and steps a full tier down onto the LOW moonlit plains** (heightAt≈0) — instead of a smooth nameless slope. Each station builds a rust-brown rock **FACE slab** + an offset **crag block** (the cliff face), a MID-side **lip cap** at lipR 28 (heightAt≈3.37 = the shelf edge you stand on), and two LOW-side **talus boulders** at talR 41 (heightAt≈0.03 = fallen rock at the cliff foot); every mesh sits on `heightAt()` so it hugs the descending slope (drop≈3.3). Rust-brown rock (0x6e5236/0x5c4630/0x7a5a38) matches the Emberwood biome vs the W35 Highcrag's grey granite. A `🪨 The Emberwood Scarp` lore infoSpot at the foot `(70,16)` carries the canon. The SW face was chosen over the literal "south face" because due-south the rise is hemmed by the Tide-Charm relic `(92,6)` + the Mooncradle Mere `(122,6)`; the SW arc threads clear of both the relic and the Riftgate `(58,46)`. **PURELY VISUAL** — every mesh is scene-added, NEVER pushed to `obstacles[]`; the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W35 Scarp). In-bounds & non-colliding: every placed point R<150 sculpted Wilds & inside the ±165 clamp; verified >15u clear of 11 nearby features (Riftgate, Tide-Charm relic, Mooncradle Mere, NE Mistfall `(104,70)`, W7 sepulcher, Emberwood Ascent foot + crown, Briar Maze, town, W4 ledge, Wayfarer's Wharf). Validated: built the verbatim function on stubbed THREE/globals with the **real `MESAS`/`heightAt`/`TOWN`** + `node --check` (SYNTAX OK on the fragment, runs → exactly 1 infoSpot) and a **496-assert** geometry test (every face/crag/lip/talus/infoSpot point in R<150 & ±165; every point >15u from all 11 features; each lip `heightAt`≥2.5 = MID; each talus `heightAt`≤1 = LOW; each lip−talus drop≥2 = descends outward = anchored on a REAL slope, not free-standing on flat ground; each face `d`∈(26,42) from the rise centre = inside the mesa slope band — ALL 496 PASS). Re-Read the exact anchor before editing; atomic reversible insert; re-Read confirmed the insert intact and the `let aimMode`/`onPerch` lines directly below undisturbed. (bash mount serves a truncated/stale tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined.) Updated WORLD_MAP.md (§2 Emberwood biome + §3b progress note "two ascents + one water-drop + TWO tier-boundary cliffs", §5 W-table +W36) and wiki.html with the player-facing entry.
- 2026-06-24 — **W37 The Emberwood Mistfall** (RE-ANCHOR an existing free-standing waterfall onto its correct tier — the FIRST re-grounding of an EXISTING feature toward WORLD_DESIGN §3b's 3-tier elevation, after the five from-scratch §3b builds W32 stair / W33 Cragfall / W34 trail / W35 + W36 scarps; the W36 run + WORLD_MAP §2 logged the explicit "next §3b step: re-anchor the two free-standing Mistfalls (`waterfall(-46,-96)`, `waterfall(104,70)`) to real cliff edges — the §3b 'no free-standing waterfall on flat ground' reconciliation"; this does the NE one, leaving the SW for next run): the NE Mistfall was `waterfall(104,70)`, which `heightAt`-probing confirmed sits at height **≈3.5 on the FLAT TOP of the Emberwood rise** (MESAS[2] `(95,55,r42,h3.5)`) — a textbook §3b violation (a waterfall floating on flat ground, no cliff above, no lower pool). Replaced that call with a new `function mkMistfallNE()` (after `mkCragfall();`, modelled on the validated W33 Cragfall) that pours the falls MID→LOW over the rise's REAL shelf edge: spill **LIP** on the MID shelf edge `(112,72)` (`heightAt`≈3.5), a wet-rock **headwall + 2 shoulders + a tilted scarp face**, a **water curtain** (reuses `fallTex`, pushed to `WATERFALLS` so it scrolls via the existing `updateWaterfalls`), and a plunge **POOL + foam + plunge-disc + 5 spray cones + 4 plunge rocks** on the LOW moonlit plains `(112,89)` (`heightAt`≈0.5) — **drop≈3.0, run 17**. Rust-brown rock (0x6e5236/0x4a3a26/0x5c4630) matches the Emberwood biome (vs W33's grey granite). A `💧 The Emberwood Mistfall` lore infoSpot on the shelf-side approach `(112,66)`. **PURELY VISUAL — 0 obstacles added**, and in fact a **net −1 obstacle** (removing the old `waterfall(104,70)` call also drops its `wildSolid(104,65.5,8)` block); the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W33 Cragfall). The binding constraint was the **W7 hidden sepulcher `(100,104)`** just NW of the pool — the original random plunge-rock scatter strayed to ≈9u of it, so the rocks were made **FIXED & biased +x/−z** and the pool pulled to z=89, putting the whole footprint **≈17u clear** of W7. In-bounds & non-colliding: every mesh R<150 sculpted Wilds & inside the ±165 clamp; verified >15u clear of all 11 nearby features (W7 sepulcher, Emberwood crown/PERCH[1], Emberwood Ascent foot, Mooncradle Mere + Tide-Charm relic, Riftgate, Briar Maze, Wayfarer's Wharf, Whispering Thicket, the SW Mistfall, Overlook[0]) and >30u from town. Validated: `node --check` (SYNTAX OK on the verbatim fragment) + a **10-check geometry test run over 500 seeds** (only the spray is random now) on the **real `MESAS`/`heightAt`/`TOWN`** (0 obstacles; exactly 1 curtain in `WATERFALLS`; exactly 1 infoSpot named "Emberwood Mistfall"; lip `heightAt`≥2.5 = MID; pool `heightAt`≤1 = LOW; real drop≥2; every mesh R<150 & ±165 across ALL seeds; every mesh >15u from all 11 features across ALL seeds; every mesh >30u from town; 17 meshes/call — ALL PASS). Re-Read the exact anchors before each Edit (another job touched the file mid-run → a stale-state bounce on one Edit, re-Read confirmed the region intact + retried); two atomic reversible inserts (the call swap + the function). (bash mount serves a truncated/STALE tail per discipline — grep on the mount found NONE of my edits, so all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined, NOT extracted from the mount.) Updated WORLD_MAP.md (§2 Emberwood biome + §3b progress note "first re-grounded existing feature; next: the SW Mistfall", §5 W-table +W37) and wiki.html with the player-facing entry.
- 2026-06-24 — **W38 The Highcrag Mistfall** (RE-ANCHOR the SECOND & LAST free-standing waterfall onto its correct tier toward WORLD_DESIGN §3b's 3-tier elevation — the explicit "next §3b step" the W37 run + WORLD_MAP §2 queued: "re-anchor the LAST free-standing Mistfall, the SW `waterfall(-46,-96)` on the Highcrag lower shelf, the same way"): the SW Mistfall was `waterfall(-46,-96)`, which `heightAt`-probing confirmed sits at height **≈3.5 on the FLAT TOP of the Highcrag LOWER shelf** (MESAS[1] `(-60,-100,r30,h3.5)`) — the last "free-standing waterfall on flat ground" §3b violation. Dropped that call (which also drops its `wildSolid(-46,-100.5,8)` block → a **net −1 obstacle**) and added `function mkMistfallSW()` (after `mkMistfallNE();`, modelled on the validated W37 Emberwood Mistfall) that pours the falls MID→LOW over the shelf's REAL north-face edge: spill **LIP** on the MID shelf edge `(-44,-92)` (`heightAt`≈3.5), a wet-rock **headwall + 2 shoulders + a tilted scarp face**, a **water curtain** (reuses `fallTex`, pushed to `WATERFALLS` so it scrolls via the existing `updateWaterfalls`), and a plunge **POOL + foam + plunge-disc + 5 spray cones + 4 FIXED plunge rocks** on the LOW moonlit plains `(-44,-76)` (`heightAt`≈0.31) — **drop≈3.19, run 16**. **Grey granite** (0x4a4658/0x2c3140/0x55505e) matches the Highcrag biome (like the W33 Cragfall, the other Highcrag falls; vs W37's Emberwood rust). The pour direction was generalised via `g.rotation.y = atan2(baseX−lipX, baseZ−lipZ)` (=0 here, a clean +z north-face pour; the same form reduces to the W33/W37 z-aligned templates), letting the falls hug whichever shelf edge it sits on. A `💧 The Highcrag Mistfall` lore infoSpot on the shelf-side approach `(-44,-66)`. **PURELY VISUAL — 0 obstacles added** (net −1 with the old wildSolid gone); the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W37 Mistfall). The binding constraint was the **W32 Highcrag Stair foot `(-44,-50)`** just N of the pool — the pool/spray were kept south so the whole footprint stays **≈17u clear** of it; also north of the upper Highcrag mesa's height-blend (x kept ≥−45 so the lip reads a clean MID 3.5, not blended high). In-bounds & non-colliding: every mesh R<150 sculpted Wilds & inside the ±165 clamp; verified >15u clear of all 14 nearby features (Highcrag Stair foot, both W35 Highcrag Scarp east stations, Cragspire + Grappling Claw, Wardstone Grotto, Grotto Key, both W33 Cragfall points, Gloomfen basin, Abandoned Hamlet, Weeping Hollow, Briar Maze, Overlook[0]) and >30u from town. Validated: `node --check` (SYNTAX OK on the verbatim fragment) + a **10-check geometry test over 500 seeds** (only the spray is random) on the **real `MESAS`/`heightAt`/`TOWN`** (0 obstacles; exactly 1 curtain in `WATERFALLS`; exactly 1 infoSpot named "Highcrag Mistfall"; lip `heightAt`≥2.5 = MID (3.50); pool `heightAt`≤1 = LOW (0.31); real drop≥2 (3.19); every mesh R<150 & ±165 across ALL seeds; every mesh >15u from all 14 features across ALL seeds; every mesh >30u from town; 17 meshes/call — ALL 500 PASS). The only initial test "failure" was a spurious >15u check against the OLD SW-Mistfall position `(-46,-96)` — the very feature being removed — which was dropped from the feature list. Re-Read the exact anchors before each Edit; two atomic reversible inserts (the call swap + the function); Grep-confirmed `mkMistfallSW` defined + called exactly once and the `waterfall(-46,-96)` CALL gone (only comment references remain) with the W30 Weeping Hollow block below undisturbed. **BOTH free-standing Mistfalls are now re-grounded (W37 NE, W38 SW) — no free-standing waterfall remains in the world.** (bash mount serves a truncated/STALE tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined, NOT extracted from the mount.) Updated WORLD_MAP.md (§2 Highcrag biome + §3b progress note "both Mistfalls re-grounded; next: a ramp down into the Gloomfen sunken basin", §5 W-table +W38) and wiki.html with the player-facing entry.
- 2026-06-24 — **W39 The Gloomfen Descent** (a switchback STAIR DOWN into the Gloomfen sunken basin — the FIRST deliberate DESCENT toward WORLD_DESIGN §3b's 3-tier elevation; the explicit "next §3b step" the W38 run + WORLD_MAP §2 queued ("carve a switchback/ramp DOWN into the Gloomfen sunken basin (MESAS[4], h−3) so the LOW↔sub-LOW transition reads deliberately"). Every prior §3b build was an ASCENT (W32 Highcrag Stair, W34 Emberwood Ascent — both up to HIGH crowns), a water-drop (W33 + the re-grounded W37/W38), or a tier-boundary cliff (W35/W36); NONE made the drop DOWN into a sub-LOW basin read as a real climb): added `function mkGloomfenDescent()` + its call in pve.html after `mkMistfallSW();` (L2039 def / L2078 call), mirroring the validated W34 ascent skeleton. Along the **Gloomfen basin's WEST wall** a switchback **fen-stone-and-timber stair** descends the polyline `[[4,-74],[16,-84],[10,-96],[24,-104]]` — from the LOW moonlit plains (top, `heightAt`≈0) down the inner slope to the sub-LOW fen floor (bottom, `heightAt`≈−3, drop 3.0), ending at the **teal pool's WEST edge** (dPoolCentre 11 > poolR 9, 2u outside the water) so the path leads down to the water's edge. ~22 **tread slabs** (damp teal-grey fen-stone, shared `BoxGeometry`) sit ON `heightAt()` so they hug the descending slope; dark-timber **rail posts** on the outer edge every 2nd tread; **landing pads** at each node; a **fen signpost** at the rim top; a glowing **teal lantern-post** at the bottom landing (the mire's water-edge marker); a `🪜 The Gloomfen Descent` lore infoSpot at the rim `(4,-74)` naming the LOW plains → sub-LOW fen drop. **PURELY VISUAL** — every mesh is scene-added, NEVER pushed to `obstacles[]`; the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W34 Ascent / W17 Road). The **WEST wall** was chosen deliberately: the **Grotto Key relic `(40,-100)`** is half-sunk in the EAST of the fen pool and the **Gloomwater river** runs at `z=-129` to the south, so a west-wall descent keeps the whole footprint >15u clear of the key and north of the river channel (`z>−123.5`). In-bounds & non-colliding: max R≈107 <150 sculpted Wilds & inside the ±165 clamp; min **16.5u** clear of every nearby feature (the Grotto Key binding), also clear of Briar Maze, the Gloomwater bridge/ford, Cragspire, Wardstone Grotto, Cragfall pool, etc.; no mesh sits inside the fen pool water. Validated: built the verbatim function on stubbed THREE/globals with the **real `MESAS`/`heightAt`/`TOWN`** + `node --check` (SYNTAX OK on the verbatim fragment) and a **144-assert** geometry test (0 obstacles; exactly 1 infoSpot named "Gloomfen Descent" at the rim `(4,-74)`; 44 meshes built; every mesh R<150 & inside ±165 & >15u from all 12 features & north of the river channel z>−123.5 & outside the r9 fen pool; top `heightAt`≈0 = LOW plains & bottom `heightAt`≤−2.9 = sub-LOW fen floor & descent≥2.8; interpolated treads descend MONOTONICALLY non-increasing; bottom landing at the pool edge (dp 11, just outside r9) — ALL 144 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkGloomfenDescent` defined (L2039) + called exactly once (L2078) with the W30 Weeping Hollow block directly below undisturbed. (bash mount serves a truncated/STALE tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined.) Updated WORLD_MAP.md (§2 Gloomfen biome +Descent & §3b progress note "FIRST descent: two ascents + one descent + two water-drops + two tier-boundary cliffs", §5 W-table +W39) and wiki.html with the player-facing entry.
- 2026-06-24 — **W40 The Gloomfen Scarp** (an INWARD-facing tier-boundary escarpment ringing the Gloomfen sunken basin's EAST rim — the THIRD tier-boundary cliff toward WORLD_DESIGN §3b's 3-tier elevation, after the W35 Highcrag Scarp (HIGH→MID) and the W36 Emberwood Scarp (MID→LOW), and the FIRST on a LOW→sub-LOW boundary; the W39 run + WORLD_MAP §2 queued "a ramp/scarp on another LOW↔sub-LOW or LOW↔MID boundary that still reads as a smooth slope", and the Gloomfen basin's rim — where the W39 Descent only cut the WEST wall — was still a bare smooth slope on its other faces): added `function mkGloomfenScarp()` + its call in pve.html after `mkEmberwoodScarp();` (L3170 def / L3197 call, before the `let aimMode` line), mirroring the validated W35/W36 scarp skeleton. Along the **Gloomfen basin's EAST rim** (7 stations on the arc θ −20°→22° at faceR 35 from the basin centre `(35,-105)`, MESAS[4] `(35,-105,r40,h-3)`) the LOW moonlit plains now visibly **break off and step a full tier DOWN into the sub-LOW fen**, instead of a smooth nameless slope. Because the Gloomfen is a **DEPRESSION** (h−3), the geometry is **INVERTED vs W35/W36**: the high side (the LOW-plains **lip cap** you stand on) is OUTSIDE at **lipR 42** (heightAt≈0 = LOW), and the low side (the **talus boulders** at the cliff foot) is INSIDE at **talR 28** (heightAt≈−2.7 = sub-LOW fen floor), so **lipR>faceR>talR** (vs the bump-edge W35/W36 where lipR<faceR<talR). Each station builds a teal-grey fen-rock **FACE slab** + an offset **crag block** (the cliff face) + an outer **lip cap** + two inner **talus boulders**; every mesh sits on `heightAt()` so it hugs the descending wall (drop≈2.7). Teal-grey fen rock (0x3a4f49/0x2f423c/0x47604f mossy lip) matches the Gloomfen biome (vs W35's grey granite, W36's rust). A `🪨 The Gloomfen Scarp` lore infoSpot on the east approach `(78,-105)` carries the canon (it completes the basin's tier reading the W39 west-wall Descent began). The **EAST rim** was chosen deliberately: the **Grotto Key relic `(40,-100)`** is half-sunk in the pool's east, the **Briar Maze `(66,-74)`** sits NE, and the **Gloomwater river** runs at `z=-129` to the south — so the arc was capped to θ∈[−20°,22°] to thread the whole band >15u clear of the key + the maze and keep every mesh north of the river channel (`z>−123.5`) and outside the fen pool (d from centre >9). **PURELY VISUAL** — every mesh is scene-added, NEVER pushed to `obstacles[]`; the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W36 Scarp). In-bounds & non-colliding: every placed point R<150 sculpted Wilds & inside the ±165 clamp; max R≈140.8 (the southernmost lip). Validated: built the verbatim function on stubbed THREE/globals with the **real `MESAS`/`heightAt`/`TOWN`** + `node --check` (SYNTAX OK on the verbatim fragment) and a **649-assert** geometry test (35 meshes; **0 obstacles**; exactly 1 infoSpot named "Gloomfen Scarp" in-bounds/north-of-river/outside-pool/>15u from all 12 features; every mesh R<150 & ±165 & z>−123.5 & d-from-pool-centre>9 & >30u from town & >15u from all 12 features (Grotto Key, Briar Maze, both Gloomfen Descent nodes, Gloomwater bridge, Cragspire, Wardstone Grotto, Weeping Hollow, Emberwood Scarp info, Riftgate, Mooncradle Mere, Abandoned Hamlet); each lip `heightAt`≥−0.3 = LOW & each talus `heightAt`≤−1.8 = sub-LOW & each lip−talus drop≥2 & lip>talus = descends INWARD (the inverted-depression check) & faceR∈(24.8,40) = in the basin slope band — ALL 649 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkGloomfenScarp` defined (L3170) + called exactly once (L3197) with the `let aimMode` line directly below and `mkEmberwoodScarp();` above undisturbed. (bash mount serves a truncated/STALE tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined, NOT extracted from the mount.) Updated WORLD_MAP.md (§2 Gloomfen biome +Scarp & §3b progress note "THREE tier-boundary cliffs (W35 HIGH→MID, W36 MID→LOW, W40 LOW→sub-LOW)", §5 W-table +W40) and wiki.html with the player-facing entry.
- 2026-06-24 — **W41 The Emberwood Crown Scarp** (§3b 3-tier elevation — the FOURTH tier-boundary cliff, the SECOND HIGH→MID one, and the LAST unaddressed boundary on the existing mesas): the Emberwood CROWN (MESAS[3] (60,105,r27,h7) — the HIGH crag where PERCH[1]/the SE Hunter's Overlook sits) now shears off on its SE face and steps a full tier DOWN onto the MID rust-wooded rise (MESAS[2] (95,55,h3.5)) below it, read as a sheer SHELF EDGE instead of a smooth nameless saddle. Added `mkEmberwoodCrownScarp()` in pve.html after `mkGloomfenScarp();` (L3198): 7 stations along arc θ −75°→−35° from the crown centre — a rust-brown rock face slab + crag block each, a HIGH-side lip cap at lipR 20 (heightAt≈5.4), and MID-side talus boulders at talR 31 (heightAt≈1.9–3.0), every mesh anchored on `heightAt` so it hugs the descending face; 🪨 lore infoSpot at the rise-side approach (83,80). The SE rim keeps the band clear of the W34 Emberwood Ascent (SW shoulder) and the W7 sepulcher (100,104). PURELY VISUAL — 0 obstacles added (nothing pushed to `obstacles[]`), so no clamp/traversal/secret/cache coords shift (a guide, like the W36 Scarp). Validated: `node --check` (SYNTAX OK on the verbatim fragment) + a 318-assert geometry test on the real MESAS/heightAt (35 meshes; 0 obstacles; exactly 1 infoSpot named "Emberwood Crown Scarp"; every mesh R<150 & ±165 & >15u from 6 features, min 20.0u to PERCH[1]; each lip heightAt≥4.5 = HIGH (5.40) & lip>face & lip>talus = descends outward to MID; each talus heightAt∈[1.5,3.6] = MID rise floor — ALL 318 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed the W41 row. Updated WORLD_MAP.md (§2 Emberwood bullet + §3b tally "FOUR tier-boundary cliffs / last mesa boundary done", §5 W-table +W41) and wiki.html with the player-facing entry.
- 2026-06-24 — **W42 The Highcrag Shelf Scarp** (§3b 3-tier elevation — the FIFTH tier-boundary cliff, the SECOND MID→LOW one, and the LAST unaddressed boundary on the five mesas, "the one remaining cramped boundary" WORLD_MAP named): the Highcrag LOWER shelf (MESAS[1] (-60,-100,r30,h3.5)) now shears off on its SE face and steps a full tier DOWN onto the LOW moonlit plains (heightAt≈0), read as a sheer SHELF EDGE instead of a smooth nameless slope. The boundary is genuinely TIGHT — threaded between the W38 Highcrag Mistfall to the N (lip (-44,-92)) and the Gloomwater river channel (z<−123.5) to the S — so the band is a COMPACT 5-station arc θ −40°→−25° at faceR 25 from (-60,-100): a grey-granite face slab + offset crag each, a MID-side lip cap at lipR 17 (heightAt≈3.50), and LOW-side talus boulders at talR 30 (heightAt≈0–0.06), every mesh anchored on heightAt so it hugs the descending slope (drop≈3.5). Grey granite matches the Highcrag biome (like W35/W38). Added `mkHighcragShelfScarp()` in pve.html after `mkEmberwoodCrownScarp();` (L3238); 🪨 lore infoSpot on the LOW east approach (-22,-112). PURELY VISUAL — 0 obstacles added (nothing pushed to obstacles[]), so no clamp/traversal/secret/cache coords shift (a guide, like the W36 Scarp). With W42, EVERY tier boundary on all five mesas now reads as a deliberate stepped cliff. Validated: `node --check` (SYNTAX OK on the verbatim fragment) + a 489-assert geometry test on the real MESAS/heightAt (25 meshes; exactly 1 infoSpot named "Highcrag Shelf Scarp"; every mesh R<150 & ±165 & north of the river channel z>−123.5, southmost mesh z≈−120.7; every mesh >15u from 15 features, the W38 Mistfall lip the binding one at 15.2u; each lip heightAt≥2.5 = MID (3.50) & each talus heightAt≤1 = LOW (≤0.06) & each station drop lip−talus≥2 (≈3.5) = descends outward — ALL 489 PASS). Re-Read the exact anchor before editing; atomic reversible insert. Updated WORLD_MAP.md (§2 Highcrag bullet §3b tally "FIVE tier-boundary cliffs / EVERY mesa boundary now a stepped cliff" + next-step note, §5 W-table +W42) and wiki.html with the player-facing entry.
- 2026-06-24 — **W43 The Emberwood Rise Path** (§3b 3-tier elevation — the FIRST deliberate ascent onto a MID shelf): the W32 Highcrag Stair and W34 Emberwood Ascent both climb to a HIGH crown (h7, the Hunter's Overlooks); the two MID shelves had cliffs but no climbing route. This adds a graded packed-earth-and-timber SWITCHBACK TRAIL up the Emberwood rise's SE face (MESAS[2] (95,55,r42,h3.5)) — from the LOW moonlit-plains toe (134,38) heightAt≈0 up onto the MID rust-wooded shelf top (103,46) heightAt≈3.5, PATH [[134,38],[122,40],[115,48],[103,46]], ~20 treads on heightAt so they hug the slope; timber rail posts on the outer edge, landing pads at each node, a signpost at the foot, an autumn-rust banner at the top. The SE face threads clear of the W36 Scarp (SW), the W37 Mistfall (NE, info (112,66) the binding feature), the Mooncradle Mere lagoon to the S (every node >24u from the centre (122,6) = dry land), the Tide-Charm relic, the W7 sepulcher and the Wayfarer's Wharf. Added `mkEmberwoodRisePath()` in pve.html after `mkEmberwoodAscent();` (~L3103/L3133); 🍂 lore infoSpot at the foot (134,38). PURELY VISUAL — 0 obstacles added (nothing pushed to obstacles[]), so no clamp/traversal/secret/cache coords shift (a guide, like the W34 Ascent). Validated: `node --check` (SYNTAX OK on the verbatim fragment) + a 535-assert geometry test on the real MESAS/heightAt (33 mesh points; ascent monotonic non-decreasing 0.00→2.76→3.50→3.50; foot heightAt<0.3 = LOW & top heightAt≥3.4 = MID & ascent≥3; every mesh R<150 & ±165 & outside town disc & outside the Mere lagoon & >15u from all 12 nearby features) + a runtime test (35 meshes; 0 obstacles; exactly 1 infoSpot named "Emberwood Rise Path" at the foot — ALL PASS). Re-Read the exact anchor before editing; atomic reversible insert. Updated WORLD_MAP.md (§2 Emberwood bullet +W43 / §3b next-step note → Highcrag lower shelf is the last unclimbed shelf, §5 W-table +W43) and wiki.html with the player-facing entry.
- 2026-06-24 — **W44 The Highcrag Shelf Stair** (§3b 3-tier elevation — the ascent for the LAST yet-unclimbed shelf; the explicit "next §3b step" the W43 run + WORLD_MAP §2 queued: "add the LAST yet-unclimbed shelf's ascent — the Highcrag LOWER shelf (MESAS[1] (-60,-100,r30,h3.5)) still has cliffs (W38 Mistfall N, W42 Shelf Scarp SE) but no climbing route UP"): the W32 Highcrag Stair / W34 Emberwood Ascent climb to HIGH crowns and W43 lifts onto the MID Emberwood rise; the Highcrag LOWER shelf had cliffs on every face but no way UP. Analysis confirmed the shelf is the most boxed-in landform in the world (the upper mesa HIGH-shadows its N/W, the W38 Mistfall lip (-44,-92) sits on its NE edge, the W42 Shelf Scarp on its SE, the W35 talus across its W/saddle, the Gloomwater channel z<-123.5 to the S) — and BOTH the W35 and W42 scarp lore already name "the worn stair to the east" on this exact narrow SE ledge. So W44 builds that stair: added `function mkHighcragShelfStair()` + its call in pve.html after `mkHighcragShelfScarp();` (L3332 def / L3362 call), mirroring the validated W43 ascent skeleton. A switchback **grey-granite STONE stair** cut into the W42 scarp's SE ledge climbs PATH `[[-36,-120],[-43,-114],[-48,-110],[-57,-106]]` — from the LOW moonlit plains (foot, heightAt≈0) up the granite face onto the MID shelf top (heightAt≈3.5, ascent 3.50). ~13 tread slabs (grey granite A/B 0x575a66/0x474a55) sit ON `heightAt()` so they hug the slope; dark-timber rail posts on the outer edge every 2nd tread, landing pads at each node, a timber signpost at the foot, a stone cairn at the top; 🪜 The Highcrag Shelf Stair lore infoSpot at the top landing `(-57,-106)` (placed at the top, not the foot, to keep clean separation from the W42 scarp's own info at (-22,-112)). **PURELY VISUAL — 0 obstacles added** (nothing pushed to obstacles[]); the hero already walks the slope via `heightAt`, so no clamp/traversal/secret/cache coords shift (a guide, like the W43 Rise Path). The stair is **deliberately co-located** with the W42 scarp it climbs (min dist 0u — the stair interlaces that cliff: the treads sit on the slope while the scarp's face slabs sit above ground, reading as "a stair cut into the scarp," exactly the lore's intent; an intended terrain integration like the W43 top-landing/PERCH delivery) and the W35 saddle it tops into (≈9.6u). Every STRICT feature stays >15u clear: the W38 Mistfall lip/pool, all gameplay/secret features (W42's OWN info the binding strict one at 15.6u), the Cragspire/Claw, Wardstone Grotto, Grotto Key, Highcrag Stair foot, Overlook[0], Cragfall, Weeping Hollow, Hamlet, Gloomfen basin — and the whole footprint is north of the Gloomwater channel (z>-123.5, 3.5u margin) and inside R<150 (maxR≈125) & ±165. Validated: `node --check` (SYNTAX OK on the verbatim fragment, wrapped + stubbed THREE/globals) + a geometry test on the real `MESAS`/`heightAt` (26 meshes; ascent monotonic non-decreasing 0.00→2.78→3.50→3.50; foot heightAt<0.3 = LOW & top heightAt≥3.0 = MID & ascent≥3; every mesh R<150 & ±165 & outside the town disc & north of the river channel & >15u from all 16 strict features) **+ a runtime test (0 obstacles; exactly 1 infoSpot named "Highcrag Shelf Stair", correctly named — ALL PASS)**. An earlier candidate PATH was rejected because its foot fell 10.3u from the W42 info; the lane was shifted south/west until even W42's own info cleared 15u (15.6u). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkHighcragShelfStair` defined (L3332) + called exactly once (L3362) with `mkHighcragShelfScarp();` (L3319) above and the `let aimMode` line (L3363) directly below undisturbed. (bash mount serves a truncated/STALE tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield inlined.) **With W44, EVERY mesa shelf/crown now has BOTH a deliberate ascent AND a tier-boundary cliff — the §3b elevation backbone is essentially solid.** Updated WORLD_MAP.md (§2 Highcrag bullet +W44 / §3b tally "backbone essentially solid" + next-step note → re-ground a non-water feature or resume the normal rotation, §5 W-table +W44) and wiki.html with the player-facing entry.
- 2026-06-24 — **W45 The Sundergate** (a SEALED mountain PASS through the Riven Spires on the NORTH brink — the §1 incremental-world-map-unlock's SECOND staged beat, ROTATING OFF the §3b elevation grind that W32–W44 all worked, now that W44 declared the §3b backbone "essentially solid" and explicitly permitted resuming the normal rotation): WORLD_DESIGN §1 names the FF-II *"a bridge, a pass, a vessel"* staged unlock; the W31 Wharf already built the SEA-route *"vessel you can't sail yet"*, so this builds the LAND-route *"pass you can't take yet"* on the opposite (north) edge. Added `function mkSundergate()` + its call in pve.html after `mkRiftgate();` (L2337 def / L2372 call, before the W19 Mossfang comment). A great ancient stone gate built into the W27 Riven Spires granite wall at gate centre `(40,171)` (the north foothill band, just PAST the ±165 brink): two snow-capped **gate-towers** (h26) flank an opening spanned by a **lintel + keystone**, the opening **barred shut** by an iron **portcullis** (5 vertical bars + 3 crossbars) + a faint violet **seal-ward** torus (it opens by story, not by force), **scree** boulders heaped at its foot (reads long-sealed), and a worn flagstone **causeway** (7 slabs) + two **waymarker cairns** climbing up to it from INSIDE the brink (echoing the W17 Wayfarer's Road). A `⛰ The Sundergate` lore infoSpot at `(40,151)` (inside the clamp, walk-reachable) carries the §1 staged-unlock canon. Complements W27's *"no road has ever crossed"* — this gate has NEVER been opened, so no road has. The north edge was chosen to pair with the east Wharf, and x=40 to sit EAST of the Sunder Chasm gap (x≤26) and the chasm cluster (vista `(-2,143)`, feather `(-2,116)`) and the NW Riven Spires info `(-110,156)`. **PURELY VISUAL — 0 obstacles added**; the gate masonry sits past the brink (z>165, where the hero already can't walk — the ±165 clamp is unchanged), the causeway/cairns/infoSpot inside the clamp (z<165), all west of the Sundering Sea (x<165), so no clamp/traversal/secret/cache coords shift and the BARRED opening reads as SEALED — NOT a gap in the map edge (the §4 "no abrupt shop-off" mandate is preserved; this is a *gated* edge, the FF-II staging hook). In-bounds: every mesh x<165 & z∈(120,205) & not sunk below ground; gate silhouette reaches y≈28. Validated: built the verbatim function on stubbed THREE/globals with the **real `MESAS`/`heightAt`/`WILD_R`/`TOWN`** + `node --check` (SYNTAX OK on the verbatim fragment) and a **156-assert** geometry/tree-walk test (33 meshes; 0 obstacles; exactly 1 infoSpot named "Sundergate" inside the ±165 clamp & >15u from all 12 nearby features — Sunder Chasm vista + Updraft Feather, Riven Spires info, Emberwood crown/PERCH1, W7 sepulcher, Riftgate, Mooncradle Mere, Whispering Thicket, Smotherwood info, Wayfarer's Wharf, Briar Maze, town; ≥1 mesh past the brink z>165 = the gate exists; ≥1 mesh inside the clamp z<165 = the causeway is reachable; every leaf-mesh world coord finite, x<165, z∈(120,205); maxY 28 = a real gate silhouette; minY 0.16 = nothing sunk below ground — ALL 156 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkSundergate` defined (L2337) + called exactly once (L2372) + exactly 1 Sundergate infoSpot with the W19 Mossfang block directly below undisturbed. (bash mount serves a truncated/STALE tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield/globals inlined.) Updated WORLD_MAP.md (§2 landmark bullet +The Sundergate, §5 W-table +W45) and wiki.html with the player-facing entry.
- 2026-06-24 — **W46 The Sundered Span** (a RUINED BRIDGE over the Sunder Chasm — the §1 incremental-world-map-unlock's THIRD staged beat, COMPLETING the FF-II *"a bridge, a pass, a vessel"* trio: W31 the Wayfarer's Wharf built the VESSEL (sea route, east), W45 the Sundergate built the PASS (land route, north), so this builds the BRIDGE; continues the normal rotation W44/W45 resumed after the §3b backbone went solid): added `function mkSunderedSpan()` + its call in pve.html after `chasmBlock` (L2959 def / L2996 call, before the W16 Whispering Thicket comment), referencing the existing `CHASM` object so it tracks the canyon's real constants. A great elder bridge once leapt the **Sunder Chasm** (W15) but is now **COLLAPSED**: a **south abutment** + flagstone **deck** (with parapet stubs) juts from the near rim at world z 121.5→128 and ends in a **jagged break** (a tilted broken slab + a snapped hanging chain), a clinging **stub** of the far deck + a **north abutment** cling to the vista shelf at z 136→141, the **middle lies fallen** on the canyon floor (3 fallen slabs + 4 rubble rocks + a snapped central-pier stump at y≈−3), and two **support piers** rise from the floor under each surviving span. Bridge centre `(-19,128)` — placed deliberately **WEST** of the central jump cluster (the Updraft Feather `(-2,116)` + the Far Vista `(-2,143)`) so every mesh stays **>15u clear** of both. A `🌉 The Sundered Span` lore infoSpot at the south approach `(-19,118)` carries the §1 staged-unlock canon AND the diegetic reason the chasm cannot be walked — *the road that crossed it is out, so only the Updraft Feather's CHARGED JUMP (W15 chasmBlock) spans the gap now* — a classic *"see it, can't use it yet"* hook (raised again only as the story earns it; no fast-travel wired). This enriches the W15 gate rather than competing with it: the broken bridge EXPLAINS why the charged jump exists. **PURELY VISUAL — 0 obstacles added**; nothing is pushed to `obstacles[]`, the ±165 clamp and `chasmBlock` are untouched, so no clamp/traversal/secret/cache coords shift (you still leap the gap; the broken bridge cannot be walked). The chasm's north region is flat (`heightAt`≈0), so the group anchors on the rim plane and the floor pieces sit at the same −3.2 as `CHASM`'s own floor. Validated: built the verbatim function on stubbed THREE/globals with the **real `MESAS`/`heightAt`/`TOWN`** + the **real `CHASM` constants** (xa−30,xb26,gapZ0 122,gapZ1 134,vcx−2,vcz143,relic −2,116) + `node --check` (SYNTAX OK on the verbatim fragment) and a **278-assert** tree-walk geometry test (29 leaf meshes; **0 obstacles added**; exactly 1 infoSpot named "Sundered Span", in-bounds; every mesh finite & R<150 & inside ±165; every mesh AND the infoSpot **>15u** from the Updraft Feather, Far Vista, Sundergate, Riven Spires info, Whispering Thicket & Emberwood crown; the span has SOUTH (z<127) AND NORTH (z>135) meshes = it actually crosses; **NO deck-level mesh (y>−1) sits in the central gap z∈[129,135]** = it is genuinely BROKEN; fallen debris DOES lie on the floor (y<−2) in the gap; piers descend toward the floor (a mesh at y∈(−2,−1)); rim `heightAt`≈0 = anchored on the flat north plane, not floating — ALL 278 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkSunderedSpan` defined (L2959) + called exactly once (L2996) + exactly 1 Sundered-Span infoSpot, with the W16 Whispering Thicket block directly below undisturbed. (bash mount serves a truncated/STALE tail per discipline → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield/globals + real CHASM constants inlined.) Updated WORLD_MAP.md (§2 landmark bullet +The Sundered Span, §5 W-table +W46) and wiki.html with the player-facing entry.
- 2026-06-26 — **NO-OP / WINDOW CLOSED** — Scheduled run at 2026-06-26T10:38Z. Per task STOP rule, the self-disable deadline (2026-06-25T01:10:00Z) has passed (`date -u` → Fri Jun 26 10:38:03 UTC 2026, ~33h past). No edits made to pve.html / WORLD_MAP.md / wiki.html. Set task tv-worldmap-build enabled=false via the scheduled-tasks update tool and recorded the closed window here. Last feature shipped was W53 The Feastgate (2026-06-24). End.
- 2026-06-24 — **W48 The Midward Gate** (the §2 layer-3 **MIDDLE REALM**'s FIRST physical presence in the overworld — a genuinely FRESH mandate: every prior run built the human Wilds (secrets/edges/road/§3b elevation W11–W44,W47), the §1 staged-unlock trio W31/W45/W46, the W29 Riftgate (= layer-2 *descent* threshold), or the 7 underworld realms W19–W25 (= layer-4); **layer-3, the connective MIDDLE REALM that §2 calls "the wide world where the world-map eventually lives," had ZERO overworld presence**): added `function mkMidwardGate()` + its call in pve.html after `mkSundergate();` (L2437 def / L2470 call, before the W19 Mossfang comment), mirroring the validated visual-landmark pattern of W29 Riftgate / W45 Sundergate. In an **OPEN stretch of the W-central plains** at centre `(-52,-18)` (flat LOW ground, `heightAt`≈0) a calmer threshold stands opposite the Riftgate's violent descent: a **henge ring of 6 pale standing stones** (leaning, moss-capped) rings a great **trilithon arch** (two uprights + a capstone, the dominant silhouette reaching y≈12.6) holding a **DORMANT between-worlds veil** (a faint teal sheet + core, low opacity — *not yet active*), over a low circular **threshold dais** with a cool glow-ring, a moss-capped **marker cairn** at the south approach `(-52,-7)` leading the eye in, and 5 drifting cool **motes**. Where the Riftgate (W29) tears DOWN to the underworld ONE-WAY, this is the road to the **MIDDLE REALM** — the layer between the human dusk above and the dark below — and a *"see it, can't cross yet"* hook tied to §4's **one-way→two-way travel** arc: the veil wakes only for one who has gone down into the dark, been hunted through it, and **found the road back** (the §4 return that turns the one-way descent into free two-way travel). A `✨ The Midward Gate` lore infoSpot at `(-52,-5)` carries the §2 middle-layer + §4 two-way-travel canon; **no portal/fast-travel is wired** (keeping the change atomic, reversible, and decoupled from the waypoint/episode systems, exactly like the W29 Riftgate and the W31/W45/W46 unlock beats). **PURELY VISUAL — 0 obstacles added**; every mesh is group-added, NEVER pushed to `obstacles[]`, so no collision/clamp/traversal/secret/cache/spawn coords shift. The W-central plains spot was chosen as the most OPEN gap in a now-dense map: min **26.3u** clear of the nearest feature (the Highcrag Stair foot), **48.1u** from town, maxR≈61.9 (<150 sculpted Wilds, inside ±165). Validated: built the verbatim function on stubbed THREE/globals (group parent-chain world-position accumulation) with the **real `MESAS`/`heightAt`/`TOWN`/`WILD_R`** + `node --check` (SYNTAX OK on the verbatim fragment) and a **1735-assert** geometry/tree-walk test (26 meshes; **0 obstacles**; exactly 1 infoSpot named "Midward Gate" in-bounds; centre flat LOW plains heightAt≈0.000; every mesh finite & R<150 & inside ±165 & >15u from **60 features + Wayfarer's-Road centreline samples** (min 26.30u) & >30u from town (48.1u); arch silhouette maxY 12.6 ≥12 — ALL 1735 PASS). Re-Read the exact anchor before editing; atomic reversible insert; Grep-confirmed `mkMidwardGate` defined (L2437) + called exactly once (L2470) with the W19 Mossfang block directly below undisturbed; Read-back confirmed the inserted block is byte-identical to the validated fragment. (bash mount serves a truncated/STALE tail per discipline — a sed slice of the mount returned a partial comment from the wrong region → all node checks ran on a sandbox-native /tmp harness with the verbatim fragment + real heightfield/globals inlined, NOT extracted from the mount.) Updated WORLD_MAP.md (§2 landmark bullet +The Midward Gate, §5 W-table +W48) and wiki.html with the player-facing entry.
