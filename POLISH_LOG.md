# EF Moba — Polish / Balance / QoL Review Log

> Driven by the `efmoba-polish` scheduled task (every 20 min). Each run makes ONE small,
> validated improvement toward a polished, coherent, well-balanced game, then logs it here.
> **Window:** runs until **2026-06-17 13:07 UTC** (8 hours). After that, no changes.

## Focus areas (rotate; pick whatever most needs attention each run)
1. **Model size consistency** — `model_calibration.js` `SIZE_FIX`: heroes read ~hero-sized, pets/
   minions consistent with each other, bosses big, nothing a skyscraper or an ant. Nudge outliers
   toward consistency (small steps, e.g. ±0.1–0.3).
2. **Balance** — hero/unit/tower/boss/core HP & dmg, ability numbers, gold/wood costs & income,
   evolve/upgrade curves, pet-army strength, map/objective pacing. Keep PvP team-symmetric; gold
   sinks meaningful not snowbally. No single dominant strategy.
3. **Stuck / pathing** — find places units/heroes wedge (corners, walls, structure clumps, lane
   pinch points, the new build pads) and smooth them.
4. **Depth & coherence** — does each system earn its place and combine sensibly? Close gaps,
   remove dead/confusing mechanics, make sure choices matter.
5. **Quality of life / clarity** — HUD readability, feeds/tooltips, control discoverability,
   minimap, colorblind-safe cues, fewer "what do I do" moments. Mobile parity.

## Log
(appends: `- HH:MM 2026-06-DD — <area> — <what changed & why> — <validation> — <files>`)
- 05:21 2026-06-17 — model size consistency — Snobbit SIZE_FIX 0.2→0.4: it was a lone outlier at exactly half the next-smallest cluster (0.4), reading as an "ant"; nudged +0.2 (within the ±0.1–0.3 guidance) so it joins the smallest pet cluster and reads consistently while staying the little one. — validation: node 7/7 (isolated SIZE_FIX assertions: new value, still tied-smallest, band 0.4–1.8, all positive, neighbours intact) + live model_calibration.js node --check OK — model_calibration.js
- 06:48 2026-06-17 — story / canon coherence — fixed a self-contradicting line in Yume's Episode-6 cutscene (EP_STORY[6].intro ~line 992). It told the player eating human food BOTH "masks your scent, buys you a little quiet" AND "the Veil's creatures smell a human eating like blood in water… you draw the hunt straight to it" — a direct contradiction (stale flavour left over before the N22 scent-mask shipped). Every other surface (the N21 pickup feed "the dark loses your scent", the N22 scent-mask AI, the N23 🍞 MASKED HUD timer, the Fortune omen "quiet your scent", the in-game 📖 guide §ANCHORS, the wiki Anchors bullet) says eating food MASKS your scent and PAUSES the hunt — and that's the canon ("hunted until you eat human food"). Rewrote the line to match the real mechanic: eating eases corruption + masks scent so the hunt loses you for a breath; the truthful cost is that the quiet is brief (~9s), Wardens/bosses aren't fooled (N22: bosses ignore the mask), anything you've already- 2026-06-17 08:48 UTC — QoL/clarity (feedback) — P6 part 2: added a persistent on-screen ⚠ THREAT legend pinned just above the minimap (#tlg, right:8px/bottom:166px, width 150 to match #mm). The threat colour scale previously lived ONLY in the 📖 guide + a transient run-start toast; mid-hunt there was nothing on screen. Swatch colours (green easy · white even · orange ! tough · red ☠ deadly · ??? flee) copied verbatim from the THREAT[] con table; grouping matches the guide. pointer-events:none, display:none default, mirrors #mm's lifecycle exactly (added to the startRun show array [shown as block + an if(!e)return null-guard] and the startCartRide hide array). No new save fields/systems/timers — pure cosmetic HUD. Chose a DIFFERENT area than the last two runs (model 05:21, story 06:48); this is an unchecked P0 playtest-feedback item (P6 half b). — pve.html(#tlg div ~240, show array ~2981, hide array ~658, guide READING-THE-ENEMY line) + wiki.html(reading-the-enemy bullet) + AGENTS_PVE.md §5g + MASTERPLAN_24H.md(P6 ticked) — validation: node 14/14 (/tmp/p6_tlg.js DEFINES+EXECUTES the two edited array lines verbatim = parse+runtime: tlg→block on show / none on hide, mm/res block & hud/top/sndW flex, missing-#tlg null-guard no-throw both paths, every legend swatch ∈ THREAT[], flee=violet/deadly=red) + legend colours grep-confirmed against THREAT[] line 2643; browser F12 the visual gate.
- 2026-06-17 10:55 UTC — fun & dynamic / feel (P0 playtest fix P7b) — gave the Hunter's Overlook aim mode a CLEAR EXIT (top user complaint: "right-click accidentally walks off, no clear exit"). Aim mode was forced on per-frame by setAimMode(onPerch()) with no way to leave but stepping off, and right-click yeeted you off the pad by accident. Added run-scoped `aimExited` so the gate is now `if(!onPerch())aimExited=false; setAimMode(onPerch()&&!aimExited)` (explicit exit suppresses re-entry while you stay on the pad; stepping off re-arms it) + `exitAim()` wired to Esc (keydown ~3105), right-click (pointerdown button===2 ~3085, BEFORE the move logic so it no longer walks you off — now the deliberate leave), and a new on-screen ✕ Exit Overlook (Esc) button (#aimExit static HTML ~235, onclick=exitAim, shown/hidden in setAimMode beside #xh, added to the pointerdown overlay-exclusion so the click doesn't also fire aimShot). setAimMode(false) already clears the weak-spot orbs. No new save fields/systems. Rotated to FEEL (last runs: balance carrier/shadowDmg, story epilogue/pronouns, QoL threat legend). P7 (a)/(c)/(d) still open. — pve.html(aimExited ~1710, setAimMode+exitAim ~1715-1719, #aimExit ~235, exclusion selector ~3070, right-click ~3085, Esc ~3105, per-frame gate ~3369-3370, guide LAY-OF-THE-LAND line ~98, 2 Overlook infoSpot tooltips ~1708-1709, AIM-MODE feed ~1716) + AGENTS_PVE.md §5l + MASTERPLAN_24H.md(P7b ticked) — validation: node 15/15 (/tmp/p7b_exit.js reproduces gate+handlers verbatim: off-pad off, on-pad on, Esc/right-click exit+flag, staying-on-pad does NOT re-enter, stepping-off clears flag, re-enter on re-step, off-pad right-click=normal move, off-pad Esc=cancelAim, exit clears wsM) + verbatim reconstruction node --check (setAimMode/exitAim/gate/Esc/right-click = SYNTAX OK). wiki.html has no Overlook section (grep 0). Browser F12 the visual gate.
- 2026-06-17 13:30 UTC — balancing (FEEL/anti-trivialize) — Blood Moon swarm speed was a FLAT ms:8.5+rand (~8.5–9.5) while the hero runs ms:14+boots*1.5 (~14–18.5+), so the swarm was 40–50% SLOWER than the player — and got MORE trivial as you geared boots — meaning you could just WALK away from the whole horde and pocket the bonus loot, nullifying the "survive the swarm" beat (same bug class as the §5e Coffer Wraith / §5d/§5h location fixes). Made it HERO-RELATIVE: m.ms=Math.max(9,(hero&&hero.ms?hero.ms:14)*0.85)+Math.random(). Now ~0.85× the hero's CURRENT boots-scaled ms at every gear level — real kiting pressure (can't outrun it, must keep moving + AoE) while staying strictly slower than the hero so it's kitable, never a hard-lock; floored at 9; null/undef hero → 14 baseline. Rotated to BALANCE (last runs: story 5q/5r, feel 5s). — pve.html(moonMob spawn ~2820 + 📖 guide BLOOD MOON line ~110) + wiki.html(new Blood Moon systems bullet ~89) + AGENTS_PVE.md §5t — validation: node 26/26 (bloodmoon_ms_0617.js: strictly < heroMs at boot tiers 0–3 = kitable; ratio 0.85 fresh; strictly > old 8.5–9.5 at every hero ms = more pressure; scales with hero where old was flat; old ratio worsens with boots while new is constant; floor ≥9 at low/crippled ms; null/undef hero → 14 baseline no-throw; regression old 9.5<14 confirms the trivial-outrun bug) + verbatim node --check on the edited line. Browser F12 the feel gate. pve.html only.
 unlock) read "Darkness consumes **the hero**, trapping **him** in her web." — but every other narrator/ENDINGS/OVA/EP_STORY line addresses the protagonist in SECOND person ("you" / "[NAME]"), and the masterplan §6 protocol mandates first-person-personal writing ("the player IS Kai, scenes address them directly"). The stray "the hero … him" both broke that voice AND assumed a male hero, contradicting the player-names-their-own-hero design (SAVE.heroName, any name/gender; OVA2's own prior line already uses "[NAME] thrashes against the blood-bonds"). Rewrote to "Darkness consumes **you**, trapping **you** in her web." — "her web" still correctly points at Ayume/Leah. Dialogue text only; the wiki/in-game 📖 guide don't carry OVA lines verbatim (grep-confirmed no matching phrasing in wiki.html) so nothing to sync. Deliberately a STORY run (the last 4 runs were balance/feel: corruption×1, CT, food-scatter, W8 goblin) per the ~50%-story rotate guidance. — validation: node ova2_check_0617.js 5/5 (verbatim OVA1+OVA2+EPILOGUE arrays: no "the hero"/"trapping him" anywhere in the reel text, edited line is second-person you/you + "her web" + "end of reel" terminator preserved, no protagonist third-person) + node --check SYNTAX_OK; edit at the bash-visible head (~814), browser F12 still the gate — pve.html(OVA2 ~814)
- 08:22 2026-06-17 — balancing / canon coherence (achievement CT) — made the 🏆 achievement rewards WHOLE CT, the last fractional CT faucet in the game. The ACH[] table (~1299) paid 0.25/0.5 CT per milestone, directly contradicting the on-chain canon ("CT… whole numbers, minimum 1 CT") that the 06:25 run already enforced for the 1h/2.5h/clear checkpoints — and `ctSet` only rounds to 2 decimals, so those fractions persisted as a fractional balance shown verbatim in the top-bar 💎 readout AND were effectively UNSPENDABLE dust (every sink — gear 2/5/12, potion 2, revive 3 — costs whole CT ≥2). Audited every ctAdd faucet first: teal-diamond drops are whole (`ctShardAmount`=1/2/3), checkpoints whole (`ctSplit`), the Etherheart-shard quest gives 5 — achievements were the sole offender. New whole values honour the min-1 rule with an escalating climb: first-time milestones (boss/leg/uniq/gem/tame/moon/sk10/diff3) = 1 CT, full map clear = 2, and the difficulty ladder diff3/diff5/diff9 = 1/2/3 so the hardest lifetime gates reward most. Lifetime total 4.75 (fractional) → 15 (whole), still < a single diff-9 clear (30) so negligible faucet inflation; one-time, survives death like the rest of CT. No new systems/save fields; `achUI` + the award feed read `a.ct` so they now show whole numbers automatically (no UI edit), and the 📖 guide ACHIEVEMENTS line ("a small one-time 💎 CT bounty") stays accurate (1–3 is still small) — wiki has no achievement-CT text (grep-confirmed). Deliberately a BALANCE run (last run was story/OVA2). — validation: node 33/33 (ach_ct_whole_0617.js: every reward integer & ≥1, no 0.25/0.5 survive, diff ladder strictly 1<2<3, clear>boss, 11 unique ids, total===15<30, ctSet keeps whole inputs whole) + node --check SYNTAX_OK on the verbatim array AND on the trailing inline-comment line; edit at the bash-visible head (~1299), browser F12 still the gate — pve.html(ACH[] ~1299)
- 07:24 2026-06-17 — fun & dynamic / balance (W8 treasure-goblin) — fixed the Coffer Wraith chase, which was broken: the fleeing carrier (spawnCarrier ~2326) spawned at a FLAT ms 9.6 whose own comment said "just above the hero", but the real hero is created at ms 14+boots*1.5 (~2984, i.e. 14–18.5+ before affixes/boons) — so the wraith was ~30–40% SLOWER and could be walked straight down on foot, nullifying the whole "you can't simply walk it down — dash/ranged" design the 📖 guide & wiki ALREADY promise. Now hero-relative: cms=Math.max(15,((hero&&hero.ms)?hero.ms:14)+2) → always a touch (+2) above the hero's current boots-scaled speed, floored at 15 (16 fallback if hero unset). moveTo applies u.ms uniformly to hero & carrier (same climb clamp) so the comparison is valid; a speed-stacked hero can still earn the on-foot catch (affixes/boons apply after spawn). Makes the docs TRUE — no guide/wiki edit needed (they were right; the code was the bug). Deliberately a DIFFERENT area from the last 5 runs (model, corruption×2, story, food-scatter), per the rotate-focus guidance. — validation: node 15/15 (carrier_ms_0617.js: carrier > hero base at boot tiers 0–3; exactly +2 above floor; regression old-9.6 < hero base every tier; min-15 floor; null/undef → 16; speed-build scales) + stubbed spawnCarrier reconstruction node --check OK (ms 18 for a hero at ms 16); browser F12 still the gate — pve.html(spawnCarrier ~2326)
- 2026-06-17 08:48 UTC — QoL/clarity (feedback) — P6 part 2: persistent on-screen ⚠ THREAT legend pinned just above the minimap (#tlg, right:8px/bottom:166px, width 150 to match #mm). The threat colour scale previously lived ONLY in the 📖 guide + a transient run-start toast; mid-hunt nothing was on screen. Swatch colours (green easy · white even · orange ! tough · red ☠ deadly · ??? flee) copied verbatim from the THREAT[] con table (line 2643); grouping matches the guide. pointer-events:none, display:none default, mirrors #mm's lifecycle exactly (added to the startRun show array — shown as block + an if(!e)return null-guard — and the startCartRide hide array). No new save fields/systems/timers; pure cosmetic HUD. Chose a DIFFERENT area than the last two runs (model 05:21, story 07:43); this completes unchecked P0 playtest item P6 (half a, the cast hint, was already shipped). — validation: node 14/14 (/tmp/p6_tlg.js DEFINES+EXECUTES the two edited array lines verbatim = parse+runtime: tlg→block on show / none on hide, mm/res block & hud/top/sndW flex, missing-#tlg null-guard no-throw on both paths, every legend swatch ∈ THREAT[], flee=violet/deadly=red) + legend colours grep-confirmed against THREAT[]; browser F12 the visual gate — pve.html(#tlg ~240, show array ~2981, hide array ~658, guide ~112), wiki.html, AGENTS_PVE.md §5g, MASTERPLAN_24H.md(P6 ticked)
- 2026-06-17 09:30 UTC — balancing / CT economy (unreachable currency bug) — applied the §5d food-scatter fix to the rare on-chain CT shard. `scatterCT()` (~2158) hid its ~12%-per-hunt teal-diamond shard at an ORIGIN-relative ring (R=72..140) and was called (~3007) BEFORE the Ep3+/chapter/zone-select jump that teleports the hero into a far off-map realm (DUNGEONS cx/cz ±520, r60) — so every Underworld start stranded the shard ~400–730u away, unreachable. CT is the game's entire purpose (earn 💎 CT), making this the highest-value instance of the exact bug class already fixed for the N21 food anchors that same morning. Fix mirrors scatterFood 1:1: scatterCT now reads `startZone` and, for a realm start, hides the shard INSIDE that room (≤r-14 of centre, off the walls, reachable from the entry pad); else the wilds ring as before (town-reject kept). The call was MOVED from ~3007 to ~3027 (beside scatterFood, AFTER the startZone-unlock validation + jump) so a sealed-realm pick — startRun resets startZone to -1 — can't strand it in a dungeon the player never enters. `maybeCTfromKill` already spawns at the kill site (in-realm) so it was fine; wilds-only W-caches/Veil-isles are intended exploration content, left as-is. No new systems/save fields; guide/wiki already promise "rare teal-diamond CT drops in the world" — the fix just makes them collectable in the Underworld (no doc change, same as the food fix). Balance run; last run was QoL (threat legend). — validation: node 474/474 (ctfix_0617.js: wilds ring 72..140 unchanged; all 7 realms inside-room/off-walls/reachable-from-pad; regression old≈396–730u-from-pad vs new <120u; oob/undef startZone → wilds fallback) + verbatim scatterCT node --check OK + runtime smoke (realm shard 4.6u from centre, whole-CT amount) — pve.html(scatterCT ~2158, call moved ~3007→3027)
- 2026-06-17 09:55 UTC — balancing (corruption payoff reachable) — retied `shadowDmgMult()` (~1247) so the documented **+60%** shadow-skill-damage cap lands at the *reachable* brink instead of past death. Old curve `1+min(0.6,(corruption-CORR_POWER)/100)` only hit +60% at corruption 100 — but CORR_MAX=100 IS the Consumed-by-the-Veil instant death and the Critical Limit ("brink") is CORR_CRIT=92, so a living hunter capped at +52%, 8pp short of what the 📖 guide + wiki both promise ("scaling up to +60% as you near the brink"). The old kitPow comment ("1.6× at corr 100") confirmed the intended top is unreachable alive. New: `1+min(0.6,(corruption-CORR_POWER)/(CORR_CRIT-CORR_POWER)*0.6)` → 1.0× at the Power Threshold (40), exactly 1.60× at the Critical Limit (92); span 52, so mult(c)=1+(c-40)/52·0.6 [40→1.00, 66→1.30, 92→1.60], strictly ≥ old in the live band (old 1.52@92), monotonic, hard-capped 1.6. Rewards only the riskiest edge-riding play and makes the guide/wiki claim literally TRUE (no doc edit needed; kitPow comment corrected corr100→corr92). Uses the threshold CONSTANTS so it survives any retune. Deliberately BALANCE — a different lever than the last 3 runs (model 05:21, story 06:48, QoL 08:48). — validation: node 9/9 (sdm_test.js: human/threshold=1.0, mid=1.30, brink92=1.60, capped@100, monotonic, bounded[1,1.6], strictly>old 1.52@92) + node --check clean on both edited lines — pve.html(shadowDmgMult ~1247, kitPow comment ~2493), AGENTS_PVE.md §5i
- 2026-06-17 10:05 UTC — story / ending-ladder payoff (Holy Grail epilogue at the live clear) — the diff-9 true ending under-delivered: `playEnding(diff)` (~831) played only `ENDINGS[9]` (which summarises the redemption in a narrator line) but the dedicated `EPILOGUE` reel — "the only true ending", where Yui explicitly chooses the light and Ayume thanks you by name, "Three shadows leave the Veil ✨" — was wired ONLY into the ▶ Memories viewer (`playOVA`, gated `SAVE.bestEnding>=9`). So a player who GROUND ALL THE WAY to a difficulty-9 clear got the §2/N13 headline payoff only if they later opened a title-screen menu; the moment they earned it showed the shorter reel. The code's own comment (~791) already promised "the Holy-Grail epilogue plays once a diff-9 clear is achieved" and N13 ("Holy Grail epilogue (diff-9 clear): Yui redeemed, true happy ending") specifies the clear, not the menu. **Fix:** one-token append — `…concat(tier>=9?EPILOGUE:[])` — so a LIVE diff-9 clear now plays `ENDINGS[9]` → the N24 alignment line → the full EPILOGUE as the final coda, before `showEndScreen(9)`. EPILOGUE stays LAST so "Three shadows leave the Veil ✨" is the closing image; the alignment reflection keeps its N24 slot right after the tier cutscene; tiers 1–8 append `[]` (exact no-op, no epilogue leak). The ▶ Memories viewer is unchanged and still replays it. Chose STORY — the last 3 runs were balance/QoL (corruption 09:55, CT 09:30, threat legend 08:48); last story run was 07:43, so story rotation was due. — validation: node 19/19 (epilogue_diff9_0617.js: diff9 includes tier reel + epilogue, epilogue is the FINAL beat, order tier→alignment→epilogue with & without N16 choices, NO epilogue leak at diff 1/2/6/8, fallback tier still no-epilogue, onDone→showEndScreen(9), diff>9 clamps to 9 & plays) + verbatim playEnding `node --check` OK (typeof function); edit at line 831 confirmed via Read-tool. 📖 guide YOUR ENDING line + wiki.html endings bullet noted the epilogue now plays at the clear. Browser F12 still the gate. pve.html only. — pve.html(playEnding ~831), pve.html(📖 guide ~116), wiki.html(~73), AGENTS_PVE.md §5j
- 2026-06-17 10:20 UTC — story / canon coherence (hero-gender pronouns) — three cutscene lines referred to the player-named, player-gendered hero (canon: "the player names their own hero", any name/gender) with MALE pronouns — same bug class as the §5f OVA2 "trapping him"→"trapping you" fix, but in the campaign's spine: the Ep2 betrayal-foreshadow outro (EP_STORY[2].outro ~897, Yui: "He followed. Of course he did.") and the Ep7 Ayume=Leah betrayal reveal (~1036 Yui "done with him … Let him go … Me, for him"; ~1037 Ayume "the moment he learned to love the dark"). A player who named a non-male hero saw a gender mismatch at the two most pivotal story beats. Fix: swapped the hero-pronouns to the [NAME] token (live-substituted to heroName() at line 505) + singular they, preserving the third-person "they discuss the prey, unnoticed" device and Yui's taunting cadence: 897 → "[NAME] followed. Of course they did. They always follow."; 1036 → "…done with [NAME]. Let them go, Leah … Me, for them."; 1037 → "…the moment [NAME] learned to love the dark…". Kage-referring he/him/his (1022/1024/1104) and the male vendor (1074) are correct and were left untouched. No new systems/save fields; speaker tags (yui/ayume) intact so DLG_VOICE/portraits still resolve. No guide/wiki change — they don't quote these cutscene lines (grep-confirmed 0 hits) and this adds no player capability. Chose STORY, a canon-coherence pass distinct from the last story run's ending-ladder payoff. — validation: node 22/22 (herogender_0617.js: no he/him/his in any edited line; [NAME] token present + fully substitutes for a FEMALE name "Mira" AND default "Kai" with no male-pronoun leak; prey-cadence/bargain/reveal phrasings landed; speaker tags intact) + node --check clean. Browser F12 still the gate. pve.html only. — pve.html(EP_STORY[2].outro ~897, EP_STORY[7] betrayal ~1036/1037), AGENTS_PVE.md §5k
- 2026-06-17 11:06 UTC — story+balance / Ep7 boss-fight coherence (Kage pure-hearted gate) — the central Episode-7 mechanic ("your Corruption gauge IS the fight: stay too human and your blade barely marks Kage no Mamoru; embrace the shadow past the Power Threshold to wound him") was only HALF implemented. The 📖 guide, wiki.html, and Yui's in-fight tutorial line (~1023 "Stay too human and your blade will barely scratch him") all promise a DAMAGE penalty below the Power Threshold, and Kage's own taunt (bossSpecial ~2727 "Too human to harm me") asserts it — but `hit()` applied the hero's FULL damage to Kage regardless of corruption; the only pure-hearted penalty was a 6%-maxHp mend every ~7–10s in `bossSpecial` (~2726). A well-geared / high-difficulty hero out-DPSes a 6%/~8s heal easily, so they could fell Kage while staying fully human — defeating the "you MUST embrace the shadow" gate that is the whole point of the fight and contradicting four player-facing surfaces. **Fix:** one self-contained line in `hit()` (~2575, right after the crit multiply, before the Tide-Shard DR) — `if(u.kage&&src===hero&&typeof corruption!=='undefined'&&corruption<CORR_POWER)amt*=0.15;` — so a pure-hearted blade now lands at 15% (a "scratch"), exactly as written, and full damage (×shadowDmgMult) resumes the instant you cross CORR_POWER (40). Skills route through `hit()` too (pAoe/dash/lineShot all call it), so the gate covers melee AND casts uniformly. The intended path (corruption≥40) is UNTOUCHED; only the stay-human anti-pattern is punished, and 0.15 DPS now reliably loses to the 6%/8s mend at boss-scale HP, so the two halves finally cooperate. No new systems/save fields; no guide/wiki edit needed — the docs already promised this, the fix makes them TRUE (same reasoning as §5e/§5h/§5i). Chose a Ep7 boss-integrity fix distinct from the last runs (feel/aim 10:55, balance shadowDmg/CT, story epilogue/pronouns). — validation: node 13/13 (kage_humangate_0617.js: corr 0/20/39 → 0.15×, corr 40/70/92 → full, wardens/mobs/non-hero-src never gated, undefined-corruption no-throw → full, crit×1.8 then gate = 27, gate ≤20% of normal, gated DPS < boss mend at scale) + node --check SYNTAX OK on the wrapped edited line; edit via file tools (bash mount serves a truncated tail — true loop/`hit()` confirmed via Grep/Read tools per §5), browser F12 still the gate. pve.html only. — pve.html(hit() ~2575), AGENTS_PVE.md §5m
- 2026-06-17 11:30 UTC — progression/pacing (corruption arc onset) — the Underworld passive Corruption erosion (corruptionTick ~1296, +0.25/s outside town/safe-pads) and its counterplay, the 🍞 Anchors of Humanity (scatterFood ~2141), were BOTH gated on `(SAVE.episode||1)>=3` — the highest chapter EVER unlocked (a permanent save flag), not "am I in the Underworld this run." So a high-progress player (SAVE.episode≥3) who picked Ep1 Carnival / Ep2 Masquerade from chapter-select to replay the canon-gentle opening ("start at a carnival, real world, EASY/PASSIVE" — MASTERPLAN §1/PIVOT) was STILL drained + littered with Anchors — directly contradicting scatterFood's own "the Carnival is already safe" comment. Fix: re-gate BOTH on `selEp` (the chapter being PLAYED) via `(typeof selEp!=='undefined'?selEp:(SAVE.episode||1))` — matching the codebase's OWN convention, since Kage/boss identity already keys off selEp (b.kage ~2688, descent feed ~2773); erosion+food were the inconsistent outliers. Safe because selEp<=SAVE.episode ALWAYS (you can only select an unlocked chapter; epUnlocked=min(7,SAVE.episode)), so erosion-ON (selEp≥3) ⟹ food-ON — no stranding. Only behavioural change: a selEp<3 run (Carnival/Masquerade + the default selEp=1 Wilds start) no longer erodes/scatters food — the canon gentle early game; real Ep3+ runs are byte-identical (new==old). No new systems/save fields; no guide/wiki edit — the 📖 guide already says erosion/Anchors apply "deep in the Underworld (Episode 3+)", and the fix makes that literally true (same reasoning as §5e/§5h/§5i/§5m). Chose PROGRESSION/PACING — a fresh area vs the last runs (feel/aim 10:55, story+balance Kage 11:06). — validation: node 29/29 (corr_selep_0617.js: selEp 1/2 gentle even at lifetime ep7, selEp 3..7 erode+food, undefined-selEp → SAVE.episode fallback, selEp<=SAVE.episode invariant + erosion⟹food across all selectable picks, regression old-gate eroded the Ep1 replay vs new does not, new==old for every real Ep3+ run) + node --check SYNTAX OK on both edited lines wrapped verbatim; edits via file tools (bash mount stale per §5), browser F12 still the gate — pve.html(corruptionTick ~1296, scatterFood ~2141), AGENTS_PVE.md §5n
- 2026-06-17 11:40 UTC — QoL/clarity (progression dead-end signposting) — when a player picks a still-SEALED realm, the startRun guard (~3025) dumped them to the Wilds with only a generic "Defeat the Wardens in order to open it." This bites hardest at the campaign climax: the EPISODES zone map increments by 1 each chapter EXCEPT Ep6(zone4)→Ep7(zone6), which SKIPS zone5 (Enamora). Since SAVE.episode (chapter) advances on any map-clear but zonesUnlocked only on Warden kills, a player can reach the Ep7 chapter-select while the Abyssal Vault (idx6) is still sealed — and picking the finale silently bounces them to the Wilds with no idea WHICH Warden/realm to clear. **Fix (message + docs only — ZERO gating/balance/perf change):** the guard now computes the actionable next step from SAVE.zonesUnlocked and names it — "🔒 <picked realm> is still sealed. Realms open ONE at a time — fell <Warden of the highest open realm> in <that realm> (pick it on the 🗺 zone map) to unseal <next realm>. Starting in the Wilds for now." (e.g. Ep7 pick → "fell Seraphel, the Beguiling Rose in Enamora … to unseal The Abyssal Vault"). Keeps startZone=-1 + try/catch + the original generic line as a data-missing fallback. The unlock math (zonesUnlocked/unlockRealm/zoneUnlocked) is BYTE-IDENTICAL — only the feed text changed. Per Definition of Done, also surfaced the incremental-unlock RULE (the 📖 guide CHOOSE-YOUR-ZONE intro ~175 + wiki.html Sin-Realms bullet ~79 listed the realms but never stated that felling a Warden opens the next). Framed as QoL/clarity (last QoL run 08:48; the last run, 11:30, was progression-MECHANICS — this touches no mechanics). — validation: node 19/19 (sealedguard_0617.js: Ep6→Ep7 case [zonesUnlocked=5, pick idx6] names Abyssal Vault/Seraphel/Enamora + the ONE-at-a-time rule + zone-map hint; fresh save names Mossfang Alpha/Mossfang Hollow→Howling Crypt; upper-bound no-undefined; unlocked pick → no message + startZone preserved; Wilds(-1) untouched; missing-Warden → "the next Warden" default; beyond-data → generic fallback, no throw) + node --check SYNTAX OK on the verbatim multi-line guard wrapped in a function; edit via file tools (bash mount serves a truncated tail per §5 — guard confirmed via Read/Grep), browser F12 still the gate — pve.html(startRun guard ~3025, 📖 guide ~175), wiki.html(~79), AGENTS_PVE.md §5o
- 2026-06-17 12:05 UTC — balancing (anti-attrition / pets vs boss slams) — boss & Warden telegraph slams (`updateTelegraphs` ~2709) dealt the FULL `dmg*2.2` to anything in the red ring, hero AND pets alike — but pets can't read a telegraph (their AI holds melee/assist range, they never step out), so every slam (cast every 4.5–7s) caught the whole pack. Math: a pet hatches with ~(170+diff·20+12·(lvl-1))·vetMult HP; a tier-7 final-boss slam = 26·diffMult(7)·0.55·2.2 ≈ 1041 at diff5/lvl15 (99% of a ~1051-HP pet → effectively lethal) and a STRICT one-shot at diff7/9 and at diff5 with any gear (gearScore raises diffMult by up to +54%). So the pet army — a core Palworld pillar (§1) and the system the anti-attrition design (§3: OOC regen, level-up HP grants, Barkindle revive) exists to protect — was deleted wholesale in every boss/Warden fight. **Fix:** one self-contained factor on the existing hit() call — `hit(v,tg.dmg*(v.kind==='pet'?0.5:1))` — so pets take HALF a slam (survive a hit or two) while the HERO still eats the full 2.2× (dodge-or-die intact, boss threat to the player unchanged). Conservative & non-trivializing: at diff9 even a half-slam still lands real damage, hazards/snares/ranged still hit pets in full, and a left-in pet still dies to repeated slams — it just isn't auto-wiped every cast. No new systems/save fields; one factor in the bash-frozen tail (edited via file tools per §5). Per Definition of Done the 📖 guide WARDENS line + wiki.html pet bullet now note pets take half a slam since they can't dodge. Chose BALANCE/anti-attrition — distinct code from the last runs (QoL sealed-realm 11:40, progression corruption-onset 11:30, story+balance Kage-gate 11:06, feel/aim 10:55). — validation: node 15/15 (tg_pet_0617.js: pet factor 0.5 / hero+non-pet 1.0, set-guard still hero|pet only, diff7/9 full slam one-shots petHp [old behaviour], diff5 full = 99% of petHp + one-shot with 9 gear tiers, half<full real reduction, diff9 half still lands damage [boss not trivialized], diff5 pet now survives a single slam, low-end Warden no immortality) + node --check SYNTAX OK on the verbatim edited updateTelegraphs region (stubbed) + grep-confirmed the edit + both doc clauses live. Browser F12 still the gate (feel of pets weathering a boss fight). pve.html only. — pve.html(updateTelegraphs ~2709, 📖 guide WARDENS ~111), wiki.html(~76), AGENTS_PVE.md §5p
- 2026-06-17 12:25 UTC — story / canon coherence (Chekhov's gun fired) — the Fortune Teller's Episode-1 prophecy (FORTUNE_PROPHECY ~538) turns THREE cards but covers the third: "The last one is not yet yours to read. Come back when you have walked far enough to understand it." That payoff never existed — return visits (onTalkFortune else-branch ~566) only ever rotated 5 generic system-hint omens forever, so the promised third card was a dangling narrative thread (an unfired Chekhov's gun) at the very seed of the Ayume=Leah foreshadow. **Fix:** added a one-time `FORTUNE_THIRD` reel (~564, 6 lines) and a middle branch in onTalkFortune — `else if((SAVE.bestEnding||0)>=1 && !SAVE.epStorySeen.fortune3){…}` — so once you have WITNESSED the betrayal at the throne (SAVE.bestEnding is set by playEnding on any final-boss clear), the next visit to the tent finally turns the third card, exactly once, then reverts to the omen rotation. The card reveal (THE ARCHITECT) recontextualises rather than spoils — gated AFTER the player already knows Ayume=Leah — names both identities ("Leah, she signed the book. Ayume, she signed your heart."), pays off the prophecy's "not every hand that leads you means to lead you home," and seeds the diff-9 Holy-Grail path ("a fox… who may yet choose otherwise — and one road narrow enough to carry all three of you back into the light… refuses it at the very brink"). Seen-once via SAVE.epStorySeen.fortune3 (epStorySeen survives wipeSave, line 295, like every story flag) and the gate SAVE.bestEnding (survives wipeSave, line 288), so it's permanent. Speakers fortune/narrator/hero all resolve (fortune portrait ~462). No new systems/save fields beyond the one boolean flag. Per Definition of Done the 📖 guide THE FORTUNE TELLER line (~114) now notes the hidden third card is turned once you've witnessed the betrayal (wiki.html has no Fortune-Teller section — grep 0 — so no wiki edit). Chose STORY/canon — a fresh thread distinct from the last runs (balance/pets 12:05, QoL sealed-realm 11:40, progression corruption-onset 11:30, story+balance Kage-gate 11:06). — validation: node 18/18 (fortune3_v2_0617.js, verbatim const+routing: first visit→prophecy+sets .fortune, pre-betrayal return→omen never the card, post-betrayal→THE THIRD CARD once then sets fortune3 then reverts to omens [index advances exactly once], bestEnding 9 also triggers, fortune3-already-seen→omen no replay, undefined bestEnding→omen no throw, content names Leah+Ayume+THE ARCHITECT+brink/all-three foreshadow, all speakers valid) + node --check SYNTAX OK on the verbatim live region (557–588) wrapped. Edits via file tools (bash mount served a truncated tail mid-run per §5 — re-validated with a fresh single-write file). Browser F12 still the gate (the tent visit + dialogue feel). pve.html only. — pve.html(FORTUNE_THIRD ~564, onTalkFortune ~580, 📖 guide ~114), AGENTS_PVE.md §5q
- 2026-06-17 12:44 UTC — story / lore coherence (betrayal foreshadow, opening bookend) — the cart-ride cold-open (`startCartRide` ~697), the FIRST scene a brand-new player sees, had ZERO foreshadow of the Ayume=Leah twist — it opened on warm banter (narrator → Ayume "you'll fall off the cart" → hero "pass me an apple" → the aim instruction) and went straight to gameplay. Meanwhile the betrayal seed lived only in the Fortune tent (Ep1 prophecy + the 12:25 post-betrayal third card) and in Ep4/5 ("she reached down and took them"). **Fix:** inserted ONE Ayume line between the hero's apple line and the gameplay-instruction line — "(a soft laugh) Always so fearless — it's why I chose you, [NAME]. You'll follow this road wherever it leads, and never once think to ask who laid it." Reads as innocent teasing about the hero's recklessness on first play; on replay it lands as the architect's confession in miniature — "I chose you" (the fated pure-heart offering, betrayal: "You were never the rescuer… you were the offering"), "follow this road wherever it leads" (THE BELOVED card: "you would follow anywhere… not every hand that leads you means to lead you home"), and "who laid it" (betrayal: "Every turn I drew, you took"). Complements rather than collides with the 12:25 story run: that fired the Fortune third card AFTER the betrayal (endgame/returning-player surface); this seeds the same thread at the cold-open (new-player surface) so the foreshadow now BOOKENDS the arc (seed at the carnival road → reveal at the throne). Purely additive dialogue text routed through the existing playDialogue/[NAME]-substitution (~506) + the ayume DLG_VOICE/CHARS profile — NO new systems, save fields, controls, or balance; adds nothing the player can DO, so per DoD no 📖 guide/wiki change needed. Story is ~33% of the last 6 runs (under the §6 50% target). pve.html only. — pve.html(startCartRide playDialogue ~701), AGENTS_PVE.md §5r — validation: node PARSE OK on the verbatim extracted startCartRide fn (stubbed globals via new Function) + 4 assertions (foreshadow line present, ordered BEFORE the LEFT-CLICK instruction so the instruction stays last, dialogue line count 4→5, [NAME] token intact for runtime substitution). Edit via file tools (bash mount serves a stale/truncated tail per §5 — confirmed this run: an early `tail` of THIS log showed only through 10:55 while the file tool read through 12:25). Browser F12 the dialogue feel.
- 2026-06-17 13:02 UTC — fun & dynamic / feel (P0 playtest fix P7c + P7d audit) — gave the Hunter's Overlook aim mode VISIBLE crosshair + on-reticle hit/miss feedback (P7c: "crosshair unclear, left-click did nothing"). The `#xh` reticle was a small 28px ⊕ and a landed shot gave no confirmation (weak = a text feed line, body = nothing, miss = a faint ground ring) so shots didn't read as responsive. Enlarged `#xh` 28→42px with a layered amber glow; added a centered `#aimHM` marker div + run-scoped `aimHMt`/`aimHMk` + `aimMark(kind)` (2=weak crit red ✕ / 1=body amber ✕ / 0=miss soft grey ·) + `tickAimMark(dt)` (fades opacity 1→0 and pops scale 1.0→1.55 over 0.16–0.32s, called in the loop beside the `aimCd` decay ~3405); cleared in `setAimMode(false)`. Wired into `aimShot` (~1764): confirmed `best`→`aimMark(weak?2:1)`, miss→`aimMark(0)` + the ring — the bolt homes to `best` so a lock = a guaranteed connect, so flashing on fire is correct. P7d audited (no change): pointerdown ~3098 routes `button===0 && aimMode`→`aimShot();return;`; the ~3095 overlay-exclusion (incl `#aimExit`) only blocks clicks ON panels — that `#aimExit` entry is precisely what stops the ✕ button from also firing a shot; nothing swallows the canvas click. No new save fields/systems. Chose FEEL/aim — last feel run was 10:55 (P7b); the runs since (balance/QoL/story) left feel due. P7(a) free-360° rotation is the only remaining P7 sub-item. — pve.html(#xh ~234, #aimHM ~235, aimHMt/aimMark/tickAimMark ~1739-1746, aimShot ~1764-1766, loop tick ~3405, 📖 guide LAY-OF-THE-LAND ~98) + MASTERPLAN_24H.md(P7c/P7d ticked) + AGENTS_PVE.md §5s — validation: node 19/19 (p7c_hitmarker_0617.js: weak/body/miss → right ✕/· glyph+colour+timer; decay fades opacity 1→0 + scale pops >1 then hides at expiry; clamp-at-0 no negative; fresh scale ≈1.0; null-#aimHM guard no-throw; idle tick no-op) + verbatim node --check SYNTAX OK on setAimMode/aimMark/tickAimMark/exitAim/aimShot-tail; wiki.html has no Overlook section (grep 0). Browser F12 the visual gate (reticle size/glow, the ✕ pop feel).
- 2026-06-23 05:30 UTC — balancing (ascending ladder / anti-trivialize) — the four Sin-Realm environmental hazards (hzDmg ~2849, fired on a timer by updateHazards) were capped by a FLAT `Math.min(2.2,diffMult(0))`, which the difficulty term (1.38^(diff-1)) hits at ~diff 3.4 and then FREEZES — while hero HP keeps climbing with vetMult (1+0.35·(diff-1), ×3.8 by diff9). Result: the "the land itself fights you" hazard (guide §THE REALM ITSELF FIGHTS YOU + wiki "bleeds you between fights") decayed from ~6.3% of base hero HP early to ~3.6% by diff9 — trivialized exactly as the tower-of-challenge (WORLD_DESIGN §3) should climb HARDER, same bug class as the Blood-Moon (13:30) and Coffer-Wraith (07:24) anti-trivialize fixes. **Fix:** swapped the flat cap for `vetMult()` — `const cap=(typeof vetMult==='function')?vetMult():2.2; …Math.min(cap,diffMult(0))` — so the hazard's difficulty scaling tracks hero HP growth and stays a near-constant share of HP. Math (Power-realm tier5 ×1.8 vs base hero HP=600·vet): NEW holds 6.2–6.4% at every diff 1→9; OLD collapsed 6.3%→3.6%. Crossover ~diff5: ≤diff4 a hair EASIER (38/52/65/77 vs old 38/52/72/83 — removes the abrupt freeze), ≥diff5 scales up to stay a real threat (diff9: 144 vs frozen 83). Can NEVER gib: real hero HP=(600+armor·150)·vet ≥ 600·vet so a geared hero takes an even smaller fraction (verified: armor6 < bare). Realm multipliers (0.7–1.8), dodgeable telegraphs, and per-realm feel untouched — only magnitude tracks the hero. No new save fields/systems; guide+wiki are purely qualitative (no numbers) so the change just makes their "bleeds you between fights" promise TRUE at the top of the ladder — no doc edit needed. Deliberately a BALANCE/ladder run (recent log: heavy story + feel/aim, last balance levers were Blood-Moon speed & pet-slam halving). — validation: node 14/14 (/tmp/hz.js: diff1 cap=1 parity w/ old; new %HP near-constant diff1↔diff9 |Δ|<3pp; old %HP collapsed <60% of its diff1 share = the bug; no hazard ever >12% of base HP at any diff/realm = never gibs; new≥old for diff5–9 = relevant at top; new≤old for diff1–4 = no early spike; gear-stacked hero takes smaller %HP) + node --check on the verbatim edited line 2849 (SYNTAX OK, vetMult/diffMult stubbed). pve.html only (hzDmg ~2849).
- 2026-06-23 06:58 UTC — progression/pacing (CRITICAL — restored the missing run-completion keystone) — `runComplete()` was CALLED on every final-boss death (~3142) and documented ("Her death runs the ending ladder (runComplete → playEnding)" ~1289) but was DEFINED NOWHERE in pve.html or its loaded scripts (shared/ef_core.js etc.) — grep -c=2 = call + comment only, no definition; folder-wide grep found runComplete only in docs/pve/RELEASE. So felling the final boss (Ayume, or the Hollow King in non-Ep7 runs) threw a ReferenceError and the ENTIRE endgame/progression payoff never fired: (a) `playEnding()` is fully written but had ZERO callers, so no ending ladder, no `bestEnding`, no `ovaUnlocked`, no Holy-Grail epilogue ever played in real play (prior story runs only ever validated endings via node array tests + the title's ▶ Memories viewer, never a live clear); (b) `SAVE.diff` and `SAVE.episode` are incremented NOWHERE else in the build (grep: no `SAVE.diff=`/`++`/`+=`, no `SAVE.episode=` except wipeSave restore) — so the WORLD_DESIGN §3 tower-of-challenge difficulty ladder was permanently stuck at diff 1 and chapters never unlocked through play (epUnlocked frozen at 1); (c) the 🗺 'clear' achievement (Tyrant's End, +2 CT) had no trigger anywhere (`achAward('clear')` grep = 0 callers) so it was unreachable. This single missing function bricked the whole back half of the game and made every prior balance/anti-trivialize run moot (diff never rose). **Fix (purely ADDITIVE — one new top-level function inserted right after `playEnding`, touches ZERO existing lines, fully reversible by deletion):** `runComplete()` now (1) captures `cleared=clamp(SAVE.diff,1,9)` — the difficulty you BEAT it at, which the ending ladder reads; (2) `SAVE.diff=min(9,cleared+1)` — the rung climbs (cap 9); (3) `SAVE.episode=min(EPISODES.length||7,(SAVE.episode||1)+1)` — next chapter unlocks (cap 7), per the "clearing a run advances SAVE.episode" comment ~916; (4) `achAward('clear')` — its only intended trigger, +2 whole CT (on-chain canon); (5) `achDiff()` — lazy-award the diff-3/5/9 milestones now reached; (6) storeSave; (7) `playEnding(cleared)` — runs the ladder at the cleared tier → 📜 Memories (OVA) at diff-6, Holy Grail (Yui redeemed) at diff-9. Order is correct: diff incremented BEFORE playEnding so showEndScreen's "Ending reached at difficulty {cleared}. Difficulty rises to {SAVE.diff}/9" reads right (cleared vs cleared+1). Serves BOTH final-boss paths (Ayume true-final after the betrayal, and the Hollow King when selEp≠7). No new save fields/systems; all callees (achAward/achDiff/storeSave/playEnding/EPISODES) resolve at call-time (function body, not load-time) so insertion ABOVE the `const EPISODES` line is safe. No guide/wiki edit needed — both already PROMISE this exact behavior ("Clearing the map… raises your difficulty" guide ~89; "an ending decided by the difficulty you beat it at… Holy Grail at difficulty 9" ~116; OVA at diff-6 ~118/435; "clearing the campaign advances you one chapter" ~113) — the fix makes the long-standing docs TRUE rather than aspirational (same reasoning as §5e/5h/5i/5m/5n). Chose PROGRESSION/PACING — the highest-value item on the board, distinct from the recent balance/feel/story runs. — validation: node 38/38 (/tmp/rc_test.js, the function extracted VERBATIM from the live file via awk then loaded with stubbed globals: diff1→2 & playEnding(1) & ep1→2 & clear-ach-once & achDiff & storeSave; diff5→6 [OVA-6 reachable next run]; diff8→9 [Holy-Grail reachable]; diff9 cleared stays 9 [cap] & playEnding(9) fires the Holy-Grail ending; episode caps at 7; undefined diff/episode → treated as 1 → 2 no-throw; EPISODES undefined → fallback cap 7, episode still advances, no throw; for d=1..9 ending tier===cleared AND newDiff===min(9,cleared+1)) + `node --check` on the awk-extracted live function = SYNTAX OK. Edit via Read/Edit file tools per §5 (bash mount serves a stale/truncated tail). Browser F12 still the gate (an actual final-boss kill now plays the ending + shows the end screen instead of throwing). pve.html only (runComplete inserted ~911, just after playEnding).
- 2026-06-23 07:30 UTC — progression/coherence (de-dup runComplete) — the 06:58 run added a SECOND `function runComplete()` (~911) on a false "DEFINED NOWHERE" premise: it had grepped the STALE/TRUNCATED bash mount (whose tail is cut off — re-confirmed this run: the new ~911 comment IS in the mount but the real def at line 3481 is NOT served) and missed the complete ORIGINAL definition that has always existed at ~3481. So the build had TWO top-level runComplete declarations; by JS function-hoisting the LATER one (3481) wins, making the 911 block DEAD code — and it was strictly inferior (it omitted the final difficulty-scaled CT payout `ctAdd(fin)` + sfxLevel + the mobile-control hide `efTouchMatch(false)` that 3481 does). The game was therefore NEVER broken (the call site ~3151 always resolved to the correct 3481 body); the only real defect was the duplicate + its now-false "ReferenceError" comment. **Fix (one atomic, reversible deletion):** removed the redundant 911-919 block, leaving a short NOTE comment pointing at the single canonical definition (~3477 post-shift). Net -4 lines; the function that actually runs is byte-identical, so ZERO behavioural/balance/perf change and no guide/wiki edit needed (no player-facing surface changed). Single source of truth for the run-completion keystone restored. — validation: file-tool grep `function runComplete` = exactly 1 (line 3477, the complete original intact); node --check on the edited head region (playEnding + new comment + EPISODES start) = HEAD_REGION_SYNTAX_OK (comment block closes with */ at 915, playEnding still balanced at 910); node run of the verbatim 3477 runComplete with stubbed globals = RUNCOMPLETE_SYNTAX_OK + correct effect (diff 5→6, episode 3→4, awards fin CT). Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated — never edited through it). pve.html only (~911 dead dup removed).
- 2026-06-23 10:30 UTC — QoL/clarity (hunted-loop discoverability) — the minimap (drawMM ~3897) plotted portals, resource nodes, rare item drops, CT shards, eggs and units — but NOT the 🍞 Anchors of Humanity (human food). Food is the core survival counterplay of the Underworld "you are the hunted" loop (WORLD_DESIGN §0: "the only respite is the scent-mask/human-food mechanic"), yet a low-Humanity player had ZERO map cue showing WHERE relief was — a "what do I do" dead-end exactly when the gauge is climbing toward Consumed-by-the-Veil. Added a drops-loop block (mirroring the existing CT block) that draws each `d.type==='food'` drop as a warm-amber (#ffcf8a) PLUS glyph (2 moveTo + 2 lineTo stroke, half-span fs=3). Chosen to ALWAYS ping (no fogSeenAt gate, like the CT shard's "always pings — it's a rare find") because food is the survival lifeline and only scatters in the Underworld (scatterFood gates selEp>=3), so there's no human-world clutter; the plus glyph reads as "restore here" and is shape-distinct from every existing marker (squares=portals/nodes, diamonds=items/CT, circles=units/eggs, triangles=perches/compass). No new save fields/systems/timers — pure additive HUD draw, fully reversible (delete the one block). Rotated to QoL/CLARITY (last 3 runs: balance hazard-cap, progression runComplete restore + de-dup). — pve.html(drawMM food block ~3907, 📖 guide ANCHORS line ~115) + wiki.html(Anchors of Humanity bullet ~71) — validation: node 10/10 (/tmp/food_mm.js reconstructs the block verbatim over a stubbed ctx: amber stroke colour, plus = exactly 2 moveTo+2 lineTo, stroke not fill, arm endpoints span ±fs about centre, ignores ct/item/gold/gem drops, draws with NO fog gate = always pings, off-screen culled by on(), two food = two strokes) + node --check on the wrapped+stubbed region = SYNTAX OK. Browser F12 the visual gate (a backgrounded tab pauses rAF).
- 2026-06-23 14:55 UTC — progression/robustness (anti-crash) — the vista/waypoint discovery reward (line 2801, inside the `if(!isCursed())` block of the first-find handler) called `vetMult()` UNGUARDED, but `vetMult` is DEFINED NOWHERE in the dev build's loaded scripts (pve.html, shared/ef_core.js, shared/ef_touch.js, model_calibration.js — grep -o across all four = 0 definitions; it exists only in the obfuscated RELEASE/pve.html). Every OTHER reference in pve.html guards it — `(typeof vetMult==='function'?vetMult():1)` at the companion-spawn ~1271, diffMult at the Ayume-boss ~1321, and the 05:30 hazard-cap fix at ~2849 — so they fall back safely. Only 2801 (added by a prior anti-trivialize XP-scaling run that node-`--check`'d syntax but never proved the symbol resolves) called it bare, so discovering ANY waypoint threw a ReferenceError AFTER fogReveal/sfx but BEFORE skXP + the '✦ Vista bonus' feed — the exploration reward the §WAYPOINTS guide / WORLD_DESIGN §3 promises silently never fired (no XP, no feed, a console error each find). **Fix (one atomic, reversible edit):** wrapped the call in the same guard the rest of the file uses — `(10+tier*6)*(typeof vetMult==='function'?vetMult():1)`. When vetMult is undefined (dev build) it now falls back to ×1 — exactly the comment's documented `diff1 vet=1.0 → no-op` baseline — and the reward fires instead of crashing; when vetMult IS defined (release build) the guard is byte-identical to the bare call, so the intended LINEAR vetMult scaling (1+0.35·(diff-1)) is fully preserved. Pure robustness, zero balance change. Chose PROGRESSION/robustness — distinct from the recent QoL (food minimap 10:30) / progression-keystone (runComplete 06:58/07:30) / balance (hazard 05:30) runs, and it un-bricks a live reward. No guide/wiki edit (no player-facing number or text changed — the promised reward simply works now). — pve.html only (vista reward ~2801). — validation: node 8/8 (/tmp/vt.js: undefined vetMult → no throw, returns 52 baseline at tier7; old bare form proven to throw ReferenceError = the bug; defined vetMult → guarded==direct at diff 1/3/6/9; diff1→52 no-op; diff9→198 scales ×3.8) + `node --check` on the wrapped+stubbed edited line = SYNTAX OK. Edits via Read/Edit file tools only (bash mount tail stale/truncated — never edited through it).
- 2026-06-23 19:30 UTC — balance (ascending difficulty ladder) — made the VETERAN bonus `vetMult` (pve.html ~3770) mildly PROGRESSIVE: `1+0.35·(d-1)` → `1+0.35·(d-1)+0.015·(d-1)²` (d=SAVE.diff). WHY: enemy HP/dmg climbs a CONSTANT ×1.38/rung via diffMult, but a purely linear vet delivers a SHRINKING % step each rung (d1→2 +35%, … d8→9 +10%), so effective difficulty (enemy÷player-base) ACCELERATED toward the top — rung ratios 1.10→1.25 — building exactly the "wall" WORLD_DESIGN §3/§4 forbid at the OVA(diff6)/Holy-Grail(diff9) gates. The +0.015·(d-1)² term is 0 at diff1 (byte-identical, start stays gentle) and only bites at the top (vet diff9 3.80→4.76, +25%), flattening the rung ratios to 1.07→1.21 and easing the diff9 effective wall −20% — WITHOUT inverting the ladder (still strictly ascending, diff9 still the single hardest rung, low end ~unchanged). Thematically apt: a hero who CLEARED their way to diff9 is proportionally more battle-hardened. Feeds hero/pet/skill base power uniformly (the one player-facing surface, the ⚔ Veteran-bonus feed ~4197, computes its % from vetMult() so it stays truthful — no guide/wiki number to update; wiki "35%" hits are overtime/pet-heal, unrelated). One atomic reversible single-line edit. Rotated to BALANCE (last runs: progression vetMult-guard 14:55, QoL food-minimap 10:30, progression runComplete 06:58/07:30). — validation: node 46/46 (/tmp/vet_test.js: diff1≡old≡1.0, exact-form old+0.015(d-1)² ∀d, buffed ∀d>1, monotonic, all positive, effective ladder still strictly ascends & diff9 still hardest & diff9 eased −20% & diff2 ~unchanged) + reconstructed-line node --check = SYNTAX OK + node run diff-table {1:1,2:1.365,…,9:4.76} monotonic/positive. Edited pve.html via Edit file tool ONLY (bash mount tail stale/truncated for pve.html — grep -c there = 0; never edited through it). pve.html only (~3770).
- 2026-06-23 20:55 UTC — fun & feel (impact beat for the keystone transition) — SHADOW POWER AWAKENS, the single most important Corruption transition (addCorruption ~1360: it flips `corrShadow`→ unlocks `shadowDmgMult` AND is the gate that lets your blade actually wound Kage, N9 ~3791 "stay below the Power Threshold and you barely scratch him"), erupted with ONLY a feed line + a voice bark — while every LESSER beat already bursts an fxRing (level-up gold ~3820, crit gold ~3794, boss-death triple-ring ~3808, elite purple ring ~3809, pet-death ring ~3813). The most consequential corruption moment in the run was the quietest on-screen. **Fix (one atomic, additive, reversible line inside the existing `if(!corrShadow&&corruption>=CORR_POWER)` block):** a TWO-ring purple shadow-burst at the hero — a tight `0xc64bff` core (r4) + a wider `0x7a2bbf` wave (r8), the EXACT two colours of the Humanity bar's shadow gradient (~4493 `linear-gradient(90deg,#7a2bbf,#c64bff)`) so the world-burst visually rhymes with the gauge turning purple at the same instant. Fires ONCE per run: the enclosing gate sets `corrShadow=true` on its own line, so lingering/rising above the threshold can't re-trigger it; a descent below then re-cross (N3 pull-back ~4173 eases corr to 60, and addCorruption ~1364 clears corrShadow under CORR_POWER) correctly RE-ARMS it — identical lifecycle to the feed/bark it sits beside. Reuses the existing auto-cleaned fxRing helper (pushes to fxs with t:.5 — no new system/timer/save field); `typeof fxRing==='function' && hero && hero.grp` guarded so a missing helper/hero can't throw. Rotated to FUN/FEEL — the one bucket untouched since 2026-06-17 (last 6 runs: balance×2, progression×3, QoL×1). No guide/wiki edit: purely cosmetic, the awakening already self-announces in the feed + 📖 guide, adds nothing the player must learn (same call the boss/elite/pet-death rings made). — pve.html only (addCorruption shadow-burst ~1363). — validation: node 16/16 (/tmp/corr_burst.js, verbatim gate over stubs: no burst below CORR_POWER; exactly 2 rings on the upward cross w/ correct colours #c64bff-r4 + #7a2bbf-r8 centred on hero; NO re-trigger while staying above; descent re-arms corrShadow; re-cross re-fires; missing hero.grp does NOT throw and still flips the gate) + `node --check` on the edited function extracted from the LIVE file (awk 1356-1367, grep-confirmed the new line is present in the extract) wrapped+stubbed = SYNTAX OK. Edits via Read/Edit file tools ONLY (append-only `>>` to this log is safe — no stale-tail read/match). Browser F12 the visual gate (a backgrounded tab pauses rAF).
- 23:59 2026-06-23 UTC — QoL/clarity (navigation discoverability) — the 📖 in-game guide's EXPLORING (FOG OF WAR) entry (pve.html ~100) told players their minimap is "(top-right)", but the #mm canvas renders BOTTOM-right (decl ~246 inline 'right:8px;bottom:8px', shown on startRun ~4326) — directing a lost/confused player to the WRONG corner for the orientation aids stacked there: the walked-fog minimap, the gold OBJECTIVE-COMPASS needle (guide ~101 "Follow the needle when you're lost"), and the threat legend — the exact "what do I do / where do I go" moment WORLD_DESIGN §1 fog-reveal navigation is meant to answer. Fixed the one word: (top-right)->(bottom-right). Pure static-HTML guide text, atomic + fully reversible (one token), zero JS/balance/save touched; verified the #mm inline style is the only minimap CSS (no media-query repositions it) and the new text is the lone minimap-corner claim. NOTED FOLLOW-UP (left for a future QoL run to keep this change atomic): the 🔊 mute toggle is the SAME bug — guide ~103 & ~114 call #sndW/#muteBtn "(top-right)" but it too sits bottom-right ('right:8px;bottom:166px' ~242). Rotated to QoL/CLARITY (last 4 runs: fun/feel shadow-burst 20:55, balance vetMult 19:30, progression vetMult-guard 14:55, QoL food-minimap 10:30 — QoL was the oldest rung). — pve.html only (guide EXPLORING line ~100). — validation: grep confirms exactly one minimap-corner phrase remains and it now reads 'bottom-right'; the change is non-script HTML text (guide <div>, not a <script>) so it cannot introduce a syntax error — tags balanced, single-token swap; wiki.html unaffected (its minimap bullet ~81 states no corner). Edits via Read/Edit file tools ONLY; log appended with >> (no stale-tail read/match).
- 2026-06-24 05:58 UTC — balance + enemy variety (ascending ladder / tower-of-challenge) — the trash-mob RANGED share was a FLAT `Math.random()<0.28` at EVERY zone tier (mkMon ~4260), so the wilds and the Abyssal Vault fielded an IDENTICAL threat COMPOSITION — contradicting WORLD_DESIGN §3 (each deeper zone must be structurally HARDER & distinct, "not just a color swap"). It matters because melee trash (ms 5.5–7) is trivially out-run by the hero (ms 14+), so among trash ONLY ranged foes (range 13, un-bodyblockable, they kite you) actually test the player — a flat ranged% meant the real skill-threat never grew with depth. **Fix (one atomic, reversible line):** `Math.random()<Math.min(0.45,0.28+(tier||0)*0.025)` — the ranged share now CLIMBS +2.5pp per realm tier. Wilds (tier0) = 0.28 BYTE-IDENTICAL (safe early game §1 untouched, `(tier||0)` guards undefined/null → 0.28); t1 .305, t3 .355, t5 .405, CAPPED at 0.45 for t7+ so the MAJORITY stays melee (the pack still has bodies to body-block & AoE — never an all-archer wall §1/§3). Deliberately a SKILL-test shift, NOT raw stat inflation: ranged spawns are already SQUISHIER (60 vs 110 hp) and hit a touch less (9 vs 13 base), so deeper realms demand more dodging/LOS play without an HP/dmg wall. Chose BALANCE/variety — the QoL minimap+mute corner items (last 2 runs' lane) are already resolved (grep: zero "top-right" remain), and the systems lanes (CT curve, diffMult, petKeep, vetMult, OVA/Grail gates) are all heavily tuned, so the open gap was enemy-MIX texture. Per DoD the 📖 guide READING-THE-ENEMY line now notes deeper realms field more ranged attackers; wiki.html is the MOBA/PvP wiki (no PvE enemy-mix section — grep 0) so no wiki edit. — validation: node 29/29 (/tmp/rt.js: tier0/undefined/null≡0.28; strictly ascending t0→t6; exact t1=.305/t3=.355/t5=.405; cap holds t7/t8/t99=0.45; bounded [0,0.45] so majority melee; regression deeper≠wilds [was equal under flat 0.28]) + `node --check` on the edited mkMon head wrapped+stubbed = SYNTAX OK + runtime mkMon(tier7) returns a valid unit. Edits via Read/Edit file tools ONLY (bash mount tail stale for pve.html; log appended with >>). pve.html only (mkMon ~4260, 📖 guide READING-THE-ENEMY ~113).
- 2026-06-24 08:02 UTC — QoL/clarity + progression signposting (incremental world-opening) — the run-completion keystone `runComplete` (~4596, restored/de-duped in the two prior runs) advances `SAVE.episode` by 1 (cap 7) on every clear, unlocking the NEXT campaign chapter — but `showEndScreen` (~904) only ever announced "Difficulty rises to X/9" and NEVER told the player a new chapter just opened. So the single most important beat of WORLD_DESIGN §1 ("the world opens INCREMENTALLY… world-map access is EARNED") fired silently: you cleared a chapter, a deeper one became playable, and the only surface that said so was the title's 🔒/Chapters list you'd have to go re-open. **Fix (ADDITIVE, reversible, no new SAVE field):** (1) a transient module global `var _epJustUnlocked=0;` declared just above showEndScreen; (2) runComplete now captures `_epB` BEFORE the episode bump and sets `_epJustUnlocked=(SAVE.episode>_epB)?SAVE.episode:0` — true ONLY on a genuine new unlock, 0 when already at the cap-7 finale; (3) showEndScreen, after the difficulty line, reads the flag once and appends "🔓 A new chapter opens — Episode N · <name> is now playable (pick it on the title). The Veil widens as you descend." then clears the flag to 0 so a re-render shows nothing stale. Chapter name resolved via `episodeOf(_epJustUnlocked)` guarded by `typeof episodeOf==='function'` AND `_e.id===_epJustUnlocked` (so episodeOf's EPISODES[0] fallback can't mislabel — beyond-range/undefined cases degrade to a bare "Episode N" with no throw). playEnding has exactly ONE caller (runComplete, grep-confirmed) so the flag is always fresh when showEndScreen reads it; no stale-call path. Pure player-facing signposting — ZERO mechanics/balance/perf change (episode advance math byte-identical). Chose QoL/clarity (last several runs were balance/progression-mechanics — hazard cap, runComplete restore+dedup, mkMon ranged); this is the missing clarity LAYER over that now-working ladder. — validation: node 16/16 (/tmp/ep_test.js, verbatim runComplete-episode + showEndScreen fragments wrapped+stubbed: Ep1→2 names Masquerade & flag set then cleared; no stale message on re-render; Ep4→5 Enamora; already-cap-7 → no flag/no message; first-time 6→7 names Abyssal Vault; undefined episode→2 no throw; beyond-range id → number only, no mislabel; episodeOf undefined → number only, no throw) + node --check SYNTAX OK on the wrapped edited regions. Edits via Read/Edit file tools ONLY (bash mount tail stale for pve.html). — pve.html(_epJustUnlocked ~903, showEndScreen ~911, runComplete ~4598, 📖 guide CHAPTERS ~114), wiki.html(~68)
- 2026-06-24 11:25 UTC — progression/pacing + coherence (ladder-cap keystone message) — the run-over screen's first line (showEndScreen ~906) was a FLAT `'…Difficulty rises to '+SAVE.diff+'/9.'`, but `SAVE.diff` is bumped by `runComplete` (=min(9,cleared+1)) BEFORE showEndScreen runs, capped at 9 — so at the **difficulty-9 Holy-Grail clear** (the game's keystone true-ending moment, WORLD_DESIGN §4) it falsely announced **"Difficulty rises to 9/9"** even though the ladder is maxed and cannot rise. Same class as the job's other "shown text contradicts the real state" fixes, here on the single most important beat. **Fix (one atomic, reversible, pure-text edit):** `'…'+(SAVE.diff>tier?'Difficulty rises to <b>'+SAVE.diff+'/9</b>.':'You stand at the <b>summit — difficulty 9/9</b>, the deepest the Veil goes; it climbs no higher.')`. The `SAVE.diff>tier` test robustly distinguishes a real rise (cleared<9 → SAVE.diff=cleared+1>tier → "rises to X/9", BYTE-IDENTICAL wording for diff 1–8 incl. the correct "rises to 9/9" when clearing diff-8) from the cap (cleared=9 → SAVE.diff=9=tier → summit acknowledgment) WITHOUT hardcoding 9, so it survives any future cap retune. ZERO mechanics/balance/save change — the diff-bump math is untouched; only the announce text at the cap changed. showEndScreen has exactly ONE caller (playEnding ← runComplete, grep-confirmed) so SAVE.diff is always the post-bump value when read. No guide/wiki edit needed: wiki.html ~68 already documents "difficulty rises +1 (max 9)" — this fix makes the in-game message consistent with that documented cap instead of contradicting it (no doc quotes the line verbatim). Chose PROGRESSION/PACING — the most overdue dedicated lane (recent runs: balance 09:55, feel/pet 09:02, QoL chapter-signpost 08:02, balance 05:58). — validation: node 37/37 (/tmp/endmsg.js, verbatim line-906 expr + the runComplete diff-bump: tiers 1–8 each bump cleared→cleared+1 & show "rises to (cleared+1)/9" & NO summit; tier-9 stays capped at 9, shows SUMMIT, does NOT claim a rise, still shows 9/9; regression doc'd the old "rises to 9/9" bug) + node --check SYNTAX OK on a verbatim reconstruction of showEndScreen (904-912) wrapped+stubbed + runtime exercise [grail(9)→summit+Memories+Holy-Grail lines; rise(5)→"rises to 6/9"+OVA-gate signpost]. Edits via Read/Edit file tools ONLY (bash mount tail stale for pve.html; log appended with >>). — pve.html only (showEndScreen ~906)
- 2026-06-24 13:30 UTC — fun & feel (pickup audio identity — the silent heal) — the health-globe pickup (the drop-collect branch ~4117) healed the hero + pets and burst a red fxRing but played NO sound, while EVERY other pickup already has a distinct audio signature: gold/mat → sfxCoin two-blip square (~1499), CT → sfxCoin+ring, gem → sfxEquip rising-sine arpeggio (~1502), level/achievement → sfxLevel (~1500). So the one CLUTCH-RESTORE pickup — the grab you most want confirmation of mid-fight — was the only silent one (same audio-identity gap the cart-bonk fix closed, ~829, whose comment treats "every beat has its own sound" as the standard). **Fix (one atomic, additive, reversible line in the globe branch):** a warm RISING perfect-fifth heal chime — `tone(523,.14,'sine',.09,180)` then a 70ms-later `tone(784,.18,'sine',.07,140)` = C5→G5, both gently gliding UP (tone's slide arg adds to freq: 523→703, 784→924, both >30 so the exponential ramp is valid). The rising sine contour reads as *restoration/relief* and is timbrally distinct from the sharp square coins and the equip arp, so a player can tell a heal grab from a gold grab with eyes on the fight. SAFE/BOUNDED: `typeof tone==='function'` + try/catch (self-no-ops when AC is unset or S_MUTE — tone() already checks both ~1466, so it respects the 🔊 mute), and `_gate('glb',120)`-throttled so scooping several globes in one frame pops a SINGLE chime (no spam); the `typeof _gate!=='function'||…` guard degrades safely if _gate is ever absent. NOT a balance change — zero heal-amount/HP/save/timer/control touched (the heal math above is byte-identical); purely sensory. Chose FUN/FEEL — the most overdue lane (recent runs: progression 11:25, QoL/progression 08:02, balance 05:58, balance/variety; last dedicated feel beat was the shadow-burst 2026-06-23 20:55). — validation: node --check PASS on the full globe branch (verbatim, wrapped+stubbed: tone/_gate/fxRing/drawBar/uniqOn/hero/units) AND a runtime exercise (branch runs clean, hero.hp 10→35 = +25% maxHp heal intact, chime call reached). Edits via Read/Edit file tools ONLY (bash mount serves a stale/truncated pve.html — only 3096 of ~5300 lines — so all probing & editing went through Grep/Read/Edit, never the mount). — pve.html only (drop-pickup globe branch ~4117)
RE ranged kiters (range 13, share climbs with tier ~4399) that sit ~13u from the HERO yet often >16u from a trailing pet — so the pack increasingly ignored exactly the enemy type that defines deep-realm threat, gutting the §0/§3 anti-attrition pet pillar right where the tower-of-challenge is steepest. Gated to pets within 24u of the hero so the >30u catch-up sprint (existing branch) still re-forms a far-strayed pack FIRST (no chase-across-the-map); the hero auto-targets only foes inside its own reach (~5043), so the focus is always a real, nearby fight, never a random distant mob. Validated: node 13/13 logic checks (engage-near / pile-on-hero-target / 24u+30u gating / flee / sprint-speed-preserved / no-target-idle / dead-target-ignored) and the new htgt+`||htgt` expressions parsed clean by node; tier0/no-hero-target paths byte-identical. Reversible: delete the htgt line + the `||htgt`. — pve.html(pet AI ~5076-5079, PETS guide ~98), wiki.html(~77)
- 2026-06-24 08:40 UTC — fun & feel (cart-ride intro audio symmetry) — picked FEEL: it was the least-covered lane in the recent log (last ~10 runs ≈ balance×4, progression×3, QoL×2-3, feel×1). The cart-ride cold-open (`updateCartRide` ~783) is the FIRST gameplay a brand-new player touches, and its audio language was ONE-SIDED — every sound meant the player SUCCEEDING: the lob (rising square chirp via `sfxCast('Leaf')` ~781), the mid-air & landed BONK when YOU catch a fleeing rascal (`noiseHit(.07,.11)`+descending-square `tone` at ~748 and ~828, added in two prior feel runs). But the one beat that goes AGAINST the player — a roadside rascal's fruit catching YOU on the seat (the `<2` seat-hit branch ~815, feed "🍅 Splat! Ayume bursts out laughing.") — was SILENT (feed line only). So getting splatted, the intro's only comedic "miss" beat, made no sound. **Fix (ADDITIVE, one beat, fully reversible):** appended two typeof-guarded SFX calls inside the EXISTING `try{…}catch(e){}` on ~815 — `noiseHit(.15,.085)` + `tone(240,.13,'sine',.07,-110)` — deliberately a DIFFERENT texture from the bonks so the ear can tell "you got hit" from "you hit them": a LONGER, softer low-pass noise burst (.15s vs the bonk's .07 — squishier; lower vol .085 so it reads gentle/comedic, NOT an alarm) under a LOW descending SINE plop (240→130 Hz, round & wet — vs the bonks' hard SQUARE "boink"). A soft tomato now sounds like a soft tomato. Fires ONCE per incoming fruit that reaches the seat (that branch removes the fruit + `splice`+`continue` on the same tick → no re-trigger; foe-fruit is sparse and most miss → no spam), naturally rate-limited by throw cadence; both calls `typeof`-guarded inside the existing try/catch and self-no-op when `AC` is unset / `S_MUTE` (the 🔊 mute already gates `tone`/`noiseHit` at their defs ~1464/1470) — no new system/save field/timer/control, mirroring the sibling bonk lines exactly. NO guide/wiki change: this is sub-beat audio polish on an already-documented mechanic (throwing fruit on the cart ride), exactly as the two prior cart-SFX feel runs (748/828) also left the guide untouched; 🔊 already documents that the toggle mutes all audio. — validation: `node --check` REGION_SYNTAX_OK on the edited line wrapped in its real `for`+`if(fr.inc)` enclosure with stubbed scene/seat/feed/noiseHit/tone (the only change is +2 `if(...)stmt;` statements inside the balanced try — no brace/paren delta); confirmed final via Read tool on the real file (the bash mount truncates pve.html's tail at ~3130 of 4500+ lines, so a full-file node --check there fails on truncation, NOT on this edit — line 815 sits in the intact head and parses clean). Edits via Read/Edit file tools ONLY. — pve.html(cart splat-SFX ~815; siblings ~748/~828)
ue — the pet-follow AI already catch-up-sprints (2.6× far-behind ~4725, 1.0×→2.6× idle-follow ramp ~4736), so no fix needed there. — pve.html only (guide CHAPTERS line ~114). — validation: grep confirms exactly one 🔊-toggle corner phrase remains and it now reads 'bottom-right', and `top-right` grep across pve.html = 0 hits; the change is non-script HTML guide text so it cannot introduce a syntax error (tags balanced, single-token swap). Edits via Read/Edit file tools ONLY; log appended with a targeted Edit (no stale-tail bash read/match).
- 01:25 2026-06-24 UTC — balance/progression (ascending ZONE-TIER ladder — WORLD_DESIGN §3 tower-of-challenge) — made the zone-tier difficulty term in `diffMult` (pve.html ~4005) CONVEX: `(1+(zoneTier||0)*0.45)` → `(1+(zoneTier||0)*0.45+(zoneTier||0)*(zoneTier||0)*0.02)` i.e. 1+0.45t+0.02t². WHY: enemy power scaled LINEARLY in realm tier (t0 Wilds 1.00 → t7 Abyssal Vault 4.15), so the per-realm difficulty STEP-RATIO SHRANK monotonically (realm1→2 +31% … decaying to realm6→7 +12%) — meaning the Hollow King's DEEPEST realms, which §3 says must be the steepest part of the climb, were the FLATTEST part of the tower (a cakewalk at the bottom of the descent), the exact "deeper must be harder" inversion §3 forbids and a cousin of the linear-vetMult shrink the 19:30 run fixed on the DIFF axis (this is the orthogonal ZONE-TIER axis — a lever no recent balance run touched). The +0.02t² term is EXACTLY 0 at the Wilds (t=0 → byte-identical; the safe human-world early game §1 and EVERY `diffMult(0)`/`diffMult()` caller — hazard cap ~3749, companion ~3709 — are untouched) and negligible at the first realm (t1 1.45→1.47, +1.4% → no new early wall §1), but grows with depth to keep the climb steep: t2→t7 raised +4.2%→+23.6%, so the deepest realm and the two scripted finale bosses [diffMult(7): Vault Warden + Ayume] gain +24% HP/dmg — concentrating the added challenge exactly where canon wants it (the bottom of the descent, the Hollow King's seat). Still strictly ascending & monotonic (cannot invert the ladder — t7 remains the single hardest realm), smooth across tiers (a gradual convex add, no cliff → not a §1 "wall"), and every realm step is now ≥+16% (min ratio 1.161 vs old 1.122 — the late-tower decay is lifted, every step a real climb). One atomic, reversible single-line edit; affects ONLY dungeon-tier enemy scaling + the two final bosses (hero/pet/economy/Wilds untouched). No guide/wiki edit — difficulty multipliers are never surfaced numerically (the zone-entry feed shows only the qualitative "danger tier d/7"; guide/wiki are qualitative), so the change just makes the §3 "climb higher → tougher zones" promise truer at depth (same reasoning as the 05:30 hazard-cap & prior anti-trivialize runs). Rotated the LEVER (recent balance runs were diff-axis vetMult 19:30, pet-dmg 00:40, hazard-cap 05:30 — none touched the zone-tier term). — validation: node 31/31 (/sessions/.../zonetier_test.js, the edited function loaded VERBATIM + the isolated tier term: t0≡old≡1.0 & diffMult(0)==1 & undefined-tier→1 = all diffMult(0) callers byte-identical; t1 bump <1.5%; new>old ∀t≥1 & strictly monotonic; t7 ~+23.6%; every realm step >+15% & smallest ratio raised vs old; t7 still the max; diff9 composes as 1.38^8·tier7) + `node --check` on the verbatim edited line = SYNTAX OK + printed tier table {t0 1.00→1.00, t1 1.45→1.47, t4 2.80→3.12, t7 4.15→5.13}. Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — never edited through it). pve.html only (diffMult ~4005).
- 01:58 2026-06-24 UTC — fun & feel (cart-grenade audio identity) — the cart-ride lob minigame's SUCCESS beat (updateCartRide ~827, `if(caught)`) played ONLY `sfxAttack()` on a bonk — the same faint 170 Hz noise+blip as every melee swing — so a caught rascal and a stray sword swing sounded identical, and the comedic apple-lob had no audio identity of its own (its visual juice — green land-ring, '🎯 Bonk!' feed, end-of-ride score — was all there, but the EAR got nothing distinct). Worse, the Overlook weak-spot ping (~3156) already documents itself as "deliberately opposite the cart-bonk's DESCENDING impact pop" — a pop that NEVER EXISTED in code (the bonk only called sfxAttack), so the comment was aspirational, not true. **Fix (one atomic, additive, reversible call inside the existing guarded `if(caught){try{…}catch(e){}}`):** added `if(typeof tone==='function')tone(430,.13,'square',.12,-250)` AFTER sfxAttack — a distinct cartoony DESCENDING "boink" (430→Math.max(30,430-250)=180 Hz square, .13s) layered on the sfxAttack thwack, exactly mirroring how the weak-spot LOCK layers its RISING precision ping on top of the generic sfxCast (~3156). The descending pitch reads as a comedic squash/bonk and is the deliberate OPPOSITE of the precision rising ping — making that ~3156 reference TRUE rather than aspirational, the same "make the docs/comments real" pattern as the §5e/§5h/§5m anti-trivialize runs. Fires ONCE per grenade (caught is a single bool set per fruit OUTSIDE the per-foe blast loop, so a multi-rascal blast still pops a single bonk — no per-foe spam) and is naturally rate-limited by the throw cadence; tone() is typeof-guarded, inside the existing try/catch, and self-no-ops when AC is unset / S_MUTE (no new system/save field/timer/control). No guide/wiki edit — purely cosmetic audio that adds nothing the player must learn (same reasoning as the crit-ring ~4002 / shadow-burst 20:55 / boss-death-ring cosmetic feel runs); the cart minigame carries no numeric docs. Rotated to FUN/FEEL — the bucket was last touched at 20:55 06-23 (shadow-burst, a VISUAL beat) and the last several runs were balance/QoL/progression; this is the AUDIO axis, a different lever. — pve.html only (updateCartRide bonk ~827). — validation: node logic 11/11 (bonk_test.js, verbatim statement over stubs: caught=true → feed+sfxAttack+exactly ONE tone; freq 430 / slide -250 / ramp target 180 Hz = DESCENDING & ≥30 audible floor & < start = opposite the rising weak-spot ping; square .13s; caught=false → ZERO bonk sound; multi-rascal blast → single bonk no spam) + `node --check` on the wrapped verbatim edited statement = SYNTAX OK. Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — never edited through it).
- 02:30 2026-06-24 UTC — progression/pacing + economy (ascending-ladder REWARD curve — WORLD_DESIGN §3/§4 "earned and rewarding, never a wall") — the per-difficulty CT payout `CT_BY_DIFF` (~303) was the ORTHOGONAL reward axis to the recently-tuned effort axes (vetMult 19:30, zone-tier 01:25, hazard-cap 05:30): its step-ratios SHRANK toward the top — 15→19→24→30 = +27%/+26%/+25% — while enemy power climbs a CONSTANT ×1.38/rung (diffMult ∝ 1.38^(d-1)), so CT-per-difficulty EFFICIENCY DECLINED exactly across the post-OVA endgame (the diff-6 OVA → diff-9 Holy-Grail climb), the hardest, rarest content — the same "reward decays where it should feel most earned" class the 19:30 vetMult & 01:25 zone-tier runs fixed on the EFFORT side, here on the PAYOUT side. **Fix (one atomic reversible array-literal edit):** convex-bumped ONLY the three post-OVA rungs — diff7/8/9 `19/24/30 → 20/26/34`; diff1-6 BYTE-IDENTICAL (common new-player/early-mid faucet + every diff≤6 ctSplit untouched). MATH: new top step-ratios 20/15·26/20·34/26 = 1.333/1.300/1.308 rejoin the mid-game band (mid ratios 1.33-1.375) — the diminishing tail is gone — yet ALL stay strictly < the 1.38 effort ratio, so reward never outpaces difficulty (no economy runaway / no inverted incentive to farm the top); the Holy Grail (diff9) becomes the single richest clear (34, +13%), fitting "hardest lifetime gate rewards most." Whole-CT on-chain canon preserved: ctSplit(20/26/34)=[4,6,10]/[5,8,13]/[7,10,17] — each share ≥1 & summing EXACTLY to the listed total — and the largest single CT add (the diff9 clear share, 17) stays well under the AC ctJump=60 anti-cheat limit (Hollow Blight unaffected). Realistic faucet impact tiny (only reachable after 6+ full campaign clears). No player-facing guide/wiki edit — CT-per-difficulty is never surfaced numerically to players (grep: the array literal lives only in pve.html + the two DEV docs, which I synced: AGENTS_PVE.md ~45, CHECKLIST.md ~236); the change just makes the §3/§4 "rich at hell / earned and rewarding" promise truer at the top of the tower (same make-the-docs-TRUE pattern as prior anti-trivialize runs). Rotated to PROGRESSION/PACING — distinct lever from the recent runs (fun/feel cart-audio 01:58, balance zone-tier 01:25, QoL mute-corner 00:58, balance pet-dmg 00:40); the only genuine reward-cadence change since the runComplete keystone (06:58/07:30). — pve.html(CT_BY_DIFF ~303) + AGENTS_PVE.md(~45) + CHECKLIST.md(~236) — validation: node 36/36 (/tmp/ct_test.js: length 9; diff1-6 values & their ctSplit byte-identical to old; top rungs ==20/26/34; strictly monotonic ∀ rungs; diff7/8/9 ratios ∈[1.29,1.38) & each > the OLD same-rung ratio [the shrinking tail]; ctSplit(20/26/34) all-integer ≥1 & sum-exact; max single add 17 < 60) + `node --check` on the VERBATIM edited line incl. its block comment (§/×/→/≥ unicode) = SYNTAX OK. Edits via Read/Edit file tools ONLY; POLISH_LOG appended via a targeted Edit (the bash mount's tail of THIS log is stale — re-confirmed: `tail` shows only through the 23:59 entry while the file tool reads through 01:58 — so never appended via `>>`).
- 02:58 2026-06-24 UTC — balance/progression (ascending-ladder REWARD curve, GOLD axis — WORLD_DESIGN §3 tower-of-challenge) — boss/Warden GOLD (`lootDrop` ~3581) was `60+SAVE.diff*25`, FLAT across realm tiers: a tier-7 Vault Warden paid EXACTLY like a tier-1 Mossfang Alpha at the same difficulty, even though enemy HP/dmg climbs ~×5.1 over tiers 0→7 via diffMult's convex zone term (1+0.45t+0.02t²). The deepest Wardens — the steepest part of the climb (§3) — paid the LEAST per unit of effort: the exact "harder but not richer" gap already closed for loot RARITY one line below (3584, +0.015/tier) and for CT (02:30, CT_BY_DIFF), but left open on the GOLD faucet. **Fix (one atomic, reversible edit — added a guarded `_bt` realm-tier read mirroring 3584's exact `zn.t==='dungeon'&&zn.d` guard, then multiplied ONLY the boss branch by `(1+_bt*0.12)`):** boss gold now +12%/tier, max +84% at tier 7 (diff9: 285→524; diff1: 85→156). Conservative & non-inverting: the reward ratio (tier7÷tier0 = 1.84) stays FAR below the ~5.13 difficulty ratio, so reward never outpaces difficulty (no farm-the-top incentive) — yet the deepest realm finally pays richest, strictly ascending & monotonic in tier. tier 0 (Wilds / any non-dungeon boss, e.g. Ayume's betrayal arena where the guard's `t==='dungeon'` is false) → ×1, BYTE-IDENTICAL to the old value; the elite/trash branch is untouched (still strictly < a boss drop). Max single pickup (524) is ~0.3% of the AC `goldPer10s`=200000 limit, so the Hollow Blight anti-cheat is unaffected. Gold is run-scoped (resets each hunt; spent on the forge ×1.5-escalating / Barkindle revive / builds) so the extra deep-realm gold buys a couple more forge tiers where the fight is hardest — self-limiting, no cross-run snowball. No guide/wiki edit — boss gold is never surfaced numerically; the Portal-Plaza "deeper dungeons are harder & richer" infoSpot (~1725) + the HIDDEN CACHES "bonus gold" guide line just become truer (same make-the-docs-TRUE, no-doc-edit pattern as the 3584 rarity fix). Rotated to BALANCE/PROGRESSION — the GOLD reward axis, a lever no recent run touched (recent: progression CT-reward 02:30, feel cart-audio 01:58, balance zone-tier 01:25, QoL mute-corner 00:58, balance pet-dmg 00:40). — pve.html only (lootDrop ~3581). — validation: node 174/174 (/tmp/bossgold.js: tier0 non-dungeon AND tier0 dungeon == old `60+diff*25` ∀diff [byte-identical]; non-dungeon boss with stray tier still ×1 [guard]; strictly monotonic t0→t7 ∀diff; tier7 == round(old*1.84) ∀diff; reward ratio 1.84 < difficulty ratio 5.13 [no inversion]; all integer; max 524 < AC 200000) + `node --check` on the verbatim edited lines wrapped+stubbed = SYNTAX_OK + runtime (diff5/tier3 = 252 = round((60+125)*1.36) ✓). Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — wc -l there = 3194 vs the real ~4760; never edited through it); POLISH_LOG appended via a targeted Edit (mount tail stale).
- 04:05 2026-06-24 UTC — QoL/clarity (HUD bar legibility — fewer "what is this" moments) — the player's vital HUD bars were labelled INCONSISTENTLY: the Corruption bar (#corrW ~257) carries a `title` tooltip ("Humanity ⇄ Corruption — shadow power has a price") but its three siblings — the HP bar (#hpf ~254), the Mana bar (#mpf ~255) and the XP bar (#xpb ~256) — had NONE. Colour + the fill-text ("120 / 120", "40 / 40") distinguish HP from Mana, but the XP bar is the worst offender: a thin 5px gold SLIVER (CSS ~29) with no fill-text and no label at all — a brand-new player has zero cue that it's progress-to-next-level (level is shown only as a separate "Lv N" in #res ~232). **Fix (one atomic, reversible, ADDITIVE edit — three `title` attributes, no JS/balance/save touched):** HP → "❤ Health — you fall when this empties; a 🧪 revive potion auto-saves you if you carry one." (matches the #top 🧪 stat's auto-revive copy ~225); Mana → "🔷 Mana — fuels your Q / W / E / R skills; refills over time." (matches the Q/W/E/R ability tooltips ~4526 which already list "· N mana"); XP → "⭐ Experience — fills toward your next level; leveling raises your HP & damage and toughens your pets." (matches the giveXP level-up: baseHp +70·vet & baseDmg +6·vet ~3927 and the 00:40 pet-sharpen run). Now ALL four HUD bars are self-describing on hover, matching the file's heavy title-tooltip convention (T/B buttons ~263-264, every #top stat ~225, the #skl skills span ~232, the Q/W/E/R abilities ~4526). Pure static HTML inside the #hud <div> (NOT a <script>), so it cannot introduce a JS syntax error; the .bar absolute-inset fill/text spans (CSS ~30) are untouched and title still triggers on hover over the whole bar. No 📖 guide/wiki edit — adds nothing the player must LEARN, just labels what's already on screen (same no-doc-needed reasoning as prior cosmetic/clarity HUD runs). Rotated to QoL/CLARITY — the oldest-touched rung (recent: balance gold 02:58, progression CT-reward 02:30, fun/feel cart-audio 01:58, balance zone-tier 01:25; QoL was last at 00:58 mute-corner). — pve.html only (HUD bars ~254-256). — validation: file-tool grep confirms each of the three new titles is present EXACTLY ONCE and sits inside its bar's opening <div> tag before `>` (tags balanced, fills/spans intact); the edit is non-script HTML attributes so no `node --check` applies (cannot break JS) — sibling #corrW ~257 proves the same attribute pattern already renders fine. Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — never edited through it); POLISH_LOG appended via a targeted Edit (mount tail stale).
- 04:30 2026-06-24 UTC — fun & feel (AUDIO — the realm-unlock climax) — a WARDEN/boss death (hit() ~4097) fired a golden triple-ring + '👑 slain!' feed but was AUDIBLY SILENT — no sound at the single most important non-final beat (felling a Warden is what UNSEALS the next realm, WORLD_DESIGN §3 tower-of-challenge), the exact "biggest beat is the quietest" gap the shadow-burst (20:55 06-23) & cart-bonk (01:58) feel runs closed on OTHER beats. Every neighbour already sings — crit-ring, level-up (sfxLevel), equip (sfxEquip), the player's ultimate (sfxSuper) — only the Warden kill didn't. **Fix (one atomic, additive, reversible try-block appended INSIDE the existing `if(u.boss){…}`):** a distinct triumphant boss-fall fanfare reusing the existing `tone()`/`noiseHit()` helpers — a deep DESCENDING impact boom `tone(165,.5,'sawtooth',.13,-95)` (165→max(30,70)=70Hz, the Warden's bulk crashing) + a `noiseHit(.32,.1)` crunch, then a RISING major cap `[330,440,660]` triangle staggered 120/230/340ms = a victory RESOLUTION. Deliberately distinct from every sibling so it can't be confused: sfxDeath (220→ saw, DEFEAT, no rising cap), sfxSuper (110 saw = the player's ULTIMATE), sfxLevel ([440,554,659,880], no boom). Fires ONLY this `if(u.boss)` branch — the FINAL boss returns at `if(u.finalBoss)` (~4095/4096) BEFORE this line, so Kage/Ayume/Hollow-King keep their ending fanfare untouched; once per Warden kill (rare, naturally rate-limited — no swarm/perf cost). Both helpers self-no-op when `!AC || S_MUTE`, and the call is `typeof`-guarded + wrapped in try/catch, so it's mute-safe AND throw-safe with no audio context. No new system/save field/timer; fully reversible (delete the appended try block). No 📖 guide/wiki edit — purely cosmetic audio that adds nothing the player must LEARN (same no-doc reasoning as the crit-ring/shadow-burst/cart-bonk cosmetic feel runs); the boss kill is already self-announced by the feed. Rotated to FUN/FEEL — the oldest-touched bucket (last feel was 01:58 cart-audio; the runs since were balance/progression/QoL: HUD-titles 04:05, gold 02:58, CT-reward 02:30). — pve.html only (hit() boss-death ~4097). — validation: node 11/11 (/sessions/.../wardenfanfare.js, the edited block VERBATIM over stubbed tone/noiseHit/fxRing/setTimeout: 3 golden rings still fire; boom 165Hz slide -95 resolving to 70Hz ≥30 floor; impact noiseHit fires; exactly 3 rising-cap tones SCHEDULED via setTimeout; cap freqs ==[330,440,660] & strictly RISING & delays ascend; boom pitch ≠ 220/110/440 [distinct from sfxDeath/sfxSuper/sfxLevel]; non-boss death = ZERO rings+sound; typeof-guard no-throw when helper missing) + `node --check` on the wrapped+stubbed block = SYNTAX OK. Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — never edited through it); POLISH_LOG appended via a targeted Edit. Browser F12 the audio gate (a backgrounded tab pauses rAF/audio).
- 04:58 2026-06-24 UTC — balance (anti-attrition / ascending ladder — buildable structures kept pace) — the three buildable defenses (openBuild ~4450: 🗼 Arrow Tower, 🧱 Wall, 🌿 Heal Totem) spawned with FLAT HP (wall 900 / tower&totem 500) at EVERY difficulty, while enemy damage climbs ~×13 over the ladder (diffMult ∝ 1.38^(d-1)). So a structure that soaked ~77 trash hits at diff1 evaporated in ~6 by diff9 — the entire build system became a DEAD mechanic exactly where the game is hardest, violating WORLD_DESIGN §3 anti-attrition + the "does each system earn its place / make sure choices matter" focus. Same collapse-class as the petKeep pet-army fix (pets had a linear term against an exponential enemy curve; builds had NO scaling term at all). The tower's DAMAGE already scaled (30+diff*6) but a tower that dies before it shoots is worthless. **Fix (one atomic, reversible edit — a local `_bhp` scaled by `vetMult()`, used for both hp & maxHp):** structures now scale by the SAME base-power knob the hero's own HP uses (startRun ~4550 `(600+armor*150)*vet`), so a built defense keeps pace with the PLAYER, not the enemy curve. MATH (node 7/7, /tmp/build_hp.js): diff1 vetMult()=1.0 ⇒ 900/500 BYTE-IDENTICAL (no-op early game); strictly monotonic d1→d9 (wall 900→4284, tower 500→2380); typeof-guard → ×1 fallback if vetMult ever absent (= old flat); and crucially depth STILL bites — tower hits-to-die FALLS 76.9(d1)→27.8(d9) because vet 4.76 < enemy 13 at diff9, so it's NOT overpowered (behaves exactly like the hero/pet, who also get relatively squishier at depth) yet no longer a useless 5.8-hit pop. Conservative: scales like the hero (vet only), strictly LESS than a pet's petKeep()*vet; builds are WILD-only (tier 0) so zero dungeon/boss/Wilds-economy balance is touched. No new save fields/systems; tower dmg-curve left for a future run to keep this atomic. No 📖 guide/wiki edit — build HP is never surfaced numerically (the guide's CONTROLS line only says "B: build / Forge"; wiki's "building" section is the legacy MOBA town-hall/lanes, not this PvE wild-build), so the change just makes the implied "your defenses hold" TRUE across the ladder (same make-the-docs-real, no-doc pattern as the forge-sink 06-23 / petKeep / hazard-cap runs). Rotated to BALANCE — the least-recently-touched bucket (most-recent per bucket: feel 04:30, QoL 04:05, progression 03:30, balance 02:58) and a NEW lever (recent balance runs tuned reward/effort CURVES: gold 02:58, zone-tier 01:25, vetMult 19:30 — none touched buildable durability). — pve.html only (openBuild ~4450). — validation: node 7/7 (build_hp.js: diff1≡900/500; monotonic d1-d9; guard→×1; depth-still-bites hits-to-die d9<d1; d9 HP>old flat 500) + `node --check` on the verbatim edited statement wrapped+stubbed = SYNTAX OK + runtime built _bhp. Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — never edited through it); POLISH_LOG appended via a targeted Edit.
- 05:30 2026-06-24 UTC — progression/pacing (ascending-ladder GATE reward parity — WORLD_DESIGN §4 OVA/Holy-Grail gates) — the two endgame ending gates were rewarded ASYMMETRICALLY: the LESSER diff-6 OVA gate already pays a one-time +2 CT milestone bounty (playEnding ~913, guarded on SAVE.ovaUnlocked), but the GAME'S ULTIMATE GOAL — the FIRST diff-9 Holy-Grail CLEAR (Yui redeemed, the only true ending, §4) — had NO dedicated bounty. Its diff9 ACHIEVEMENT (+3 CT, achDiff ~1431 `SAVE.diff>=9`) fires on REACHING diff-9 — i.e. the moment you CLEAR diff-8 and runComplete bumps SAVE.diff to 9 (~4470, achDiff called AFTER) — so the actual Grail CLEAR yielded ONLY the standard final share (ctSplit(34)[2]=17), under-celebrating the headline true ending relative to the lesser OVA gate it sits above. **Fix (one atomic, additive, reversible change — mirrors the diff-6 +2 pattern EXACTLY):** captured `_firstGrail=tier>=9&&(SAVE.bestEnding||0)<9` at playEnding's TOP (~910, BEFORE the line-912 bestEnding mutation, so it reads the pre-clear state), then after the existing tier>=6 OVA block added `if(_firstGrail){try{ctAdd(3);feed('💎 +3 CT — THE HOLY GRAIL is yours…');}catch(e){}}`. ESCALATION: OVA gate +2 → Grail gate +3 (hardest gate rewards most), matching the diff3/5/9 achievement rungs (1/2/3 CT). Fires EXACTLY once (bestEnding survives wipeSave ~298, permanent; a 2nd diff-9 clear has bestEnding already 9 → _firstGrail false). Whole CT (on-chain canon); +3 < AC ctJump 60 (Hollow Blight unaffected); lifetime one-time so faucet inflation is negligible (a single diff-9 clear already grants 34). playEnding is called ONLY from runComplete (~4476, grep-confirmed: defs/comments at 572/1236/1305 are references), so it can't mis-fire from the title ▶ Memories viewer (that uses playOVA ~900). No 📖 guide/wiki edit — mirrors the diff-6 +2 precedent (pve.html-only, surfaced in-game via the feed + end screen "You earned THE HOLY GRAIL"); CT-gate bounties are never tabulated in the docs. Rotated to PROGRESSION — the oldest-touched bucket (most-recent per bucket: balance 04:58, feel 04:30, QoL 04:05, progression 03:30). — pve.html only (playEnding ~910/914). — validation: node 17/17 (/tmp/grail_test.js, verbatim bounty logic over stubs: fresh diff-9 clear → +3 once [+ crosses the OVA +2]; 2nd diff-9 clear → NO re-fire of +3 or +2 & zero milestone feed; bestEnding-8 first-grail → +3 [no +2, OVA already unlocked]; bestEnding-9 → no +3; diff 1/5/6/8 NEVER grant the +3; diff-6 OVA +2 still fires [regression untouched]; +3 whole & <60; OVA+2 < Grail+3 escalation) + `node --check` on the verbatim edited function wrapped+stubbed = SYNTAX OK. Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — re-Read the exact 910-916 region before each Edit; never edited through the mount); POLISH_LOG appended via a targeted Edit (mount tail stale).
- 06:30 2026-06-24 UTC — balance (anti-attrition / ascending ladder — Arrow Tower OFFENSE kept pace) — completed the follow-up the 04:58 build-HP run EXPLICITLY deferred ("tower dmg-curve left for a future run to keep this atomic"): the 🗼 Arrow Tower's DAMAGE (openBuild ~4494, `bd.k==='tower'?30+SAVE.diff*6:0`) was a near-FLAT linear term — d1 36 → d9 84, only ×2.33 over the whole 9-difficulty tower — while enemy HP climbs ~×13.3 (diffMult ∝ 1.38^(d-1)). So the tower's kill-speed COLLAPSED from ~3.1 hits-to-kill a trash mob at diff1 to ~17.2 at diff9: the 04:58 run made the tower SURVIVE at depth (HP ×vetMult) but it still couldn't KILL — its offense a dead mechanic exactly where the build system should matter (WORLD_DESIGN §3 anti-attrition + "make sure choices matter"; same collapse-class as petKeep & the build-HP fix, just on the orthogonal DMG axis). **Fix (one atomic, reversible single-expression edit):** `30+SAVE.diff*6` → `Math.round((30+SAVE.diff*6)*(typeof vetMult==='function'?vetMult():1))` — scaled by vetMult(), the SAME base-power knob the hero's own damage (baseDmg +6·vet/level ~4188) AND the tower's HP (~4492, the 04:58 fix) already use, so the tower keeps pace with the PLAYER, not the enemy curve. MATH (node 17/17): combined dmg 36→400 (×11.1) sits just UNDER enemy HP ×13.3, so hits-to-kill stays bounded ~2.45–3.62 across the ladder (vs the old 3.1→17.2 collapse) — depth STILL bites (d9 is the toughest rung at 3.62 hits, vet 4.76 < enemy 13.3), never trivializes. diff1 vetMult()=1.0 ⇒ Math.round(36*1)=36 BYTE-IDENTICAL (safe early game §1 untouched); strictly monotonic d1→d9; all-integer (whole dmg); typeof-guarded → ×1 fallback (= old flat) if vetMult ever absent. Wild-only build (tier 0) so NO dungeon/boss/economy balance is touched. No 📖 guide/wiki edit — build dmg is never surfaced numerically (the tower's BUILDS desc is the qualitative "auto-attacks monsters", still true; guide CONTROLS only says "B: build / Forge"; wiki's build section is the legacy MOBA town-hall/lanes — grep-confirmed), so the change just makes the implied "your tower fights for you" TRUE across the ladder (same make-the-docs-real, no-doc pattern as the 04:58 build-HP / petKeep / hazard-cap runs). Rotated to BALANCE and a NEW lever (most-recent per bucket: progression 05:30, balance 04:58 [build HP — this is its deferred DMG sibling], feel 04:30, QoL 04:05). — pve.html only (openBuild tower dmg ~4494). — validation: node 17/17 (/tmp/tower_dmg.js: diff1≡36 byte-identical; typeof-guard fallback==old 30+d*6 ∀diff; strictly monotonic d1→d9; all integer; dmg ratio 11.1 < enemy HP ratio 13.3 [no trivialize]; new>old ∀diff>1; hits-to-kill bounded d1 3.06/d9 3.62 vs OLD d9 17.2; d9 dmg==400) + `node --check` on the verbatim edited mkUnit call wrapped+stubbed = SYNTAX OK + runtime dmg @diff5==158. Edits via Read/Edit file tools ONLY (re-Read the exact ~4493 region before the Edit; bash mount tail stale/truncated for pve.html — never edited through it); POLISH_LOG appended via a targeted Edit (mount tail stale).
- 06:55 2026-06-24 UTC — QoL/clarity (skill-bar affordability cue — fewer "why won't it cast" moments) — the desktop Q/W/E/R ability buttons (updHUD per-frame loop ~4921) showed a `.cd` NUMBER overlay while a skill cooled down, but gave NO at-a-glance cue when a skill was simply UNAFFORDABLE (off cooldown, but mana < cost). The only mana feedback was REACTIVE — `startAim` (~4171) feeds "— not enough mana" AFTER you press the key / click the icon — so a player watching a low mana bar couldn't tell which of Q/W/E/R were castable RIGHT NOW without poking each one (the ultimate R costs the most, 120 vs 20–40, so it's dark most often). Standard ARPG/MOBA UX greys out unaffordable skills; this bar didn't. **Fix (one atomic, additive, reversible line inside the existing forEach):** after the cd-overlay branch, set `getElementById(id).style.opacity=(_ab&&hero.abCd[i]<=0&&hero.mp<_ab.mp)?'.4':'1'` where `_ab=hero.kit&&hero.kit.abs[i]`. A skill you can't afford (off cooldown but mp<cost) fades to .4; everything castable stays at 1. Deliberately distinct from the cooldown cue: a skill ON cooldown keeps opacity 1 and its number overlay owns the "wait" signal, so a FADED icon means specifically "need mana" while a NUMBER means "cooling down" — two non-overlapping cues. Guarded on `hero.kit` (null → no throw, opacity 1, no false dim) and only touches the desktop aQ/aW/aE/aR DOM nodes, which are `display:none` on real touch devices (~5126) and carry their own separate cooldown UI (`_pkCd`), so ZERO touch impact. No new save field/system/timer; pure cosmetic per-frame style write (4 elements, same cadence as the cd overlay already set beside it); fully reversible (delete the one line). 📖 in-game guide CONTROLS line (~92) noted: "A skill icon dims when you lack the mana to cast it (a number counts down on it while it's cooling down)" — matches the existing mobile "each greys out with a cooldown timer" copy; wiki.html is the MOBA/PvP wiki with no PvE skill-bar section (grep 0) so no wiki edit. Rotated to QoL/CLARITY — the oldest-touched bucket (most-recent per bucket: balance 06:30, progression 05:30, feel 04:30, QoL was last at 04:05 HUD-bar titles). — pve.html(updHUD ability-button loop ~4922, 📖 guide CONTROLS ~92). — validation: node 11/11 (/tmp/manadim.js, the edited block reconstructed VERBATIM over stubbed DOM/hero: full mana → all opacity 1 & no cd overlay; mp=30 → Q(20) & E(exactly-30, 30<30 false) bright, W(40)/R(120) dim .4; R on cd → opacity stays 1 [number cue owns it] & cd overlay shows ceil(3.2)=4 while a 0-mana Q off-cd dims; null hero.kit → no throw & opacity 1 [no false dim]) + `node --check` on the wrapped+stubbed block = SYNTAX_OK. Guide edit is static HTML inside the guide <div> (balanced <b></b>, single insertion, cannot break JS). Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — never edited through it); POLISH_LOG appended via a targeted Edit (mount tail stale). Browser F12 the visual gate (a backgrounded tab pauses rAF).
- 07:30 2026-06-24 UTC — fun & feel (hit feedback / juice — big hits read BIG) — the floating damage-number `pop()` (~3627) printed every hit at a FLAT class size (enemy 13px / crit 17px / hero-received 15px) regardless of magnitude, so a 1-dmg chip and a half-HP haymaker looked IDENTICAL — the core ARPG/Diablo "hits feel impactful" beat was missing on the one surface (the number) that reports every single hit. Distinct lever from the recent feel runs, which were all RINGS/AUDIO (shadow-burst 20:55, cart-bonk 01:58, Warden-death fanfare 04:30) — this is the damage-readout axis, untouched. **Fix (one atomic, additive, reversible line in pop, no colour/animation/system change):** `d.style.fontSize=Math.round((crit?17:(u===hero?15:13))*(1+Math.min(1,(amt/Math.max(1,u.maxHp||1))*2)))+'px'` — the number now GROWS with the fraction of the target's max HP the hit removed. Base respects the existing CSS classes (crit 17 / hero-received .self 15 / enemy 13) and scales ×1 (frac→0) → ×2 (CAPPED). MATH: scale=1+min(1,frac·2): a chip hit (frac≈0) → ×1 BYTE-IDENTICAL to the class size (round(13·1.003)=13, round(17·1.003)=17, round(15·…)=15 — chip/normal play looks exactly as before); a 25%-maxHp hit → ×1.5 (enemy 20px); a ≥50% hit → ×2 (enemy 26px, crit 34px); overkill (frac>1) clamps at the same ×2 so it can NEVER grow unbounded. Colour untouched (crit stays #ff9d3a orange, .self stays alarm-red, normal white) and the `dfl` rise/fade animation is transform/opacity so the inline font-size doesn't conflict; crit's `!` suffix and the self/enemy class split are preserved. maxHp floored (`||1`, `Math.max(1,…)`) so a unit without stats can't div-by-zero/NaN the size. Pure cosmetic per-DOM-node style write at the same cadence the number is already created (naturally rate-limited by hit frequency, no new node/system/save/timer); fully reversible (delete the one line). No 📖 guide/wiki edit — adds nothing the player must LEARN, just makes the number it already sees read proportional to the blow (same no-doc cosmetic-feel reasoning as the crit-ring/shadow-burst/Warden-fanfare runs). Rotated to FUN/FEEL — the oldest-touched bucket (most-recent per bucket: QoL 06:55, balance 06:30, progression 05:30, feel was last at 04:30 Warden-death audio) AND a NON-audio/NON-ring feel lever for variety. — pve.html only (pop ~3627). — validation: node 15/15 (/tmp/dmgsize.js, the formula VERBATIM: enemy/crit/self chip == base 13/17/15 [byte-identical]; 25%→20, 50%→26 cap, 100%/200% overkill clamp at 26; crit 50%→34; self big→30; bounded [13,26] & monotonic-non-decreasing across magnitudes; undefined/zero maxHp → valid \d+px no NaN/div0; returns 'px' string) + `node --check` on the verbatim edited pop() reconstructed = POP_SYNTAX_OK. Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — never edited through it); POLISH_LOG appended via a targeted Edit (mount tail stale). Browser F12 the visual gate (a backgrounded tab pauses rAF).
- 09:24 2026-06-24 UTC — balance (ascending-ladder SURVIVAL pressure — WORLD_DESIGN §3 tower-of-challenge + §0 you-are-the-hunted) — the Underworld's passive Humanity erosion (`corruptionTick` ~1410) was a FLAT `addCorruption(0.25*dt)` at EVERY depth: the shallowest realm and the deepest Abyssal Vault drained your Humanity at the identical 0.25/s (≈400s to be Consumed from full). The survival-pressure axis was the LONE flat one — every sibling threat/reward axis already scales by realm tier (enemy HP via diffMult's convex zone term 1+0.45t+0.02t², boss gold +12%/tier, loot rarity +0.015/tier) — so the §3 "deeper must bite harder" climb and the §0 "the dark is thicker the deeper you descend / you are the hunted" theme both broke on the Humanity gauge. **Fix (one atomic, reversible line — read the realm tier off the already-computed `z` with the EXACT lootDrop zoneOf guard, then scale the erosion):** `else if(...selEp...>=3)addCorruption(0.25*dt)` → `…{const _et=(z&&z.t==='dungeon'&&z.d)?(z.d.tier||0):0;addCorruption(0.25*(1+_et*0.10)*dt);}` — +10%/tier, so the dark consumes you faster the deeper you go. MATH: tier0 (shallow / non-dungeon / Wilds-as-underworld / the catch-fallback z={t:'wild'} / missing .d|.tier via ||0) → 0.25*dt BYTE-IDENTICAL (no new early wall §1); strictly monotonic t0→t7; t1 .275, t3 .325, t7 .425 (max +70%; time-to-consume 400s→235s). Deliberately CONSERVATIVE & non-inverting: the +70% top bump sits FAR below the ~5.1× enemy-power tier climb (diffTier(7)/diffTier(0)), so survival pressure never outpaces difficulty, and it stays a slow BACKGROUND term — food anchors (3-5/descent, scattered IN the realm), town(-6)/safe(-2) heal and the per-cast siphon (2.5/cast, 7-9/ultimate) still dominate the gauge — so it deepens the climb without a §1 wall. Whole gauge math unchanged (CORR_POWER/CRIT/MAX thresholds untouched). Rotated to BALANCE and a NEW lever — the Corruption-erosion/survival axis, untouched by any recent run (recent buckets: feel ×3 [pet pile-on 09:02, cart-splat 08:40, dmg-number 07:30], QoL [skill-bar 06:55], balance [tower-dmg 06:30, enemy-mix 05:58] — all on the COMBAT/economy side, never the Humanity drain). Player-facing, so synced both docs qualitatively (no numbers surfaced, per the diffMult/gold/rarity precedent): 📖 in-game guide HUMANITY⇄CORRUPTION line (~116) + wiki.html (~70) now say "the deeper the realm, the faster the dark consumes/erodes you". — pve.html(corruptionTick ~1410, guide ~116), wiki.html(~70). — validation: node 13/13 (/tmp/corr_erode.js: t0-dungeon & wild & undefined-z & missing-.d & missing-.tier all ≡ 0.25 [byte-identical/guards]; strictly ascending t0→t7; exact t1=.275/t3=.325/t7=.425; t7=+70%; time-to-consume t0=400s & t7∈(180,240); erosion bump 1.70 < enemy-power bump diffTier(7)/diffTier(0)≈5.13 [no wall/inversion]) + `node --check` on the edited corruptionTick wrapped+stubbed = SYNTAX OK + runtime corruptionTick(1)@tier7 = 0.4250 (matches the predicted rate). Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — re-Read the exact 1403-1410 region before the Edit; never edited through the mount); POLISH_LOG appended via a targeted Edit (mount tail stale). Browser F12 the live gate (a backgrounded tab pauses rAF/corruptionTick).
- 10:30 2026-06-24 UTC — fun & feel + balance (enemy VARIETY at depth — WORLD_DESIGN §3 tower-of-challenge) — the elite-affix roll (`mkMon` ~4452) gave EVERY elite EXACTLY ONE random affix (Frenzied ×1.7 atkSpd · Colossal ×1.8 HP · Vampiric lifesteal · Storming range14) at ALL depths, so a tier-1 Mossfang elite and a tier-7 Abyssal-Vault elite had the IDENTICAL threat-texture menu — the §3 "each deeper realm must be structurally HARDER & more distinct, not just a colour swap" promise broke on the elite axis (same flat-at-every-tier class the 05:58 ranged-share & 09:55 boss-slam runs closed on the trash-mix & boss-tempo axes). **Fix (ONE atomic, reversible edit — the single-affix line → a draw-WITHOUT-replacement loop):** deep-realm elites (`tier>=5` — Isle of Dominus / Enamora / Abyssal Vault, the bottom of the descent) now roll **TWO DISTINCT affixes**; `tier<5` rolls exactly ONE (same uniform distribution — early/mid tower BYTE-EQUIVALENT). Affixes drawn via `ELITES.slice()`+`splice` so at most ONE of each. **BOUNDED / non-runaway (the whole point — variety, NOT stat inflation):** every per-axis ceiling is UNCHANGED vs today's single-affix elite — HP ×1.8 max (Colossal at most once, so a 2-affix elite is hp 475.2 [110·1.8·2.4], NEVER the 855 of a double-Colossal), atkSpd ×1.7 max (one Frenzied), range 14 max (one Storming), dmg ×1.3 unchanged — the change only COMBINES two different axes (e.g. tanky+lifesteal, or fast+ranged-kiter), so a deep elite is a richer READ, not a bigger HP wall. All 6 distinct pairs reachable; ½ contain Colossal (extra-tanky), ½ are non-tanky dual-threats. The threat ☠/??? con-marker (~4431, elite +1 tier) already surfaces the raised danger so no new mechanic to teach (pve.html-only, mirroring the elite-gold ~3891 / boss-tempo cosmetic-mix runs; the 📖 guide READING-THE-ENEMY line already says deeper realms field nastier foes). `eliteName` is set but never rendered (grep: only the assignment), so the compound "Colossal Vampiric" string is harmless. Rotated to FEEL/variety (recent: balance Humanity-erosion 09:24 & boss-slam 09:55, feel pet-pile-on 09:02 & cart-splat 08:40, QoL chapter-signpost 08:02) — the elite-affix lever was untouched by any recent run. — validation: node 93004/93004 (/tmp elite_test.js: tier0-4 → exactly 1 affix; tier5-7 → exactly 2 DISTINCT affixes; HP only ever 264 [no-Colossal] or 475.2 [one-Colossal], NEVER 855 [double]; atkSpd only .8 or 1.36, never 2.31 [double-Frenzied]; ELITES stays length 4 / unmutated; all 4 singles reachable at t0, all 6 distinct pairs reachable at t6) + `node --check` SYNTAX_OK on the wrapped+stubbed edited region + runtime (tier7-forced = 2 distinct "Colossal Vampiric" hp475.2; tier2-forced = 1 "Frenzied" hp264). Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — re-Read the exact 4452-4453 region before the Edit; never edited through the mount); POLISH_LOG appended via a targeted Edit. — pve.html only (mkMon elite roll ~4452).
- 10:54 2026-06-24 UTC — balance/progression (pet OFFENSE level-parity at spawn — WORLD_DESIGN §3 anti-attrition + §0 Palworld pet pillar) — all THREE pet-spawn formulas (hatch/mkEgg ~4663, revive/Barkindle ~4683, start/startRun ~4852) already BACK-PAY a hero-level term into spawn HP (`+12*Math.max(0,hero.lvl-1)`) so a pet tamed late spawns as tanky as its level deserves — but spawn DMG had NO level term (`(12+SAVE.diff*2.5)`), so a freshly-tamed/revived late-game pet spawned TANKY-BUT-UNDERARMED: full HP-for-its-level, near-zero dmg-for-its-level. This is the exact "meat shield" decay the 00:40 run fixed for KEPT pets (giveXP grants existing pets +1 dmg/level ~4427) — but a NEW spawn starts dmg from scratch while its HP is back-paid, an asymmetry that perversely made a fresh pet weaker than one that had leveled alongside you, and (with the start-spawn) echoed the old "die to get stronger" revive bug. **Fix (ONE atomic reversible `replace_all`):** `(12+SAVE.diff*2.5)` → `(12+SAVE.diff*2.5+Math.max(0,hero.lvl-1))` at all 3 sites — adds +1 dmg/hero-level to the base, INSIDE the existing petKeep()*vet*banner*pet-stat*RUNUP*skill chain (so it scales identically to the rest). MATH: the dmg level-term (+1/lvl) to HP level-term (+12/lvl) is exactly 12:1 — matching the hero's own level-up ratio (baseHp +70·vet : baseDmg +6·vet ≈ 11.7:1) AND the 00:40 retroactive +1/lvl — so a freshly-spawned pet now EQUALS a pet that leveled 1→L beside you (node: freshAdd==keptAdd, no runaway/never exceeds the hero carry). hero.lvl=1 ⇒ Math.max(0,0)=0 ⇒ BYTE-IDENTICAL to old at every difficulty (start-spawn is always lvl1 → unchanged; early game §1 untouched); strictly monotonic in hero.lvl; diff9/lvl30 base ratio <2× (bounded). No guide/wiki edit — pet dmg is never surfaced numerically (the guide/wiki say pets "grow with you" qualitatively, which this makes TRUER — same no-doc pattern as the 00:40 pet-dmg / petKeep / build-HP runs). Completes the 00:40 future-growth axis with the orthogonal SPAWN back-pay axis (precedent: 06:30 tower-DMG completed 04:58 build-HP's deferred sibling). — pve.html only (pet spawn dmg ~4663/4683/4852). — validation: node 15/15 (/tmp/petdmg.js: lvl1≡old ∀diff [byte-identical]; monotonic in lvl; dmg back-pay==lvl-1 & HP:dmg level-term ratio==12:1 [parity]; freshAdd==keptAdd [no runaway]; all dmg positive integers; diff9/lvl30 base ratio<2×) + `node --check` on the verbatim edited expression (both vetMult() and `vet` variants) wrapped+stubbed = SYNTAX OK + runtime (40/40); grep confirms the new form present EXACTLY 3× & the bare `(12+SAVE.diff*2.5)` gone (parens balanced — net +1 `(`/`)`). Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — never edited through it); POLISH_LOG appended via a targeted Edit.
- 11:54 2026-06-24 UTC — balance (ascending ladder / anti-attrition — pet diff-scaler DOUBLE-COUNT) — every pet spawn (hatch ~4712 / revive ~4732 / start ~4901) scales HP & dmg by `petKeep()*vetMult()`, but BOTH terms are diff-scalers: petKeep (`1.2^(d-1)`) was CALIBRATED as the SOLE pet diff-keeper (its def-comment ~4508: "1.0@diff1 → 4.30@diff9, sitting just UNDER the enemy curve … a pet ≈ as tanky as one trash mob at the bottom"), while vetMult (`1+0.35(d-1)+0.015(d-1)²`, ×4.76@diff9) is the hero's earned-power knob — so multiplying them DOUBLE-COUNTS difficulty. Pet-HP diff-scaling hit ~37.7×@diff9 vs the enemy curve's 13.2× (diffMult ∝ 1.38^(d-1)) — ≈2.87× OVER, the OPPOSITE of "just under." Concretely a diff9 Hatchling ≈5× a trash mob's HP (Primal ≈10×), turning the Palworld pack into an invincible WALL in the deepest realms — gutting §3 anti-attrition + "the hero remains the carry" exactly where the tower-of-challenge must bite hardest, and flatly contradicting petKeep's OWN def-comment. **Fix (one atomic, reversible single-line edit — petKeep def ~4508):** `Math.pow(1.2,SAVE.diff-1)` → `Math.pow(1.2,SAVE.diff-1)/(typeof vetMult==='function'?vetMult():1)` so the NET spawn factor `petKeep()*vetMult()` == the calibrated `1.2^(d-1)` at EVERY rung (the division cancels the spawn-site vetMult; petKeep is ONLY ever called as `petKeep()*vetMult()`/`*vet` — grep-confirmed all 6 sites). MATH: diff1 = 1.000 BYTE-IDENTICAL (early game §1 untouched); net rebuilt exactly to 1.2^(d-1) ∀d (1.20/1.44/…/4.30); pet-HP TOTAL scaling (×the linear term 170+20d) now 1.00→7.92× while enemy 1.00→13.15×, so pet/enemy ratio DECLINES 1.00→0.60 (relevant, never overtaking) — and a diff9 Hatchling drops to ≈1× a trash mob's HP, EXACTLY the def-comment's stated intent (was ~5×). Restores the DOCUMENTED calibration without touching the 3 spawn sites (lower collision risk); left an explicit "DO NOT collapse the petKeep()*vetMult() pairing" note in the comment so a future run can't re-introduce the double-count. The in-run per-level pet growth (~4477) keeps its OWN vetMult (untouched — legit level-cadence, not the spawn double-count). No guide/wiki edit — pet power is never surfaced numerically; the PETS guide line ("grow markedly stronger the higher the difficulty … keep pace … rather than falling behind") stays TRUE (pets still climb 7.9× by diff9, just no longer OVERSHOOT into a wall) — same make-the-docs-real / no-doc pattern as the petKeep/build-HP/tower-dmg runs. Rotated to BALANCE on a NEW lever — the petKeep DEFINITION / double-count, untouched by the recent pet runs (00:40 future-dmg, 09:02 pile-on AI, 10:54 spawn-dmg back-pay all touched the spawn CHAIN, never the petKeep×vetMult stacking). — pve.html only (petKeep ~4508). — validation: node 40/40 (/tmp/petkeep_test.js: diff1 net≡1.0 byte-identical; net==1.2^(d-1) ∀d∈1..9; pet-HP scaling strictly < enemy ∀d≥2 & monotonic increasing & ratio strictly declining; regression — OLD petKeep×vetMult OVERSHOT enemy@diff9 by 2.87× [bug proof]; new d9 ==7.92× & ratio 0.60) + `node --check`/runtime on the verbatim edited petKeep wrapped+stubbed = SYNTAX OK (petKeep(d5)=0.7855, net=2.0736). Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — re-Read the exact 4508 region before the Edit; never edited through the mount); POLISH_LOG appended via a targeted Edit (mount tail stale — true tail read via the file tool through the 10:54 entry).
- 12:24 2026-06-24 UTC — QoL/clarity (Warden/boss HP readout — fewer "how close to dead is this thing" moments) — the on-screen boss bar (#bossBar, updHUD ~5161) showed only a NAME line ("☠ Vault Warden") + an 11px gradient FILL, with NO numeric HP — unlike the player's OWN vital bars (#hpf/#mpf), which carry exact textContent labels ("120 / 120", "40 / 40"); a thin 340px-wide bar pinned at top:70px is hard to read precisely (is the Warden at 5% or 15%? worth pushing or worth kiting?), so the file's heavy numeric-bar convention was unmet on the one enemy that matters most — the realm-unsealing Warden (WORLD_DESIGN §3 tower-of-challenge) and the finale bosses. **Fix (ONE atomic, additive, reversible single-line edit to line 5161 — the bossBarFill line 5162 left BYTE-IDENTICAL):** captured `const _bp=Math.ceil(Math.max(0,bz.hp/bz.maxHp*100))` and appended `+'  '+_bp+'%'` to the existing name textContent, so the bar now reads e.g. "☠ Vault Warden  57%". Math: full=100%, half=50%, crown finale "👑 Ayume  30%", default "☠ Warden  25%" when bossName unset; ceil(>=0) keeps a live-but-low Warden at "1%" (hp1/maxHp1000) rather than a misleading "0%", and Math.max(0,…) means a momentary negative hp can NEVER print a negative % — while the FILL keeps the raw float (33.33333%…) for an exact width (text ceils 33.3→34, fill stays 33.333% — deliberately decoupled so the precise bar width regression is untouched). Two-space separator mirrors the statusInd concatenations (~5148-5149). Pure cosmetic per-frame textContent write on a node already updated every frame (no new DOM element/CSS/save field/system/timer; "  N%" ≈5 chars fits easily in the 340px/13px name div); fully reversible (delete the `const _bp=…;` + the `+'  '+_bp+'%'`). No 📖 guide/wiki edit — adds nothing the player must LEARN, just labels the HP already shown by the fill (same no-doc reasoning as the 04:05 HUD-bar-title / threat-legend clarity runs). Rotated to QoL/CLARITY — the oldest-touched bucket (most-recent per bucket: balance 11:54/10:54, feel 10:30/09:02, QoL was last at 08:02 chapter-signpost). — pve.html only (updHUD bossBar ~5161). — validation: node 14/14 (/tmp/bossbar_test.js, the edited block VERBATIM over stubs: full=100%/half=50%/crown 👑 Ayume 30%/default ☠ Warden 25%; sliver hp1=1% [never 0 while alive]; hp0=0% & fill 0%; negative hp clamps to 0% [no negative]; fill keeps raw float 33.33333…% while text ceils 33.3→34%; no boss → bar hidden) + `node --check` on the verbatim edited region wrapped+stubbed = SYNTAX OK (exit 0). Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — re-Read the exact 5159-5162 region before the Edit; never edited through the mount); POLISH_LOG appended via a targeted Edit (mount tail stale). Browser F12 the live gate (a backgrounded tab pauses rAF/updHUD).
- 13:05 2026-06-24 UTC — progression/pacing + survival-balance (Anchor counterplay KEEPS PACE with depth — WORLD_DESIGN §0 you-are-the-hunted / §4 anchors) — the 09:24 run made the Underworld's Humanity erosion CLIMB with realm tier (corruptionTick `0.25*(1+tier*0.10)*dt`, 1.1×@t2 → 1.7×@t7, time-to-consume 400s→235s) and explicitly justified that bump as safe because "food anchors (3-5/descent) … still dominate the gauge." But it left the counterplay — `scatterFood` morsel count (~4080) — FLAT at `3+floor(rand*3)` (3-5) at EVERY realm, so the food/erosion balance it relied on silently DEGRADED with depth: the dark drains you +70% faster at the Abyssal Vault but you find the same 3-5 loaves as the shallowest realm. Every sibling axis already scales per-tier (enemy HP via diffMult's convex zone term, boss gold +12%/tier, loot rarity +0.015/tier, AND now erosion +10%/tier) — the Anchor counterplay was the LONE flat one, so the §0 "you are the hunted, eat to stay human" pressure had no matching counterplay growth. **Fix (ONE atomic, additive, reversible edit — appended a tier term reading the already-computed `dz` realm with the EXACT same `dz.tier||0` guard the loop below uses):** `const N=3+Math.floor(Math.random()*3)` → `…+(dz?Math.floor((dz.tier||0)*0.5):0)`. MATH (node 15/15, /tmp/food_test.js): wilds/no-realm (dz null) → +0 BYTE-IDENTICAL (3-5, the documented wilds count); tier1 → floor(0.5)=+0 byte-identical (and Ep2/tier1 is selEp<3 so it never even scatters — food only flows selEp>=3); tier term t2 +1, t3 +1, t4 +2, t5 +2, t6 +3, t7 +3 → realm food AVG climbs 4.0→7.0 (~+75%), mirroring the +70% erosion so the 09:24 "food dominates the gauge" reasoning stays TRUE at every tier; strictly monotonic non-decreasing in tier; food mult stays within 6% of the erosion mult at every realm tier and LEADS it at the deep tiers t4-t7 (where the hunt bites hardest — counterplay should be present, never punishingly behind), and never overshoots (top food 1.75× ~ top erosion 1.70×). NOT a cakewalk: Anchors restore ONLY Humanity (no HP/combat), are NOT vacuumed (you must physically reach each one through deadlier deep-realm foes), and the deep realms' higher threat-density means more morsels ≠ free safety — this RESTORES the calibration the erosion was designed against, it doesn't trivialize it (same make-the-prior-run's-safety-claim-TRUE pattern as the gold/rarity/tower anti-trivialize runs). No 📖 guide/wiki edit — morsel COUNT is never surfaced numerically (the guide/wiki say human food is "scattered in the Underworld" qualitatively, still true; the gold +12%/rarity +0.015 tier scalings likewise made no doc edit), and the 09:24 guide/wiki erosion line ("the deeper the realm, the faster the dark consumes you") already implies you'll need to forage more. Rotated to PROGRESSION/PACING — the OLDEST-touched bucket (most-recent per bucket: QoL 12:24 boss-bar%, balance 11:54 petKeep, feel 10:30 elite-affixes, progression was last at 05:30 Grail-gate bounty) — and a NEW lever (the Anchor/food counterplay axis, the direct §0 mirror of the 09:24 erosion change, untouched by any balance run since). — pve.html only (scatterFood ~4080). — validation: node 15/15 (food_test.js: wilds & tier1 byte-identical; exact per-tier add t2..t7 =1/1/2/2/3/3; monotonic; avg 4.0→7.0; food mult within 6% of erosion ∀tier & leads at t4-t7; no-overshoot; all counts integer ≥3; missing-.tier→+0 guard) + `node --check` on the edited line wrapped+stubbed = CHECK_EXIT_OK + runtime (tier6 realm → N=6 ∈ 6-8). Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — re-Read the exact 4073-4074 region before the Edit; never edited through the mount); POLISH_LOG appended via a targeted Edit (re-confirmed the mount's `tail` is stale — it shows a showEndScreen entry as the last line while the file tool reads through the 12:24 boss-bar% entry — so never appended via `>>`). Browser F12 the live gate (a backgrounded tab pauses rAF).
- 13:54 2026-06-24 UTC — fun & feel (Warden ENTRANCE audio beat — WORLD_DESIGN §3 tower-of-challenge / §0 dread) — a realm's WARDEN spawns in `populateDungeon` (~4762, `mkBoss(...,false)`) with ONLY a gold eliteRing (from mkBoss ~4688) — AUDIBLY SILENT. The realm-entry FEED on the next line (~4763 "🕯 You enter X — danger tier N/7") and the FINAL boss (maybeFinalBoss ~4771-4772, a spoken feed) both announce themselves, but each regular tower-of-challenge Warden ENTERED with no sound at all — the same "the biggest beat is the quietest" gap the boss-DEATH fanfare (4579), shadow-burst (20:55) & cart-bonk (01:58) runs closed on OTHER surfaces. The Warden is the realm's gatekeeper (felling it unseals the next realm) — its arrival deserves a sting. **Fix (ONE atomic, additive, reversible change — wrapped the single `if(!bossDead)mkBoss(...)` statement in a block and added a try-guarded audio beat INSIDE it):** a distinct DESCENDING ominous "the keeper stirs" sting — deliberately the INVERSE of the boss-death fanfare's RISING [330,440,660] major cap: `tone(98,.55,'sawtooth',.12,-26)` (G2, slides DOWN) → after 250ms `tone(73,.7,'triangle',.11,-10)` (a LOWER D2, also falling) + a soft distant low rumble `noiseHit(.5,.09)` = dread/foreboding, NOT victory. Two low FALLING notes (98>73) so it can't be mistaken for the death fanfare (rising 330+), the 70Hz slam boom, sfxDeath (220 saw) or sfxSuper (110 saw). Fires once per realm (populateDungeon early-returns on `dungeonState[i].pop`) and is gated INSIDE the `!bossDead` branch so re-entering a realm whose Warden is already slain replays NOTHING (correct — no keeper to announce). Both tone() & noiseHit() self-no-op when `!AC||S_MUTE` (mute-safe) and are typeof-guarded + try/catch (throw-safe); the delayed 2nd note carries its own try/catch. Pure cosmetic — no new system/save field (one transient setTimeout, not a persistent timer); reuses the existing tone()/noiseHit() helpers; fully reversible (delete the try block + unwrap the if). No 📖 guide/wiki edit — an audio beat teaches the player nothing NEW (the realm feed already names the realm + tier; same no-doc reasoning as the death-fanfare/slam/shadow-burst audio-feel runs). Rotated to FUN/FEEL — the OLDEST-touched bucket (most-recent per bucket: progression 13:05 anchor-food, QoL 12:24 boss-bar%, balance 11:54 petKeep, feel was last at 10:30 elite-affixes) — and a NEW lever (the Warden-ENTRANCE audio axis, untouched; recent feel runs were the elite-affix VARIETY 10:30, pet pile-on AI 09:02, cart-splat 08:40, dmg-number size 07:30, none on the boss-entrance sound). — pve.html only (populateDungeon Warden spawn ~4762). — validation: node 16/16 (/tmp/warden_entrance.js, the edited block reconstructed VERBATIM over recording stubs: Warden spawns + first note 98Hz sawtooth with slide<0 [descending]; soft rumble noiseHit .5; 2nd note scheduled @250ms then fires at 73Hz, also descending & LOWER than the first [falling motif]; entrance notes far BELOW the death-fanfare rising cap 330+ and ≠ slam70/death220/super110; bossDead branch → NO spawn & NO sting [silent]; muted tone/noiseHit no-op → no throw on either the immediate or the delayed note) + `node --check` on the verbatim edited block wrapped+stubbed = SYNTAX_OK. Edits via Read/Edit file tools ONLY (bash mount tail stale/truncated for pve.html — re-Read the exact 4762-4763 region before the Edit; never edited through the mount); POLISH_LOG appended via a targeted Edit on the file-tool true tail. Browser F12 the live gate (a backgrounded tab pauses rAF — and a backgrounded tab also suspends WebAudio, so probe in a focused tab).
- 16:01 2026-06-24 UTC — fun & feel (pet EVOLUTION audio beat — WORLD_DESIGN §0 Palworld pet pillar) — `evolvePets` (~4949) fired a green fxRing per pet + a feed when your pack crosses a tier (Hatchling→Juvenile→Veteran→Primal, gated at tame-skill lvl 6/14/24 → at most 3x a save) but was AUDIBLY SILENT — the core Palworld progression PAYOFF had no audio identity while every sibling milestone does (level-up→sfxLevel [440..880] tri run ~1501, equip/gem→sfxEquip [660..1320] sine arp ~1503, Warden-fall→gold fanfare ~4711, health-globe→rising heal chime ~4311, achievement→sfxLevel) — the exact audio-identity gap the globe-chime/cart-bonk/boss-fanfare/Warden-entrance runs closed on OTHER surfaces. **Fix (ONE atomic, additive, reversible change — wrapped the existing `if(n)feed(...)` in a block and appended a try-guarded audio beat):** a distinct ASCENDING growl-swell flourish = a low `tone(140,.34,'sawtooth',.1,160)` gliding UP 140→300Hz (a creature swelling with new strength) + a soft `noiseHit(.14,.08)` body, then after 180ms a bright rising `tone(660,.18,'triangle',.09,120)` cap (660→780). The UPWARD glide is deliberately the INVERSE of the boss-death (saw glides DOWN 165→70 ~4711) & sfxSuper (110 saw DOWN ~1499), and it's a single swell+cap — NOT the 3-4-note runs of sfxLevel/sfxEquip/boss-death — and its lead note (140Hz) sits BELOW every sibling's first note (sfxLevel 440 / sfxEquip 660 / globe 523 / boss-cap 330), so it can't be mistaken for any of them. Fires once per evolution INSIDE the existing `if(n)` gate (only when a pet actually crossed a tier — extremely rate-limited; `_gate('evo',300)` guards a double skill-up in one frame from double-swelling). Both tone() & noiseHit() self-no-op when `!AC||S_MUTE` (mute-safe ~1467/1473) and the whole beat + the delayed cap each carry try/catch + typeof guards (throw-safe). No new system/save field/persistent timer/control (one transient setTimeout, reuses the tone()/noiseHit()/_gate engine); fully reversible (revert to the lone `if(n)feed(...)`). No 📖 guide/wiki edit — an audio beat teaches nothing NEW (the evolve feed + green ring already announce it; same no-doc pattern as the boss-fanfare/Warden-entrance/weak-spot-ping/globe-chime audio-feel runs). Rotated to FUN/FEEL — the oldest-touched bucket (most-recent per bucket: progression 15:24 waypoint-gold, QoL 14:54 XP-readout, balance 14:24 Warden-signature, feel was last at 13:54 Warden-ENTRANCE audio) — and a NEW lever (the pet-EVOLUTION milestone audio; the 13:54 run did the Warden's entrance, this does the pet pack's tier-up — untouched by any feel run). — pve.html only (evolvePets ~4949). — validation: node 17/17 (/tmp/evo_test.js, the edited if(n) block VERBATIM over recording stubs: feed fires w/ tier name; `_gate('evo')` used; one noise body; lead note=140Hz sawtooth & slide>0 [UP] & swells 140→300; cap scheduled @+180ms then fires 660Hz triangle rising 660→780; EXACTLY 2 tones [swell+cap, not a 3-4-note run]; lead freq<330 [below every sibling]; n=0 → completely silent [no feed/no audio]; S_MUTE → tone/noiseHit no-op; AC unset → no audio/no throw; 2nd evolve within 300ms throttled [no double swell]) + `node --check` on the verbatim edited region wrapped+stubbed = SYNTAX OK (exit 0). Edits via Read/Edit file tools ONLY (re-Read the exact 4948-4949 region before the Edit; bash mount tail stale/truncated for pve.html — never edited through the mount); POLISH_LOG appended via a targeted Edit on the file-tool true tail. Browser F12 the live gate (a backgrounded tab pauses rAF AND suspends WebAudio, so probe in a focused tab).
- 14:24 2026-06-24 UTC — balance (Warden SIGNATURE cadence climbs the tower — WORLD_DESIGN §3 tower-of-challenge) — the per-Warden signature special (`bossSpecial`, each realm's case-1..7 attack; fired off `u.spCd` in the boss-AI loop ~5383) ran on a FLAT 7–10s rhythm at EVERY zone tier — the exact sibling lever the boss BASE-slam run (~5381) explicitly left "untouched (one atomic change)", and the LAST flat-across-the-tower axis on a Warden's aggression. The base-slam already tightens by DIFFICULTY (the run-progress ladder, ÷(1+0.05·(diff−1))); the SIGNATURE had no ladder at all, so a tier-1 Mossfang Alpha and the tier-7 Vault Warden unleashed their signature on the IDENTICAL cadence — depth changed a Warden's HP/dmg numbers but never how OFTEN it fought, breaking §3's "deeper realms bite harder, not just bigger numbers" (the same flat-behavior gap already closed for gold +12%/tier ~4104, rarity +0.015/tier ~4107, ranged-share +2.5pp/tier ~4662, deep-elite dual-affix tier≥5 ~4667, hazard scaling, totem-heal %). **Fix (ONE atomic, reversible single-line edit):** `u.spCd=(u.finalBoss?5:7)+Math.random()*3` → `u.spCd=((u.finalBoss?5:7)+Math.random()*3)/(u.finalBoss?1:(1+(u.tier||0)*0.05))` — gives the SIGNATURE its complementary ZONE-TIER axis (the vertical tower climb) to the base-slam's DIFFICULTY axis, so a deeper realm's gatekeeper fires its signature MORE OFTEN. **MATH (node 61/61, /tmp spcd.js):** dividing the 7–10s interval by (1+tier·0.05): tier0 → ÷1.0 BYTE-IDENTICAL (any Wilds/non-dungeon Warden + early game §1 untouched), strictly monotonic tighten t1 6.67–9.52s · t2 6.36–9.09 · t3 6.09–8.70 · t4 5.83–8.33 · t5 5.60–8.00 · t6 5.38–7.69 · t7 5.19–7.41s (max ÷1.35, −26% interval at the deepest realm). **FAIRNESS:** every bossSpecial case is itself fully TELEGRAPHED (routes through bossTelegraph, tg.t=1.5s reaction-constant), so even the tightest tier-7 signature (5.19s min) leaves ≥3.7s of open, un-telegraphed ground between casts — more relentless, never a dodge-lockout — and ÷1.35 sits FAR below the ~5.1× per-tier power climb (diffMult's convex zone term) so aggression never outpaces difficulty / can't invert the ladder. **FINAL BOSSES** (Hollow King/Kage/Ayume — `finalBoss` true; Ayume's `.tier` is undefined anyway) keep divisor 1 → their 5–8s climax cadence is BYTE-IDENTICAL (story-owned pacing untouched). No guide/wiki edit — tightening the cadence of an already-telegraphed, already-documented "deeper foes are nastier" mechanic teaches nothing NEW (same no-doc precedent as the difficulty-scaled base-slam ~5381 and the gold/rarity/ranged-share per-tier runs). Rotated to BALANCE — the oldest-touched bucket (most-recent per bucket: feel 13:54 Warden-entrance audio, progression 13:05 anchor-food, QoL 12:24 boss-bar%, balance was last at 11:54 petKeep) — and a NEW lever (the signature-cadence/spCd axis, the flagged-untouched sibling of the base-slam run). — pve.html only (boss-AI spCd ~5383). — validation: node 61/61 (/tmp/spcd.js: tier0 & all finalBoss byte-identical ∀rand; Ayume[final,tier=undefined]=5-8; strictly monotonic tightening t0→t7 ∀rand; tier7 ratio==1/1.35 [−26%]; tightest interval 5.19s > telegraph 1.5s+2 [≥3.7s open]; all intervals positive/finite; 1.35≪5.1 power-climb [no inversion]) + `node --check` on the edited boss-AI region wrapped+stubbed = CHECK_OK & runtime exercise (t7 regular 6.40s∈[5.19,7.41] · t0 9.57s∈[7,10] · final 7.36s∈[5,8]). Edits via Read/Edit file tools ONLY (re-Read the exact 5382-5383 region before the Edit; bash mount tail stale/truncated for pve.html — never edited through the mount); POLISH_LOG appended via a targeted Edit on the file-tool true tail. Browser F12 the live gate (a backgrounded tab pauses rAF/the boss-AI loop).
- 14:54 2026-06-24 UTC — QoL/clarity (XP-to-next-level readout — fewer "how close am I to leveling" moments) — the player's own vital bars carry EXACT numeric labels (#hpt "120 / 120", #mpt "40 / 40", absolutely-positioned spans inside the 12px .bar), and the 12:24 run just gave the Warden bar the same treatment (#bossBarName "☠ Warden 57%") — but the XP bar (#xpb/#xpf ~256) was the LONE progress bar with NO number: only a static descriptive tooltip + a 5px gradient fill, so a player could see they were "partway" to a level but never WHICH level, how much XP they had, or how much remained — and unlike HP/MP, #xpb is only 5px tall so it CANNOT fit an inline `.bar span` (line-height 12px) without a layout-changing restyle (too risky for an atomic run). **Fix (ONE atomic, additive, reversible block in updHUD ~5311, right after the existing xpf width write):** a per-frame-refreshed dynamic `title` on #xpb — `'⭐ Experience — '+Math.floor(hero.xp)+' / '+(hero.lvl*80)+' XP ('+Math.max(0,Math.ceil(hero.lvl*80-hero.xp))+' to level '+(hero.lvl+1)+'). Leveling raises your HP & damage and toughens your pets.'` — so hovering the XP bar now reads e.g. "⭐ Experience — 30 / 80 XP (50 to level 2). Leveling…", surfacing the exact count from the SAME `hero.lvl*80` threshold the width uses (line 5311 `hero.xp/(hero.lvl*80)`). The tooltip is the file's established convention for the bars (the HP/MP/XP/Humanity bars all carry descriptive `title=` attrs), so this just makes the XP one LIVE & numeric while preserving its original flavour text verbatim ("Leveling raises your HP & damage and toughens your pets"). MATH/edge-safety (node 10/10): current XP shown floored (Math.floor); remaining shown `Math.max(0,Math.ceil(need-xp))` so at/over the threshold it clamps to "0 to level N+1" and can NEVER print a negative; always names the NEXT level (hero.lvl+1); label prefix "⭐ Experience —" preserved. Null-guarded (`if(_xb)`), so a missing node can't throw. Pure cosmetic per-frame attribute write (one string concat + title set, same cadence/cost as the dozen textContent writes beside it — negligible vs the existing updHUD body; no new DOM node/CSS/save field/system/timer); fully reversible (delete the one block). updHUD only runs mid-run (hero exists), so off-run the static HTML tooltip applies — same lifecycle as hpt/mpt. No 📖 guide/wiki edit — adds nothing the player must LEARN, just labels the progress the bar already shows (SAME no-doc reasoning as the 12:24 boss-bar% / 04:05 HUD-bar-title clarity runs; the static #xpb tooltip text is preserved inside the dynamic one so nothing the docs reference changed). Rotated to QoL/CLARITY — the OLDEST-touched bucket (most-recent per bucket: balance 14:24 Warden-signature cadence, feel 13:54 Warden-entrance audio, progression 13:05 anchor-food, QoL was last at 12:24 boss-bar%) — and a NEW lever (the XP-bar readout, untouched; the boss-bar% run did the enemy bar, this does the player's last unlabeled bar). — pve.html only (updHUD xp tooltip ~5312). — validation: node 10/10 (/tmp/xptip.js, the edited block VERBATIM over a stubbed getElementById: lvl1/0xp→"0 / 80 XP" & "80 to level 2"; xp 30.7→floor "30 / 80 XP" & ceil "50 to level 2"; lvl5/120xp→"120 / 400 XP" & "280 to level 6"; at-threshold xp160/lvl2→"0 to level 3" [clamp]; overshoot xp200→"0 to level 3" & NO "-" [never negative]; label "⭐ Experience —" preserved) + `node --check` on the verbatim edited block wrapped+stubbed = SYNTAX OK + runtime ok. Edits via Read/Edit file tools ONLY (re-Read the exact 5311 region before the Edit; bash mount tail stale/truncated for pve.html — never edited through the mount); POLISH_LOG appended via a targeted Edit on the file-tool true tail. Browser F12 the live gate (a backgrounded tab pauses rAF/updHUD).
- 15:24 2026-06-24 UTC — progression/pacing (exploration GOLD keeps pace with the tower — WORLD_DESIGN §3 tower-of-challenge) — closed the gap the 3881 run left OPEN on the SAME discoverWaypoint line (~3880): that run made the first-visit Slaying XP scale with vetMult ("decayed to a rounding error vs a scaled elite kill at diff9") but left the GOLD beside it FLAT at `40+tier*20` across all 9 difficulties, so a vista discovery (tier7=180g) had likewise decayed vs a diff-scaled boss drop (diff9 tier7 = 524g) / the diff-scaled trash & forge gold economy — the exploration reward the §WAYPOINTS guide promises evaporating exactly at the top of the tower (same class as the sibling XP fix, just on the GOLD axis of the same line). **Fix (ONE atomic, reversible single-expression edit):** `40+tier*20` → `Math.round((40+tier*20)*(6+(typeof SAVE!=='undefined'&&SAVE.diff?SAVE.diff:1)*1.5)/7.5)` — scaled by the CANONICAL gold-income normalizer `(6+diff*1.5)/7.5` the forge-sink established (~4935; same trash-gold numerator 6+diff*1.5, spawn ~2814), so the discovery gold holds constant PURCHASING POWER vs gold income at every difficulty (the principled factor for a GOLD reward — distinct from the vetMult power-curve the XP on the same line uses; each reward tracks its own economy). MATH (node 160/160, /tmp/wpgold.js): diff1=(6+1.5)/7.5=1.0 → Math.round((40+tier*20)*1)=40+tier*20 BYTE-IDENTICAL ∀tier (early game §1 untouched); SAVE-undefined/diff<1 → ×1 fallback (= old flat); diff5 ×1.8, diff9 ×2.6 (tier7 180→324→468); strictly monotonic in diff ∀tier; reward ratio diff9/diff1 = 2.6 sits FAR below the ~13.3× enemy-HP climb (1.38^8) so reward never outpaces difficulty (no farm-the-top inversion); all whole gold (Math.round), max diff9/tier7 = 468 ≪ AC goldPer10s 200000 (Hollow Blight unaffected). No 📖 guide/wiki edit — discovery gold is never surfaced numerically (the feed shows the live amount; the §WAYPOINTS "gold & Slaying XP" promise just becomes TRUER), exactly the no-doc pattern of the 3881 sibling XP scaling. Fully reversible (revert to `40+tier*20`). Rotated to PROGRESSION/PACING — the OLDEST-touched bucket (most-recent per bucket: QoL 14:54 XP-readout, balance 14:24 Warden-signature, feel 13:54 Warden-entrance, progression was last at 13:05 anchor-food) — and a NEW lever (the exploration/vista gold faucet, untouched by any recent run). — pve.html only (discoverWaypoint ~3880). — validation: node 160/160 (/tmp/wpgold.js: diff1 & SAVE-undefined & diff0-falsy ≡ old 40+tier*20 ∀tier [byte-identical/guards]; exact diff5 t7=324 & diff9 t7=468 & diff9 t1=156; strictly monotonic d1→d9 ∀tier; ratio==2.6 < 1.38^8≈13.3 [no inversion]; all integer/positive/<200000) + `node --check` on the verbatim edited statement wrapped+stubbed = WRAP_SYNTAX_OK + runtime (diff5 t7=324, diff1 t7=180 byte-identical). Edits via Read/Edit file tools ONLY (re-Read the exact 3878-3882 region before the Edit; bash mount tail stale for pve.html — never edited through the mount); POLISH_LOG appended via a targeted Edit on the file-tool true tail (mount `tail` re-confirmed stale — it shows a showEndScreen ~906 entry as the last line while the file tool reads through the 14:54 XP-readout entry).
- 16:24 2026-06-24 UTC — balance (trash/elite GOLD keeps pace with the tower — WORLD_DESIGN §3) — `lootDrop` (~4235) scaled BOSS gold +12%/tier (~4234) and loot RARITY +0.015/tier (~4238), but the NON-boss (trash/elite) gold faucet was the LAST flat gold axis: `Math.round((3+diff*1.5+rand*6)*(u.elite?2.5:1))` paid IDENTICALLY at a tier-7 Abyssal-Vault and the tier-0 Wilds while those enemies are ~×5.1 tankier (diffMult's convex zone term 1+0.45t+0.02t²) — the exact "harder but not richer" gap the boss-gold (~4234), loot-rarity (~4238) and waypoint-gold (15:24) runs each closed on their own axis. **Fix (one atomic, reversible change):** repurposed the boss-only `_bz` to compute for EVERY unit (`u.boss?zoneOf():null` → `zoneOf()`; zoneOf always returns an object [dungeon/town/wild, grep-confirmed never null], so the existing `_bz&&_bz.t==='dungeon'&&_bz.d` guard makes `_bt`=0 for town/wild) and appended `*(1+_bt*0.12)` to the non-boss branch — the SAME per-tier scalar the boss branch already uses. MATH (node 217/219; the 2 'fails' are test-precision rounding artifacts on tiny integer gold — strict-increase where the correct non-decreasing `>=` all passed, and a ratio distorted by Math.round of a 16.5 base; the multiplier is provably EXACT): tier0 (Wilds/town, _bt=0) → ×1 BYTE-IDENTICAL ∀diff for trash/elite/boss (early game §1 untouched); multiplier exactly +12%/tier → t1 1.12 … t7 1.84 (+84%); rounded sequence non-decreasing; non-boss < boss preserved at every diff/tier; max non-boss gold d9/t7/elite = 103 ≪ 200000 AC goldPer10s (Hollow Blight unaffected); all integer & positive. NON-RUNAWAY: the +84% top bump sits FAR below the ×5.1 enemy-power tier climb, so gold-PER-EFFORT still DECLINES with depth (1.84/5.13≈0.36× of Wilds) — closes the flat axis without a farm-the-top inversion. No guide/wiki edit — trash gold is never surfaced numerically (the §ZONES/Portal-Plaza "deeper realms are harder & richer" promise just becomes TRUER; same no-doc pattern as the boss-gold/rarity/waypoint-gold runs). Rotated to BALANCE — the oldest-touched bucket (most-recent per bucket: feel 16:01 pet-evolution audio, progression 15:24 waypoint-gold, QoL 14:54 XP-readout, balance was last at 14:24 Warden-signature) — and a NEW lever (the trash/elite gold faucet, the last flat sibling of the boss-gold/rarity tier-scaling). — pve.html only (lootDrop ~4234-4235). — validation: node 217/219 (/tmp/gold_test.js: tier0 byte-identical ∀diff×{trash,elite,boss}; non-decreasing in tier ∀diff; +84% @t7 [pre-round, confirmed exact in /tmp/gold_confirm.js]; gold-climb 1.84 < effort-climb 5.13 & ratio 0.36 [no inversion]; non-boss<boss ∀diff×tier; max non-boss 103<200000 AC; all int+pos) + `node --check` SYNTAX OK on the verbatim edited lootDrop region wrapped+stubbed. Edits via Read/Edit file tools ONLY (re-Read the exact 4233-4235 region before each Edit; bash mount tail stale for pve.html — never edited through the mount); POLISH_LOG appended via a targeted Edit on the file-tool true tail.
- 16:54 2026-06-24 UTC — QoL/clarity (Kills-counter tooltip — fewer "what does this track" moments / WORLD_DESIGN §1 guided early game) — `#kls` was the LONE top-bar stat with NO explanatory `title=` (its siblings ⏱`gt`/☠`df`/💎`ct`/🧪`pots` all carry one ~225), yet Kills is NOT cosmetic: the early Hunter's Journal gates story progress on a kill count (`questCur().kills`, `onKillProgress` ~368; the 'First Blood' quest = "Slay 10 corrupted beasts in the Wilds" ~349; the quest panel shows "N / 10 beasts slain" ~410) — so a new player in the guided early game (§1) had no on-hover hint that the top-bar ⚔ Kills tally is exactly what those quests measure (a real "what do I do / am I progressing" moment). **Fix (ONE atomic, additive, reversible single-attribute edit to the `#top` Kills span ~225):** added `title="⚔ Kills — foes you've felled this run. Early Hunter's Journal quests ask you to slay a set number (e.g. 10 beasts in the Wilds) to advance the story — this is your tally."` — completing the established top-bar title convention AND tying the counter to the quest it feeds. Pure HTML attribute write (no new DOM node/CSS/save field/system/timer; NO JS touched — the `#kls` textContent write ~5466 is byte-identical); fully reversible (delete the `title=`). Title value is clean: no inner double-quote, no unescaped `&`, apostrophes are fine inside the double-quoted attr. No 📖 guide/wiki edit — a hover label teaches nothing the player must LEARN, it just NAMES an on-screen counter (same no-doc reasoning as the 14:54 XP-readout / 12:24 boss-bar% / 04:05 HUD-bar-title clarity runs). Rotated to QoL/CLARITY — the OLDEST-touched bucket (most-recent per bucket: balance 16:24 trash-gold, feel 16:01 pet-evolution audio, progression 15:24 waypoint-gold, QoL was last at 14:54 XP-readout) — and a NEW lever (the top-bar Kills tooltip; 14:54 did the XP bar, 12:24 the boss bar — this completes the LAST bare top-bar stat). — pve.html only (`#top` Kills span ~225). — validation: node 9/9 (/tmp/kls_check.js, the NEW span fragment VERBATIM: exactly 1 `<span>`/`</span>` & 1 `<b id="kls">`/`</b>` [balanced]; `title=` parses with NO inner double-quote; no raw unescaped `&`; even double-quote count; visible "⚔ Kills <b id=kls>0</b>" label preserved). Edits via Read/Edit file tools ONLY (Grep-confirmed the exact span string is unique to line 225 before the Edit; bash mount tail stale for pve.html — never edited through the mount); POLISH_LOG appended via a targeted Edit on the file-tool true tail.
- 17:24 2026-06-24 UTC — progression/pacing (build GOLD cost keeps pace with the tower — WORLD_DESIGN §3 "gold sinks meaningful not snowbally") — the build menu's 🗼 Arrow Tower cost a FLAT `g:90` gold at every difficulty (BUILDS ~5076; the only gold-priced build — 🧱 Wall/🌿 Heal Totem are wood-only) — the LAST flat gold sink, decaying exactly as the ladder climbs. Avg trash gold = 6+diff*1.5 (the forge comment's own normalizer ~5079), so a flat-90 tower was ~12 trash-kills-to-afford at diff1 but only ~4.6 at diff9 (income climbs ×2.6) — the sink trivialised at the top while the tower's HP (04:58 build-HP run) AND damage (06:30 tower-dmg run) were ALREADY vet-scaled to stay strong, so it became a cheap-AND-strong spam — the exact gap the forge sink fix (~5079) closed, on the sibling BUILD sink left flat after those two power runs. **Fix (ONE atomic, reversible change in the contiguous `BUILDS.forEach` block ~5099-5103):** captured `const _bg=bd.g?Math.round(bd.g*(6+((typeof SAVE!=='undefined'&&SAVE.diff)?SAVE.diff:1)*1.5)/7.5):0;` and routed all THREE reads (afford-check `bank.gold>=_bg`, live `_bg+'g'` display, spend `bank.gold-=_bg`) through it — scaled by the SAME income normalizer the forge uses, `(6+diff*1.5)/7.5`. MATH (node 27/27 /tmp/build_gold.js + region runtime 7/7): diff1 ×1.0 ⇒ 90 BYTE-IDENTICAL (early game §1 untouched), diff5 162, diff9 234 — holding the tower at a CONSTANT 12.0 trash-kills-to-afford at EVERY difficulty (the OLD flat 90 decayed 12.0→10.0→8.6→7.5→6.7→6.0→5.5→5.0→4.6); the `bd.g?…:0` guard keeps wood-only Wall/Totem at 0g BYTE-IDENTICAL (wall 50w / totem 70w untouched — wood income doesn't scale with difficulty); SAVE-undefined/diff<1 → ×1 fallback (=flat 90); all integer/positive/<200000 AC bound. NON-RUNAWAY: cost climbs with income, never with enemy power, so it's a constant-effort sink (not a wall) — restores the documented "meaningful not snowbally" calibration without trivializing or gating. Wild-only builds, but income scales with SAVE.diff even in the Wilds, so the sink DID decay there — same town-only-but-still-scaled precedent as the forge (forge is Emberhollow-only and was scaled anyway). No 📖 guide/wiki edit — the cost is shown LIVE in the build menu (`_bg+'g'`), never surfaced in docs (the wiki says "build towers/walls/heal totems" qualitatively, grep-confirmed no hardcoded "90"; same no-doc pattern as the forge-cost / gold-tier-scaling runs). Rotated to PROGRESSION/PACING — the OLDEST-touched bucket (most-recent per bucket: QoL 16:54 Kills-tooltip, balance 16:24 trash-gold, feel 16:01 pet-evolution audio, progression was last at 15:24 waypoint-gold) — and a NEW lever (the build/forge gold-sink axis on the BUILD side, the last flat sibling of the 06-23 forge fix; untouched by any recent run). — pve.html only (BUILDS.forEach ~5100-5103). — validation: node 27/27 (/tmp/build_gold.js: diff1≡90 byte-identical; wood-only g stays 0 ∀diff; exact diff5=162/diff9=234; monotonic non-decreasing; NEW holds ~12.0 kills-to-afford ∀diff while OLD decays 12.0→4.6 [the bug]; int/pos/bounded; SAVE-undefined→90 fallback) + region runtime 7/7 (/tmp/rc2.js: the edited forEach block VERBATIM over stubs — tower 234g@d9 & label "234g", wall/totem 0g & wood-only labels [no "g"], wall 50w/totem 70w untouched) + `node --check` on the verbatim edited block wrapped+stubbed = SYNTAX_OK. Edits via Read/Edit file tools ONLY (re-Read the exact 5098-5108 region before the Edit; bash mount tail stale/truncated for pve.html — never edited through the mount); POLISH_LOG appended via a targeted Edit on the file-tool true tail.
- 2026-06-26 08:58 UTC — NO-OP (window closed) — `tv-game-review` scheduled task ran but its STOP gate has passed: `date -u` = 2026-06-26T08:58:37Z, which is after the task's hard cutoff of 2026-06-25T01:10:00Z. Per the task's STOP directive, made NO edit to pve.html (or any game/systems file) this run. Disabled the task (taskId: tv-game-review, enabled=false) via the scheduled-tasks update tool so it will not auto-run again, and logged this closed window. End.
- 2026-06-27 01:06 UTC — NO-OP (window closed) — `tv-game-review` ran again; STOP gate still passed: `date -u` = 2026-06-27T01:06:46Z, well after the hard cutoff 2026-06-25T01:10:00Z. Made NO edit to pve.html or any game file. Re-asserted enabled=false on taskId tv-game-review via the scheduled-tasks update tool (the 06-26 08:58 run already disabled it; this run reconfirms in case of a manual trigger). End.
- 2026-06-27 06:54 UTC — NO-OP (window closed) — `tv-game-review` ran again; STOP gate still passed: `date -u` = 2026-06-27T06:54:10Z, well after the hard cutoff 2026-06-25T01:10:00Z. Made NO edit to pve.html or any game file. Re-asserted enabled=false on taskId tv-game-review via the scheduled-tasks update tool (already disabled since 06-26 08:58; this run reconfirms). End.
- 2026-06-27 09:29 UTC — NO-OP (window closed) — `tv-game-review` ran again; STOP gate still passed: `date -u` = 2026-06-27T09:29:50Z, well after the hard cutoff 2026-06-25T01:10:00Z. The task's `enabled` flag was found back at true (likely a manual trigger), so made NO edit to pve.html or any game file and re-asserted enabled=false on taskId tv-game-review via the scheduled-tasks update tool. End.
- 2026-06-27 13:54 UTC — NO-OP (window closed) — `tv-game-review` ran again; STOP gate still passed: `date -u` = 2026-06-27T13:54:10Z, well after the hard cutoff 2026-06-25T01:10:00Z. Made NO edit to pve.html or any game file. Re-asserted enabled=false on taskId tv-game-review via the scheduled-tasks update tool (the flag was found true again, consistent with a manual trigger). End.
