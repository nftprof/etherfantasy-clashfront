# EFFECTS_LOG

Log of effects/selection visual improvements to `index.html` (one small validated change per scheduled run).

- 2026-06-30T08:41Z — **Build-placement ghost → shared gradient-disc style (targeting lane).** The tower/expansion placement preview (`buildGhost`, ~L3351) was the last targeting/placement telegraph still drawn as a flat solid `RingGeometry(2.4,3.2)` with a hard edge, out of step with the AoE/line/arc aim previews which now use the soft-fill + bright-rim disc (`makeAoeTex`/`_aoeTex`). Swapped it to `CircleGeometry(1,40)` mapped with the shared `_aoeTex` (the SAME texture the ring/arc previews already use — no new texture), `depthWrite:false` + `renderOrder:2` to match `aimRing`, and adjusted the per-frame scale in `updateBuild()` from `r/2.8` to `r*1.15` so the footprint outer radius is unchanged (old ring outer ≈ r·3.2/2.8). Green (`0x55ff88`) valid / red (`0xff5555`) blocked tinting is preserved via the existing `material.color.setHex`. Now every placement/aim telegraph speaks one visual language: soft area fill + bright outline, not a flat ring. **Perf:** ONE pre-existing pooled mesh reused (no new mesh), REUSES the already-shared `_aoeTex` (zero new texture/geometry alloc), scale set inside the existing `updateBuild` per-frame call (no new loop/alloc); cosmetic client render/UI lane only — build placement is host/solo UI, no gameplay/net change. **Validation:** copied the `buildGhost` construction + `updateBuild` body into scratch `/tmp/buildghost_check_*.js` with stubbed THREE, `node --check` passed + asserted: geometry is a disc, material reuses `_aoeTex`, `depthWrite===false`, `renderOrder===2`, lies flat (rotation.x=−π/2), base footprint scales to r·1.15, and green/red tint toggles correctly on valid vs blocked.

- 2026-06-30T00:42Z — **Hero super (R) layered impact.** Added `fxSuperImpact(p,r,col)` helper (after `fxStructureFall`, ~L1731): wide team-tinted `ring_shock` ground decal + rising `glow_soft` energy dome + bright white `impact_star` core flash + `impact_lines` debris ring, scaled by super radius. Wired into `castSkill` super branch (ai===3, host path beside existing superKnock/fxRing) and the guest local-cast branch (super only). Previously the super only got a flat vector `fxRing`. **Perf:** reuses existing fx/ sprites + `fxs` pool (160 cap) + `fxLevel>=2` gate; no new textures/geometry; event-driven (one per super cast, ~handful/game); zero per-frame cost. **Validation:** copied helper to scratch `/tmp/fxsuper_check.js`, `node --check` passed + asserted callable with/without args; confirmed all 4 textures exist in fx/.
- 2026-06-30T06:42Z — **Hero SUPER (R) aim-telegraph danger pulse.** The ult preview already swapped to a red-orange colour vs the gold of a regular skill, but otherwise held a steady opacity like any basic ability. Made the SUPER telegraph *breathe*: in `updateAim()` (~L3416) added `_sp=0.83+0.17*Math.sin(performance.now()*0.008)` (range 0.66→1.0, ~1.3s period) applied as an opacity multiplier on whichever preview is active (`aimRing`/`aimLine`/`aimArc`), gated to `aimIdx===3` only — regular skills get multiplier 1 so their base opacity is preserved exactly. Added `AIM_OP={ring:.92,line:.6,arc:.55}` (the previews' base opacities) near `aimIdx`, and reset `aimRing.material.opacity=AIM_OP.ring` in the flash-aim (`amAim`) branch so a prior super-aim can't leave the ring stuck mid-dim. **Perf:** `updateAim()` already runs per-frame following the cursor and only ONE preview is visible at a time, so this is just a `sin()`+multiply on one material's opacity — no new alloc, geometry, texture, or library; cosmetic, client render/UI lane only; no gameplay/net change. **Validation:** copied `updateAim` + stubs to scratch `/tmp/aimpulse_check_*.js`, `node --check` passed + asserted: regular ring/line/arc opacity == base (×1), super ring opacity in [0.66,1.0]×base and visible, pulse oscillates >0.5 span over 2s, and the `amAim` branch restores base opacity after a dimmed super frame; separately verified the 0.83±0.17 band yields 0.660→1.000.
- 2026-06-30T08:04Z — **YOUR-HERO self beacon (selection lane).** Added an always-on, distinct CYAN (0x66e0ff) thin ground ring under the player's own `pHero`, separate from the green selection ring (which only shows while selected). Lets you locate your hero at a glance even after box-selecting creeps elsewhere. New `getHeroSelfMark()` (lazy single mesh, RingGeometry 1.18–1.4, depthWrite:false, renderOrder 5) beside `getSelRing`; `updateSelRings()` now updates it at the end of its existing per-frame loop — sits just outside the selection footprint (s=pHero.h*0.66), gentle opacity pulse 0.34→0.50, hidden when pHero is dead/absent. Cyan reads as "you" vs green "selected"; the two rings nest cleanly when the hero is selected. **Perf:** ONE pooled reused mesh, no new loop (rides the existing updateSelRings per-frame pass), no per-frame alloc/texture/geometry; cheap MeshBasic ring; cosmetic client render/UI lane only, no gameplay/net change (guests just don't draw it if they skip updateSelRings — feature-safe). **Validation:** copied `getHeroSelfMark`+`updateSelRings`+stubs to scratch `/tmp/selfmark_check_*.js`, `node --check` passed + asserted: visible & opacity in [0.34,0.50] when hero alive, distinct mesh & cyan tint vs selection ring, hidden when hero dead and when pHero null, and pooled (same mesh reused across frames).
olor:col})`, default gold `0xffe06a` when `col` undefined), fired as the orb spawns — mirrors the basic-attack muzzle flash so casts read with the same launch punch. ~L2173. **Perf:** purely cosmetic, client render/FX lane; one pooled billboard per cast, hero-only (≤2 heroes at once), event-driven (one per cast); reuses the existing `flash_blue` texture (confirmed present in fx/) + `fxBill`'s `fxLevel>=2` gate + `fxs` 160-cap pool; no new textures/geometry/library, zero per-frame allocation, no gameplay/net change. **Validation:** copied edited `castAt` to scratch `/tmp/outputs/fxcastat_check.js` with mocked THREE/scene/projs/fxBill, `node --check` + ran asserts: hero cast → exactly one `flash_blue` billboard tinted by skill `col`; undefined `col` → default gold; non-hero caster → no flash; projectile still spawned in all three paths.
- 2026-06-30T02:24Z — **Move-order click ping (command feedback).** Right-click MOVE orders only showed the flat vector `fxRing` at the destination — no textured confirmation, unlike the now-polished combat bursts. Added `fxMovePing(p,col)` helper (after `fxNovaBurst`, ~L1756): one soft expanding `ring_shock` ground decal (size 2.6, opacity .6, grow 1.5, short 0.42s life) tinted by order kind, layered ON TOP of the existing `fxRing` (which stays as the always-on low-FPS-tier base). Wired into both player move destinations: the online-guest move branch (`fxMovePing(pt,0x55ff77)`, ~L2778) and the host/solo hero move branch (`fxMovePing(dest,rec?gold:shift?blue:green)`, ~L2799) so queued (shift) / recruit-walk / plain moves read with their existing colour. Attack/gather/repair orders left unchanged. **Perf:** purely cosmetic, client render/UI lane; one pooled ground sprite per right-click (throttled by the human's clicks, event-driven), reuses the existing `ring_shock` texture (confirmed present in fx/) + `fxGround`'s `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (guest path still sends the same `netSendInput` move). **Validation:** copied helper to scratch `/outputs/fxmoveping_check.js` with mocked `fxGround`, `node --check` + asserts passed: exactly one `ring_shock` decal at `p` tinted by `col`, default green when `col` omitted, gated to 0 decals at `fxLevel>=2`, and reads `x,z` off a grp.position-style dest.
- 2026-06-30T02:41Z — **Selected-unit ring pulse/breath.** The selection rings (`getSelRing`/`updateSelRings`, ~L2667) were static flat rings (fixed opacity .85, fixed scale) — readable but lifeless next to the now-polished combat FX. Added a zero-allocation pulse in `updateSelRings`: a `gameT`-driven phase `ph=gameT*5+i*0.6` drives a subtle opacity breath (`0.7+0.25*sin(ph)`) and a tiny scale breath (`*(1+0.04*sin(ph))`); the `i*0.6` per-ring phase offset gives a gentle shimmer across multi-select so a box-selected group reads as alive. `gameT` is `typeof`-guarded (falls back to 0) so the ring still shows if `gameT` is undefined. **Perf:** purely cosmetic, client render/UI lane; mutates only existing per-ring material.opacity + scale inside the already-per-frame `updateSelRings` loop — NO new allocation (no Vector3/material/geometry/texture created per frame), no new library, no gameplay/net change. **Validation:** copied `updateSelRings`+mocked `getSelRing`/rings to scratch `/outputs/fxselring_check.js`, `node --check` + asserts passed: 2 selected units → both visible, opacity within [0.45,0.95] & scale>0, the two rings differ same-frame (phase offset works), a dead unit hides its ring, and the `gameT===undefined` path still shows the ring.
- 2026-06-30T03:02Z — **Super-knockback per-victim impact spark.** `superKnock` (the super-only AoE knockback, `castSkill` ai===3) threw enemies with NO per-victim visual — only the central `fxSuperImpact` fired at the cast point. Added a debris-scatter burst at each knocked unit's body (`fxBill(_vp.x,(v.h||3)*0.6,_vp.z,'impact_lines',{size:2.8,life:0.3,grow:1.5,color:_kc,opacity:.9})`) inside the in-range branch, tinted by the caster's `heroColor` (default gold `0xffe06a`) so launched enemies read as physically hit by the ult — matching the central super impact. ~L2176. **Perf:** purely cosmetic, client render/FX lane; one pooled billboard per knocked unit (only a handful in an AoE), event-driven (super casts only, host path), reuses the existing `impact_lines` texture (confirmed present in fx/, used by fxHit/fxSuperImpact/fxStructureFall) + `fxBill`'s `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (knockback itself unchanged — guests get it via snapshots). **Validation:** copied edited `superKnock` to scratch `/tmp/fxsuperknock_check.js` with mocked `fxBill`/`knockUnit`/units, `node --check` + asserts passed: 2 in-range enemies → exactly 2 sparks (ally/out-of-range/dead all skipped), each `impact_lines` tinted by hero colour with height = `v.h*0.6`; src without heroColor → default gold; undefined `v.h` → 3*0.6 fallback.
- 2026-06-30T03:22Z — **Attack-order target-lock ping (command feedback).** Right-click ATTACK orders only showed the flat vector `fxRing(en,0xff5555,2)` — no textured confirmation, unlike MOVE orders which got `fxMovePing` last run. Added `fxAtkPing(v,col)` helper (right after `fxMovePing`, ~L1769): a sharp red `ring_shock` ground ring at the target's feet + a small bright `impact_star` lock spark at the body (`(v.h||3)*0.6` height), tinted red, layered ON TOP of the existing `fxRing` (kept as the always-on low-FPS-tier base). Wired into all three attack-order spots: the online-guest atk branch (~L2796), the soldier/worker group `else if(en)` branch (~L2810), and the host/solo hero attack branch (~L2815) — each now `fxAtkPing(en,0xff5555)` beside its existing red `fxRing`. Move/gather/repair pings unchanged. **Perf:** purely cosmetic, client render/UI lane; two pooled sprites per right-click-attack (event-driven, throttled by the human's clicks), reuses existing `ring_shock`+`impact_star` textures + `fxGround`/`fxBill` `fxLevel>=2` gates + `fxs` 160-cap pool; null/no-grp guarded; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (guest path still sends the same `netSendInput` atk). **Validation:** copied helper to scratch `/tmp/fxatk_check.js` with mocked `fxGround`/`fxBill`, `node --check` + asserts passed: exactly 2 sprites (`ring_shock` ground + `impact_star` bill) tinted by `col` at the target, spark height = `v.h*0.6`, default red `0xff5555` + `h=3` fallback when omitted, null/`{}` target → 0 sprites, and `fxLevel>=2` → 0 sprites.
- 2026-06-30T03:42Z — **Hero dash-streak (E-mobility skills).** The `dash` E-abilities (Ember Rush / Tide Dash / Vine Dash / Burrow Dash) just snapped the hero to the end point with one `fxRing` — reading like an instant teleport, not a slide. Added `fxDashStreak(from,to,col)` helper (after `fxNovaBurst`, ~L1756): an element-tinted `kai_slash` ground motion-streak laid along the dash path (stretched via `aspect=min(6,dl/2.4+1)`, oriented `atan2(-dz,dx)` like `fxSlash`) + a soft `glow_soft` departure puff at the start point. Wired into `dash()` (~L2200): captures the start pos BEFORE the move, fires only for a TRUE dash (`!exact` — blink/Void-Shift teleports keep their pop rings), hero-only, and only when `d>1` (skips no-op dashes), tinted by `h.heroColor` (default `0x88ccff`). **Perf:** purely cosmetic, client render/FX lane; 2 pooled sprites per dash cast, event-driven, host/solo only (guests get visuals via snapshots); reuses existing `kai_slash`+`glow_soft` textures (confirmed present in fx/) + `fxGround`/`fxBill` `fxLevel>=2` gates + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (dash movement unchanged). **Validation:** copied `fxDashStreak`+`dash` to scratch `/tmp/fxdash_check.js` with mocked THREE/fxGround/fxBill, `node --check` + asserts passed: true hero dash → exactly 2 fx (`kai_slash`+`glow_soft`) tinted by kit colour, streak `rot` toward travel dir; blink (`exact`) → 0; sub-1 dash → 0; non-hero → 0; `fxLevel>=2` → 0.
- 2026-06-30T04:02Z — **Hero hit-spark element tint (basic + ranged).** The melee swing decal was element-tinted (fxSlash) but the matching impact burst (`fxHit`, ~L1692) was always white on a normal hit. Added an optional 3rd `col` param to `fxHit(v,big,col)` that tints ONLY the normal `impact_lines` spark; the super-effective `impact_star` stays the distinct gold `0xffd27a` advantage signal, and `col` undefined keeps the legacy white spark so all non-hero callers are unchanged. Passed `u.heroColor` at the hero melee branch in `tryAttack` (~L2065, beside the existing element-tinted fxSlash) and `src.heroColor` at the ranged-hero impact in `dmgUnit` (~L1882) so both melee and arrow/orb hero hits now spark in their element (fire/water/etc.). The non-hero projectile-impact `fxHit` at ~L4417 (tower/minion fire) deliberately left untinted → stays white. **Perf:** purely cosmetic, client render/FX lane; mirrors the proven fxSlash tint pattern — no new texture/geometry/library, no signature change for legacy callers; reuses the existing `impact_lines`/`impact_star` textures + `fxBill`'s `fxLevel>=2` gate + `fxs` 160-cap pool; event-driven (hero hits only, ≤2 heroes at once), zero per-frame allocation, no gameplay/net change. **Validation:** copied edited `fxHit` to scratch `/outputs/fxhit_check.js` with mocked `fxBill`, `node --check` + asserts passed: hero normal hit → 1 `impact_lines` spark tinted to the passed element col at height `h*0.6`; super-effective → `impact_star` stays gold even when col passed; col omitted → legacy white; undefined `v.h` → `3*0.6` fallback.
- 2026-06-30T04:23Z — **Hero projectile in-flight trail.** Hero shots had a muzzle/cast flash at launch (`shoot`/`castAt`) and an impact spark on hit (`fxHit`), but NOTHING in between — orbs/bolts just slid silently. Tagged hero-fired projectiles with a trail tint `_tc` at spawn: `shoot()` push (~L2057) sets `_tc:src.kind==='hero'?c:undefined` (the projectile's own colour `c`), and `castAt()` push (~L2222) sets `_tc:h.kind==='hero'?(col!==undefined?col:0xffe06a):undefined` (skill `col`, default gold). In the per-frame projectile loop (~L4422, right after the position advance) added a self-throttled trail: `if(pr._tc!==undefined){pr._tt=(pr._tt||0)-dt;if(pr._tt<=0){pr._tt=0.05;fxBill(pos,'glow_soft',{size:1.3,life:0.24,grow:0.4,color:pr._tc,opacity:.5})}}` — a faint element-tinted puff dropped along the path (~one / 0.05s). **Perf:** purely cosmetic, client render/FX lane; hero-only (`_tc` set only for hero src → ≤ a few in flight at once), self-throttled per-projectile so non-hero shots cost only one subtract+compare (zero alloc, no fx), reuses existing `glow_soft` tex + `fxBill`'s `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, no gameplay/net change (projectile motion/damage untouched; guests get visuals via snapshots). **Validation:** copied tagging logic (shoot/castAt) + the loop snippet to scratch `/outputs/fxtrail_check.js` with a mocked `fxBill`, `node --check` + asserts passed: hero shot tagged with proj colour, minion shot untagged, hero cast → default gold / tinted / non-hero untagged; 10 frames @60fps → ~3-4 `glow_soft` puffs all tinted to `_tc`; untagged projectile → 0 puffs; `fxLevel>=2` → 0 puffs.
- 2026-06-30T04:42Z — **Hero super (R) cast WINDUP at the caster.** The ultimate already had a layered IMPACT at the target (`fxSuperImpact` at `pt`), but the cast moment at the HERO got only the same generic `flash_blue` flash every skill gets — so an ult didn't read as a distinct "power up HERE → impact lands THERE" beat. Added `fxSuperCast(p,col)` helper (right after `fxSuperImpact`, ~L1749): a team-tinted `ring_shock` charging ground ring at the hero's feet + a tall rising `glow_soft` power column + a bright white `impact_star` core flash. Wired into `castSkill` inside the `ai===3` block (~L3285), placed BEFORE the host/guest split (beside `sfxSuper`/`addShake`/`speakName`) so it fires exactly once for both the guest local-cast and the host path, tinted by `pHero.heroColor` (default gold `0xffe06a`). **Perf:** purely cosmetic, client render/FX lane; 3 pooled sprites per super cast (a handful/game), event-driven, fired at the caster; reuses existing `ring_shock`/`glow_soft`/`impact_star` textures (all confirmed present in fx/) + `fxGround`/`fxBill` `fxLevel>=2` gates + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change. **Validation:** copied helper to scratch `/outputs/fxsupercast_check.js` with mocked `fxGround`/`fxBill`, `node --check` + asserts passed: tinted call → exactly 3 sprites (`ring_shock` ground + `glow_soft` + `impact_star`) at the hero, ring+column tinted by `col` while the core stays white, default gold when `col` omitted, and `fxLevel>=2` → 0 sprites; re-read the wired `castSkill` region (3281-3288) — well-formed, fires once before the NET split.
- 2026-06-30T05:03Z — **Cursor hover-highlight ring (selection visual).** Units had selection rings (`updateSelRings`) and a now-pulsing breath, but NO hover feedback — you couldn't read what a click would pick/target before clicking. Added `getHoverRing()` (one lazily-built reusable thin `RingGeometry(1.0,1.18,28)` mesh, depthWrite:false, renderOrder 6, sits just OUTSIDE the selection ring) + `updateHoverRing()` (right after `updateSelRings`, ~L2716): on hover it shows on the unit under the cursor, tinted by relationship (ally `0x6effa0` / enemy `0xff6666` / wild `0xffd34d`), scaled `h*0.62`, and is SKIPPED for already-selected units (no double-ring) and dead/no-unit (hidden). Wired into the existing `mousemove` handler (~L3207) — `updateHoverRing()` after the `intersectPlane(gp,mouseWorld)` — reusing the already-computed `mouseWorld` + the cheap bounded `pickUnit` (whose `bd=5` threshold means it only lights up near a unit). **Perf:** purely cosmetic, client render/UI lane; ONE reusable mesh repositioned event-only on mousemove (browser-throttled, no per-frame loop), reuses `pickUnit` (bounded `units` loop, no allocation) + `mouseWorld`; mutates only the existing mesh's visible/scale/position/color — zero per-frame allocation, no new texture/geometry-per-frame/library, no gameplay/net change (read-only pick). **Validation:** copied `getHoverRing`/`updateHoverRing` + the real `pickUnit` to scratch `/outputs/fxhover_check.js` with mocked THREE/scene, `node --check` + 11 asserts passed: empty units → hidden; enemy → visible+red, scale `h*0.62`, y=0.1, mesh reused across calls (no realloc); ally → green; wild → gold; cursor far (beyond pickUnit threshold) → hidden; already-selected unit → skipped; dead unit → hidden.
- 2026-06-30T05:24Z — **Hero W self-buff aura (empower skills).** The W-slot self-buff (`abBuff`, ~L2131 — War Cry / Overcharge / Swarm / Fortify / Tailwind / Rally) only showed the flat vector `fxRing(h.grp.position,col,4)` on cast — none of the polish the combat/super/nova bursts now have, so empowering yourself read as a non-event. Added `fxBuffAura(p,col)` helper (right after `fxSuperCast`, ~L1755): a tight team/element-tinted `ring_shock` empowerment surge ring at the feet + a tall rising `glow_soft` aura column ("powering up") + a bright white `impact_star` core spark at the chest, tinted by the buff colour (default gold `0xffe06a`). Wired into `abBuff.f` beside the existing `fxRing` (kept as the always-on low-FPS-tier base). Deliberately lighter than `fxSuperCast` (it's a W, not an ult): 3 small sprites, shorter life. **Perf:** purely cosmetic, client render/FX lane; 3 pooled sprites per buff cast, hero-only (`abBuff` is a hero W), event-driven; reuses existing `ring_shock`/`glow_soft`/`impact_star` textures (all confirmed present in fx/) + `fxGround`/`fxBill` `fxLevel>=2` gates + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (`h.hasteT=5` buff unchanged; guests get visuals via snapshots). **Validation:** copied helper + `abBuff` to scratch `/outputs/fxbuffaura_check.js` with mocked `fxGround`/`fxBill`/`fxRing`, `node --check` + asserts passed: tinted buff cast → base `fxRing` kept + exactly 3 textured sprites (`ring_shock` ground + `glow_soft` column tinted by col, `impact_star` core stays white) at the hero, `hasteT` still applied; default gold when col omitted; `fxLevel>=2` → 0 aura sprites.
- 2026-06-30T05:41Z — **Super (R) aim telegraph danger-colour.** Every skill's aim preview (`updateAim`, ~L3379) drew with the same gold rim (`0xffe06a`) — the ultimate's landing telegraph was indistinguishable from a basic ability while aiming. Added a per-skill aim colour `_aimCol=(aimIdx===3)?0xff5a3c:0xffe06a` (hot red-orange for the super, gold for everything else) and applied it via `material.color.setHex(_aimCol)` to whichever of the three previews is shown — `aimRing` (circle AoE), `aimLine` (line skills), `aimArc` (Kai's cleave cone) — so the ult reads as a distinct DANGER telegraph in all three preview styles. Removed the old unconditional `aimRing.material.color.setHex(0xffe06a)` reset (now handled inside each branch); the a-move orange (`0xffa94d`, returns early) is untouched. **Perf:** purely cosmetic, client render/UI lane; just a `setHex` on the EXISTING preview material the loop already mutated — no new geometry/texture/material/library, zero added allocation, no gameplay/net change. **Validation:** copied `updateAim` to scratch `/tmp/aim_check.js` with mocked ring/line/arc meshes, `node --check` + 7 asserts passed: regular ring/line → gold; super ring/line/arc → `0xff5a3c`; a-move → orange preserved (early return); no-hero → all three previews hidden, no throw.
- 2026-06-30T06:05Z — **Hero takedown-confirm spark (killer-side kill feedback).** A hero death fired `fxDeathBurst` at the VICTIM, but the KILLER got no on-body visual confirming the takedown — only audio/banners. Added `fxTakedown(p,col)` helper (right after `fxAtkPing`, ~L1813): a team-tinted `ring_shock` victory ring at the killer's feet + a bright warm-white `impact_star` kill-confirm spark, so a takedown reads AT the killer (the recognised MOBA kill-confirm pop), not only as the death burst at the victim. Wired into the hero-kill block (`if(killer&&killer.kind==='hero'){killer.kills...}`, ~L1975) as `fxTakedown(killer.grp.position,killer.team===T.B?0xffd34d:0xff8866)` — gold ring for YOUR kills, warm-red for the enemy's; the star core stays a constant warm white. `killer.grp`-guarded. **Perf:** purely cosmetic, client render/FX lane; 2 pooled sprites per hero takedown (rare, event-driven — fires only on a hero killing a hero), reuses existing `ring_shock`+`impact_star` textures (both confirmed present in fx/) + `fxGround`/`fxBill` `fxLevel>=2` gates + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (kill logic untouched; guests get visuals via snapshots). **Validation:** copied helper to scratch `/outputs/fxtakedown_check.js` with mocked `fxGround`/`fxBill`, `node --check` + asserts passed: player kill → exactly 2 sprites (`ring_shock` gold ring at killer + `impact_star` warm-white core at y=3.2); enemy kill → red ring, core unchanged; default gold when col omitted; null pos → 0 sprites; `fxLevel>=2` → 0 sprites.
- 2026-06-30T06:22Z — **Hero line-skill beam element tint.** The piercing line Q (`lineShot`, ~L2258 — Chain Bolt / Dragon Breath / Ice Shard / Gust / Seed Barrage / Sting Volley) fired a generic pale-green `fxBeam(...,0xaaffcc)` regardless of element — the last hero cast type still NOT element-tinted, unlike the already-tinted muzzle flash (`shoot`/`castAt`), in-flight projectile trail, `fxNovaBurst`, and `fxSlash`. Tinted the beam by the hero's kit colour: `fxBeam(...,(h&&h.heroColor!==undefined?h.heroColor:0xaaffcc))` — colour pulled from `h.heroColor` (NO new param, since the shared `EF_CORE.buildKit` calls this same `lineShot` primitive with 4 args), so both kit paths (shared module + inline fallback) and host/solo benefit; guests get the visual via snapshots. Pale-green retained as the fallback when a caster has no `heroColor`. **Perf:** purely cosmetic, client render/FX lane; zero new allocation — `fxBeam` already built the beam mesh, only the colour arg changes; no new texture/geometry/library, event-driven (one per line cast), zero per-frame cost, no gameplay/net change. **Validation:** copied edited `lineShot` to scratch `/tmp/linebeam_check.js` with mocked THREE/fxBeam/dmgUnit, `node --check` + asserts passed: hero with `heroColor` → beam tinted to it (`0xffe04a`); hero without `heroColor` → pale-green `0xaaffcc` fallback.
- 2026-06-30T01:41Z — **Hero basic-attack slash element tint.** The melee hero swing decal (`fxSlash` → `kai_slash` ground sprite) was always white, giving no per-element flavor on basics. Added an optional 5th `col` param to `fxSlash(u,tx,tz,name,col)` that passes through to the `fxGround` decal's `color` (undefined keeps the original white, so all other behavior is unchanged); the sole caller — the hero melee branch in `tryAttack` (~L2034) — now passes `u.heroColor` (the hero's kit/element colour), so both the player and enemy hero's swings now read as their element (fire/water/etc.). **Perf:** purely cosmetic, client render/FX lane; reuses the existing `kai_slash` texture + `fxGround`'s `fxLevel>=2` gate + `fxs` pool; no new textures/geometry/library; event-driven (hero attacks only, ≤2 heroes at once); zero per-frame allocation. **Validation:** copied `fxSlash` to scratch `/tmp/fxslash_check.js` with a stub `fxGround`, `node --check` passed + asserted the tint is forwarded when `col` given and stays `undefined` (white) for legacy no-col calls.
- 2026-06-30T07:02Z — **Queued-waypoint marker path readability.** The SHIFT-queued waypoint rings (`updateQueueMarkers`/`getQMark`, ~L2910) all rendered with the same fixed opacity (.7) and scale regardless of position in the queue — readable but the player couldn't tell which pending order fires NEXT vs which is last in the chain. Made the path read in order: each marker now gets a sequential opacity falloff down the queue (`op=Math.max(0.3,0.78-mi*0.06)`, floored at .3 so deep queues stay visible), and the NEXT/active waypoint (`mi===0`) breathes (`op*=0.78+0.22*Math.sin(gameT*5)`) and sits a touch larger (`scale.setScalar(1.18)` vs 1) so the eye snaps to where the unit goes first. `gameT` is `typeof`-guarded (falls back to 0). Per-order colour tint (attack red / gather gold / repair / amove orange / move blue) unchanged. **Perf:** purely cosmetic, client render/UI lane; mutates only existing pooled-ring `material.opacity`+`scale` inside the already-per-frame `updateQueueMarkers` (≤QMAX=24 rings, only when a queue exists) — NO new allocation (no Vector3/material/geometry/texture per frame), no new library, no gameplay/net change. **Validation:** copied `updateQueueMarkers`+mocked `getQMark`/pool to scratch `/outputs/fxqmark_check.js`, `node --check` + 30 asserts passed: 4-order queue → all visible, idx0 scale 1.18 & later 1, monotonic opacity falloff idx1>idx2>idx3, per-order colour tints correct; idx0 pulse span >0.1 oscillating within (0,0.78] over 2s; 20-deep queue → every marker ≥0.3 floor; empty queue → all hidden; falls back to first selected unit with a queue.
- 2026-06-30T07:21Z — **Control-group assign confirmation (selection visual).** Binding a control group with Ctrl/Alt+digit (`keydown` handler, ~L3570) only gave a feed line + `sfxClick` — no on-unit confirmation that the bind took, unlike the now-polished MOVE/ATTACK pings. Added `fxGroupAssign(list,col)` helper (right after `fxTakedown`, ~L1823): a quick soft blue `ring_shock` ground pop at each bound unit's feet, tinted the same `0x9bd1ff` as the feed message, capped to the first 12 units (`GROUP_PING_CAP`) so a large box-selected group can't flood the pool; dead/no-`grp` units skipped. Wired into the assign branch beside `feed(...)`/`sfxClick()` as `fxGroupAssign(list)`. **Perf:** purely cosmetic, client render/UI lane; ≤12 pooled ground decals per bind (event-driven, throttled by the keypress, player-issued only), reuses the existing `ring_shock` texture + `fxGround`'s `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (bind logic untouched; guest path already returns early). **Validation:** copied helper to scratch `/tmp/fxgroup_check.js` with a mocked `fxGround`, `node --check` + asserts passed: 3 live units → 3 `ring_shock` pops tinted default blue at each unit pos; dead/no-grp/null skipped (4 in → 1 out); 20-unit group → capped at 12; empty/null list → 0; custom col forwarded; `fxLevel>=2` → 0 pops.
- 2026-06-30T07:41Z — **Hero super (R) projection beam (caster→impact link).** The ult had a windup beat at the caster (`fxSuperCast`) and a layered impact at the target (`fxSuperImpact`), but nothing connected them — a ranged ult cast across the map didn't read as the hero *projecting* power to a distant point. Added `fxSuperLink(from,to,col)` helper (right after `fxSuperCast`, ~L1767): a brief team-tinted additive `BoxGeometry(0.5,0.5,d)` bolt (mirrors `fxBeam`) oriented caster→impact via `lookAt`, on a short pooled life (`t:0.3,s:0.4`). Skips self-centred ults — returns early when the impact is within ~6u of the caster (`dx*dx+dz*dz<36`) so it never draws a degenerate stub. Wired into `castSkill`'s `ai===3` block beside `fxSuperCast` (~L3350), BEFORE the host/guest split, so both guest local-cast and host fire it once, tinted by `pHero.heroColor` (default gold `0xffe06a`). **Perf:** purely cosmetic, client render/FX lane; ONE pooled mesh per super cast (a handful/game), event-driven, reuses the `fxBeam`-style box + `fxs` 160-cap pool + an explicit `fxLevel>=2` gate; no new texture, no per-frame allocation, no gameplay/net change. **Validation:** copied the helper to scratch `/outputs/fxlink2.js` with mocked THREE/scene/fxs/killObj, `node --check` + 14 asserts passed: ranged ult → exactly 1 pooled additive beam, length == caster→impact distance, centred between the two points, tinted by hero colour, renderOrder 9, life `t:0.3`/`s:0.4`; self-centred (<6u) → nothing drawn while just past 6u draws it; `fxLevel>=2` → gated to 0; default gold when col omitted; respects the ~160 fxs cap and evicts oldest via `killObj`. Re-read the wired `castSkill` region (3347-3350) — well-formed, fires once before the NET split.
- 2026-06-30T08:24Z — **YOUR-HERO self-beacon LOW-HP danger read (selection/awareness lane).** The always-on cyan self-beacon ring (`heroSelfMark` in `updateSelRings`, ~L2784) held the same cyan tint + gentle pulse regardless of your hero's health, so a near-death hero gave no peripheral warning. Made the beacon react to health: when `pHero.hp/pHero.maxHp < 0.3` it flips from cyan `0x66e0ff` to a warm-red `0xff4d4d` with a faster, deeper pulse (`sin(_t*7)`, opacity band 0.15→0.75) vs the calm cyan band (0.18→0.50, `sin(_t*2.2)`) — so a low hero is obvious at a glance even at the screen edge. Pure per-frame mutation of the EXISTING pooled mesh's `material.color`/`opacity` (set fresh each frame, so it flips back to cyan the instant health recovers); `maxHp>0` guarded so a 0-maxHp hero stays cyan (no div-by-zero false alarm). **Perf:** purely cosmetic, client render/UI lane; rides the already-per-frame `updateSelRings` self-mark block — NO new allocation (no Vector3/material/geometry/texture/library per frame), just one fraction + `setHex` + `sin` on the existing mesh; no gameplay/net change (reads `hp`/`maxHp` read-only; guests just don't draw it if they skip `updateSelRings`). **Validation:** copied `updateSelRings`+mocked meshes/pHero to scratch `/outputs/selfmark_hp_check.js`, `node --check` + 11 asserts passed: healthy → cyan & opacity in [0.18,0.50]; 20% hp → red, opacity in [0.15,0.75], pulse span >0.4; exactly 30% stays cyan while 29.9% flips red (strict `<0.3`); dead/`null pHero` → hidden; `maxHp===0` → stays cyan (no div-by-zero red).
- 2026-06-30T09:24Z — **Box-select confirmation pop (selection lane).** A single left-click on a unit already gets a confirm `fxRing` (`leftClickAt`, ~L2878), but a drag BOX-SELECT (`boxSelect`, ~L2862) gave NO per-unit visual — the only feedback was the rings appearing via `updateSelRings`. Wired one call to the existing `fxGroupAssign(picked,0x6effa0)` at the end of `boxSelect` (after `selUnits=picked;inspect;buildUnitPanel`), tinted the SAME marquee-green (`0x6effa0`) as the `#selbox` drag rectangle border + selection rings, so every unit that lands in the box answers with a quick soft `ring_shock` ground pop. Reused the already-capped (`GROUP_PING_CAP=12`) + `fxLevel`-gated `fxGroupAssign` helper rather than adding a new one — zero new code path, identical proven pattern to the control-group-assign pop, just a different tint. **Perf:** purely cosmetic, client render/UI lane; ≤12 pooled `ring_shock` ground decals per box-select (event-driven, throttled by the human's drag-release, host/solo only — `boxSelect` returns at top for guests), reuses the existing `ring_shock` texture + `fxGround`'s `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (selection logic untouched). **Validation:** copied `fxGroupAssign` + the edited `boxSelect` with mocked THREE/units/clearSel/inspect to scratch `/outputs/boxsel_check_*.js`, `node --check` + 6 asserts passed: 3 units in box → exactly 3 `ring_shock` pops all tinted `0x6effa0`; empty selection → 0 pops + `deselectAll`; guest mode → early return, 0 pops; 20-unit group → capped at 12; `fxLevel>=2` → 0 pops; dead/null units skipped inside the pop loop (3 in → 1 out).
- 2026-06-30T09:02Z — **Hero SUPER (R) impact leading-edge shockwave front (double-boom).** `fxSuperImpact` (~L1743 — the ult landing burst) fired its ground shock, energy dome, core flash and debris ring all at the SAME radius, so a super read as a single flat pop with no sense of a blast pushing outward. Added ONE pooled `ring_shock` ground decal right after the inner team-tinted ring: a wider white leading edge (`size:6.4*sc` vs `5.0*sc`), faster expansion (`grow:2.4` vs `1.6`) and lower opacity (`.5` vs `.9`) so the white front visibly OUTRUNS the inner colour ring → a two-stage "double boom" reading as the blast radius shoving out. White (not team-tinted) so it reads as the raw shock edge regardless of element; scales with the super radius `r` like the rest of the burst; layered on top of the existing 4 sprites + the central `fxRing`/`addShake`/`superKnock` so the big-moment beat just got deeper, not busier. **Perf:** purely cosmetic, client render/FX lane; ONE extra pooled ground sprite per super cast (a handful/game), event-driven, reuses the existing `ring_shock` texture (confirmed present in fx/, already used by the inner ring) + `fxGround`'s `fxLevel>=2` gate (function returns at top before any sprite) + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (guests get visuals via snapshots/their own `fxSuperImpact` call). **Validation:** copied the edited `fxSuperImpact` to scratch `/outputs/fxsuperimpact_echo_check.js` with mocked `fxGround`/`fxBill`, `node --check` + 14 asserts passed: tinted super → exactly 2 `ring_shock` rings where the outer is larger, grows faster, is fainter, is white, and reaches a larger end radius (size×grow) than the inner — i.e. it outruns it; inner keeps the element tint; the 3 original billboards (dome/core/debris) unchanged; default col → inner gold while outer stays white; `fxLevel>=2` → 0 sprites.
- 2026-06-30T09:42Z — **Hero basic melee ALTERNATING swing side (L/R combo read).** The hero melee swing decal (`fxSlash`→`kai_slash`, ~L1686) always laid the same streak centered in front of the attacker oriented straight at the target, so a flurry of basics looked like the *same* decal stamped twice — no sense of a real combo. Added an optional 6th `side` param (+1/-1): the streak is shoved laterally to the attacker's left/right (perpendicular `(-dz,dx)` of facing × `s*0.9`) and its Y-orientation tilted `±0.5` rad (~28°), so consecutive swings sweep from OPPOSITE sides. `side` undefined/0 = the exact legacy centered swing, so every non-hero caller is unchanged. Caller: the hero melee branch in `tryAttack` (~L2131) now flips a per-unit `u._slashSide` (1→-1→1…) each swing and passes it beside the existing `u.heroColor` tint, so the player's (and enemy hero's) basics read as a left-right combo. **Perf:** purely cosmetic, client render/FX lane; NO extra sprite — same single `kai_slash` ground decal, just a lateral offset + rot tilt from values already in hand (two mults), zero new texture/geometry/material/alloc; reuses `fxGround`'s `fxLevel>=2` gate + `fxs` 160-cap pool; event-driven (hero melee hits only, ≤2 heroes at once), no per-frame cost, no gameplay/net change (damage/targeting untouched; guests get visuals via snapshots). **Validation:** copied `fxSlash` + the caller toggle to scratch `/outputs/fxslash_side_check.js` with a mocked `fxGround`, `node --check` + 11 asserts passed: legacy (no side) → exactly one decal, zero lateral offset, base rot, tint forwarded; `side+1` → center shoved +0.9 perpendicular & rot `+0.5`; `side-1` → shoved -0.9 & rot `-0.5` (opposite of +1, differ same-target); per-unit toggle alternates `1,-1,1,-1`.

- 2026-06-30T10:08Z — **Hero SUPER (R) impact lingering AFTER-GLOW (settle/aftermath beat).** `fxSuperImpact` (~L1748 — the ult landing burst) was all SHARP, short-lived flashes: every one of its 5 sprites lives ≤0.6s, so the impact point goes from a big bright pop straight to nothing with no sense of energy *settling* into the ground. Added ONE pooled `glow_soft` GROUND decal as a 6th element: a soft team/element-tinted residue (`size:3.4*sc`, `y:0.11` so it sits LOWEST, under the two shock rings; `grow:0.2` so it barely expands; `life:0.95` so it fades over ~3x the bursts above). Because the shared fxs fade loop sets `opacity=t*1.8` purely from remaining life (the `opacity` option only seeds frame 0), a longer life is exactly what produces a slow fade — the impact point keeps glowing as the sharp dome/core/debris/double-boom flashes clear, reading as the blast's heat/energy lingering. Scales with super radius `r` via the existing `sc` and is tinted by `col` (default gold `0xffe06a`) like the rest of the burst. **Perf:** purely cosmetic, client render/FX lane; ONE extra pooled ground sprite per super cast (a handful/game), event-driven, reuses the existing `glow_soft` texture (confirmed present in fx/, already used by this function's energy dome) + `fxGround`'s `fxLevel>=2` gate (function returns at top before any sprite) + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (guests get visuals via their own `fxSuperImpact` call). **Validation:** copied the edited `fxSuperImpact` to scratch `/outputs/fxsuper_afterglow_check.js` with mocked `fxGround`/`fxBill`, `node --check` + 12 asserts passed: tinted super → 3 ground + 3 bill (the 5 originals UNCHANGED + the 1 new after-glow); after-glow uses `glow_soft`, is `col`-tinted, sits lowest (`y=0.11` < both shock rings), `life=0.95`, `grow=0.2`, and is the ONLY sprite with life >0.6s; scales with radius (`size=3.4*sc`); default gold when `col` omitted; `r` omitted → `sc=1.25` → size 4.25; `fxLevel>=2` → 0 sprites (after-glow gated too).

- 2026-06-30T10:42Z — **Attack-move command ping (command feedback).** Issuing an a-move order (`heroAmoveCmd`, ~L2357) only showed the flat vector `fxRing(pt,0xffa94d,1.8)` — no textured confirmation, unlike MOVE (`fxMovePing`) and ATTACK (`fxAtkPing`) orders which got pings in earlier runs, leaving a-move the last command type without one. Added `fxAmovePing(p,col)` helper (right after `fxMovePing`, ~L1836): the aggressive sibling of the move ping — a `ring_shock` destination ground pop in the a-move orange (`0xffa94d`, `size:2.8`) PLUS a small `impact_star` "engage" spark just above the point (`y:1.1`, shorter life) to read as combat intent vs a plain move's single soft ring. Since a-move targets a GROUND point (no unit to lock), the spark sits over the destination, not on a body. Wired into BOTH a-move paths beside the existing `fxRing`: the guest branch (`netSendInput({a:'amove'})`, ~L2359) and the host/solo branch (`pHero.amT=...`, ~L2360), both `fxAmovePing(pt,0xffa94d)`. **Perf:** purely cosmetic, client render/UI lane; two pooled sprites per a-move command (event-driven, throttled by the human's clicks, player-issued only), reuses existing `ring_shock`+`impact_star` textures (both confirmed present in fx/, used throughout) + `fxGround`/`fxBill` `fxLevel>=2` gates + `fxs` 160-cap pool; null-guarded; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (the `amT` order itself unchanged; guest still sends the same `netSendInput` amove). **Validation:** copied helper to scratch `/outputs/fxamoveping_check.js` with mocked `fxGround`/`fxBill`, `node --check` + 8 asserts passed: default a-move → exactly 1 `ring_shock` ground ring + 1 `impact_star` spark both tinted orange `0xffa94d`, ground at the destination, spark above it at `y:1.1` with shorter life than the ring; custom col forwarded to both; null point → 0 sprites; `fxLevel>=2` → 0 sprites.
- 2026-06-30T11:02Z — **Hero LEVEL-UP rising ascension column (level-up read).** `fxLevelUp` (~L1716 — the level-up burst, hero-only via `giveXP`) had a golden ground ring + a warm `glow_soft` flare at y2.4 + a bright `impact_star` spark, but NO tall vertical element — so a level-up popped at the feet without the "ascending / powering up" vertical read that `fxRespawn` and `fxSuperCast` both have. Added ONE pooled `glow_soft` billboard high above the hero (`y:4.8`, `size:4.4`, `grow:0.3` so it barely expands → reads as a standing shaft not an expanding puff, `life:0.6`, warm gold `0xffe89a`, `opacity:.7` so it's fainter than the flare beneath it) — stacked over the existing y2.4 flare it forms a two-stage vertical glow column rising off the ascension ring. Kept the ground ring, flare and spark exactly as-is. **Perf:** purely cosmetic, client render/FX lane; ONE extra pooled billboard per level-up (rare, event-driven — hero level-ups only), reuses the existing `glow_soft` texture (already used by this function's flare) + `fxBill`'s `fxLevel>=2` gate (function returns at top before any sprite) + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (level-up logic untouched). **Validation:** copied the edited `fxLevelUp` to scratch `/outputs/fxlevelup_column_check.js` with mocked `fxGround`/`fxBill`, `node --check` + 10 asserts passed: level-up → 1 ground ring + 3 billboards (flare+column+spark), exactly two `glow_soft` sprites, the new column is `glow_soft` at y4.8 sitting above the spark, barely expands (`grow<0.5`) while tall/wide (`size>=4`), fainter than the flare, and the original spark is unchanged; `fxLevel>=2` → 0 sprites.

- 2026-06-30T11:24Z — **Hero AoE nova (Q/W/E) leading-edge shockwave FRONT (double-boom, kit consistency).** `fxNovaBurst` (~L1812 — the AoE skill burst behind `abNova`: Fireball / Arcane Bolt etc.) fired its ground shock, blast glow and core spark all at the SAME radius, so a hero AoE nova popped as a single flat ring — the same flat reading the SUPER had before it got its leading-edge front. Added ONE pooled `ring_shock` ground decal right after the inner element-tinted ring: a wider white leading edge (`size:s*1.7` vs `s*1.3`), faster expansion (`grow:2.0` vs `1.4`), lower opacity (`.45` vs `.85`) and a touch lower (`y:0.14` vs `0.16`) so the white front visibly OUTRUNS the inner colour ring (end radius `s*1.7*2.0=s*3.4` vs inner `s*1.3*1.4≈s*1.82`) → the same two-stage "blast pushing outward" beat the ult uses (`fxSuperImpact`), so the hero's whole AoE kit now speaks one expansion language. White (not element-tinted) so it reads as the raw shock edge regardless of element; scales with the nova radius `s` like the rest of the burst; the inner ring keeps its element tint and the glow/spark billboards are unchanged. **Perf:** purely cosmetic, client render/FX lane; ONE extra pooled ground sprite per nova cast (event-driven, hero Q/W/E only), reuses the existing `ring_shock` texture (confirmed present in fx/, already used by the inner ring) + `fxGround`'s `fxLevel>=2` gate (function returns at top before any sprite) + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (guests get visuals via snapshots). **Validation:** copied the edited `fxNovaBurst` to scratch `/tmp/nova_doubleboom_check.js` with mocked `fxGround`/`fxBill`, `node --check` + 14 asserts passed: tinted nova → exactly 2 `ring_shock` rings where the outer is larger, grows faster, is fainter, is white, sits lower, and reaches a larger end radius (size×grow) than the inner — i.e. it outruns it; inner keeps the element tint; the 2 billboards (glow/spark) unchanged; default col → inner gold while outer stays white; `r` omitted → `s=5.5`; small `r` clamps to `s=3`; `fxLevel>=2` → 0 sprites.
- 2026-06-30T11:42Z — **Focus-target reticle under your hero's current attack target (selection/awareness lane).** The selection lane had a green selection ring, a cursor-driven hover ring (`updateHoverRing`) and the cyan self-beacon, but NOTHING marking what your OWN hero is committed to attacking — you couldn't read your hero's locked target at a glance during a fight. Added `getFocusRing()` (one lazily-built reusable thin `RingGeometry(1.05,1.3,30)` mesh, orange-red `0xff6a2c`, depthWrite:false, renderOrder 6) beside `getHeroSelfMark`, and a focus block at the END of `updateSelRings` (rides the existing per-frame pass — no new loop): when `pHero` is alive and `pHero.target` is a live ENEMY (`!dead && grp && team!==pHero.team`) the reticle shows under that target, scaled `(h||3)*0.7`, with a sharp fast pulse (`0.55+0.30*sin(gameT*6)`) that reads as "locked on"; hidden when the hero has no live enemy target / is dead / target is friendly. Distinct orange-red vs the hover ring's red `0xff6666` and selection green `0x6effa0` so the three selection signals don't blur together. `gameT` is `typeof`-guarded via the function's existing `_t`. **Perf:** purely cosmetic, client render/UI lane; ONE pooled reused mesh repositioned inside the already-per-frame `updateSelRings` — NO new allocation (no Vector3/material/geometry/texture/library per frame), just one `sin` + scale/position mutation on the existing mesh; no gameplay/net change (reads `pHero.target` read-only; guests just don't draw it if they skip `updateSelRings` — feature-safe). **Validation:** copied `getFocusRing`+`updateSelRings`+stubs to scratch `/outputs/focusring_check.js`, `node --check` + 13 asserts passed: no hero → hidden; live enemy target → visible, orange `0xff6a2c`, scaled `h*0.7`, positioned at target, lifted off ground; opacity pulses within [0.25,0.85] with span >0.4 over 3s; dead target → hidden; ally (same team) target → hidden; null target → hidden; dead hero → hidden; pooled (same mesh reused); target without `h` → `3*0.7` fallback.
- 2026-06-30T12:03Z — **Hero SUPER (R) impact PILLAR OF LIGHT (vertical big-moment read).** `fxSuperImpact` (~L1778 — the ult landing burst) had a LOW rising energy dome (`glow_soft` at y2.4) but NO tall vertical element, so the impact spread outward along the ground without the "pillar of light crashing down" vertical read — even though `fxRespawn` (y4.2 spawn-beam column) and `fxLevelUp` (y4.8 ascension column) both have one. Added ONE pooled `glow_soft` billboard high above the strike point (`y:6.0` — the HIGHEST sprite in the burst, `size:5.0*sc`, `grow:0.25` so it barely expands → reads as a STANDING shaft not an expanding puff, `life:0.5`, team/element-tinted by `col`, `opacity:.7` so it's fainter than the white core beneath it). Stacked over the existing low dome it forms a tall two-stage light column rising off the double-boom shock rings, giving the ult the vertical anchor the spawn/level-up beats already have. Scales with the super radius `r` via the existing `sc`; all 6 prior sprites (2 shock rings, dome, core, debris, after-glow) unchanged. **Perf:** purely cosmetic, client render/FX lane; ONE extra pooled billboard per super cast (a handful/game), event-driven, reuses the existing `glow_soft` texture (confirmed present in fx/, already used by this function's dome + after-glow) + `fxBill`'s `fxLevel>=2` gate (function returns at top before any sprite) + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (guests get visuals via their own `fxSuperImpact` call). **Validation:** copied the edited `fxSuperImpact` to scratch `/outputs/fxsuper_pillar_check.js` with mocked `fxGround`/`fxBill`, `node --check` + 12 asserts passed: tinted super → 3 ground + 4 bill (the 6 originals UNCHANGED + the 1 new pillar); pillar uses `glow_soft`, is `col`-tinted, sits at y6.0 = the HIGHEST billboard (vertical read), barely grows (`grow<0.5`), and scales with radius (`size=5.0*sc`, sc=2 at r16); the low dome at y2.4 is unchanged; default gold when `col` omitted; `r` omitted → `sc=1.25`; `fxLevel>=2` → 0 sprites.
- 2026-06-30T10:24Z — **Regular Q/W/E cast GROUND glyph (hero skill-cast read).** A non-ult skill cast got only the upward `flash_blue` cast billboard at the hero (`castSkill`, ~L3400) — no ground footprint, so a spell read as a floating puff rather than something channelled at the hero's feet. Only the SUPER had a ground cast-ring (via `fxSuperCast`). Added `fxCastGlyph(p,col)` helper (right after `fxBuffAura`, ~L1796): ONE quick element-tinted `ring_shock` ground cast-circle (`size:3.6`, `y:0.12`, `life:0.34`, `grow:1.6`, `opacity:.5`) — deliberately a single light, short, faint pooled decal because skills cast far more often than supers/buffs (keeps the fxs cap + perf rule honored). Wired CENTRALLY in `castSkill` beside the existing `flash_blue` billboard, gated `if(ai!==3)` so the super keeps `fxSuperCast`'s heavier ground ring instead and never doubles up; tinted by `pHero.heroColor` (default blue `0x88bbff`). Because it sits in `castSkill` (not inside a kit ability), it applies to EVERY hero, EVERY Q/W/E, on BOTH the shared-`EF_CORE` kit path and the inline fallback — and runs before the guest `netSendInput` return so guests see it locally too. **Perf:** purely cosmetic, client render/FX lane; ONE pooled `ring_shock` ground decal per skill cast (event-driven, hero-only — `castSkill` is `pHero`-gated), reuses the existing `ring_shock` texture (confirmed present in fx/, used throughout) + `fxGround`'s `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (cast logic untouched). **Validation:** copied `fxCastGlyph` + the gated wiring to scratch `/outputs/castglyph_check.js` with a mocked `fxGround`, `node --check` + 9 asserts passed: regular cast → exactly 1 `ring_shock` decal at the hero tinted by `heroColor`, faint+short (`opacity .5`, `life .34`); super (`ai===3`) → 0 (gated); all three Q/W/E slots fire it; default blue when `col` omitted; `fxLevel>=2` → 0 decals.
- 2026-06-30T12:46Z — **LAST-HIT secure spark (creep/minion/jungle — core MOBA loop feedback).** Landing the killing blow on a creep with YOUR hero (`dmgUnit` death block, the three `killer===pHero` branches: minion ~L2056, soldier ~L2060, wild ~L2064) already paid a gold number (`popGold`) + a coin sfx (`sfxCoin`), but had NO fx sprite — the most-repeated action in a MOBA's laning phase, last-hitting, had zero visual confirm while every command/combat beat around it got polished. Added `fxLastHit(p,h)` helper (right after `fxTakedown`, ~L1909): ONE small bright-gold (`0xffd34d`) `impact_star` "coin pop" at the slain creep's chest (`(h||3)*0.55`, `size:1.5`, `life:0.26`, `grow:1.3`, `opacity:.9`). Deliberately a SINGLE cheap billboard (no ground ring) because last-hits fire FAR more often than supers/takedowns — kept minimal to honor the pool/perf rule. Wired caller-gated inside each `if(killer===pHero){...}` block beside the existing `popGold`/`sfxCoin` so it only fires for YOUR secures (never enemy/ally creep kills); the wild branch adds `if(!u.boss)` so the boss keeps its own grand fanfare (`sfxBoss`/`bigBanner`) un-clashed. `u.grp` is still valid here (the branch runs before `removeUnit`). **Perf:** purely cosmetic, client render/FX lane; ONE pooled `impact_star` billboard per YOUR-secured creep (a 6-creep AoE wave clear → 6 sparks, far under the `fxs` 160 cap), event-driven, reuses the existing `impact_star` texture (confirmed present in fx/, used by fxAtkPing/fxTakedown) + `fxBill`'s `fxLevel>=2` gate + `fxs` pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (gold/XP/cs logic untouched; guests get visuals via snapshots). **Validation:** copied `fxLastHit` + the three emulated kill branches to scratch `/outputs/fxlasthit_check.js` with mocked `fxBill`/`popGold`/`sfxCoin`/`_gate`, `node --check` + 11 asserts passed: YOUR minion/soldier/wild secure → exactly 1 gold `impact_star` at `h*0.55`; enemy-hero secure → 0 (caller-gated to pHero); boss → 0 (suppressed for its fanfare); `fxLevel>=2` → 0; missing `h` → `3*0.55` fallback; null pos → 0; 6-creep wave → 6 sparks (<<160 cap).
- 2026-06-30T12:21Z — **Hero DEATH burst leading-edge shockwave FRONT (double-boom, FX-language consistency).** `fxDeathBurst` (~L1736 — the victim-side hero-death punch) fired its ground shock ring, soul puff and core flash all at the SAME radius, so a hero falling read as one flat pop — the same flat reading the SUPER (`fxSuperImpact`) and AoE nova (`fxNovaBurst`) had before each got its leading-edge front. Added ONE pooled `ring_shock` ground decal right after the inner team-tinted ring: a wider white leading edge (`size:6.0` vs `4.4`), faster expansion (`grow:2.2` vs `1.5`), lower opacity (`.5` vs `.85`) and a touch lower (`y:0.13` vs `0.14`) so the white front visibly OUTRUNS the inner team-tinted ring (end radius `6.0*2.2=13.2` vs inner `4.4*1.5=6.6`) → the same two-stage "blast pushing outward" beat the ult and nova use, so a hero takedown now speaks the same expansion language as the rest of the kit. White (not team-tinted) so it reads as the raw shock edge regardless of team; the inner ring keeps its team tint and the soul-puff/core billboards are unchanged. **Perf:** purely cosmetic, client render/FX lane; ONE extra pooled ground sprite per hero death (rare, event-driven — heroes only, a handful/game), reuses the existing `ring_shock` texture (confirmed present in fx/, already used by the inner ring) + `fxGround`'s `fxLevel>=2` gate (function returns at top before any sprite) + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (guests get visuals via snapshots/their own `fxDeathBurst` call). **Validation:** copied the edited `fxDeathBurst` to scratch `/outputs/fxdeath_doubleboom_check.js` with mocked `fxGround`/`fxBill`, `node --check` + 13 asserts passed: tinted death → exactly 2 `ring_shock` rings where the outer is larger, grows faster, is fainter, is white, sits lower, and reaches a larger end radius (size×grow) than the inner — i.e. it outruns it; inner keeps the team tint; the 2 billboards (soul puff/core) unchanged; default col → inner & outer both white; `fxLevel>=2` → 0 sprites.
- 2026-06-30T13:05Z — **Hero ground-slam (abRing Q/W) textured nova burst (kit FX parity).** `abRing` (~L2260 — the self-centred ground-slam AoE: Quake/Stomp-style, 38+8/lvl dmg + 2.5s slow, radius 8) fired ONLY the flat vector `fxRing(h.grp.position,col,8)` on cast, while its direct sibling `abNova` (the lobbed blast) already gets the rich textured `fxNovaBurst` (element-tinted ground shock + leading-edge white double-boom front + blast glow + core spark). Two abilities in the same `ARCH` table thus read with wildly different polish — the slam looked like a placeholder next to the nova. Added `fxNovaBurst(h.grp.position,8,col)` beside the existing `fxRing` (kept as the always-on low-FPS-tier base), matching the slam's gameplay radius (8) and tinted by the kit element `col`, so the ground-slam now reads with the same two-stage "blast pushing outward" beat as the nova/super/death bursts — the hero's whole AoE kit finally speaks one expansion language. REUSED the existing `fxNovaBurst` helper verbatim (no new helper, no new texture/geometry); the `aoe(...)` damage/slow call is untouched. **Perf:** purely cosmetic, client render/FX lane; ONE call to the already-`fxLevel`-gated + `fxs`-160-capped `fxNovaBurst` per slam cast (event-driven, hero Q/W only), reuses the existing `ring_shock`/`glow_soft`/`impact_star` textures (all confirmed present in fx/, already used by the nova/super); no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (the `aoe` dmg/slow logic unchanged; guests get visuals via snapshots). **Validation:** copied the edited `abRing` + stubbed `fxRing`/`fxNovaBurst`/`aoe` to scratch `/outputs/abring_check.js`, `node --check` + 5 assert groups passed: base `fxRing(r8,col)` still fires; `fxNovaBurst` now ALSO fires centred on the hero at radius 8 tinted by `col`; the `aoe` call is unchanged (r8, 38+lvl*8 dmg, 2.5s slow); burst fires BEFORE the aoe (cosmetic-first, no logic dependence); ability metadata (cd10/mp40/self1) intact. Call order: `fxRing,fxNovaBurst,aoe`.
- 2026-06-30T13:48Z — **Line-skill aim ENDPOINT CAP (targeting lane consistency).** The line-skill aim preview (`updateAim` line branch, ~L3575 — Chain Bolt / Dragon Breath / Ice Shard / Gust / Seed Barrage / Sting Volley) drew ONLY a beam (`aimLine`) with no marker for WHERE it reaches, while the ring preview shows its full AoE disc and the arc shows its cone — so a line skill-shot gave no read of its clamped max range/landing point. Reused the otherwise-IDLE `aimRing` disc (already the shared `_aoeTex` soft-fill+bright-rim, the SAME texture/style the ring & arc previews use) as a small soft landing cap at the line's clamped reach `(ex,ez)`: in the line branch, instead of `aimRing.visible=false`, now `aimRing.visible=true` positioned at the endpoint, scaled small (1.6, vs the full `cfg.size` footprint a true AoE ring uses), tinted by the existing `_aimCol` (gold regular / red-orange super) and inheriting the `_sp` super danger-pulse, at `AIM_OP.ring*_sp*0.8` opacity so it's a subtle cap, not a second AoE. Every targeting preview now speaks one gradient-disc language AND the line shows its reach. The ring/arc/else branches still set `aimRing.visible` explicitly (arc & hideAim hide it; the ring branch re-scales it to full size), so switching skills leaves no stuck cap. **Perf:** purely cosmetic, client render/UI lane; REUSES an EXISTING mesh (`aimRing`) that line mode previously hid + the already-shared `_aoeTex` — NO new geometry/texture/material/library/alloc; `updateAim` already runs per-frame and shows only one preview at a time, so this is just position/scale/opacity/setHex on a mesh that was sitting idle during line aim; no gameplay/net change (aim preview is host/solo UI, read-only cursor). **Validation:** copied `updateAim`+stubbed ring/line/arc meshes to scratch `/outputs/aimcap_check.js`, `node --check` + 11 asserts passed: regular line → beam + cap both visible (arc hidden), cap at the clamped endpoint, small scale 1.6 (not full AoE), gold tint, opacity `ring*0.8`; cursor beyond range → cap clamps to `size` not cursor; super line → cap red `0xff5a3c` + pulse band; switch to ring → `aimRing` back to full footprint (line hidden); arc mode → `aimRing` hidden (no leftover cap); dead hero → all previews incl cap hidden.
- 2026-06-30T14:05Z — **Hero BLINK (E) warp-out/warp-in materialize (mobility FX parity).** `abBlink` (~L2263 — the instant teleport E: 17-range, cd13) drew ONLY two flat vector `fxRing(...,3)` pops, one at the origin and one at the destination — none of the textured polish the dash E (`fxDashStreak`) or recall (`fxRespawn`) got, and the dash-streak DELIBERATELY skips blink (it gates on `!exact` so true teleports keep their pop rings), so a blink was the last hero mobility cast with zero textured read. Added `fxBlinkPop(x,z,col)` helper (right after `fxDashStreak`, ~L1871): a quick warp ground ring (`ring_shock`, `size:3.4`, `y:0.13`, `life:0.32`, `grow:1.7`, `opacity:.7`) + a short vertical warp-energy puff (`glow_soft`, `y:2.0`, `size:2.8`, `grow:0.5`, `opacity:.65`) tinted by the kit `col` (default warp-blue `0x9bb8ff`). Wired into `abBlink.f`: captures the origin `ox,oz` as plain locals BEFORE `dash(h,pt,17,true)` (NO Vector3 clone), fires `fxBlinkPop` at the origin (warp-OUT, stamped where the hero vanished) and again at `h.grp.position` after the teleport (warp-IN at the landing point), both layered beside the kept flat `fxRing`s (which stay as the always-on low-FPS-tier base). Now a blink reads as a real "vanish HERE → reappear THERE" warp, matching the dash/recall mobility beats. **Perf:** purely cosmetic, client render/FX lane; 2 pooled sprites per warp point = 4 per blink cast (event-driven, hero E only, cd13 → rare), reuses the existing `ring_shock`/`glow_soft` textures (both confirmed present in fx/, used throughout) + `fxGround`/`fxBill` `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (teleport movement/`dash` unchanged; guests get visuals via snapshots). **Validation:** copied `fxBlinkPop` + the edited `abBlink` to scratch `/outputs/blinkpop_check.js` with mocked `fxGround`/`fxBill`/`fxRing`/`dash`, `node --check` + asserts passed: full blink → 2 flat `fxRing`s kept (origin+dest), exactly 2 `ring_shock` ground rings + 2 vertical `glow_soft` puffs (y2.0) tinted by col, warp-OUT captured at the ORIGIN before the dash moves the hero and warp-IN at the DESTINATION, `dash` still `exact:true`; default warp-blue when col omitted; `fxLevel>=2` → 0 sprites.
- 2026-06-30T14:25Z — **RANGED hero super-effective basic → gold advantage ring (basic-attack type-read parity).** A MELEE hero landing a super-effective basic (`tryAttack`, mu>1) gets a clear gold `fxRing(t.grp.position,0xffaa33,1.6)` "super effective!" ring at the target on top of the impact, but the RANGED hero path (`dmgUnit`, ~L2035 — arrow/orb impact) only got the element-tinted `fxHit` big spark with NO matching advantage ring — so a type-advantage basic read distinctly only for melee, not ranged. Added `if(mult&&mult>1)fxRing(u.grp.position,0xffaa33,1.6)` inside the existing ranged-hero impact guard (`src.kind==='hero'&&src.range>4&&amt>0&&u.team!==src.team`), firing the SAME gold ring the melee path uses so a type-advantage basic now reads identically whether the hero is melee or ranged. REUSED the existing `fxRing` primitive (the always-on low-FPS base ring, RingGeometry, pooled + 160-cap) verbatim — no new helper/texture/geometry. The guard already restricts this to ranged HEROES (≤2 sources) and `mult>1` only (super-effective hits), so it never fires for melee (the dmgUnit guard skips `range<=4` → no double-ring with tryAttack's own ring), neutral/resisted hits, or non-hero shooters (towers/minions). **Perf:** purely cosmetic, client render/FX lane; ONE pooled vector ring per ranged-hero super-effective basic (event-driven, hero-only, advantage-gated → rare), reuses the existing `fxRing` mesh path + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (damage/typeMult logic untouched; guests get visuals via snapshots). **Validation:** copied the edited `dmgUnit` guard + stubbed `fxHit`/`fxRing` to scratch `/outputs/superfx_ranged_check.js`, `node --check` + 5 asserts passed: ranged super-effective (mult2) → `fxHit` big element-tinted + gold `0xffaa33` ring size 1.6 (matches melee); neutral (mult1) → impact only, NO ring; resisted (mult0.5) → small impact, NO ring; melee src (range1) in dmgUnit → fires nothing (avoids double-ring with tryAttack); non-hero ranged (tower) → fires nothing.
- 2026-06-30T19:21Z — **Summoner-spell FLASH (D) warp-out/warp-in materialize (mobility FX parity, final blink gap).** `heroFlash` (~L2583 — the D summoner spell: a short ~14u blink, cd120) drew ONLY two flat vector `fxRing(h.grp.position,0xffe06a,3)` pops (origin + landing), exactly the placeholder look the E-blink (`abBlink`) had before it got `fxBlinkPop` last run — leaving Flash the LAST hero mobility/teleport cast without a textured warp read (dash→`fxDashStreak`, E-blink→`fxBlinkPop`, recall→`fxRespawn` all already polished). REUSED the existing, already-validated `fxBlinkPop(x,z,col)` helper verbatim (warp ground ring `ring_shock` + vertical `glow_soft` energy puff) — NO new helper/texture/geometry. Wired into `heroFlash`: captured the origin as plain locals `_fox,_foz` BEFORE the move (NO Vector3 alloc — `fxBlinkPop` takes x,z), fired `fxBlinkPop(_fox,_foz,0xffe06a)` at the origin (warp-OUT where the hero vanished) beside the first `fxRing`, then `fxBlinkPop(h.grp.position.x,h.grp.position.z,0xffe06a)` after the `addScaledVector(dir,14)`+clamp/avoid (warp-IN at the landing point) beside the second `fxRing` (both flat rings kept as the always-on low-FPS base). Tinted Flash's own gold `0xffe06a` (vs the E-blink's default warp-blue) to match its existing rings, so Flash now reads as a real "vanish HERE → reappear THERE" warp like the E-blink. **Perf:** purely cosmetic, client render/FX lane; 2 pooled sprites per warp point = 4 per Flash (event-driven, hero D only, cd120 → very rare), reuses the existing `ring_shock`/`glow_soft` textures + `fxGround`/`fxBill` `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (the blink move/`flashCd` unchanged; guest path returns early before any fx and the host applies `heroFlash` via the server, getting the warp there). **Validation:** copied `fxBlinkPop`+the edited `heroFlash` to scratch `/outputs/flashpop_check_v1.js` with mocked `fxGround`/`fxBill`/`fxRing`/THREE, `node --check` + 11 asserts passed: facing-dir Flash → 2 flat `fxRing`s kept + exactly 2 `ring_shock` ground rings + 2 `glow_soft` puffs (y2.0) all gold `0xffe06a`, warp-OUT ground at the ORIGIN (captured before the +14 move) and warp-IN at the DESTINATION, `flashCd` set to 120; `fxLevel>=2` → 0 warp pops while the flat rings still fire; guest mode → early return, 0 fx.
- 2026-06-30T14:42Z — **Focus-target reticle EXECUTE read (kill-secure awareness lane).** The focus reticle (`getFocusRing`/`updateSelRings`, ~L2897 — the orange-red ring under your hero's current attack target) held the same orange-red tint + pulse regardless of the target's health, so a near-dead focused enemy gave no "this kill is securable" read. Made it react to the target's HP, mirroring the proven low-HP self-beacon pattern (`heroSelfMark`): when the focused enemy drops below 30% (`_ft.hp/_ft.maxHp < 0.3`) the reticle flips from orange-red `0xff6a2c` to a hot red `0xff2a2a` with a faster + deeper pulse (`sin(_t*9)`, opacity band 0.15→0.95 vs the calm 0.25→0.85 at `sin(_t*6)`) and a slightly larger footprint (`fs*1.12` vs `fs`) so you can see the execute is on at a glance. `maxHp>0` guarded (a 0-maxHp target stays orange — no div-by-zero false execute); color/opacity/scale set fresh each frame so it snaps back to orange-red the instant the target heals or you switch focus. **Perf:** purely cosmetic, client render/UI lane; rides the already-per-frame `updateSelRings` focus block — NO new allocation (no Vector3/material/geometry/texture/library per frame), just one fraction + `setHex` + `sin` + scale on the EXISTING pooled mesh; no gameplay/net change (reads `hp`/`maxHp` read-only; guests just don't draw it if they skip `updateSelRings` — feature-safe). **Validation:** copied the focus block + stubbed mesh/pHero to scratch `/outputs/focus_execute_check.js`, `node --check` + 18 asserts passed: healthy target → orange-red, scale `h*0.7`, band ~[0.25,0.85]; <30% → hot red `0xff2a2a`, scale `fs*1.12`, deeper band ~[0.15,0.95]; exactly 30% stays orange while 29.7% flips (strict `<0.3`); heals back → orange + base scale restored; `maxHp===0` → no false execute; friendly/dead/null target & dead hero → hidden; missing `h` → `3*0.7` fallback.
- 2026-06-30T15:08Z — **Self-centred SUPER (R) impact FX land on the HERO, not the cursor (big-moment placement fix).** `castSkill`'s super branch (~L3502) always fired the big-moment impact FX — `fxSuperImpact`, the super base `fxRing`, and the `fxSuperLink` projection bolt — at `pt`, but `quickCastPoint` ALWAYS returns the cursor/joystick point regardless of `A.self`. So the two SELF-centred ults (World Freeze `self:1` / Avatar of War `self:1`), whose effect actually detonates at the hero (`aoe(h,h.grp.position,...)` / self heal+haste), splashed their entire layered impact (the double-boom rings, pillar of light, dome, core, after-glow, debris) and the caster→impact link out at the CURSOR — completely detached from where the freeze/buff lands. The `fxSuperLink` comment even claimed it was "skipped for self-centred ults," but it wasn't: passing the cursor `pt` drew a bogus power-projection bolt from the hero to the cursor on a self-cast ult. Added one local `const _ip=A.self?pHero.grp.position:pt;` right after the cooldown/mp line and routed the three COSMETIC super calls through it: `fxSuperLink(hero,_ip,...)` (self → from==to → the helper's `<6u` early-return fires, so NO bolt), the guest-branch `fxSuperImpact(_ip,...)`, and the host-branch `fxRing(_ip,...)`+`fxSuperImpact(_ip,...)`. Targeted blasts (Cataclysm, no `self`) keep `_ip==pt` so they're byte-identical. `_ip` is a LIVE reference to `pHero.grp.position` (no clone → zero alloc). **Deliberately left `superKnock(pHero,pt.x,pt.z,...)` UNCHANGED** — it moves units (gameplay), so this stays a pure client render/FX fix; the knockback center is untouched. **Perf:** purely cosmetic, client render/FX lane; NO new sprite/texture/geometry/material/library — just repoints existing super FX calls at a hero-vs-cursor reference chosen once per cast; zero per-frame cost, zero allocation; host-branch change sits after the guest `return` so the net path is untouched (guests resolve supers server-side; the guest branch's cosmetic `fxSuperImpact` now also lands on the hero for self ults while `netSendInput` still sends the real `pt`). **Validation:** copied the edited `castSkill` body into scratch `/outputs/superip_check.js` with recording stubs for every FX/knock call, `node --check` + 12 asserts passed: TARGETED super → impact/ring/link/knock all at the cursor (unchanged); SELF super (World Freeze) → impact + super ring at the HERO, link `from==to` (so fxSuperLink early-returns → no bogus bolt), and knock STILL at the cursor (gameplay untouched); Avatar of War → impact at the hero; GUEST self super → cosmetic impact at the hero while the generic landing ring + `netSendInput` keep `pt`; a regular Q/W/E cast emits no super impact/knock at all.
- 2026-06-30T13:25Z — **Hero RECALL teleport materialize (warp-out + fxRespawn arrival).** The recall ability (`heroRecall`→channel/completion in the pHero update loop, ~L4600) only ever drew FLAT vector rings: a growing `fxRing` pulse during the 4s channel and a single `fxRing(...,6)` on arrival at the fountain — none of the textured polish the respawn/super/cast beats have, so warping home read like a placeholder. Made the COMPLETION a proper teleport: (1) a quick recall-blue `ring_shock` warp-out ground puff at the channel spot (`fxGround(pos.x,pos.z,'ring_shock',{size:4.2,y:0.13,life:0.4,grow:1.7,color:0x9bd1ff,opacity:.7})`) fired BEFORE the `grp.position.set(fountain)` so it stamps where the hero vanished; then (2) reused the existing proven `fxRespawn(h.grp.position,0x9bd1ff)` AT the fountain pad beside the kept base `fxRing(...,6)` — giving the arrival the same ground ring + rising `glow_soft` beam column + `impact_star` core that respawns get, tinted recall-blue (`0x9bd1ff`, matching the channel pulse + "🏠 Recalled home" feed). The channel pulse, cancel logic, teleport target and feed/camera-follow are all untouched. **Perf:** purely cosmetic, client render/FX lane; ~4 pooled sprites per completed recall (rare, event-driven — a handful/game), reuses the existing `ring_shock`/`glow_soft`/`impact_star` textures (all confirmed present in fx/, already used by `fxRespawn`) + `fxGround`/`fxBill` `fxLevel>=2` gates + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation; runs in the host/solo pHero loop only (guests recall server-authoritatively → get visuals via snapshots), no gameplay/net change (teleport destination + recall timing unchanged). **Validation:** copied the edited completion block + stubbed `fxGround`/`fxBill`/`fxRing`/`fxRespawn`/`fountainOf` to scratch `/outputs/recallfx_check.js`, `node --check` + 13 asserts passed: warp-out `ring_shock` fires at the ORIGIN before the teleport tinted `0x9bd1ff`; hero moves to the fountain; base arrival `fxRing(6)` preserved; `fxRespawn` ground ring + `glow_soft` column (y4.2) both blue while the `impact_star` core stays white; exactly 2 ground decals (warp-out + arrival ring) + 2 billboards (column + core); pHero camera-follow + "🏠 Recalled home" feed still fire; `fxLevel>=2` → 0 ground/0 bill (all new sprites gated) while the teleport still happens (gameplay unaffected).
- 2026-06-30T15:42Z — **Selected-unit ring RELATIONSHIP tint (selection lane consistency).** The selection rings (`updateSelRings`/`getSelRing`, ~L2887) were ALWAYS green `0x6effa0` regardless of who was selected — but a single left-click on an ENEMY or NEUTRAL unit (`leftClickAt` sets `selUnits=[u]` to inspect any unit, not just yours), so click-to-inspect on a foe drew a misleading "this is yours" green ring, out of step with the hover ring (`updateHoverRing`, ~L2920) which already tints by relationship (`hv.team===T.B?green:hv.team===T.R?red:gold`). Added ONE `r.material.color.setHex(u.team===T.B?0x6effa0:(u.team===T.R?0xff6666:0xffd34d))` inside the existing per-ring visible branch so the selection ring now speaks the SAME language as the hover ring: your/ally units keep the established green, an inspected enemy shows red `0xff6666`, a wild/neutral unit shows gold `0xffd34d`. Your-own case is byte-identical to before (still green), so no regression for the common box-select; set fresh each frame so it tracks team changes and nests cleanly with the orange-red focus reticle when you select the enemy you're attacking. **Perf:** purely cosmetic, client render/UI lane; ONE `setHex` on the EXISTING pooled ring material inside the already-per-frame `updateSelRings` loop — NO new allocation (no Vector3/material/geometry/texture/library per frame), no new mesh; reuses the same `T.B/T.R` relationship test the hover ring already uses; no gameplay/net change (reads `u.team` read-only; guests just don't draw it if they skip `updateSelRings` — feature-safe). **Validation:** copied `updateSelRings` (+stubbed pooled rings/self-mark/focus blocks) to scratch `/outputs/selring_tint_check.js`, `node --check` + 7 asserts passed: team B → green (unchanged), team R → red `0xff6666`, neutral → gold `0xffd34d`; dead unit → hidden, no color crash; mixed multi-select keeps per-unit tint (ring0 green / ring1 red); the tint expression matches the hover ring's exactly.
- 2026-06-30T15:23Z — **Hero shot FIZZLE when the target dies mid-flight (projectile-FX closure).** In the projectile loop (~L4697) a homing shot whose target died while the orb/arrow was still in the air hit `if(pr.tgt&&pr.tgt.dead){killObj(pr.m);projs.splice(i,1);continue;}` — removed SILENTLY, no visual, even though every other hero-shot beat is polished (muzzle flash at launch via `shoot`/`castAt`, the in-flight `glow_soft` trail, the impact spark on a live hit). So your shot vanishing on a dropped frame was the one gap. Added ONE faint element-tinted `glow_soft` dissipate puff (`size:1.6,life:0.3,grow:1.4,opacity:.55`) at the projectile's last position, tinted by the projectile's OWN trail tint `pr._tc`, fired only when `pr._tc!==undefined` (i.e. hero-fired — `_tc` is set only for hero src in `shoot`/`castAt`), so it reads as the shot fizzling out instead of teleporting away. Placed BEFORE the existing `killObj`/`splice` so cleanup is unchanged. **Perf:** purely cosmetic, client render/FX lane; ONE pooled billboard, event-driven and RARE (only when YOUR target dies in the exact window the shot is mid-air), reuses the existing `glow_soft` texture (confirmed present in fx/, already used by the in-flight trail) + `fxBill`'s `fxLevel>=2` gate + `fxs` 160-cap pool; non-hero projectiles cost only a single `!==undefined` compare (zero alloc — they keep vanishing silently), no new texture/geometry/library, no gameplay/net change (projectile cleanup/removal logic untouched; guests get visuals via snapshots). **Validation:** copied the edited branch body + mocked `fxBill`/`killObj`/`projs.splice` to scratch `/outputs/fizzle_check.js`, `node --check` + asserts passed: hero shot (`_tc` set) + dead target → exactly 1 `glow_soft` fizzle tinted by `_tc` at the shot's last pos, then cleaned up (`killObj`+`splice`); non-hero shot (`_tc` undefined) + dead target → 0 fx but still cleaned up; live target → branch skipped, no fx/cleanup; `fxLevel>=2` → 0 fizzle while cleanup still runs.
- 2026-06-30T16:05Z — **Hero HEAL restore glow (Avatar of War SUPER self-heal — a whole missing FX category).** Healing had NO fx sprite anywhere in the game — every heal site (`fxHeal`-less) showed only a `popHeal` number and at most a flat vector `fxRing`, so the most readable MOBA "you got restored" beat was entirely absent while every command/combat/super beat around it got polished. Added `fxHeal(p,col)` helper (right after `fxBuffAura`, ~L1837): a soft GREEN restoration ground ring (`ring_shock`, `size:2.8,y:0.15,life:0.5,grow:1.2,opacity:.6`) + a rising green restore column (`glow_soft`, `y:2.6,size:3.0,grow:0.3` so it barely expands → a standing glow not a puff, `opacity:.7`) + a bright sparkle at the chest (`spark`, `y:2.2,size:1.8,opacity:.9`), defaulting to heal-green `0x5dff9b` (matching the `.dmg.heal` pop colour) — deliberately GREEN and distinct from the element-tinted `fxBuffAura`, so a restore reads as healing, not empowerment. Wired into the **HERO SUPER** Avatar of War self-heal (`buildSkill` war branch, ~L2294 — `f:(h)=>{...h.hp+=maxHp*.3;hasteT=8;popHeal;fxRing(...,8);}`) beside the kept base `fxRing` as `fxHeal(h.grp.position)`, so the +30%-HP ult now shows a clear green restoration burst on top of its layered `fxSuperImpact` (which lands on the hero via the self-placement fix). The `spark` texture is confirmed present in fx/. **Perf:** purely cosmetic, client render/FX lane; 3 pooled sprites per super cast (a handful/game), event-driven, hero-only; reuses existing `ring_shock`/`glow_soft`/`spark` textures (all confirmed present in fx/) + `fxGround`/`fxBill` `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (heal/`hasteT` logic untouched; guests get visuals via snapshots). **Validation:** copied `fxHeal` + the edited Avatar-of-War `f` to scratch `/tmp/fxheal_check.js` with mocked `fxGround`/`fxBill`/`fxRing`/`drawBar`/`popHeal`, `node --check` + 18 asserts passed: default heal → 1 green `ring_shock` ring + 1 green `glow_soft` column at y2.6 (barely expands) + 1 `spark` at y2.2, all at the heal pos; custom col forwarded to ring+column while sparkle stays bright `0xeafff2`; `fxLevel>=2` → 0 sprites; Avatar super → hp 40→70 (30% restore), `hasteT=8`, base `fxRing(8)` preserved AND `fxHeal` green ring+column+sparkle at the hero; full-hp hero → clamps at maxHp (no overheal) while haste + cosmetic fx still fire.
- 2026-06-30T17:05Z — **Hero LINE-skill (Q) beam-terminus landing flash (skillshot impact telegraph).** `lineShot` (~L2394 — the piercing Q: Chain Bolt / Dragon Breath / Ice Shard / Gust / Seed Barrage / Sting Volley) drew an element-tinted beam (`fxBeam`) + element-tinted per-unit impacts (`fxHit` via `dmgUnit`) along its path, but the beam's END — its clamped max reach `end` — had NO landing telegraph, unlike the nova (`fxNovaBurst`) and super (`fxSuperImpact`) which burst where they land. So a line shot that hit nothing read as a beam fading into empty space with no sense of *reaching* its range. Added `fxLineImpact(p,col)` helper (right after `fxNovaBurst`, ~L1880): a SMALL element-tinted beam-terminus flash — one `ring_shock` ground ring (`size:2.6,y:0.13,life:0.3,grow:1.5,opacity:.55`) + one soft `glow_soft` end-of-beam puff (`y:1.4,size:2.2,life:0.26,grow:1.2,opacity:.6`), deliberately LIGHTER than the nova burst (2 sprites, no white double-boom front) since a line Q fires far more often than a nova/super. Wired into `lineShot` immediately after the `fxBeam` call, passing the SAME `end` point + the SAME tint expression (`h.heroColor` else pale-green `0xaaffcc`) so the terminus flash matches the beam colour exactly. Now the piercing Q visibly *lands* at the end of its range, consistent with the nova/super landing telegraphs. **Perf:** purely cosmetic, client render/FX lane; 2 pooled sprites per line cast (event-driven, hero Q only), reuses the existing `ring_shock`/`glow_soft` textures (both confirmed present in fx/, used throughout) + `fxGround`/`fxBill` `fxLevel>=2` gate (helper returns at top before any sprite) + `fxs` 160-cap pool; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (the beam/dmg-along-line logic untouched; guests get visuals via snapshots). **Validation:** copied `fxLineImpact` + the edited `lineShot` to scratch `/outputs/lineimpact_check.js` with stubbed THREE.Vector3/`fxGround`/`fxBill`/`fxBeam`/`dmgUnit`, `node --check` + asserts passed: tinted hero cast → beam fires + exactly 1 `ring_shock` ground ring + 1 `glow_soft` flash at the clamped reach (`x=12` for a dir·len=12 shot), both tinted to the beam colour, flash lifted to y1.4, enemy in-line still damaged; only 2 terminus sprites (lighter than nova); hero without `heroColor` → beam + terminus both fall back to pale-green `0xaaffcc`; `fxLevel>=2` → 0 terminus sprites while the beam still fires; default-col `fxLineImpact` → pale-green.
- 2026-06-30T16:28Z — **Hero SUPER (R) READY anticipation pulse (big-moment awareness, new beat).** The ultimate had a rich CAST beat (`fxSuperCast`/`fxSuperLink`) and a layered IMPACT (`fxSuperImpact`), but NOTHING marked the moment the ult became *available* again — the player had to watch the R cooldown number to know their big play was back. Added `fxSuperReady(p,col)` helper (right after `fxSuperCast`, ~L1810): a one-shot, deliberately RESTRAINED pulse on the hero — a quick team/element-tinted `ring_shock` ground halo (`size:3.6,y:0.16,life:0.55,grow:1.6,opacity:.6`) + a single rising `impact_star` "charged" spark (`y:2.6,size:2.0,opacity:.8`), lighter than `fxSuperCast` (no tall power column) since it fires every cooldown cycle. Wired into `updHUD` right after the abQ/W/E/abR cooldown-overlay loop (~L4521): tracks `h._rWasCd` (stored ON the hero, not a new global) and fires `fxSuperReady(h.grp.position, h.heroColor||0xffe06a)` exactly ONCE on the `abCd[3]` cd>0→0 transition, gated to `!h.dead && running && h.grp`. Because `abCd[3]` starts at 0 (never-on-cd), there's no spurious flash at game start; the pulse only appears after a super has actually been cast and its cooldown completes. Works for host & guest alike (both set/tick `pHero.abCd[3]` locally). **Perf:** purely cosmetic, client render/UI lane; 2 pooled sprites fired ONCE per ult cooldown cycle (~once per 110–200s), event-driven off a single boolean transition inside the already-per-frame `updHUD` — the per-frame cost is one compare + one bool assignment (zero alloc on the common path), reuses the existing `ring_shock`/`impact_star` textures (both confirmed present in fx/, used throughout) + `fxGround`/`fxBill` `fxLevel>=2` gate + `fxs` 160-cap pool; no new texture/geometry/library, no gameplay/net change (reads `abCd[3]` read-only; cooldown logic untouched). **Validation:** copied `fxSuperReady` + the emulated `updHUD` transition block to scratch `/outputs/superready_check.js` with recording `fxGround`/`fxBill`, `node --check` + 13 asserts passed: no fire at game start (never on cd) or while on cd; `_rWasCd` latches true on cd; exactly 1 `ring_shock` halo + 1 `impact_
- 2026-06-30T19:42Z — **Cursor HOVER ring EXECUTE read (selection/kill-secure awareness lane).** The cursor hover ring (`updateHoverRing`/`getHoverRing`, ~L3091) tinted by relationship (your green / enemy red / neutral gold) at a FLAT `.5` opacity regardless of the hovered unit's health, so a near-dead enemy/jungle camp under the cursor gave no "this kill is securable" read — even though the focus reticle (`getFocusRing`, your hero's locked target) already got exactly this low-HP execute behaviour and `fxLastHit` shows the project values last-hit/secure feedback. Extended the SAME proven `heroSelfMark`/focus-execute pattern to the hover ring: when the hovered unit is ATTACKABLE (`pHero && hv.team!==pHero.team` → enemy OR neutral, never your own) and below 30% HP (`hv.maxHp>0 && hv.hp/hv.maxHp<0.3`), the ring flips from its steady relationship tint to hot red `0xff2a2a` with a fast deep pulse (`0.6+0.3*sin(gameT*9)` → opacity band [0.3,0.9] vs the flat 0.5) and a slightly larger footprint (`s*1.12`), so a securable kill under the cursor pops at a glance; otherwise it keeps the exact prior team tint + flat `.5` + base scale (set fresh each frame so it snaps back the instant the unit heals, you move off, or you hover your own/an ally). `maxHp>0` guarded (a 0-maxHp pick can't div-by-zero into a false execute); `pHero`/`gameT` `typeof`-guarded. Nests cleanly with the focus reticle (locked target) and selection-ring tints — three distinct selection signals that now all speak HP/relationship. **Perf:** purely cosmetic, client render/UI lane; rides the already-per-frame `updateHoverRing` (one `pickUnit` it already did) — NO new allocation (no Vector3/material/geometry/texture/mesh/library per frame), just one fraction + `setHex` + `sin` + scale/opacity on the EXISTING pooled `hoverRing` mesh; no gameplay/net change (reads `hp`/`maxHp`/`team` read-only; guests that skip `updateHoverRing` simply don't draw it — feature-safe, never blocks input). **Validation:** copied the edited `updateHoverRing` + stubbed pooled ring/`pickUnit`/`pHero` to scratch `/outputs/hover_execute_check.js`, `node --check` + 20 asserts passed: healthy enemy → red team tint, flat .5, base scale; <30% enemy → hot-red `0xff2a2a`, scale ×1.12, opacity pulses within [0.3,0.9] with span >0.5; low NEUTRAL → executes too (attackable); low ALLY → stays green, never executes; exactly 30% stays team tint while 29% flips (strict `<0.3`); `maxHp===0` → no false execute; no `pHero` → no execute; selected unit under cursor → hidden (no double-ring); nothing hovered → hidden; heal back to 90% → team tint + base scale + flat .5 all restored.

- 2026-06-30T20:21Z — **GATHER / REPAIR worker-order ping (command-feedback lane completion).** Right-clicking a resource node (gather) or a damaged structure (repair) with workers selected (`rightClickCommand`, ~L3293) only ever showed the flat vector `fxRing` — leaving gather & repair the LAST command types without a textured ping, while MOVE (`fxMovePing`), ATTACK (`fxAtkPing`) and A-MOVE (`fxAmovePing`) all got one in earlier runs. Added `fxWorkerPing(p,col)` helper (right after `fxAtkPing`, ~L2094): a soft `ring_shock` ground ring at the node/structure + a gentle rising `glow_soft` "work" puff (deliberately NOT a combat `impact_star` — these are economy orders, so it reads calmer than the attack/a-move pings), tinted by the order colour. Wired beside the existing `fxRing` in both branches: gather → tinted by resource (`node.type==='tree'?0x6fd08a` wood-green `:0xffd34d` gold, hoisted to a `_ncol` local so the ping matches the base ring exactly), repair → repair-blue `0x9fe0ff` (same as the "🔧 Workers repairing" feed). The flat `fxRing` stays as the always-on low-FPS-tier base. Note: focused on the primary `rightClickCommand` order path (symmetric with move/atk/amove); the secondary direct-gather site (~L3660) keeps its own `fxRing` — a possible future ping site. Bias note: hero super/attack/Q-W-E lanes are now heavily saturated (adding another hero sprite risks visual clutter vs the minimalism rule), so this run completes the remaining command-feedback gap with the same proven, capped, gated pattern. **Perf:** purely cosmetic, client render/UI lane; two pooled sprites per worker order (event-driven, throttled by the human's clicks, player-issued only), reuses the existing `ring_shock`+`glow_soft` textures (both confirmed present in fx/, used throughout) + `fxGround`/`fxBill` `fxLevel>=2` gate + `fxs` 160-cap pool; null-guarded; no new texture/geometry/library, zero per-frame allocation, no gameplay/net change (the gather/repair order itself unchanged; online-guest path returns early before this block). **Validation:** copied `fxWorkerPing` + the emulated gather/repair branches to scratch `/outputs/fxworkerping_check.js` with mocked `fxGround`/`fxBill`/`fxRing`, `node --check` clean + 12 asserts passed: tree gather → base `fxRing` kept + 1 `ring_shock` + 1 `glow_soft` all wood-green at the node (y1.4 puff); gold gather → all three gold `0xffd34d`; repair → base ring kept + ping both repair-blue `0x9fe0ff`; economy puff is `glow_soft` only (no combat `impact_star`); `fxLevel>=2` → base `fxRing` still fires while 0 ping sprites (gated); null pos → 0 sprites; default col → repair-blue. Re-read the wired `rightClickCommand` region (L3293-3294) — well-formed, fires beside the kept base ring.
- 2026-06-30T20:43Z — **Line-skill aim OUT-OF-RANGE range-wall cue (targeting lane readability).** The line-aim endpoint cap (`updateAim` line branch, ~L3871 — the soft `aimRing` disc reused as a landing marker at the beam's clamped reach) looked IDENTICAL whether the cursor was in range or beyond it: in both cases it drew at scale 1.6 / `AIM_OP.ring*_sp*0.8` opacity. So a skillshot (Chain Bolt / Dragon Breath / Ice Shard / Gust / Seed Barrage / Sting Volley) aimed past its max range gave NO read that the shot is being clamped SHORT of the cursor — the beam just stopped with no "this is as far as it reaches" signal. Added ONE compare on values already in hand: `_oor=dist>len+0.01` (true only when the cursor is beyond the skill's max range, since `len=min(max(dist,1),cfg.size)` caps at the range). When clamped, the endpoint cap is EMPHASISED as a hard range wall — larger (`scale 2.1` vs `1.6`) and brighter (opacity factor `1` vs `0.8`); in range it stays the subtle marker exactly as before. Inherits the existing `_aimCol` danger tint (gold regular / red-orange super) and `_sp` super danger-pulse, so a super-ult line aimed out of range still pulses while showing the bigger range-wall cap. **Perf:** purely cosmetic, client render/UI lane; REUSES the SAME idle `aimRing` mesh the cap already drew on — NO new geometry/texture/material/mesh/library/alloc; `updateAim` already runs per-frame showing only one preview at a time, so this is one `>` compare + a ternary feeding the opacity/scale set the branch already did; no gameplay/net change (aim preview is host/solo cursor UI, read-only). **Validation:** copied the edited line branch + stubbed ring/line/arc meshes to scratch `/outputs/aimoor_check.js`, `node --check` clean + 14 asserts passed: in-range (dist6<12) → cap at the cursor, subtle scale 1.6 & opacity `ring*0.8`, beam len 6; out-of-range (dist20>12) → cap clamped to range 12 (not the cursor), emphasised scale 2.1 & opacity `ring*1`, beam len 12, and BOTH bigger AND brighter than in-range; exactly at max range (dist==12) → stays subtle (strict `>len+0.01`); super line out-of-range → cap red-orange `0xff5a3c`, retains the `_sp` pulse multiplier AND the 2.1 emphasis.
- 2026-06-30T20:03Z — **Control-group RECALL confirm pop (selection-lane symmetry).** Pressing a digit to RECALL a control group (`onkeydown` 234567890 branch, ~L3960) only did `sfxClick()` + a silent selection-ring swap, while ASSIGNING a group (Ctrl+digit) pops a blue `fxGroupAssign` ring on each unit — asymmetric, no visual "these units" read on recall. Added `fxGroupAssign(g,0x6effa0)` (selection-green, matching the selected-ring colour) so recall reads symmetrically with assign, distinguished by colour (blue=assign / green=select). Perf: reuses existing fxGroupAssign → inherits its 12-unit GROUP_PING_CAP, ring_shock tex + fxs pool + fxLevel>=2 gate; event-driven (one keypress), zero per-frame alloc, client-only (no gameplay/net change). Validation: extracted the recall branch to a scratch .js, `node --check` clean + ran the empty/non-empty branch w/ stubbed fxGroupAssign(list,col) — no throw, signature matches.
