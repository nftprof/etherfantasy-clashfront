# HUD / Mobile-controls polish log

One small, reversible, validated improvement per run. Newest at bottom.

- 2026-06-28T15:53Z — **Mobile zoom control.** Added touch-only `#zoomCtl` (+/−) buttons mapped to `camZoom` via new `efZoom(d)` helper, clamped to the same 0.6–1.8 range as the desktop scroll-wheel. Parked on the left edge, vertically centred, clear of the bottom-left joystick and bottom-right skill cluster; safe-area-inset aware. Gated behind `.ef-touch` (`display:none` by default) so **desktop is completely unchanged**. **Mobile impact:** phones (no scroll-wheel) can now zoom in/out. **Perf:** CSS-only buttons + a one-line clamp on tap — zero per-frame cost, camZoom already read each frame. **Validation:** copied efZoom+clamp into outputs/zoom_clamp_check.js, `node --check` + assertions (step, both clamps, full sweep stays in [0.6,1.8]) → all passed. Re-read CSS/HTML/JS regions; selectors (`.ef-touch`, `camZoom`) confirmed to match existing code.
- 2026-06-28T23:08Z — **Disabled mobile overscroll (pull-to-refresh + modal scroll-chaining).** Audited the file (grep) — there was NO `overscroll-behavior` anywhere. `html,body{overflow:hidden}` (line 14) stops the page itself scrolling, but on Chrome/Android a downward swipe near the top still triggers the browser **pull-to-refresh**, which would reload the match mid-game — and the centred scrollable modals added by prior runs (`#help.show`/`#shop`/`#am`/`#score`, all `overflow:auto`, plus the portrait champion-select `#heroCards`) let a scroll gesture **chain** into the page once you hit the panel's end (rubber-band). Both read as unpolished vs AoV/Wild Rift. Added two `.ef-touch`-gated rules grouped with the foundation rules (right under the tap-highlight rule): `.ef-touch{overscroll-behavior:none;-webkit-overscroll-behavior:none}` to kill pull-to-refresh/rubber-band on the document, and `.ef-touch #help.show,.ef-touch #shop,.ef-touch #am,.ef-touch #score,.ef-touch #heroCards{overscroll-behavior:contain}` so scrolling to a modal's end doesn't bleed to the body. `overscroll-behavior` is NOT inherited, so the root rule and the per-modal rule are both required. **Mobile impact:** no accidental page-reload from a swipe during play; modal scrolling stays contained — native-app feel. **Desktop:** untouched — rules are `.ef-touch`-gated and `overscroll-behavior` only affects touch/trackpad overscroll; mouse-wheel play is unchanged. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied both rules into `outputs/overscroll_check.css`, node balance check → braces 0 / parens 0 balanced, exactly 2 rules, 0 decls missing a colon, root `overscroll-behavior:none` + `overscroll-behavior:contain` both regex-matched, all 5 modal selectors present & `.ef-touch`-gated → ALL PASS. Grepped first: confirmed no pre-existing `overscroll-behavior` to collide with and that `html,body{overflow:hidden}` (line 14) + the four modal `overflow:auto` rules / `#heroCards` (line 20) exist as targeted. Re-read edited region (lines 89–96) — comment opened/closed, two balanced rules, `.ef-touch`/`#help.show`/`#shop`/`#am`/`#score`/`#heroCards` all confirmed against existing selectors. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
`/`#fsBtn` selectors all match existing IDs; verified geometry (minimap bottom 158 < row 166; ❔ 8–38 vs ⛶ 46–76 = 8px gap; feed 204 > row bottom 196).
- 2026-06-28T16:41Z — **Fixed the remaining mobile minimap overlap (⋯ extras drawer).** The prior run cleared ❔/⛶ off the minimap but the third top-right control — `#efExtraTgl` (the ⋯ extras toggle injected by `shared/ef_touch.js`) — was still parked at `top:12/right:16` with an 11vmin (~52px) box, sitting *on top of* the 150×150 minimap (`top:8`→158px): it hid the minimap corner and stole its camera-jump taps. Added three `.ef-touch`-gated overrides in `index.html` (no edit to shared/ef_touch.js, no JS): pulled ⋯ into the same below-minimap row as the 3rd item (`right:calc(84px+safe-area-inset-right)` → 84–114px; row now ❔8–38 · ⛶46–76 · ⋯84–114, 8px gaps) sized 30px to match; reflowed its drawer (`#efExtra` 🛒📣⏹📦) into a compact horizontal strip opening LEFT along the same 166px band (`right:122` → x 122–266) with `#efExtra .eb` shrunk 11vmin→30px. All clear of minimap above (bottom 158, 8px clearance) and feed below (top 204). **Mobile impact:** minimap corner fully visible & tappable; extras drawer now reads as part of the same tidy top-right control row instead of floating over the map. **Desktop:** untouched — `#efTouch` is only built on touch devices and every rule is `.ef-touch`-scoped. **Perf:** CSS-only, zero JS, zero per-frame cost. **Validation:** scratch `efextra_css_check.css` → braces 3/3 BALANCED, no decls missing colon; geometry asserted in node (toggle 84–114, drawer 122–266, all gaps ≥8, minimap/feed clearances ok); re-read edited region (lines 84–93) — comment closed, selectors `#efExtraTgl`/`#efExtra`/`.eb` confirmed present in ef_touch.js, `.ef-touch` class confirmed added to documentElement.
- 2026-06-28T16:52Z — **Aligned the minimap to the notch-aware right-edge stack (touch only).** Every element in the mobile top-right column — `#helpTgl` ❔, `#fsBtn` ⛶, `#efExtraTgl` ⋯, `#feed` — already uses `right:max(8px,env(safe-area-inset-right))`, but `#mm` (the minimap, top of that column) was still pinned to a flat `right:8px`. On a notched phone in landscape that left the minimap's right edge misaligned with (and poking past) the controls beneath it, into the notch/rounded-corner zone. Added one `.ef-touch #mm{right:max(8px,env(safe-area-inset-right))}` override so the whole right column shares a single clean edge and the minimap clears the notch. **Mobile impact:** consistent right-edge alignment of minimap + control row + feed on notched devices; minimap no longer hidden under the notch in landscape. **Desktop:** untouched — base `#mm{right:8px}` (line 64) unchanged and the override is `.ef-touch`-gated; non-notched touch devices resolve `max(8px,0)=8px`, so no visual change there either. **Perf:** CSS-only, zero JS, zero per-frame cost. **Validation:** scratch `outputs/mm_inset_check.css` → braces 3/3 & parens 6/6 BALANCED, no decl missing colon; re-read edited region (lines 79–89) — comment opened/closed, single balanced rule, `.ef-touch`/`#mm` both confirmed against existing selectors (`#mm` base at line 64).
- 2026-06-28T17:08Z — **Joystick dead-zone (mobile control feel).** The shared virtual stick (`shared/ef_touch.js`, used by both the MOBA and EF Hunt) reported raw magnitude `cl/R` straight through, so a light resting finger or jitter near centre still produced a direction and drifted the hero. Added a 0.12 dead-zone in `move()` with a smooth rescale `(mag-DZ)/(1-DZ)` so output stays 0 inside the zone and ramps a clean 0→1 beyond it — **no speed loss at the rim**, and the knob still visually tracks the finger fully. Also hardened `aimDir()`: when the stick is held but inside the dead-zone (`x≈z≈0`) it now falls back to `_last` (last deliberate direction / default forward) instead of returning a zero vector, so quick-cast skill aiming can't fire with no direction. Aim-memory threshold (`mag>0.25`) unchanged. **Mobile impact:** no accidental drift on a barely-touched stick; steadier movement + reliable skill aim. **Desktop:** untouched — `EF_TOUCH` only initialises on touch devices; keyboard+mouse path never runs this code. **Perf:** two scalar ops added inside the existing pointermove handler (event-driven, not per-frame); zero cost in the 60fps render loop. **Validation:** copied the edited `move()` core + `aimDir()` into `outputs/joy_deadzone_check.js`, `node --check` + 12 assertions (dead-zone at 0.05/0.12 → 0, smooth ramp at 0.13, midpoint 0.56→0.5, full 1.0→1.0, clamp beyond rim, direction preserved, aimDir dead-zone fallback to `_last`, aimDir live-held, `_last` update threshold) → all passed. Re-read both edited regions (lines 20, 113–122); only the joystick `move()`/`aimDir` touched, no other call sites affected.
- 2026-06-28T17:23Z — **Mobile hide/show-HUD toggle (declutter).** Added a touch-only 👁 button as the first item in the existing `#zoomCtl` left column; `efHudToggle()` flips a `.ef-hud-hidden` class on `<html>` whose CSS hides ONLY the informational overlays (`#stats` player panel + `#feed` kill feed) via `display:none!important`. Deliberately touches NO control — joystick, skill bank, minimap, zoom, fullscreen and the top-bar buttons (quick-cast/cam-lock/FF/ping/town-hall) all stay live, so no affordance is ever removed and the same tap restores everything (glyph 👁↔🙈, title, and a dimmed `.on` state sync on toggle). Placed inside `#zoomCtl` (already positioned, `.ef-touch`-gated, notch-aware, cleared of joystick/skill thumbs) → reused `#zoomCtl button` styling, zero new layout math, zero overlap risk. **Mobile impact:** one-tap cleaner view for screenshots/immersion on small screens; reversible. **Desktop:** untouched — `#zoomCtl` is `display:none` until `.ef-touch`, so the button never shows and `.ef-hud-hidden` is never set. **Perf:** pure class flip + CSS — no JS layout reads, zero per-frame cost in the Three.js loop. **Validation:** copied `efHudToggle` + a DOM/classList stub into `outputs/hudtoggle_check.js`, `node --check` + 11 assertions (initial shown, 1st tap hides + glyph/title/`.on` sync, 2nd tap shows + clears, odd/even toggle parity over 12 flips with no class leak, missing-button no-throw) → ALL PASSED. Re-read edited CSS (108–115), HTML (`#zoomCtl` row) and JS (`efHudToggle`) regions; braces balanced, `#stats`/`#feed`/`#hudTgl`/`#zoomCtl` selectors all confirmed against existing IDs.
- 2026-06-28T17:36Z — **Notch-aware left edge for the stats panel (landscape).** The right-column inset work (16:52) gave `#mm`/`#feed`/`#helpTgl`/`#fsBtn`/`#efExtraTgl` a shared `right:max(8px,env(safe-area-inset-right))` edge, and `#zoomCtl` already uses `left:max(10px,env(safe-area-inset-left))` — but `#stats` (the always-visible top-left player panel: Lv/XP/gold/wood/pets/units/K-D-CS) was still pinned to a flat `left:8px`. In **landscape** — the normal MOBA orientation — a notched phone's cutout/rounded corner sits on the LEFT edge, so the panel slid under the notch. Added one `.ef-touch #stats{left:max(8px,env(safe-area-inset-left))}` override (left-edge mirror of the existing `#mm` right-edge fix) so the whole left edge clears the notch and lines up with `#zoomCtl` below it. **Mobile impact:** stats panel fully visible (not under the notch) on notched phones in landscape; consistent left-edge alignment of the left column. **Desktop:** untouched — base `#stats{left:8px}` (line 61) unchanged, override is `.ef-touch`-gated; non-notched touch resolves `max(8px,0)=8px` so no visual change there either. **Perf:** CSS-only, zero JS, zero per-frame cost. **Validation:** CSS-only change — re-read edited region (lines 89–96): comment opened/closed, single balanced rule, `max()`/`env()` parens balanced (3/3), `.ef-touch`/`#stats` confirmed against existing selectors (`#stats` base at line 61, `.ef-touch` used throughout). Mirrors the proven 16:52 `#mm` inset pattern exactly.
- 2026-06-28T17:53Z — **Mobile tap-feedback parity for the remaining touch buttons.** Audited every on-screen mobile control: the skill thumbs (`#efBtns .eb`, ef_touch.js:42), the zoom +/− (`#zoomCtl button:active`) and the ⛶ fullscreen (`#fsBtn:active`) all gave a press-down `filter:brightness(1.3);transform:scale(.93)` cue — but three tap targets had NONE and felt dead on touch: the ❔ help toggle (`#helpTgl`), the ⋯ extras toggle (`#efExtraTgl`) and the extras-drawer items 🛒📣⏹📦 (`#efExtra .eb`). Added one `.ef-touch`-gated rule giving all three the SAME brightness+scale cue so every mobile tap responds identically (top-tier-MOBA tactile feel). Verified none carry a base `transform` that the `:active` scale could fight: grepped ef_touch.js — `efspin` animates only `#efRotate .ic`, and the three targets' base rules set no transform, so the scale reverts cleanly on release. **Mobile impact:** consistent, responsive press feedback across the whole HUD control set; no dead-feeling buttons. **Desktop:** untouched — rule is `.ef-touch`-scoped and `#efExtraTgl`/`#efExtra` are only built on touch devices; desktop `#helpTgl` click behaviour unchanged. **Perf:** CSS-only `:active` pseudo-class, zero JS, zero per-frame cost in the 60fps loop. **Validation:** copied the new rule into `outputs/active_feedback_check.css` and ran a node balance check → braces 1/1, parens 2/2, all decls have a colon, 3 selectors parsed. Re-read edited region (lines 131–137): comment opened/closed, single balanced rule; `.ef-touch`/`#helpTgl`/`#efExtraTgl`/`#efExtra .eb` all confirmed against existing selectors (`#helpTgl` 72/82, `#efExtraTgl` 104, `#efExtra .eb` 106). Matches the existing `#fsBtn:active` values exactly.
- 2026-06-28T18:22Z — **Capped the mobile kill feed so it can't pile into the skill cluster.** `feed()` (index.html:1494) `prepend`s each kill (newest on top, oldest at the bottom) and auto-removes each entry after 5.6s — but during a teamfight/ace, several kills land inside that window and stack DOWN the right edge. With `#feed` previously unbounded (`top:204px`, no max-height), on a short landscape phone that growing column ran straight over the bottom-right skill thumbs. Added `max-height:38vh;overflow:hidden` to the existing `.ef-touch #feed` rule: the column now shows only the most recent entries and clips the rest. Because newest are *prepended at the top*, `overflow:hidden` hides the OLDEST (least-relevant) entries — exactly the right ones to drop. `#feed` is `pointer-events:none`, so capping it never affects taps on the skills beneath. **Mobile impact:** kill feed stays a tidy, bounded strip under the minimap instead of sprawling over the skill cluster during bursts. **Desktop:** untouched — base `#feed{top:180;width:210}` (line 72) has no max-height and the cap is `.ef-touch`-gated. **Perf:** CSS-only, zero JS, zero per-frame cost. **Validation:** scratch `outputs/feed_cap_check.css` → braces 1/1 & parens 2/2 BALANCED, 4 decls all have a colon, `max-height:38vh`+`overflow:hidden` present; re-read edited region (lines 90–96) — comment opened/closed, single balanced rule; confirmed `feed()` uses `f.prepend(d)` (newest-first) so clipping drops oldest, `#feed` carries `pointer-events:none` (line 72), and `.ef-touch`/`#feed` match existing selectors.
- 2026-06-28T18:07Z — **Added the missing viewport meta tag (mobile foundation).** The document head had `charset` + `title` but NO `<meta name="viewport">` — so every phone rendered the page at the default ~980px layout width and scaled it down: tiny UI, and all the `vmin`-sized touch controls (joystick/skill thumbs at 34/14vmin) computed off the wrong viewport. Worse, `env(safe-area-inset-*)` only returns non-zero when the page opts in via `viewport-fit=cover`, so **every notch-aware rule added in prior runs (`#mm`, `#stats`, `#feed`, `#zoomCtl`, `#helpTgl`, `#fsBtn`, `#efExtraTgl`, the joystick/skill insets) was silently resolving to its `max(...,0)` fallback and doing nothing.** Added `<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">` right after `charset`. `width=device-width`+`initial-scale=1` fixes the scale so vmin controls size correctly; `viewport-fit=cover` ACTIVATES all the existing safe-area work at once; `user-scalable=no`/`maximum-scale=1` stop accidental two-finger/double-tap page-zoom from knocking the HUD out of alignment mid-match (the game already has its own +/− camZoom). **Mobile impact:** correct 1:1 scaling, properly-sized thumb controls, and notch insets that finally take effect — single biggest mobile correctness win. **Desktop:** completely unchanged — desktop browsers ignore the viewport meta entirely. **Perf:** one static `<meta>`, zero JS, zero per-frame cost. **Validation:** markup-only — re-read head region (lines 1–13): comment `<!--`…`-->` balanced, single self-closing `<meta>` with a balanced double-quoted `content` attribute, tag order valid (charset → viewport → title → style). Confirmed via grep there was no pre-existing `viewport`/`user-scalable` meta to duplicate, and that the safe-area rules it enables (`env(safe-area-inset-*)` on `#mm`/`#stats`/`#feed`/`#zoomCtl`/joystick) already exist in index.html + ef_touch.js.
- 2026-06-28T18:54Z — **Bumped the mobile top-right control row to 44px tap targets.** The ❔ help toggle (`#helpTgl`), ⛶ fullscreen (`#fsBtn`), ⋯ extras toggle (`#efExtraTgl`) and the extras-drawer items 🛒📣⏹📦 (`#efExtra .eb`) were all sized **30px** — below the iOS HIG (44pt) / Material (48dp) minimum and inconsistent with the `#zoomCtl` +/− buttons (already 44px), so they were fiddly to hit on a phone. Enlarged all four to **44px** and reflowed the below-minimap row right→left with the same 8px gaps: ❔ `right:8`, ⛶ `right:60`, ⋯ `right:112` (outer edge 156 ≤ the minimap's right-8→158 width, so the row still tucks neatly under the 150px minimap), and the ⋯ drawer to `right:164` (8px past ⋯'s 156 outer edge, opens left as a horizontal strip). Nudged `#feed` `top:204→214` so the kill feed clears the now-taller row (bottom 166→210). Radius 8→10 and font-sizes scaled up to fit the larger glyphs. **Mobile impact:** comfortable, standard-sized hit targets for help/fullscreen/extras — no more mis-taps; visually consistent with the zoom column. **Desktop:** untouched — `#helpTgl` size override is `.ef-touch`-gated (base 30px at left:10/bottom:10 unchanged), `#fsBtn`/`#efExtra*` are touch-only (never shown/built on desktop). **Perf:** CSS-only, zero JS, zero per-frame cost. **Validation:** (1) `outputs/toprow_44_geom.js` — node assertions on the new geometry: 8px gaps between all three buttons, row outer edge 156 ≤ minimap edge 158, row top 166 > minimap bottom 158, feed top 214 > row bottom 210, drawer 8px gap past ⋯, and drawer fits within 568/667/844px-wide landscape viewports → ALL PASS. (2) `outputs/toprow_44_csscheck.css` — braces 0 / parens 0 balanced, all 6 rules' decls have a colon → PASS. Re-read edited regions (lines 86–123 row/feed/extras, 136–141 `#fsBtn`); selectors `.ef-touch`/`#helpTgl`/`#feed`/`#fsBtn`/`#efExtraTgl`/`#efExtra .eb`/`#mm` all confirmed against existing IDs; minimap width (150) and base `#helpTgl{30px}` unchanged.
- 2026-06-28T18:36Z — **Tap-responsiveness parity (touch-action:manipulation) for the remaining mobile buttons.** Audited touch-action across every on-screen tap target: `#fsBtn` and `#zoomCtl button` already declare `touch-action:manipulation` (eliminates the legacy ~300ms tap delay and stops taps being swallowed by double-tap-zoom / pan gesture recognition), but three were missing it — the ❔ help toggle `#helpTgl` (index.html, shown on both desktop & mobile) and the ⋯ extras toggle `#efExtraTgl` + its drawer items 🛒📣⏹📦 (`#efExtra .eb`, injected by ef_touch.js with only the *container* `#efTouch{touch-action:none}`, no per-button manipulation). Added `touch-action:manipulation` to the `#helpTgl` base rule (line 79; benefits touch-screen desktops too, inert where there's no touch) and a new `.ef-touch`-gated rule `#efExtraTgl,#efExtra .eb{touch-action:manipulation}` (no edit to ef_touch.js). Now every mobile control fires instantly and identically — completes the tap-feedback parity work from 17:53Z with the input-latency half. **Mobile impact:** snappier, lag-free taps on the help/extras controls; no gesture-eaten presses. **Desktop:** untouched — `touch-action` only affects touch input, and the extras rule is `.ef-touch`-scoped (drawer only exists on touch). **Perf:** CSS-only, zero JS, zero per-frame cost. **Validation:** copied both edited rules into `outputs/touchaction_parity_check.css` and ran a node balance check → braces 0, parens 0, every rule has a colon, 2 rules, `touch-action:manipulation` ×2 → PASS. Re-read edited regions (line 79 base rule; the new rule directly under the 17:53Z `:active` parity rule); confirmed `#fsBtn`/`#zoomCtl button` already had it (lines 126/141), and `#helpTgl`/`#efExtraTgl`/`#efExtra .eb` match existing selectors.
- 2026-06-28T19:22Z — **Added a touch-only portrait "rotate your device" overlay (orientation handling).** A MOBA is a landscape game; in PORTRAIT the bottom-left joystick, bottom-right skill cluster, minimap and top bar all cram into a narrow tall column and overlap, with wrong camera framing. Added a full-screen `#rotateHint` overlay (📱 icon + "Rotate your device" / "EF Moba plays best in landscape") shown ONLY on touch + portrait via `@media (orientation:portrait){.ef-touch #rotateHint{display:flex}}` — the Wild Rift / Arena of Valor / Honor of Kings pattern. Opaque `#0a0e14` background fully hides the broken portrait layout; `z-index:9999` sits above all HUD. Element is `display:none` by default (markup added once after `#pingwheel`), so it never appears on desktop (`.ef-touch`-gated) and vanishes the instant the phone is turned to landscape. **Mobile impact:** clean, professional prompt to orient the device correctly instead of a jumbled portrait HUD. **Desktop:** untouched — `.ef-touch` gate + the element is hidden by default; desktop never matches the rule. **Perf:** the only animation is the icon's `transform:rotate` (GPU/compositor, no layout/paint) and it runs ONLY while the overlay is shown — in landscape (actual play) the element is `display:none`, so zero cost in the 60fps Three.js loop. **Validation:** copied the new block into `outputs/rotatehint_check.css` and ran a node balance check → braces 0, parens 0, 0 decls missing a colon, `@media (orientation:portrait)` + `@keyframes rhSpin` both present → PASS. Grepped first: no pre-existing `#rotateHint`/`@media (orientation:portrait)`/`rhSpin`/orientation handling to collide with; confirmed `.ef-touch` is added to `document.documentElement` in shared/ef_touch.js:165. Re-read edited CSS (lines 168–180) and the added markup — well-formed, single overlay element, selectors consistent.
- 2026-06-28T19:07Z — **Hid the keyboard/mouse-only hint string from the mobile top bar.** The desktop `#top` bar (timer + 6 control buttons) ends with a trailing span `TAB ⇢ scoreboard · A ⇢ attack-move · Alt+click ⇢ ping` — a ~200px keyboard/mouse-only legend (TAB, attack-move `A`, Alt+click) that has no meaning on a phone, yet still padded out the centered, **non-wrapping** top row so it overflowed toward `#stats` (top-left) and the minimap (top-right) on narrow landscape screens. Tagged that one span with `class="kbHint"` (its inline style otherwise unchanged) and added one `.ef-touch .kbHint{display:none}` rule, grouped with the existing `.ef-touch` "hide desktop-only HUD" rules (right under the `#amBtn/#shopBtn/#thBtn` hide). Removing content can only make the bar narrower → strictly reduces top-edge overlap, never adds any. **Mobile impact:** less top-bar clutter and less crowding of the stats panel / minimap; no irrelevant keyboard hints shown on touch. **Desktop:** untouched — the hint still shows (`.ef-touch`-gated; span style unchanged), and the only `kbHint` selector is this one rule. **Perf:** CSS-only, zero JS, zero per-frame cost. **Validation:** grep confirmed exactly two `kbHint` occurrences (the rule at line 90 + the single span at line 290) — class applied to one element only; copied the rule into `outputs/kbhint_check.css` and ran a node balance check → braces 0, parens 0, has colon, regex-matched `.ef-touch .kbHint{display:none}` → PASS; re-read edited CSS region (lines 83–91: well-formed, comment opened/closed, `.ef-touch`/`#stats`/`#mm` selectors confirmed against existing IDs) and the edited `#top` span.
- 2026-06-28T19:36Z — **Suppressed native mobile tap chrome (grey/blue tap-highlight + long-press callout).** Audited the file (grep) — there was NO `-webkit-tap-highlight-color` or `-webkit-touch-callout` anywhere, so WebKit/Blink mobile browsers were painting their default grey/blue highlight box over every tapped element and could pop the long-press copy/save callout. On a fast-tap MOBA that flashes on every joystick / skill thumb / top-bar / zoom / fullscreen / extras press, and the callout can fire on a deliberately *held* control (the StarCraft-style command-queue hold, the town-hall double-tap) — both read as cheap/unpolished vs AoV/Wild Rift. Added one rule `.ef-touch{-webkit-tap-highlight-color:transparent;-webkit-touch-callout:none}` grouped with the other `.ef-touch` foundation rules (right above `.ef-touch #hud`). `-webkit-tap-highlight-color` is an *inherited* property, so setting it on the `.ef-touch` `<html>` cascades to ALL HUD elements AND the `#efTouch` controls injected by shared/ef_touch.js — single rule, full coverage, no edit to ef_touch.js. **Mobile impact:** no flash box and no long-press menu on any control; presses look clean and native-app-like. **Desktop:** untouched — `.ef-touch` is only added on touch devices and neither property has any effect with a mouse. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied the rule into `outputs/tapchrome_check.css` and ran a node balance check → braces 0, parens 0, 2 decls, 0 missing-colon, `-webkit-tap-highlight-color:transparent` + `-webkit-touch-callout:none` both regex-matched → PASS. Re-read edited region (lines 83–90): comment opened/closed, single balanced rule, `.ef-touch` confirmed against the existing class (added to `document.documentElement` in shared/ef_touch.js:165); preserves all existing behaviour (command-queue hold, town-hall single/double-tap, quick-cast, cam-lock, HP modes, minimap, ping wheel, edge-pan untouched).
- 2026-06-28T19:48Z — **Removed a duplicate/conflicting portrait "rotate" overlay (deferred to the canonical menu-aware gate).** Discovered TWO full-screen portrait overlays competing: `shared/ef_touch.js`'s original `#efRotate` gate (z:9500, built in buildUI) AND the `#rotateHint` overlay a prior run added to index.html (19:22Z, z:9999). They conflicted: `#efRotate` is strictly better — (a) MENU-AWARE (`checkOrient()` early-returns when `T._menu`, so it stays hidden during champion-select/lobby and does NOT cover the `#heroCards` screen that the `(pointer:coarse)` media query at lines 25–33 was specifically built to make portrait-playable), and (b) it carries a "▶ Play full-screen + lock landscape" button. But `#rotateHint` sat at the HIGHER z:9999, so in portrait it (1) covered `#efRotate`, hiding its fullscreen button, and (2) blanket-covered the portrait-friendly lobby — a regression. Removed ONLY the `@media (orientation:portrait){.ef-touch #rotateHint{display:flex}}` show rule so `#rotateHint` keeps its base `display:none` and `#efRotate` becomes the single clean gate; left the markup + its `.rhIcon/.rhTxt/.rhSub/@keyframes rhSpin` rules in place (harmless, never rendered) for a one-line revert. **Mobile impact:** one portrait gate instead of two stacked; the fullscreen-entry button is reachable again, and the portrait champion-select lobby is no longer blocked. **Desktop:** untouched — both overlays were always `.ef-touch`-gated, so desktop never showed either. **Perf:** CSS-only (removed a rule), zero JS, zero per-frame cost; in landscape (actual play) nothing rendered before or after. **Validation:** markup/CSS-only — grep confirmed NO JS references to `#rotateHint`/`rhSpin` (only CSS + the one `<div id="rotateHint">` at line 310); re-read edited region (lines 183–195): comment `/*`…`*/` balanced, the `@media` show rule gone, `#rotateHint` base rule still `display:none`, surrounding rules intact. Confirmed against shared/ef_touch.js that `#efRotate` exists (line 54), is built with the fullscreen button (95–101), and `checkOrient()` (143–150) is menu-aware via `T._menu`; preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T20:08Z — **Mobile tap targets for the #top control bar.** The top bar's six control buttons (⚑FF, ⚡quick-cast, 🎥follow, ❤bars, 🏰town-hall, 📣ping) carried inline `font-size:12px;padding:1px 7px` → only ~18px tall, well under the 44px the right-column controls (❔/⛶/⋯, zoom +/−) already use, and unchanged on touch — fiddly to thumb on a phone and the last sub-spec tap targets in the HUD. Added three `.ef-touch`-gated rules (grouped under the `.kbHint` hide): `#top button{padding:9px 9px!important;font-size:13px!important;touch-action:manipulation}` (≈18→~31px tall hit area; `!important` because the size is set inline; `touch-action:manipulation` kills the 300ms tap delay / gesture-swallow, completing the parity work from the 18:36Z run), a matching `:active{filter:brightness(1.3);transform:scale(.94)}` press cue (completes the 17:53Z tap-feedback parity for the top bar), and `#top{gap:8px}` to tighten the flex gap 16→8px. Since `#top` does NOT wrap, the only overlap risk is WIDTH: the gap cut frees ~64px (8 gaps ×8px) while the taller buttons add only ~36px of width, so the bar gets **narrower**, strictly reducing any crowding of `#stats` (left) / the minimap (right). Bar height grows ~18→~33px (top:8→~41px), still clear of `#score` at top:54. No base transform on these buttons (no `.qcBtn.on`/`.camBtn.on` rule exists — only `.mpBtn.on`/`#hudTgl.on`/`#pingwheel.on`), so the `:active` scale reverts cleanly. **Mobile impact:** comfortable, responsive, visually-consistent top-bar buttons — preserves quick-cast/cam-lock/HP-mode/town-hall/ping/FF behaviour, just bigger & snappier. **Desktop:** untouched — every rule is `.ef-touch`-gated; the compact mouse layout (inline 12px/1px·7px, gap:16px) is unchanged. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied the 3 rules into `outputs/topbar_tap_check.css`, node balance check → braces 0, parens 0, 3 rules, 0 missing-colon, all four regex assertions (gap:8px, padding+font !important, touch-action, :active cue) → PASS. Re-read edited region (lines 96–109): comment opened/closed, three balanced rules; confirmed `#top`/`.ef-touch` match existing selectors and the six `#top` buttons carry the inline sizing the `!important` overrides target.
- 2026-06-28T20:23Z — **Fixed the mobile selection panel hiding behind the joystick.** `#sel` (the unit/node/hero info panel + its action buttons: control/build/inspect) is anchored bottom-LEFT (`left:8;bottom:84`, line 222) — exactly where the bottom-left virtual joystick sits. The joystick lives in `#efTouch` at `z-index:9000` while `#sel` is only `z-index:20`, so on touch the joystick renders OVER the panel: it was occluded and its tap buttons unreachable whenever you selected a unit/node on a phone. Added one `.ef-touch`-gated rule relocating `#sel` to the bottom-CENTRE — `left:50%;right:auto;transform:translateX(-50%);bottom:84px` — matching the `#build` menu's existing anchor exactly. Safe because `#sel` and `#build` are MUTUALLY EXCLUSIVE: every show path clears the other (`buildUnitPanel`/`showNodePanel`/`showInfoPanel` call `clearBuild()`; `openBaseMenu`/`enterBuild` call `clearSel()`), so they never coexist in that slot, and the bottom-centre gap between the joystick (left) and skill cluster (right) is otherwise empty on mobile (`#hud` is hidden via `.ef-touch`). `max-width` clamped to `min(70vw,240px)` so it can't overrun into the joystick/skills on the narrowest landscape phones. `#sel` carries no base transform and no `:active` rule, so the added `translateX` is conflict-free. **Mobile impact:** selecting a unit/node now shows a fully visible, tappable panel in the clear bottom-centre instead of a panel buried under the joystick. **Desktop:** untouched — base `#sel{left:8;bottom:84}` (line 222) unchanged and the override is `.ef-touch`-gated. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied the rule into `outputs/sel_center_check.css`, node balance check → braces 0, parens 0, 5 decls, 0 missing-colon, and asserted parity with `#build` (centered `left:50%`+`translateX(-50%)`, `bottom:84px`, `right:auto`, `max-width:min(70vw,240px)`) → ALL PASS. Confirmed via grep that `#build` (line 216) uses the same `left:50%;bottom:84px;translateX(-50%)` anchor and that the show/clear paths are mutually exclusive (clearBuild/clearSel cross-calls at lines 2474/2600/2842/2976); re-read edited region (lines 137–148) — comment opened/closed, single balanced rule, `.ef-touch`/`#sel` confirmed against existing selectors (`#sel` base at line 222, z:20 vs `#efTouch` z:9000 in shared/ef_touch.js:35).
- 2026-06-28T20:36Z — **Mobile tap targets for the selection-panel action buttons (`#sel .pb`).** After the 20:23Z run relocated `#sel` (unit/node/hero info panel) to the clear bottom-centre on touch, its action buttons — `.pb` (control / build / inspect, line 237) — were still the last mouse-sized tap targets in the HUD: `padding:7px 10px;font-size:11px` → only ~26px tall, below the 44px the rest of the mobile HUD (right-column ❔/⛶/⋯, zoom +/−, top bar) already uses, and unchanged on touch. Added two `.ef-touch`-gated rules grouped right under the 20:23Z `.ef-touch #sel` rule: `#sel .pb{padding:12px 13px;font-size:13px;touch-action:manipulation}` (~26→~40px hit area; `touch-action:manipulation` kills the 300ms tap delay / gesture-swallow — input-latency parity) and a `:active{filter:brightness(1.3);transform:scale(.94)}` press cue (tap-feedback parity). `#sel .pbtns` already `flex-wrap`s inside the 70vw/240px-clamped panel, so the larger buttons simply reflow and the panel grows UP into the empty bottom-centre gap (joystick left, skills right) — no new overlap, and `#sel` carries no base transform so the `:active` scale reverts cleanly (no prior `:active`/transform on `.pb`). **Mobile impact:** comfortable, responsive, visually-consistent action buttons when a unit/node is selected — completes the tap-target/feedback parity across the whole mobile HUD. **Desktop:** untouched — both rules are `.ef-touch`-gated; base `#sel .pb` (line 237) and its `:hover` (238) unchanged. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied both rules into `outputs/sel_pb_tap_check.js`, `node --check` + assertions (braces 0, parens 0, exactly 2 rules, every decl has a colon, `touch-action:manipulation` / `padding:12px 13px` / `font-size:13px` / `:active` cue all regex-matched, approx tap height ≥40px) → ALL PASS. Grepped first: no pre-existing `.ef-touch #sel .pb` rule (only base `#sel .pb`/`:hover` at 237–238); re-read edited region — comment opened/closed, two balanced rules, `.ef-touch`/`#sel .pb`/`.pbtns` confirmed against existing selectors. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T20:54Z — **Fixed the mobile Hero Shop opening behind the skill cluster.** `#shop` (the hero item shop, reachable on touch via the ⋯ drawer's 🛒) is anchored bottom-RIGHT (`bottom:52;right:10;width:270;max-height:62vh`, base rule line ~273) at `z-index:22` — directly under the bottom-right virtual **skill cluster** `#efBtns`, which `shared/ef_touch.js` parks at `z-index:9000`. So on a phone the skill thumbs rendered OVER the open shop: its lower item rows were occluded and un-tappable behind the joystick skills (the exact occlusion class fixed for `#sel` at 20:23Z). Added one `.ef-touch`-gated rule promoting the shop to a centred modal **above** the controls, mirroring the existing `.ef-touch #help.show` pattern: `#shop{position:fixed;left:50%;right:auto;top:50%;bottom:auto;transform:translate(-50%,-50%);width:min(86vw,300px);max-height:74vh;z-index:9300}`. `z:9300 > 9000` so the whole shop is visible and tappable; centred + clamped width/height fits landscape. The shop is a pause-style buy menu (you tap items, you don't cast from it), so briefly covering the idle controls while open is fine — `toggleShop()` closing it restores everything. On a 667×375 landscape phone the 300px-wide centred panel (x 183–483) clears the bottom-left joystick (≤128px), so movement stays reachable around it. **Mobile impact:** the Hero Shop is fully visible and every item is tappable instead of hiding behind the skill cluster. **Desktop:** untouched — rule is `.ef-touch`-gated; base bottom-right `#shop` layout unchanged. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied the rule into `outputs/shop_modal_check.js`, `node --check` + assertions (braces 0 / parens 0 balanced, every decl has a colon, selector `.ef-touch #shop` beats base `#shop` on specificity so order-independent, `z-index:9300` / `position:fixed` / dead-centre `50%`+`translate(-50%,-50%)` / `right:auto`+`bottom:auto` / `max-height:74vh` / `width:min(86vw,300px)` all present, and centred-shop-vs-joystick geometry 183.5>128) → ALL PASS. Re-read edited region (lines 159–168): comment opened/closed, single balanced rule; confirmed via Read there was no pre-existing `.ef-touch #shop` override (base `#shop` only), `#efBtns` z:9000 in `shared/ef_touch.js:35/39`, and the `.ef-touch #help.show` modal precedent (z:9300) it mirrors. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T21:06Z — **Made the mobile sound control notch-aware (right-edge alignment).** `#sndCtl` (mute 🔊 + volume slider) sits just LEFT of the minimap via a flat inline `right:166px` (= the minimap's 158px right edge + an 8px gap). When the 16:52Z run gave `#mm` `right:max(8px,env(safe-area-inset-right))`, the minimap shifts LEFT by the inset on a notched phone in landscape — but `#sndCtl` stayed pinned at 166px, so the minimap's left edge slid OVER the sound control (mute/volume occluded under the map corner). Added one `.ef-touch`-gated rule `#sndCtl{right:calc(158px + max(8px,env(safe-area-inset-right)))!important}` so it tracks the same inset and keeps a constant 8px gap to the minimap at any notch depth (`!important` to beat the inline `right:166px`). This is the right-column counterpart to the proven 16:52Z `#mm` / 17:36Z `#stats` inset fixes — completes the notch alignment of the top-right cluster. **Mobile impact:** mute/volume stays fully visible and tappable beside the minimap on notched landscape phones instead of hiding under it; on non-notched touch it resolves to 158+8=166 = the existing position (no change). **Desktop:** untouched — rule is `.ef-touch`-gated and base inline `right:166px` is unchanged. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied the rule into `outputs/sndctl_inset_check.js`, `node --check` + assertions (braces 0 / parens 0 balanced, decl has a colon, prop is `right`, `!important` present to beat inline, and an 8px constant gap to the minimap verified across insets 0/8/20/44/59 with base(0)=166, notch(44)=202) → ALL PASS. Re-read edited region (lines 137–145): comment opened/closed, single balanced rule; confirmed `#sndCtl` is the inline `right:166px` element at line ~366, `#mm` uses `right:max(8px,inset)` (line 130), and `.ef-touch`/`#sndCtl` match existing selectors. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T21:20Z — **Fixed mobile build / Town-Hall menu overflow + tap targets.** `#build` (the construct/production menu opened by the town-hall single/double-tap and node selection) is a NON-WRAPPING centered flex row (`display:flex`, `translateX(-50%)`, line 255) whose `.btn` items are `min-width:92px`. `openBaseMenu` can stack up to ~6 of them (⬆Upgrade Defense · ✨Summon Pets · ⚔Train Footman · 🏹Train Archer · ⭐Evolve Hero + the title span) → ~600px wide, so on a narrow landscape phone the row overran BOTH screen edges and the outer buttons fell off-screen / unreachable; the `.btn` were also still mouse-sized (`padding:8px 12px`/`font:12px` → ~30px tall), the last sub-44px tap targets in the HUD. Added three `.ef-touch`-gated rules (grouped with the `#sel` panel work): `#build{flex-wrap:wrap;justify-content:center;max-width:min(94vw,440px)}` so the row WRAPS and stays on-screen (it grows UP into the empty bottom-centre gap between joystick & skills — the same slot `#sel` uses; they're mutually exclusive so no collision), `#build .btn{padding:12px 14px;font-size:13px;min-width:104px;touch-action:manipulation}` for a ~44px hit area + no 300ms tap delay, and a `:active{filter:brightness(1.3);transform:scale(.96)}` press cue — completing the tap-target/feedback/latency parity across the whole mobile control set. `#build .btn` carries no base transform (only `:hover` border-color), so the `:active` scale reverts cleanly. **Mobile impact:** the build/production menu stays fully on-screen and every option is reachable & comfortably tappable instead of overflowing off both edges. **Desktop:** untouched — every rule is `.ef-touch`-gated; the compact single-row mouse layout (inline-driven, min-width:92px) is unchanged. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied the 3 rules into `outputs/build_wrap_check.js`, `node --check` + assertions (braces 0 / parens 0 balanced, exactly 3 rules, every decl has a colon, `flex-wrap:wrap` / `max-width:min(94vw,440px)` / `touch-action:manipulation` / `min-width:104px` / `:active` cue all regex-matched; geometry: container caps at min(94vw,440px) ≤ viewport at 568/667/740/844px-wide landscape, ≥3 buttons/row → 6 buttons wrap to ≤2 rows; approx tap height ~47px ≥44) → ALL PASS. Grepped first: no pre-existing `.ef-touch #build` rule (base `#build`/`.btn`/`:hover` at 255–258 only); re-read edited region (lines 167–179) — comment opened/closed, three balanced rules, `.ef-touch`/`#build`/`#build .btn` confirmed against existing selectors; confirmed `openBaseMenu` (line 2892) appends up to ~6 `.btn` via `display:flex` and that `#sel`/`#build` are mutually exclusive (clearSel/clearBuild cross-calls). Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T21:52Z — **Added a mobile scoreboard toggle (📊) — the scoreboard was desktop-only.** The per-hero Lv/K/D/CS/items scoreboard (`#score`, `showScore()` index.html:~3325) is opened on desktop by HOLDING **Tab** — an input that doesn't exist on a phone, so on mobile the scoreboard was completely **unreachable** (the only `showScore` callers were the keydown/keyup/blur handlers). Added a touch-only **📊** button as the first item in the existing `#zoomCtl` left column (mirroring the 👁 `#hudTgl` pattern), wired to a new `efScoreToggle()` that tap-toggles via the existing `showScore()` (which already guards `running && !gameOver` and renders the rows); open state is read straight off the panel's inline `display` so there's no separate flag to drift, and the button gets an `.on` highlight synced to the result. Also promoted `#score` on touch to a **centred, width-clamped, scrollable modal** (`.ef-touch #score{position:fixed;dead-centre;min-width:0;width:min(94vw,460px);max-height:80vh;overflow:auto;z-index:9300}`) — the base panel is a top-anchored 400px-min box at `top:54` that would overlap the now-taller mobile top bar and overrun a narrow landscape phone; the modal sits above the z:9000 skill cluster (mirrors the `.ef-touch #help.show`/`#shop` precedent) so the whole table is visible and the close-tap works. The fixed grid (130+32+28+28+42 + gaps = 290px) fits the 460px cap. **Mobile impact:** players can finally check the scoreboard (levels/KDA/CS/items for both teams) on a phone — a real feature gap closed. **Desktop:** untouched — `#zoomCtl`/📊 are `.ef-touch`-only (never shown on desktop), the `#score` override is `.ef-touch`-gated (base top-anchored hold-to-view panel unchanged), and the Tab hold/keyup/blur path is preserved. **Perf:** CSS-only modal + a tap-only DOM read+flip; `showScore` only builds rows on open — zero per-frame cost in the 60fps Three.js loop. **Validation:** copied `efScoreToggle` + DOM/`showScore` stubs into `outputs/scoretgl_check.js`, `node --check` + 11 assertions (starts hidden, tap-1 opens+renders+lights button, tap-2 closes+clears, 12-flip parity no drift, blocked when `gameOver` and button stays off, no-button no-throw, CSS braces/parens balanced, `z-index:9300`, `min-width:0` lets it clamp, dead-centre, grid fits 460px cap) → ALL PASS. Re-read edited regions: CSS rule (after `.ef-touch #shop`), the `#zoomCtl` markup (📊 prepended), and `efScoreToggle` (next to `showScore`); confirmed `showScore`/`#score`/`#zoomCtl`/`#scoreTgl` selectors match existing IDs and the Tab handlers are intact. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T21:38Z — **Mobile ping-wheel cancel tap target (44px).** The ping wheel is reachable on touch via the ⋯ extras drawer's 📣 (`onTap → openWheelCenter()`, index.html:4631/3237) — its action slices (`.pwSlice`) are 64px (≥44), but the centre ✕ cancel (`.pwCenter`, line 287) was only **34px**, the last sub-44px tap target left in the mobile HUD. Added one `.ef-touch`-gated rule `#pingwheel .pwCenter{width:44px;height:44px;font-size:18px}` so cancel is comfortably thumbable, completing the tap-target parity across the whole mobile control set. **Safe / no overlap:** `openWheel` lays slices on a radius `R=80` (slice 64px → inner edge ~48px from centre); a 44px centre (radius 22) keeps a ~26px gap to the slice ring, and the existing `pickByAngle` centre dead-zone (`<26px`) already matches a 22px-radius hit area — so geometry and cancel logic stay consistent. **Mobile impact:** the wheel's cancel button is now a standard 44px target like every other mobile control; the surrounding slices are unchanged. **Desktop:** untouched — rule is `.ef-touch`-gated; base `.pwCenter{34px}` (mouse-precision) unchanged. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied the rule into `outputs/pwcenter_check.js`, `node --check` + assertions (braces 0 / parens 0 balanced, every decl has a colon, `width:44px`+`height:44px` present; geometry: slice-ring gap = (80−32)−22 = 26 > 0 so no overlap, centre radius 22 < the 26px pickByAngle dead-zone) → ALL PASS. Re-read edited region (the new rule sits right under the 18:36Z touch-action parity rule); confirmed `.pwCenter`/`.pwSlice` base rules at lines 284/287, `#pingwheel`/`.ef-touch` match existing selectors, and 📣→`openWheelCenter` wiring at index.html:4631. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T22:09Z — **Fixed the mobile zoom/info column overlapping the joystick (1×4 → 2×2 grid).** Over successive runs `#zoomCtl` (the touch-only left-edge stack) accumulated **four** 44px buttons — 📊 scoreboard, 👁 hide-HUD, + / − zoom (index.html:429) — but stayed a single vertical `flex-direction:column`, so it grew to ~203px tall. Anchored `top:50%;transform:translateY(-50%)` (line 214), its centered bottom edge = `H/2+101.5`, which on every landscape phone sat **~41–61px OVER the bottom-left joystick** (`#efJoy`, top edge ≈ `H−20−min(34vmin,170)`); since `#zoomCtl` is `z:9100` > the joystick's `z:9000` (shared/ef_touch.js:35/37), the +/− buttons rendered above and **stole taps from the stick's upper-left** — degrading the primary movement control. (Prior "clear of the joystick" notes were written when the column had only 2 buttons.) Changed ONLY `.ef-touch #zoomCtl{display:flex}` → `display:grid;grid-template-columns:repeat(2,44px);gap:9px` (line 222) so the four buttons lay out as a compact **2×2 grid** (📊👁 / +−), ~97px tall — halving the footprint. Source order unchanged, so grouping stays sensible (info toggles top row, zoom bottom row); buttons keep their 44px size & all `#zoomCtl button` styling. **Mobile impact:** joystick is fully clear on phones ≥430px tall and the residual overlap on the very smallest (≤414px) drops from ~60px to ≤8.5px; also cuts the top-edge crowding of `#stats` (78→25px worst case). **Desktop:** untouched — base `#zoomCtl{display:none}` and the grid rule is `.ef-touch`-gated, so it never shows on desktop. **Perf:** CSS-only (one declaration changed), zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** (1) `outputs/zoomgrid_geom.js` — `node` geometry sweep across H=375…568: 1×4 joystick-overlap 7.5–61.5px vs 2×2 0–8.5px, grid ≤ column at EVERY height (`true`), grid height asserted =97 → PASS. (2) `outputs/zoomgrid_css.css` balance check → braces 1/1, parens 1/1, 3 decls all have a colon, `display:grid`+`grid-template-columns:repeat(2,44px)`+`gap:9px` all present → PASS. Re-read edited region (lines 213–224): comment opened/closed, single balanced rule; confirmed `#zoomCtl` has 4 buttons (line 429), base `gap:9px`/`z:9100` (line 214) and `#efJoy` `z:9000`+`bottom:max(20px,inset)`+`34vmin/170` (ef_touch.js:35,37) match the geometry used. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan, plus the 👁/📊/+/− toggles themselves — all untouched).
- 2026-06-28T22:21Z — **Added a backdrop scrim behind the three centred mobile modals.** `#help.show`, `#shop` and `#score` were promoted by prior runs to centred fixed modals at `z:9300` (above the z:9000 joystick/skill cluster) but with NO backdrop — the live, full-brightness battle rendered right up to each panel's edge, which reads as cheap/unfocused next to AoV/Wild Rift where a buy/score/help modal dims the field behind it. Added one `.ef-touch`-gated rule giving all three a pure-CSS scrim via a huge-spread box-shadow — `box-shadow:0 0 0 100vmax rgba(5,8,14,.62)` — which paints a single dim layer over EVERYTHING below z:9300 (game canvas + joystick/skills/zoom HUD) while each modal's own opaque (.93–.96 alpha) content stays crisp on top of its own shadow. **No extra DOM, no JS, no new stacking/backdrop element** (the box-shadow trick avoids the usual full-screen overlay div). It only paints while a modal is actually shown — all three are `display:none` / `.show`-gated when closed (`#shop` base `display:none` line 328, `#score` base `display:none` line 63 + toggled by `showScore`, `#help` needs `.show`) — so during real play nothing renders. The scrim follows the panels' 10–12px border-radius. Dimming the idle controls behind a buy/score/help modal is the intended top-tier-MOBA focus behaviour (and consistent with the 20:54Z note that briefly covering idle controls while the shop is open is fine). **Mobile impact:** modals read as proper focused overlays with the battle dimmed behind them, not floating panels over a bright field. **Desktop:** untouched — rule is `.ef-touch`-gated; the desktop bottom-anchored shop / top-anchored score / corner help are unchanged. **Perf:** box-shadow is rasterised once into the modal's existing layer; compositing that static dim over the moving canvas is GPU compositing (no layout, no per-frame repaint) — zero cost in the 60fps Three.js loop, and it isn't even painted unless a modal is open. **Validation:** scratch `outputs/modal_scrim_check.css` → braces 0 / parens 0 BALANCED, exactly 1 rule, 0 decls missing a colon, `box-shadow:0 0 0 100vmax rgba(5,8,14,.62)` regex-matched, all three selectors `.ef-touch`-gated, exactly 3 comma-separated selectors → ALL PASS. Grepped first: no pre-existing `100vmax`/scrim/backdrop on these modals (only an unrelated `backdrop-filter` on the HP tooltip 3381 and the victory overlay 4412). Re-read edited region (lines 198–209): comment opened/closed, single balanced rule directly under the `.ef-touch #score` rule; confirmed `#help.show`/`#shop`/`#score` z:9300 + display-gating against existing rules. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T22:37Z — **Fixed the mobile Asset Manager opening behind the skill cluster.** `#am` (the 📦 "Asset Manager" — heading + intro + 5 `.glb` file-load slots: pHero/pPet/eHero/ePet/minion) is reachable on touch via the ⋯ extras drawer's 📦 (`onTap → toggleAM()`, index.html:4678 → sets `#am` `display:block`). But its base layout anchors it `top:210px;right:8px;width:240px` at `z-index:22` (index.html:296) — wedged under the below-minimap control row, OVER the kill feed (`top:214`), and crucially BELOW the bottom-right virtual **skill cluster** `#efBtns`, which shared/ef_touch.js parks at `z-index:9000` (ef_touch.js:39). So on a phone the skill thumbs rendered OVER the open panel: its lower file-input slots were occluded and un-tappable behind the skills (the exact occlusion class already fixed for `#sel` 20:23Z, `#shop` 20:54Z, `#score` 21:52Z). Added one `.ef-touch`-gated rule promoting it to a centred, scrollable modal **above** the controls, mirroring the existing `.ef-touch #shop`/`#score`/`#help.show` pattern: `#am{position:fixed;left:50%;right:auto;top:50%;bottom:auto;transform:translate(-50%,-50%);width:min(86vw,300px);max-height:74vh;overflow:auto;z-index:9300}`. `z:9300 > 9000` so the whole panel is visible and every slot tappable; `overflow:auto` lets all 5 slots scroll within the clamped height on a short landscape phone. Also added `.ef-touch #am` to the existing modal-scrim selector (line ~221) so it dims the field behind it like the other three modals. The Asset Manager is a setup/config panel (you load models, you don't cast), so briefly covering the idle controls while open is fine — `toggleAM()` closing it restores everything. **Mobile impact:** the Asset Manager is fully visible and all five `.glb` load slots are reachable instead of hiding behind the skill cluster. **Desktop:** untouched — both edits are `.ef-touch`-gated; base top-right `#am` layout (line 296) is unchanged. **Perf:** CSS-only (centred modal + a box-shadow scrim that only paints while `#am` is `display:block`), zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** copied both rules into `outputs/am_modal_check.js`, `node --check` + assertions (braces 0 / parens 0 balanced, every decl has a colon, modal mirrors `#shop` — `position:fixed`/dead-centre `50%`+`translate(-50%,-50%)`/`right:auto`+`bottom:auto`/`width:min(86vw,300px)`/`max-height:74vh`/`overflow:auto`/`z-index:9300`, z:9300>9000 beats the cluster, scrim now 4 comma-separated selectors incl. `.ef-touch #am` with the value intact, `.ef-touch #am` specificity (0,2,0) beats base `#am` (0,1,0) so order-independent) → ALL PASS. Grepped first: no pre-existing `.ef-touch #am` override (only base `#am` at 296–298); confirmed `#am` shown via `toggleAM()` (1555) wired to the 📦 drawer item (4678), `#efBtns` z:9000 (ef_touch.js:39), and the `.ef-touch #shop` modal precedent it mirrors. Re-read edited regions (new rule under `.ef-touch #shop`; scrim selector). Note: like the existing modals, `#am` isn't mutually-exclusive-guarded vs `#shop`/`#score` also being open — two centred modals would stack, but that matches existing behaviour and the Asset Manager is a rarely-mid-match config panel. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T22:51Z — **Mobile tap targets for the pre/post-game menu screens (`.ov` buttons).** All prior tap-target work covered the in-match HUD, but the overlay screens — `#login`, `#select` (champion select), `#lobby`, `#end` (game-over), which all share `class="ov"` — never got mobile sizing. The worst offenders sit on champion select: the casting/camera/health-bar toggles `⚡quick-cast` / `🎥follow` / `❤bars` (inline `padding:3px 10px;font-size:12px` → ~21px tall) and the mode/side buttons `.mpBtn` (🤖NPC ⚡Quick 🌐Host 🔗Join + side-select; ~33px) — both well under the iOS HIG 44pt / Material 48dp min the rest of the HUD already standardized on, so they're fiddly to thumb on a phone (the FIRST controls a mobile player ever touches). Added ONE `.ef-touch`-gated rule `.ef-touch .ov button{min-height:44px}` so every overlay-screen button gets a comfortable ≥44px hit height. **Safe / no relayout:** a native `<button>` vertically centers its label at any height (UA default), so `min-height` alone keeps text centered with NO `display` change; and `min-height` never changes WIDTH, so the dense, centered, single-line champion-select toggle row can't overflow toward the screen edges. Already-tall buttons (`#loginBtn`, `Play Again`, `.lobbyGo` ≈ 43px) are effectively a no-op; the `<a class="wikiLink">` links are anchors, not buttons, so they're untouched. **Mobile impact:** comfortable, consistent tap targets on the login / champion-select / lobby / game-over screens — completes the 44px tap-target parity across the *entire* product, not just the in-match HUD. **Desktop:** untouched — rule is `.ef-touch`-gated; the compact mouse-sized menu buttons are unchanged. **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop. **Validation:** grepped first — no pre-existing `.ef-touch .ov` rule; copied the rule into `outputs/ov_tap_check.css` and ran a node balance check → braces 0 / parens 0 balanced, exactly 1 rule, 0 decls missing a colon, selector `.ef-touch .ov button`, `.ef-touch`-gated, `min-height:44px` present → ALL PASS. Confirmed via grep that `#login`/`#select`/`#lobby`/`#end` all carry `class="ov"` (lines 385/394/407/425) and that the small toggle buttons (`.qcBtn`/`.camBtn`/`.hbBtn` inline 3px·10px, line 398) + `.mpBtn` (line 373) are the sub-44px targets the rule fixes; re-read edited region (lines 96–108) — comment opened/closed, single balanced rule, `.ef-touch`/`.ov` confirmed against existing selectors. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T23:38Z — **Added `prefers-reduced-motion` support for the centred mobile modals (accessibility).** Grepped the file — there was NO `prefers-reduced-motion` rule anywhere, yet the 23:24Z run added an `efModalPop` scale/opacity entrance "pop" to the four centred touch modals (`#help.show`, `#shop`, `#score`, `#am`). Respecting the OS-level reduced-motion setting (for vestibular sensitivity) is a standard top-tier-app accessibility item that was missing. Added one `@media (prefers-reduced-motion:reduce){.ef-touch #help.show,.ef-touch #shop,.ef-touch #score,.ef-touch #am{animation:none}}` rule directly under the `efModalPop` keyframe, reusing the exact same `.ef-touch` four-modal selector group. **No position jump:** `animation:none` makes each modal fall back to its resting CSS transform `translate(-50%,-50%)` — identical to the keyframe `to` frame — so the modal simply appears instantly, dead-centre, with zero displacement; the centering established by the `#shop`/`#score`/`#am`/`#help.show` rules is preserved. **Safe:** no JS depends on the animation (no `animationend` handler anywhere — confirmed the modal pop is purely cosmetic, unlike the JS-lifecycle `dfloat`/`fd`/`efPop` animations which were deliberately NOT touched), so disabling it only removes the visual easing. **Mobile impact:** players who enable reduced-motion get instant, non-animated modals — no scale/fade — matching their accessibility preference. **Desktop:** untouched — rule is `.ef-touch`-gated (the entrance animation it suppresses is itself touch-only). **Perf:** CSS-only, zero JS, zero per-frame cost in the 60fps Three.js loop (it only suppresses an already-compositor-only open animation). **Validation:** copied the rule into `outputs/reduced_motion_check.css`, node balance check → braces 0 / parens 0 balanced, `@media (prefers-reduced-motion:reduce)` present, `animation:none` present, exactly 4 comma-separated `.ef-touch #` selectors, all four modal IDs (`#help.show`/`#shop`/`#score`/`#am`) present, decl has a colon → ALL PASS. Re-read edited region (lines 249–258): the new `@media` rule sits directly under the `efModalPop` keyframe and reuses its exact selector group; confirmed via grep there was no pre-existing `prefers-reduced-motion` to collide with and that the four modals rest at `transform:translate(-50%,-50%)` so `animation:none` is jump-free. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
- 2026-06-28T23:24Z — **Modal entrance "pop" animation for the centred mobile modals.** The four centred touch modals — `#help.show`, `#shop`, `#score`, `#am` (all promoted to `position:fixed` dead-centre at `z:9300` by prior runs, all resting at `transform:translate(-50%,-50%)`) — appeared **instantly** with no transition, which reads abrupt/cheap next to AoV/Wild Rift where a buy/score/help panel eases in. Added one `.ef-touch`-gated rule `animation:efModalPop .16s ease-out` on the same four-selector group as the 22:21Z scrim, plus a `@keyframes efModalPop{from{opacity:0;transform:translate(-50%,-50%) scale(.94)}to{opacity:1;transform:translate(-50%,-50%) scale(1)}}`. **Safety (no position jump):** the keyframe `to` transform is EXACTLY the modals' resting transform `translate(-50%,-50%)` (with `scale(1)`, visually identical) and there is no `forwards`, so when the 0.16s animation ends the element settles back to its CSS transform with zero displacement — the dead-centre positioning the `#shop`/`#score`/`#am`/`#help.show` rules establish is preserved. The animation fires only on OPEN (the modals are `display:none`/`.show`-gated when closed, and a CSS animation replays on the `display:none→shown` render), so nothing runs during actual play. The 22:21Z box-shadow scrim fades in with it (opacity ramp), which is the desirable focus effect; at the transient `scale(.94)` the `100vmax` scrim is still ≈94vmax of spread, far more than needed to cover the screen from centre, so no un-dimmed flash. **Mobile impact:** modals ease/pop in like a top-tier mobile MOBA instead of snapping on. **Desktop:** untouched — rule is `.ef-touch`-gated; desktop modals are unchanged. **Perf:** `opacity`+`transform` are the only animated properties — compositor-only (no layout, no paint) — and the animation isn't even present unless a modal is opening, so zero per-frame cost in the 60fps Three.js loop. **Validation:** copied the rule + keyframe into `outputs/modal_pop_check.css` and ran a node check → braces 4/4 & parens 4/4 balanced, `animation:efModalPop .16s ease-out` present, keyframe `from{}to{}` shape matched, end transform asserted `== translate(-50%,-50%) scale(1)` (== resting), `from` = `opacity:0`+`scale(.94)`, `to` = `opacity:1`, and ONLY `opacity`/`transform` animated (no layout-triggering props) → ALL PASS. Re-read edited region (directly under the 22:21Z scrim rule): comment opened/closed, the animation rule reuses the exact 4-selector scrim group, keyframe well-formed; confirmed all four modals rest at `transform:translate(-50%,-50%)` (`#help.show` line 127, `#shop` 206, `#am` 218, `#score` 227) so the single shared keyframe is correct for all. Preserves all existing behaviour (quick-cast, cam-lock, HP modes, minimap, ping wheel, command-queue hold, town-hall single/double-tap, edge-pan untouched).
